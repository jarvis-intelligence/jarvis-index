---
phase: 06-plugin-skills-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-plugin.mjs
  - README.md
  - .codex-plugin/plugin.json
  - plugin/skills/jarvis-use/SKILL.md
  - package.json
  - .github/workflows/checks.yml
autonomous: true
requirements: [D-01, D-12, D-13, D-14, SC-1, SC-4]
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-12(a)/SC-4: `node scripts/check-plugin.mjs` exits 1 with at least three `P1: `-prefixed stderr lines against the pre-fix tree, and exits 0 with no `P1: ` line once the drift is fixed — the guard is demonstrated red before it is trusted green (D-13)."
    - "D-01/SC-1: root `README.md`, `.codex-plugin/plugin.json` `interface.longDescription`, and `plugin/skills/jarvis-use/SKILL.md` all state ten tools; the root README tool table carries an `indexRepo` row; no shipped surface names a jarvis MCP tool in snake_case."
    - "D-14: an edit to `README.md`, `setup.sh`, or `scripts/check-plugin.mjs` triggers the Manifest checks workflow on both `push` and `pull_request`, and that workflow runs the new guard."
    - "`npm run check:plugin` resolves to the new guard, matching the `check:manifests` alias convention at `package.json:13`."
  artifacts:
    - scripts/check-plugin.mjs
    - README.md
    - .codex-plugin/plugin.json
    - plugin/skills/jarvis-use/SKILL.md
    - package.json
    - .github/workflows/checks.yml
  key_links:
    - "`plugin/skills/jarvis-use/references/tool-roster.md` `### <name>(` headings are P1's single source of truth (10 headings at :7, :11, :15, :19, :23, :26, :31, :35, :39, :43). Restructuring those headings breaks the parser — this plan must not touch that file."
    - "`.github/workflows/checks.yml` duplicates its path filter across the `push` (:8-16) and `pull_request` (:17-25) blocks. An addition to only one block silently disables the guard for the other trigger class."
    - "P1 derives the roster from this repo only. It cannot see `../jarvis`, so it cannot detect the server registering an 11th tool — that limitation must be stated in the script header."
---

<objective>
Prove the entire Phase 6 machine on one vertical slice: the tool-count drift class (D-01) gets a
real guard dimension (P1), that dimension is demonstrated failing against today's tree, the drift is
fixed, the dimension goes green, and the guard is wired into CI so a regression cannot land silently.

Purpose: this is the tracer. It touches every layer the phase will modify — guard script, shipped
content, npm alias, CI workflow, path filter — on the thinnest possible path. If the architecture is
wrong (roster parsing, `process.cwd()` seam, path-filter shape), it fails here after one commit
instead of after ten.

Output: `scripts/check-plugin.mjs` carrying dimension P1, three corrected drift sites, a
`check:plugin` npm alias, and a `Check plugin surface` CI step with the D-14 path-filter additions.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-plugin-skills-enhancement/06-CONTEXT.md
@.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md
@.planning/phases/06-plugin-skills-enhancement/06-PATTERNS.md
@.planning/phases/06-plugin-skills-enhancement/06-VALIDATION.md
@.claude/CLAUDE.md
@scripts/check-manifests.mjs
@scripts/verify-build.mjs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: scripts/check-plugin.mjs with dimension P1, demonstrated RED on today's tree</name>
  <files>scripts/check-plugin.mjs</files>
  <read_first>
    - `scripts/check-manifests.mjs` — the house style to copy exactly: `#!/usr/bin/env node`, the `process.cwd()` testability-seam header comment at :4-6, zero-dependency ESM importing only `node:fs` + `node:path`, the read-parse loop that `continue`s on a read failure so one missing file does not mask other dimensions (:27-41), and the single terminal `process.exit(1)` with a one-line `ok:` summary (:65-69).
    - `scripts/verify-build.mjs` — the two shapes this script needs that `check-manifests.mjs` lacks: the numbered-dimension header comment block (:2-26, dimensions V2/V3/V4/V5/V9/V10 each with a name, a contract sentence, and the honest limitation inline) and the dimension-tagged collector `fail(dimension, detail)` (:37-41). Also the multi-dimension `ok:` summary form at :239-241 and the `process.argv` default-reading style at :29-30.
    - `plugin/skills/jarvis-use/references/tool-roster.md` — P1's source of truth. Ten `### ` headings at lines 7, 11, 15, 19, 23, 26, 31, 35, 39, 43, each of the form `### name(args) → dict`.
    - `README.md` lines 18-38 — the current count sentence at :20 and the nine-row grouped tool table at :22-32.
    - `.codex-plugin/plugin.json` line 25 — `interface.longDescription`, whose tool-count clause is currently wrong; note the `\u2014` escaped em dashes that must survive untouched.
    - `plugin/skills/jarvis-use/SKILL.md` line 75 — the trigger-examples line carrying two Python function names where MCP tool names belong.
    - `plugin/README.md` lines 3 and 7 — already-correct positive subjects P1 must keep asserting.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *Target 6 — Guard design (D-12, D-13)*, sub-section *P1*, for the recommended design (C + one line of A) and the exact limitation wording to record.
  </read_first>
  <behavior>
    - Parsing `plugin/skills/jarvis-use/references/tool-roster.md` yields a set of exactly 10 camelCase tool names; a set size other than 10 fails P1 with the parsed size in the message.
    - Given the current `README.md`, P1 fails naming `README.md`: the count word is not `Ten`/`ten`, the stale word is present, and `indexRepo` does not appear in the tool table.
    - Given the current `.codex-plugin/plugin.json`, P1 fails naming that file: `interface.longDescription` carries the stale count word.
    - Given the current `plugin/skills/jarvis-use/SKILL.md`, P1 fails naming that file and the offending token: a snake_case form of a roster tool name is present.
    - Given the current `plugin/README.md`, P1 does not fail: every roster name appears, the count word is correct, the stale word is absent.
    - `plugin/skills/jarvis-use/SKILL.md:60` and `plugin/skills/jarvis-use/references/tool-roster.md:24` mention upstream `scip` `v0.9.0`; P1 must not flag either — it is a third-party version string, not a jarvis pin or a tool name.
    - Run from a directory that is not the repo root, every subject path resolves against `process.cwd()`, so the script runs unmodified inside a `git worktree` copy.
  </behavior>
  <action>
Create `scripts/check-plugin.mjs` as a zero-dependency ESM Node script in the exact house style of `scripts/check-manifests.mjs`: shebang `#!/usr/bin/env node`, a header comment that states the script's purpose and reproduces the `process.cwd()` testability-seam rationale verbatim in shape, imports limited to `node:fs` and `node:path`, a module-level `let failed = false` plus the dimension-tagged collector `fail(dimension, detail)` copied in shape from `scripts/verify-build.mjs:37-41`, one terminal `process.exit(1)`, and a one-line `ok:` summary naming the dimensions that passed.

Add a numbered-dimension header comment block in the `scripts/verify-build.mjs:2-26` style. This plan implements only `P1`; reserve `P2a`, `P2b`, `P3`, `P4`, `P5` as named entries in that block with a one-line contract each so later plans extend rather than restructure. Use the `P` prefix so nothing collides with `verify-build.mjs`'s `V*` dimensions.

P1's contract: the shipped surface agrees with itself on the jarvis MCP tool set. Implement it as:

Parse `plugin/skills/jarvis-use/references/tool-roster.md` for lines matching a heading pattern that captures the identifier between `### ` and the opening parenthesis. Collect those identifiers into a `Set`. Assert the set size is exactly `10`; a different size fails P1 with the observed size, because the roster is the hand-maintained point of truth and a silent change to it would weaken every downstream assertion.

Against that set, assert:
(1) `plugin/README.md` contains every roster name, contains `Ten` or `ten` as a standalone word, and contains no case-insensitive standalone `nine`;
(2) root `README.md` contains every roster name, contains `Ten` or `ten` as a standalone word, and contains no case-insensitive standalone `nine`;
(3) `.codex-plugin/plugin.json` `interface.longDescription` contains no case-insensitive standalone `nine`;
(4) none of `plugin/skills/jarvis-setup/SKILL.md`, `plugin/skills/jarvis-use/SKILL.md`, `plugin/skills/jarvis-issues/SKILL.md`, `plugin/skills/jarvis-use/references/tool-roster.md` contains a word-bounded snake_case transliteration of any roster name — build the forbidden-token list mechanically from the roster set by lower-casing each camel hump and joining with an underscore, so the list cannot drift from the roster.

Report each violation through `fail('P1', …)` with the relative path and the offending token or missing name, so one run reports every violation rather than the first.

In P1's header entry record the limitation verbatim in substance: P1 derives the roster from this repository and cannot see `../jarvis`, which is not available in CI — it converts "the surface disagrees with the server" into "the surface disagrees with itself" and cannot detect the server registering an eleventh tool. A maintainer must re-read `../jarvis/src/jarvis/server.py` when the server changes; P1 is not a server diff.

Make no content edits in this task. Run the script against the unmodified tree and capture its stderr as the red-before-green evidence D-13 requires; that failing output belongs in this plan's SUMMARY.
  </action>
  <verify>
    <automated>node scripts/check-plugin.mjs; test $? -eq 1</automated>
    <fails_when>non-zero exit from this pipeline — meaning `check-plugin.mjs` exited 0 and P1 detected none of the three known drift sites on the pre-fix tree</fails_when>
    <automated>node scripts/check-plugin.mjs 2>&amp;1 | grep -c '^P1: '</automated>
    <fails_when>the printed count is less than 3</fails_when>
    <automated>node scripts/check-plugin.mjs 2>&amp;1 | grep -c -F 'plugin/README.md'</automated>
    <fails_when>the printed count is not 0 — `plugin/README.md` is already correct and a P1 failure naming it means the positive assertions are mis-implemented</fails_when>
    <automated>node scripts/check-manifests.mjs</automated>
    <fails_when>non-zero exit — the existing three invariants must stay green; this task adds a sibling script and changes nothing they read</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/check-plugin.mjs` exists, starts with `#!/usr/bin/env node`, and its import statements name only `node:fs` and `node:path` — no `package.json` dependency is added.
    - Every subject path in the script is resolved through `resolve(process.cwd(), relPath)`; the string `import.meta.url` does not appear.
    - The header comment block names all six dimensions `P1`, `P2a`, `P2b`, `P3`, `P4`, `P5`, and P1's entry states that the guard cannot see `../jarvis` and therefore cannot detect an eleventh server-side tool.
    - The failure collector has the signature `fail(dimension, detail)` and every P1 violation is emitted through it with a `P1: ` prefix on stderr.
    - `node scripts/check-plugin.mjs` on the unmodified tree exits 1 and prints `P1: ` lines naming `README.md`, `.codex-plugin/plugin.json`, and `plugin/skills/jarvis-use/SKILL.md`.
    - The same run prints no `P1: ` line naming `plugin/README.md`, `plugin/skills/jarvis-use/references/tool-roster.md`, or `plugin/skills/jarvis-setup/SKILL.md`.
    - The forbidden snake_case token list is derived in code from the parsed roster set, not written as a literal array.
    - `plugin/skills/jarvis-use/references/tool-roster.md` is byte-unchanged by this task.
  </acceptance_criteria>
  <done>`scripts/check-plugin.mjs` implements P1, exits 1 on the current tree with at least three `P1: ` lines naming exactly the three known drift sites, and its failing output is captured as red-before-green evidence. No content file is edited.</done>
  <reversibility rating="reversible">A new sibling script wired to nothing yet; deleting the file restores the prior state exactly.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Fix the three tool-count drift sites — P1 goes green</name>
  <files>README.md, .codex-plugin/plugin.json, plugin/skills/jarvis-use/SKILL.md</files>
  <read_first>
    - `README.md` lines 18-38 — the count sentence at :20, the three-column grouped table at :22-32 (`Navigate` / `Search` / `Scope` row groups), and the roster link at :38. `.claude/CLAUDE.md:145` caps this file at 300 lines and requires it stay a router, not a manual.
    - `plugin/README.md` lines 3, 7-15 — the tone, ordering, and one-line-per-tool phrasing to match, including the existing `indexRepo` description at :15.
    - `.codex-plugin/plugin.json` lines 22-45 — the whole `interface` block, so the `longDescription` edit preserves the `\u2014` escapes and 2-space indent (`.claude/CLAUDE.md:108`).
    - `plugin/skills/jarvis-use/SKILL.md` lines 73-76 — the trigger-examples section; line 75 is the subject, line 76 must stay unchanged.
    - `plugin/skills/jarvis-use/references/tool-roster.md` lines 31-34 — `indexRepo`'s authoritative signature and behaviour, the source for the new README row's wording.
    - `scripts/check-plugin.mjs` — the P1 implementation written in Task 1, so the fixes satisfy the actual assertions rather than a guess at them.
  </read_first>
  <action>
Make root `README.md:20` read `Ten MCP tools, exposed to any MCP client:` and add one row to the grouped table at `:22-32` for `indexRepo`, placed in the `Scope` group immediately above the existing `getIndexStatus` row so the two index-lifecycle tools sit together. Write its `Answers` cell from `plugin/skills/jarvis-use/references/tool-roster.md:31-32` in the one-line register the table already uses: `indexRepo` builds an index for a local git repo and returns immediately for polling. The table must end with ten data rows and the `Navigate` / `Search` / `Scope` group labels unchanged. Do not restructure the table or touch the roster link at `:38`.

In `.codex-plugin/plugin.json`, rewrite `interface.longDescription` at `:25` so its tool-count clause reads `through ten MCP tools.` Change nothing else in the string — the two `\u2014` escapes, the SCIP/Zoekt clause, and the trailing skills sentence stay byte-identical. Do not touch `version` at `:3` or the URLs at `:39-40`; those belong to plan 06-07, which bumps the version and retargets both URLs at the release tag.

In `plugin/skills/jarvis-use/SKILL.md:75`, replace the two snake_case Python function names in the trigger examples with their MCP camelCase names `indexRepo` and `blastRadius`, keeping the surrounding backticks, quoting, and the other two examples exactly as they are. This is the exact drift class D-12(a) exists to catch, per `06-CONTEXT.md` § Specific Ideas.

Change nothing in `plugin/README.md` — `:3` and `:7` are already correct and are P1's positive subjects.
  </action>
  <verify>
    <automated>node scripts/check-plugin.mjs</automated>
    <fails_when>non-zero exit, or any line matching `^P1: ` printed to stderr</fails_when>
    <automated>node scripts/check-manifests.mjs</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>node scripts/check-plugin.mjs 2>&amp;1 | grep -c -F 'README.md'</automated>
    <fails_when>the printed count is not 0 — a P1 finding still names a README, so the stale count word survives somewhere</fails_when>
    <automated>grep -c -F 'indexRepo' README.md</automated>
    <fails_when>the printed count is 0</fails_when>
    <automated>claude plugin validate ./plugin --strict</automated>
    <fails_when>non-zero exit, or any line containing `error` in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `README.md:20` is exactly `Ten MCP tools, exposed to any MCP client:`.
    - The `README.md` tool table has ten data rows, one per roster name, with the `indexRepo` row inside the `Scope` group directly above `getIndexStatus`, and the `Navigate` / `Search` / `Scope` labels unchanged.
    - `.codex-plugin/plugin.json` `interface.longDescription` contains the substring `through ten MCP tools.` and still contains two `\u2014` escape sequences.
    - `.codex-plugin/plugin.json` `version` is still `0.9.0` and `interface.privacyPolicyURL` / `interface.termsOfServiceURL` are byte-unchanged — the release bump is plan 06-07's.
    - `plugin/skills/jarvis-use/SKILL.md:75` names `indexRepo` and `blastRadius` and contains no word-bounded snake_case form of any roster tool name; line 76 is byte-unchanged.
    - `plugin/README.md`, `plugin/skills/jarvis-setup/SKILL.md`, `plugin/skills/jarvis-issues/SKILL.md`, and `plugin/skills/jarvis-use/references/tool-roster.md` are byte-unchanged by this task.
    - `node scripts/check-plugin.mjs` exits 0 and prints an `ok:` line naming P1.
  </acceptance_criteria>
  <done>All three drift sites state ten tools and name `indexRepo` in MCP camelCase; P1 exits 0; `check-manifests.mjs` and `claude plugin validate ./plugin --strict` both stay green.</done>
  <reversibility rating="reversible">Prose and one JSON string; the pre-fix wording is recoverable from git history and the guard proves either state.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Wire the guard into npm and CI, with the D-14 path-filter additions in both blocks</name>
  <files>package.json, .github/workflows/checks.yml</files>
  <read_first>
    - `package.json` lines 9-15 — the `scripts` block; `check:manifests` at :13 is the alias convention to mirror, and `devDependencies` at :16-23 must not change.
    - `.github/workflows/checks.yml` in full (42 lines) — the header comment at :1-4 explaining why the workflow exists separately, the path filter duplicated verbatim across `push` (:8-16) and `pull_request` (:17-25), and the job at :30-42 whose `Check manifests` step at :41-42 is the sibling to copy.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *Target 7 — `checks.yml` path filter (D-14)* — the three additions and the explicit finding that `plugin/commands/**`, `plugin/hooks/**`, and `plugin/agents/**` need no entry because `plugin/**` already covers them, plus the reasoning for accepting `setup.sh` sync churn.
    - `.claude/CLAUDE.md` lines 16 and 86 — `setup.sh` is synced from the private repo and must never be edited here. It enters this plan only as a path-filter string.
  </read_first>
  <action>
In `package.json`, add `"check:plugin": "node scripts/check-plugin.mjs"` immediately after the `check:manifests` entry at `:13`. Add no dependency and no `devDependencies` entry — the guard is zero-dependency, matching both existing scripts.

In `.github/workflows/checks.yml`, add these three entries to the path filter in **both** the `push` block and the `pull_request` block, keeping the existing single-quoted style and ordering the new script beside the existing `scripts/check-manifests.mjs` entry: `'scripts/check-plugin.mjs'`, `'README.md'`, `'setup.sh'`. Both blocks are copy-paste twins; an addition to only one silently disables the guard for that trigger class. Add no entry for `plugin/commands/**`, `plugin/hooks/**`, or `plugin/agents/**`.

Add a `Check plugin surface` step running `node scripts/check-plugin.mjs` directly after the existing `Check manifests` step, at the same indentation, inside the same `manifest-checks` job so it reuses the Node 22 setup already pinned at `:36-39`.

Above the new `'setup.sh'` filter entry, add a comment in the `:1-4` register recording why a sync-owned file is in the filter: `setup.sh` is overwritten here by the private repo's sync workflow, so a sync that changes `--only` flag values or ctags behaviour must re-validate the installer claims the skills make. Do not modify `setup.sh` itself.

Do not add the vendor-validator steps here; the pinned `claude` CLI install and the two `claude plugin validate --strict` steps are plan 06-05's D-17 work.
  </action>
  <verify>
    <automated>grep -c -F 'scripts/check-plugin.mjs' .github/workflows/checks.yml</automated>
    <fails_when>the printed count is not 3 — two path-filter entries plus one run step</fails_when>
    <automated>grep -c -F "'README.md'" .github/workflows/checks.yml</automated>
    <fails_when>the printed count is not 2</fails_when>
    <automated>grep -c -F "'setup.sh'" .github/workflows/checks.yml</automated>
    <fails_when>the printed count is not 2</fails_when>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/checks.yml','utf8');process.exit(y.includes('Check plugin surface')?0:1)"</automated>
    <fails_when>non-zero exit — the new CI step name is absent from the workflow</fails_when>
    <automated>npm run check:plugin</automated>
    <fails_when>non-zero exit, or output containing `Missing script`</fails_when>
    <automated>npm run check:manifests</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>git diff --stat -- setup.sh</automated>
    <fails_when>any output is printed — `setup.sh` must be byte-unchanged</fails_when>
  </verify>
  <acceptance_criteria>
    - `package.json` `scripts` contains `"check:plugin": "node scripts/check-plugin.mjs"`; `devDependencies` is byte-unchanged.
    - `.github/workflows/checks.yml` `push.paths` and `pull_request.paths` each contain exactly the six original entries plus `'scripts/check-plugin.mjs'`, `'README.md'`, and `'setup.sh'` — nine entries per block, identical between the two blocks.
    - Neither block gains an entry for `plugin/commands/**`, `plugin/hooks/**`, or `plugin/agents/**`.
    - A step named `Check plugin surface` running `node scripts/check-plugin.mjs` exists in the `manifest-checks` job after `Check manifests`, with no separate `actions/setup-node` invocation of its own.
    - A comment above the `'setup.sh'` entry states that the file is sync-owned and explains why it is nevertheless in the filter.
    - `setup.sh` has no diff against `HEAD`.
    - `npm run check:plugin` and `npm run check:manifests` both exit 0.
  </acceptance_criteria>
  <done>`npm run check:plugin` runs the new guard; both `checks.yml` path-filter blocks carry the three D-14 additions identically; the `Check plugin surface` step runs in CI; `setup.sh` is untouched.</done>
  <reversibility rating="reversible">Two config files; reverting the diff restores the prior trigger surface with no published side effect.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo content → CI runner | `node scripts/check-plugin.mjs` reads repository files chosen by a contributor's diff and runs on a GitHub-hosted runner with `contents: read` |
| repo content → installed agent | root `README.md` and `.codex-plugin/plugin.json` `longDescription` are read by humans and marketplace surfaces; `plugin/skills/jarvis-use/SKILL.md` is instruction text an agent executes against |
| upstream sync → this repo | `setup.sh` arrives as a commit from the private repo's sync workflow and now triggers this workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | `plugin/skills/jarvis-use/SKILL.md` trigger examples | medium | mitigate | Task 2 replaces the snake_case names that steer an agent to call a non-existent tool; P1's roster-derived forbidden-token scan makes the regression un-landable |
| T-06-02 | Repudiation | `scripts/check-plugin.mjs` P1 | medium | mitigate | P1's stated limitation is written into the script header, so a future maintainer cannot read a green P1 as proof the surface matches the server |
| T-06-03 | Denial of Service | CI job triggered by `setup.sh` sync commits | low | accept | One extra `ubuntu-latest` job per upstream sync running sub-second pure-filesystem assertions; research measured the cost and recommends accepting it (Target 7) |
| T-06-04 | Elevation of Privilege | `check-plugin.mjs` file reads | low | accept | The script only reads files under the checkout, imports nothing beyond `node:fs`/`node:path`, spawns no process, and makes no network call in this plan |
| T-06-05 | Information Disclosure | guard stderr in public CI logs | low | accept | Failure details are relative repo paths and tool names, all already public in the repository |
</threat_model>

## Artifacts this phase produces

Symbols and paths created by Phase 6. None exists before this phase, so source-grounding must not
flag them as unresolved references.

| Artifact | Kind | Created by |
|---|---|---|
| `scripts/check-plugin.mjs` | guard script | 06-01 |
| `P1` dimension — roster set-equality + roster-derived snake_case scan | guard dimension | 06-01 |
| `check:plugin` npm alias | `package.json` script | 06-01 |
| `Check plugin surface` CI step | `checks.yml` step | 06-01 |
| `plugin/skills/jarvis-setup/references/troubleshooting.md` | skill reference | 06-02 |
| `plugin/hooks/jarvis-index-status.sh` | POSIX probe script | 06-03 |
| `plugin/hooks/hooks.json` | hook config (Claude + Codex) | 06-03 |
| `plugin/hooks/cursor.json` | hook config (Cursor) | 06-03 |
| Cursor manifest `hooks` path field | manifest key | 06-03 |
| `plugin/commands/index.md` — `/jarvis:index` | slash command | 06-04 |
| `plugin/commands/status.md` — `/jarvis:status` | slash command | 06-04 |
| `plugin/agents/jarvis-navigator.md` | navigator subagent | 06-04 |
| `$schema` key on `plugin/.claude-plugin/plugin.json` | manifest key | 06-04 |
| `Install Claude Code CLI` CI step | `checks.yml` step | 06-05 |
| `Validate plugin + marketplace` CI step | `checks.yml` step | 06-05 |
| `P3` dimension — both `marketplace.json` valid | guard dimension | 06-06 |
| `P4` dimension — skill/command/agent frontmatter allowed-keys | guard dimension | 06-06 |
| `P5` dimension — referenced assets/components exist, hook executable | guard dimension | 06-06 |
| `P2a` dimension — manifest version ↔ `jarvis-mcp>=` floor | guard dimension | 06-07 |
| `P2b` dimension — `blob/<ref>` ↔ `v<version>` + tag existence | guard dimension | 06-07 |
| `--release` flag on `scripts/check-plugin.mjs` | CLI flag | 06-07 |
| `v0.9.1` git tag | release tag | 06-08 |

<verification>
1. `node scripts/check-manifests.mjs && node scripts/check-plugin.mjs` exits 0 from the project root.
2. `claude plugin validate ./plugin --strict` and `claude plugin validate . --strict` both exit 0.
3. The Task 1 red-before-green stderr capture is recorded in the plan SUMMARY, naming the three
   drift sites P1 detected before Task 2.
4. `git diff --stat -- setup.sh` prints nothing.
5. `.github/workflows/checks.yml` `push.paths` and `pull_request.paths` are textually identical
   lists.
</verification>

<success_criteria>
- D-01 / SC-1: every tool count and list on the surfaces this plan touches states ten and names
  `indexRepo`; no shipped file names a jarvis MCP tool in snake_case.
- D-12(a) / SC-4: P1 exists, was demonstrated red on the pre-fix tree, and is green after the fix.
- D-13: the failing output is captured, not asserted.
- D-14: both path-filter blocks cover `scripts/check-plugin.mjs`, `README.md`, and `setup.sh`.
- No test framework, linter, or formatter is introduced; no file under
  `/Users/ddphuong/Projects/jarvis-ai/jarvis` is touched; `setup.sh` is unedited.
</success_criteria>

<output>
Create `.planning/phases/06-plugin-skills-enhancement/06-01-SUMMARY.md` when done, including the
verbatim Task 1 red output as D-13 evidence.
</output>
