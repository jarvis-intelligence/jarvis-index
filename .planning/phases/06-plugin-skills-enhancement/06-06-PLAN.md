---
phase: 06-plugin-skills-enhancement
plan: 06
type: tdd
wave: 3
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - scripts/check-plugin.mjs
autonomous: true
requirements: [D-12, D-13, SC-3, SC-4]
estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-12(c) / SC-4: dimension P3 asserts both `marketplace.json` files parse, carry `name` in lowercase kebab-case plus `owner.name` plus a non-empty `plugins` array, that every `plugins[].name` equals the corresponding `plugin.json` `name`, that every `plugins[].source` begins `./` with no `..` and no backslash and resolves to a directory containing the expected manifest, and that no entry declares a component field."
    - "D-12(d) / SC-4: dimension P4 asserts the frontmatter key set of every `plugin/skills/*/SKILL.md`, `plugin/commands/*.md`, and `plugin/agents/*.md` against that component's allowed set, and fails loudly on `hooks`, `mcpServers`, or `permissionMode` in an agent — the three keys unsupported in plugin scope for security reasons."
    - "D-12(d) / research C-4: P4 is hand-rolled, because `claude plugin validate --strict` was measured not to report unknown frontmatter keys in skills, commands, or agents."
    - "SC-3 / SC-4: dimension P5 asserts that every path referenced by a manifest, a hook config, or the `jarvis-setup` retrieval command resolves to an existing file, and that `plugin/hooks/jarvis-index-status.sh` has an executable mode bit."
    - "D-13: each of P3, P4, and P5 was demonstrated failing before it was trusted passing — P4 naturally against a pre-phase worktree, P3 and P5 through a deliberate scratch mutation, because neither has a natural pre-fix failure."
    - "`node scripts/check-plugin.mjs` stays hermetic: pure filesystem, no network, no spawned process, sub-second, runnable unmodified from any working directory through the `process.cwd()` seam."
  artifacts:
    - scripts/check-plugin.mjs
  key_links:
    - "`scripts/check-manifests.mjs:55-63` already asserts byte-identity of the two MCP configs and `:43-53` already asserts three-way `version` equality. P3 and P2a must not duplicate either; they assert the relations those two do not cover."
    - "The `process.cwd()` seam at `scripts/check-manifests.mjs:4-6` is what makes D-13 mechanical: `git worktree add` a pre-phase commit, copy the script in, `cd` there, run it unmodified."
    - "P4's command allowed-set MUST include `name`: Claude Code ignores it in a command file but Cursor's documented command frontmatter requires it, and plan 06-04 ships it in both command files."
    - "P4's skill allowed-set is exactly the Agent Skills six — `name`, `description`, `license`, `compatibility`, `metadata`, `allowed-tools` — and the key plan 06-02 deleted is not a member; that is the assertion that stops it coming back."
    - "P5 reads `plugin/hooks/hooks.json`'s command string, which contains a plugin-root variable reference. P5 must resolve that variable to `plugin/` rather than treating the literal string as a path."
---

<objective>
Close the remaining three drift classes with automated guards: marketplace-manifest validity, skill and
component frontmatter conformance, and referenced-path existence. Each is demonstrated red before it is
trusted green.

Purpose: these are the classes nothing guards today. Neither `marketplace.json` is read by any script
in the repo. The vendor validator was measured not to report unknown frontmatter keys, so the Agent
Skills conformance plan 06-02 just achieved has no enforcement behind it. And a manifest that points at
a deleted asset or a non-executable hook script fails silently at install time, not in CI.

Output: `scripts/check-plugin.mjs` extended with dimensions P3, P4, and P5, still hermetic, still
zero-dependency, with each dimension's red evidence captured.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-plugin-skills-enhancement/06-CONTEXT.md
@.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md
@.planning/phases/06-plugin-skills-enhancement/06-PATTERNS.md
@.planning/phases/06-plugin-skills-enhancement/06-VALIDATION.md
@.planning/phases/06-plugin-skills-enhancement/06-01-SUMMARY.md
@.planning/phases/06-plugin-skills-enhancement/06-02-SUMMARY.md
@.planning/phases/06-plugin-skills-enhancement/06-03-SUMMARY.md
@.planning/phases/06-plugin-skills-enhancement/06-04-SUMMARY.md
@scripts/check-manifests.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dimension P3 — both marketplace manifests are valid and their sources resolve</name>
  <files>scripts/check-plugin.mjs</files>
  <read_first>
    - `scripts/check-plugin.mjs` as it stands after plan 06-01 — the header dimension block with P3's reserved one-line contract, the `fail(dimension, detail)` collector, the `process.cwd()` seam, and the single terminal exit.
    - `scripts/check-manifests.mjs` lines 10-14 and 25-63 — the three `plugin.json` paths it covers, and the two dimensions P3 must not duplicate: three-way `version` equality at :43-53 and MCP-config byte-identity at :55-63. Neither marketplace file appears anywhere in that file.
    - `.claude-plugin/marketplace.json` in full — `name`, `metadata.description`, `owner.name`, `owner.email`, one `plugins` entry with `name`, `source`, `description`.
    - `.cursor-plugin/marketplace.json` in full — the same object with `owner` before `metadata` and a Cursor-worded description; key order differs, so P3 must read by key and never by position.
    - `plugin/.claude-plugin/plugin.json` and `plugin/.cursor-plugin/plugin.json` — the `name` values each marketplace entry must match.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *Target 6 → P3* — the six sub-assertions, the conflicting-manifests error a component field in a marketplace entry produces, and the note that Claude Code re-checks reserved names on every load.
    - `.claude/CLAUDE.md` line 102 — `name` must be lowercase kebab-case and identical between `.cursor-plugin/marketplace.json` and `plugin/.cursor-plugin/plugin.json`, because Cursor's validator hard-errors on a mismatch.
  </read_first>
  <behavior>
    - Both marketplace files parse as JSON; an unparseable file fails P3 naming the file and the parse error, and does not abort the other dimensions.
    - A marketplace `name` that is absent, empty, or contains an uppercase letter, an underscore, or a space fails P3.
    - A missing `owner.name`, or a `plugins` value that is not a non-empty array, fails P3.
    - A `plugins[].name` that differs from the `name` in the manifest its `source` resolves to fails P3, naming both values.
    - A `plugins[].source` that does not begin with `./`, or contains `..`, or contains a backslash, fails P3 naming the offending value.
    - A `plugins[].source` that resolves to a path that is not an existing directory, or to a directory that does not contain the expected client manifest, fails P3.
    - A `plugins[]` entry declaring any of `skills`, `commands`, `agents`, `hooks`, or `mcpServers` fails P3, because a component field there conflicts with `plugin.json`.
    - On the tree as it stands after plans 06-02 through 06-04, P3 emits no failure.
    - Run with `plugins[0].source` pointed at a non-existent directory in a scratch copy, P3 fails.
  </behavior>
  <action>
Add dimension P3 to `scripts/check-plugin.mjs`, filling in the contract line reserved for it in the
header block during plan 06-01. Keep the file zero-dependency: `node:fs` and `node:path` only, every
path resolved through the `process.cwd()` seam, every violation reported through
`fail('P3', detail)`, and the single terminal exit untouched.

Define the two subject files as a module-level constant pairing each marketplace path with the client
manifest its entries must resolve to: `.claude-plugin/marketplace.json` pairs with
`.claude-plugin/plugin.json` inside the source directory, and `.cursor-plugin/marketplace.json` pairs
with `.cursor-plugin/plugin.json` inside the source directory. Read both with the same
read-parse-and-continue shape `scripts/check-manifests.mjs:27-41` uses, so one unreadable file does not
mask the other dimensions.

Implement the six sub-assertions in this order, each with its own `fail('P3', …)` message naming the
file and the offending value: JSON parse; `name` present and matching lowercase kebab-case; `owner.name`
present and non-empty and `plugins` a non-empty array; `plugins[].name` equal to the `name` field of
the manifest reached through that entry's `source`; `plugins[].source` beginning with `./` with no `..`
segment and no backslash and resolving to an existing directory that contains the expected client
manifest; and no `plugins[]` entry carrying any of the five component keys `skills`, `commands`,
`agents`, `hooks`, `mcpServers`.

Do not re-assert three-way `version` equality or MCP-config byte-identity — `check-manifests.mjs`
already owns both, and duplicating them would give two files the same contract.

Record P3's limitation in its header entry, in the honest style P1 already uses: Claude Code also
rejects a marketplace whose `name` collides with a reserved name, and that list is re-checked by the
client on every load and is not enumerated here — P3 asserts shape and resolution, not name
availability.

Demonstrate red before green. P3 has no natural pre-fix failure, so make the failure deliberately:
copy the repository tree to a scratch directory, point `plugins[0].source` in the scratch copy's
`.claude-plugin/marketplace.json` at a directory that does not exist, run the script from that
directory using the `process.cwd()` seam, and capture the `P3:` failure line as evidence. Repeat with a
component key injected into the scratch copy's `plugins[0]` entry to prove the sixth sub-assertion
fires. Delete the scratch copy; commit nothing from it. A guard never seen failing is not a guard.
  </action>
  <verify>
    <automated>node scripts/check-plugin.mjs</automated>
    <fails_when>non-zero exit, or any line matching `^P3: ` printed to stderr against the real tree</fails_when>
    <automated>node scripts/check-plugin.mjs 2>&amp;1 | grep -c -F 'P3'</automated>
    <fails_when>the printed count is not 0</fails_when>
    <automated>node scripts/check-plugin.mjs | grep -c -F 'P3'</automated>
    <fails_when>the printed count is 0 — the success summary line must name P3 among the green dimensions</fails_when>
    <automated>grep -c -E "^import .* from 'node:(fs|path)'" scripts/check-plugin.mjs</automated>
    <fails_when>the printed count is not 2 — the script must import nothing beyond `node:fs` and `node:path`</fails_when>
    <automated>grep -c -F 'import.meta.url' scripts/check-plugin.mjs</automated>
    <fails_when>the printed count is not 0 — every path must resolve through the `process.cwd()` seam</fails_when>
    <automated>grep -c -F 'fetch(' scripts/check-plugin.mjs</automated>
    <fails_when>the printed count is not 0 — no dimension in this plan may make an HTTP request; the one network dimension this phase adds is `--release`-gated and lands in plan 06-07 using `git ls-remote`, never HTTP</fails_when>
    <automated>node scripts/check-manifests.mjs</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/check-plugin.mjs` implements all six P3 sub-assertions over both `.claude-plugin/marketplace.json` and `.cursor-plugin/marketplace.json`, reading fields by key and never by position.
    - Every P3 violation is emitted as a `P3: ` prefixed stderr line naming the file and the offending value.
    - The script still imports only `node:fs` and `node:path`, contains no `import.meta.url`, and makes no HTTP request; every dimension in this plan is pure filesystem.
    - `node scripts/check-plugin.mjs` exits 0 on the real tree and its `ok:` summary names P3.
    - The header block's P3 entry states the reserved-name limitation explicitly.
    - Two red demonstrations are captured in the SUMMARY: a non-resolving `source` and an injected component key, each with its verbatim `P3: ` line and the exit-1 status.
    - No scratch directory remains in `git status --porcelain`, and `scripts/check-manifests.mjs` is byte-unchanged.
  </acceptance_criteria>
  <done>P3 asserts all six marketplace invariants over both files, is green on the real tree, and was demonstrated red twice against deliberate scratch mutations.</done>
  <reversibility rating="reversible">Additive assertions in a guard script; removing the P3 block restores the prior guard surface exactly.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dimension P4 — skill, command, and agent frontmatter against their allowed key sets</name>
  <files>scripts/check-plugin.mjs</files>
  <read_first>
    - `scripts/check-plugin.mjs` as it stands after Task 1 — the header block with P4's reserved contract line, and the P3 read-parse-and-continue shape to reuse.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *Target 6 → P4* — the zero-dependency parsing recipe: require the file's first line to be exactly the three-hyphen delimiter, slice to the next delimiter line, collect top-level keys with a leading-identifier match on non-indented lines only so an indented line belonging to a nested map is not mistaken for a top-level key.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *Target 4* — the authoritative Agent Skills allowed set and the constraint that `name` and `description` are the two required members, `name` must match the parent directory name, and `description` is 1 to 1024 characters.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *Target 3* — the plugin-scope agent key list and the security exclusion of `hooks`, `mcpServers`, and `permissionMode`.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *C-4* — the measured proof that `claude plugin validate --strict` reports none of this, which is why P4 must be hand-rolled.
    - `plugin/skills/jarvis-setup/SKILL.md`, `plugin/skills/jarvis-use/SKILL.md`, `plugin/skills/jarvis-issues/SKILL.md` lines 1-4 — the two-key frontmatter blocks plan 06-02 produced; `jarvis-use`'s `description` is double-quoted because it contains a colon, so the parser must tolerate a quoted value.
    - `plugin/commands/index.md` and `plugin/commands/status.md` lines 1-6 — the four-key command frontmatter plan 06-04 produced.
    - `plugin/agents/jarvis-navigator.md` lines 1-8 — the six-key agent frontmatter plan 06-04 produced.
  </read_first>
  <behavior>
    - A subject file whose first line is not exactly the three-hyphen delimiter fails P4, because Claude Code reads frontmatter only in that case.
    - A subject file with no closing delimiter line fails P4.
    - A skill frontmatter key outside the set `name`, `description`, `license`, `compatibility`, `metadata`, `allowed-tools` fails P4, naming the file and the offending key.
    - A skill missing `name` or `description` fails P4; a skill whose `name` differs from its parent directory name fails P4, naming both; a `description` of zero length or more than 1024 characters fails P4.
    - An indented line inside a nested `metadata` map is not reported as a top-level key.
    - A command frontmatter key outside the command allowed set fails P4; a command missing `name` or `description` fails P4; a command declaring `paths` fails P4.
    - An agent frontmatter key outside the agent allowed set fails P4; an agent declaring `hooks`, `mcpServers`, or `permissionMode` fails P4 with a message naming the plugin-scope security exclusion specifically rather than a generic unknown-key message.
    - The key that plan 06-02 deleted from all three skills is a member of none of the three allowed sets, so its reappearance in any subject file fails P4.
    - On the tree as it stands after plans 06-02 through 06-04, P4 emits no failure.
    - Run against a pre-phase worktree where all three `SKILL.md` still carry that key at line 4, P4 fails three times.
  </behavior>
  <action>
Add dimension P4 to `scripts/check-plugin.mjs`, filling in the contract line reserved for it in plan
06-01's header block. Parse frontmatter without a YAML dependency, exactly as research prescribes:
require the file's first line to be the three-hyphen delimiter, slice to the next delimiter line,
and collect top-level keys by matching a leading identifier followed by a colon on non-indented lines
only — an indented line belongs to a nested map such as `metadata` and is not a top-level key.

Define three allowed sets as module-level constants, each with a comment naming its source of truth:

The skill set is exactly the Agent Skills six: `name`, `description`, `license`, `compatibility`,
`metadata`, `allowed-tools`. Required members are `name` and `description`. Additionally assert that
`name` equals the parent directory name and that `description` has between 1 and 1024 characters.

The command set is the documented Claude Code command frontmatter set, minus `paths`, plus `name`:
`name`, `description`, `argument-hint`, `arguments`, `disable-model-invocation`, `user-invocable`,
`allowed-tools`, `disallowed-tools`, `model`, `effort`, `when_to_use`, `metadata`. Required members are
`name` and `description`. Comment that `name` is included deliberately — Claude Code ignores it in a
command file, but Cursor's documented command frontmatter requires it — and that `paths` is excluded
deliberately so a command can never silently narrow its own applicability.

The agent set is the documented plugin-scope list: `name`, `description`, `model`, `effort`,
`maxTurns`, `tools`, `disallowedTools`, `skills`, `memory`, `background`, `isolation`. Required members
are `name` and `description`. Treat `hooks`, `mcpServers`, and `permissionMode` as a separate
explicitly-forbidden set so their appearance produces a message naming the plugin-scope security
exclusion, not a generic unknown-key message — Claude Code loads a plugin agent even with unparseable
frontmatter, falling back to the filename and a generic description, so this is the only place the
mistake becomes visible.

Enumerate subjects by directory listing rather than a hard-coded file list: every `SKILL.md` under
`plugin/skills/*/`, every markdown file directly under `plugin/commands/`, and every markdown file
directly under `plugin/agents/`. A component directory that does not exist is not a failure — it means
that component is not shipped — but an existing directory containing zero markdown files is a failure,
because an empty component directory is a packaging mistake.

Report every violation through `fail('P4', detail)` naming the relative path and the offending key or
value, and record in P4's header entry that the vendor validator does not cover any of this, so nobody
later deletes P4 believing `claude plugin validate --strict` has it handled.

Demonstrate red before green the natural way: `git worktree add` a scratch worktree at the commit
immediately before this phase began, copy the current `scripts/check-plugin.mjs` into it, run it from
that worktree through the `process.cwd()` seam, and capture the three `P4:` failure lines naming the
three `SKILL.md` files that still carried the deleted key at line 4. Remove the worktree afterwards.
  </action>
  <verify>
    <automated>node scripts/check-plugin.mjs</automated>
    <fails_when>non-zero exit, or any line matching `^P4: ` printed to stderr against the real tree</fails_when>
    <automated>node scripts/check-plugin.mjs | grep -c -F 'P4'</automated>
    <fails_when>the printed count is 0 — the success summary line must name P4 among the green dimensions</fails_when>
    <automated>grep -c -F 'permissionMode' scripts/check-plugin.mjs</automated>
    <fails_when>the printed count is 0 — the plugin-scope security exclusion must be asserted by name</fails_when>
    <automated>grep -c -F 'allowed-tools' scripts/check-plugin.mjs</automated>
    <fails_when>the printed count is 0 — the Agent Skills allowed set must be present in full</fails_when>
    <automated>grep -c -E "^import .* from 'node:(fs|path)'" scripts/check-plugin.mjs</automated>
    <fails_when>the printed count is not 2 — no YAML dependency may be introduced</fails_when>
    <automated>grep -c -F 'fetch(' scripts/check-plugin.mjs</automated>
    <fails_when>the printed count is not 0</fails_when>
    <automated>node scripts/check-manifests.mjs &amp;&amp; node scripts/check-plugin.mjs</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - P4 enumerates its subjects by directory listing and covers every `plugin/skills/*/SKILL.md`, every markdown file directly under `plugin/commands/`, and every markdown file directly under `plugin/agents/`.
    - The three allowed sets are module-level constants, each with a comment naming its source of truth; the skill set has exactly six members and includes `allowed-tools`.
    - The command set includes `name` with a comment explaining why, and excludes `paths`.
    - `hooks`, `mcpServers`, and `permissionMode` are handled as a separate forbidden set whose failure message names the plugin-scope security exclusion.
    - Frontmatter parsing requires the three-hyphen delimiter on line 1, stops at the closing delimiter, and ignores indented lines when collecting top-level keys.
    - An absent component directory produces no failure; an existing component directory with zero markdown files produces a failure.
    - No YAML library is added; the script still imports only `node:fs` and `node:path` and remains free of network calls and spawned processes.
    - `node scripts/check-plugin.mjs` exits 0 on the real tree and its `ok:` summary names P4.
    - The pre-phase-worktree run is captured in the SUMMARY with its three verbatim `P4: ` lines and exit-1 status, and no worktree remains registered.
  </acceptance_criteria>
  <done>P4 enforces the Agent Skills allowed set on all three skills and the documented key sets on the new commands and agent, names the plugin-scope security exclusion explicitly, is green on the real tree, and was demonstrated red three times against the pre-phase worktree.</done>
  <reversibility rating="reversible">Additive assertions; the allowed sets are literals that can be widened deliberately when a client documents a new key.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dimension P5 — every referenced path resolves and the hook script is executable</name>
  <files>scripts/check-plugin.mjs</files>
  <read_first>
    - `scripts/check-plugin.mjs` as it stands after Task 2 — the header block with P5's reserved contract line and the established read-parse-and-continue shape.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *Target 6 → P5* — the subject list and the executable-bit check via the mode bits, plus Claude Code's own troubleshooting entry naming a non-executable script as the reason hooks do not fire.
    - `.planning/phases/06-plugin-skills-enhancement/06-EVIDENCE.md` § *Not caught by CI* item 11 — asset references exist today but nothing keeps them alive.
    - `plugin/.cursor-plugin/plugin.json` in full — the `logo` value, which is plugin-root-relative, plus the `skills`, `mcpServers`, and (after plan 06-03) `hooks` path fields.
    - `.codex-plugin/plugin.json` lines 21 and 41-44 — `skills`, `interface.composerIcon`, and `interface.logo`, all of which are repository-root-relative, not plugin-root-relative. P5 must resolve the two families differently or it will report false failures.
    - `plugin/hooks/hooks.json` and `plugin/hooks/cursor.json` — the two command strings P5 resolves to `plugin/hooks/jarvis-index-status.sh`; the Claude/Codex one contains a plugin-root variable reference that must be resolved to `plugin/` rather than treated as a literal path segment.
    - `plugin/skills/jarvis-setup/SKILL.md` — the single on-demand retrieval command plan 06-02 added, and `plugin/skills/jarvis-setup/references/troubleshooting.md` with its `## Symptoms and fixes` heading, the two halves of the D-10 proof the validation strategy requires.
    - `plugin/skills/jarvis-use/SKILL.md` line 41 and `plugin/skills/jarvis-use/references/tool-roster.md` — the pre-existing instance of the same retrieval pattern, so P5's check covers both skills rather than special-casing the new one.
  </read_first>
  <behavior>
    - Every path-valued field in `plugin/.cursor-plugin/plugin.json` — `logo`, `skills`, `mcpServers`, `hooks` — resolves, relative to `plugin/`, to an existing file or directory; a missing target fails P5 naming the field and the resolved path.
    - `.codex-plugin/plugin.json`'s `skills`, `interface.composerIcon`, and `interface.logo` resolve relative to the repository root; a missing target fails P5.
    - The command string in `plugin/hooks/hooks.json` resolves, after substituting the plugin-root variable with `plugin/`, to an existing file; a missing target fails P5.
    - The command string in `plugin/hooks/cursor.json` resolves, relative to `plugin/`, to the same existing file.
    - `plugin/hooks/jarvis-index-status.sh` exists and its mode carries at least one execute bit; a non-executable file fails P5 with a message naming the hooks-not-firing symptom.
    - Every `references/<file>.md` named by a retrieval command inside any `plugin/skills/*/SKILL.md` exists relative to that skill directory, and contains the heading the retrieval command greps for; a missing file or a missing heading fails P5 naming both.
    - An existing `plugin/commands/` or `plugin/agents/` directory whose markdown files are zero bytes fails P5.
    - On the tree as it stands after plans 06-02 through 06-04, P5 emits no failure.
    - Run against a scratch copy with `plugin/assets/app-icon.png` deleted, P5 fails; run against a scratch copy where the hook script's execute bits are cleared, P5 fails.
  </behavior>
  <action>
Add dimension P5 to `scripts/check-plugin.mjs`, filling in the contract line reserved for it in plan
06-01's header block. Keep the script hermetic and zero-dependency; add `existsSync` and `statSync` to
the existing `node:fs` import rather than importing a new module.

Collect the subject paths from the manifests themselves rather than hard-coding them, so a future field
addition is covered automatically. Handle the two relativity families separately and comment why: every
path field in `plugin/.cursor-plugin/plugin.json` is plugin-root-relative and resolves against
`plugin/`, while `.codex-plugin/plugin.json`'s `skills` and its two `interface` icon fields are
repository-root-relative. Conflating the two is the single most likely way to make P5 report a false
failure.

For the hook configs, read the command string out of each file and resolve it to a filesystem path:
substitute the plugin-root variable reference in the Claude/Codex file with `plugin/`, strip surrounding
quotes, and assert the target exists. Then assert `plugin/hooks/jarvis-index-status.sh` carries at
least one execute bit in its mode, with a failure message naming the symptom a missing bit causes —
hooks silently do not fire — so the next maintainer sees the consequence, not just the assertion.

For the on-demand references, scan each `plugin/skills/*/SKILL.md` for retrieval commands naming a file
under `references/`, assert the named file exists relative to that skill directory, and assert it
contains the heading the retrieval command searches for. That is the mechanical half of D-10: the move
is only real if the pointer resolves and the anchor is there. Apply it to both skills that use the
pattern, not only the new one.

Assert that an existing `plugin/commands/` or `plugin/agents/` directory contains at least one markdown
file and that none of them is zero bytes — an empty component file installs cleanly and does nothing,
which is the worst kind of failure.

Finish the success summary so the `ok:` line names every green dimension in the P-series, in the
multi-dimension style `scripts/verify-build.mjs:239-241` uses.

Demonstrate red before green. P5 has no natural pre-fix failure, so mutate deliberately: copy the tree
to a scratch directory, delete `plugin/assets/app-icon.png` in the copy, run the script from there and
capture the `P5:` failure line; then restore it, clear the execute bits on the copy's hook script, run
again and capture that `P5:` line too. Delete the scratch copy; commit nothing from it.
  </action>
  <verify>
    <automated>node scripts/check-plugin.mjs</automated>
    <fails_when>non-zero exit, or any line matching `^P5: ` printed to stderr against the real tree</fails_when>
    <automated>node scripts/check-plugin.mjs | grep -c -E 'P1.*P3.*P4.*P5'</automated>
    <fails_when>the printed count is 0 — the success summary must name every green P-dimension on one line</fails_when>
    <automated>grep -c -F '0o111' scripts/check-plugin.mjs</automated>
    <fails_when>the printed count is 0 — the executable-bit assertion is absent</fails_when>
    <automated>grep -c -F 'troubleshooting.md' scripts/check-plugin.mjs</automated>
    <fails_when>the printed count is not 0 — reference targets must be discovered by scanning the retrieval commands, not hard-coded</fails_when>
    <automated>grep -c -E "^import .* from 'node:(fs|path)'" scripts/check-plugin.mjs</automated>
    <fails_when>the printed count is not 2</fails_when>
    <automated>grep -c -F 'fetch(' scripts/check-plugin.mjs</automated>
    <fails_when>the printed count is not 0</fails_when>
    <automated>node scripts/check-manifests.mjs &amp;&amp; node scripts/check-plugin.mjs</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>claude plugin validate ./plugin --strict &amp;&amp; claude plugin validate . --strict</automated>
    <fails_when>non-zero exit, or output containing the phrase `unknown hook event`</fails_when>
  </verify>
  <acceptance_criteria>
    - P5 resolves plugin-root-relative fields against `plugin/` and repository-root-relative fields against the repository root, with a comment stating why the two families differ.
    - Subject paths are collected from the manifests and hook configs rather than hard-coded; the script contains no literal `troubleshooting.md`.
    - The hook command string is resolved by substituting the plugin-root variable, and the executable-bit assertion uses the mode-bit mask with a failure message naming the hooks-not-firing symptom.
    - Every `references/` target named by a retrieval command in any `plugin/skills/*/SKILL.md` is asserted to exist and to contain the heading that command searches for — covering both `jarvis-use` and `jarvis-setup`.
    - An existing `plugin/commands/` or `plugin/agents/` directory is asserted to hold at least one non-empty markdown file.
    - The success summary is a single `ok:` line naming P1, P3, P4, and P5.
    - The script still imports only `node:fs` and `node:path`, with no network call and no spawned process.
    - Two red demonstrations are captured in the SUMMARY — deleted asset and cleared execute bits — each with its verbatim `P5: ` line and exit-1 status.
    - No scratch directory remains in `git status --porcelain`; `plugin/assets/app-icon.png` and `plugin/assets/jarvis-small.svg` have no diff against `HEAD`.
  </acceptance_criteria>
  <done>P5 proves every referenced path resolves, the hook script is executable, and both on-demand references land their anchors; it is green on the real tree and was demonstrated red twice.</done>
  <reversibility rating="reversible">Additive assertions in a guard script; no shipped artifact changes.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository content → CI verdict | `scripts/check-plugin.mjs` reads contributor-chosen files and decides whether the job passes |
| manifest path fields → filesystem resolution | P3 and P5 resolve strings from JSON manifests into filesystem paths |
| guard coverage → maintainer belief | a dimension that looks stronger than it is licenses a false sense of safety |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-29 | Tampering | a `plugins[].source` escaping the repository | high | mitigate | P3 rejects any `source` that does not begin `./`, that contains a `..` segment, or that contains a backslash, before resolving it — so a marketplace entry can never point an install at a path outside the repository |
| T-06-30 | Elevation of Privilege | an agent frontmatter re-introducing `hooks`, `mcpServers`, or `permissionMode` | high | mitigate | P4 treats those three as an explicitly forbidden set with a dedicated failure message, closing the silent-degradation path Claude Code's filename fallback would otherwise hide |
| T-06-31 | Tampering | a manifest pointing at a path an attacker could later create | medium | mitigate | P5 asserts every referenced path exists at CI time, so a dangling reference is a red build rather than a slot waiting to be filled |
| T-06-32 | Denial of Service | a non-executable hook script | medium | mitigate | P5's mode-bit assertion, with a failure message naming the hooks-not-firing symptom |
| T-06-33 | Repudiation | P3's shape check mistaken for name-availability validation | medium | mitigate | P3's header entry states that Claude Code's reserved-name list is re-checked by the client on every load and is not duplicated here |
| T-06-34 | Repudiation | trusting the vendor validator to cover frontmatter | high | mitigate | P4's header entry records the measured negative, so nobody deletes the hand-rolled guard believing `claude plugin validate --strict` covers it |
| T-06-35 | Denial of Service | the guard itself reaching the network | low | mitigate | Every dimension in this plan is pure filesystem and sub-second; no HTTP request is possible. The single network dimension this phase adds is `--release`-gated in plan 06-07, so the default PR-time run stays hermetic |
</threat_model>

## Artifacts this phase produces

Symbols and paths created by Phase 6. None exists before this phase, so source-grounding must not
flag them as unresolved references.

| Artifact | Kind | Created by |
|---|---|---|
| `scripts/check-plugin.mjs` | guard script | 06-01 |
| `P1` dimension — roster set-equality + roster-derived snake_case scan | guard dimension | 06-01 |
| `check:plugin` npm alias | `package.json` script | 06-01 |
| `Check plugin surface` CI step | `checks.yml` step | 06-01 |
| `plugin/skills/jarvis-setup/references/troubleshooting.md` | skill reference | 06-02 |
| `plugin/hooks/jarvis-index-status.sh` | POSIX probe script | 06-03 |
| `plugin/hooks/hooks.json` | hook config (Claude + Codex) | 06-03 |
| `plugin/hooks/cursor.json` | hook config (Cursor) | 06-03 |
| Cursor manifest `hooks` path field | manifest key | 06-03 |
| `plugin/commands/index.md` — `/jarvis:index` | slash command | 06-04 |
| `plugin/commands/status.md` — `/jarvis:status` | slash command | 06-04 |
| `plugin/agents/jarvis-navigator.md` | navigator subagent | 06-04 |
| `$schema` key on `plugin/.claude-plugin/plugin.json` | manifest key | 06-04 |
| `Install Claude Code CLI` CI step | `checks.yml` step | 06-05 |
| `Validate plugin + marketplace` CI step | `checks.yml` step | 06-05 |
| `P3` dimension — both `marketplace.json` valid | guard dimension | 06-06 |
| `P4` dimension — skill/command/agent frontmatter allowed-keys | guard dimension | 06-06 |
| `P5` dimension — referenced assets/components exist, hook executable | guard dimension | 06-06 |
| `P2a` dimension — manifest version ↔ `jarvis-mcp>=` floor | guard dimension | 06-07 |
| `P2b` dimension — `blob/<ref>` ↔ `v<version>` + tag existence | guard dimension | 06-07 |
| `--release` flag on `scripts/check-plugin.mjs` | CLI flag | 06-07 |
| `v0.9.1` git tag | release tag | 06-08 |

<verification>
1. `node scripts/check-manifests.mjs && node scripts/check-plugin.mjs` exits 0 from the project root,
   and the `ok:` line names P1, P3, P4, and P5.
2. `claude plugin validate ./plugin --strict && claude plugin validate . --strict` exits 0.
3. `scripts/check-plugin.mjs` imports only `node:fs` and `node:path`, contains no `import.meta.url`,
   and makes no HTTP request; every dimension in this plan is pure filesystem.
4. Five red demonstrations are captured in the SUMMARY: two for P3, one triple for P4 against the
   pre-phase worktree, and two for P5.
5. `git status --porcelain` lists no scratch directory, and `git worktree list` shows no leftover
   pre-phase worktree.
6. `scripts/check-manifests.mjs` is byte-unchanged.
</verification>

<success_criteria>
- D-12(c) / SC-4: P3 covers both marketplace manifests with all six sub-assertions — the first
  assertion of any kind over those two files.
- D-12(d) / SC-4: P4 enforces the Agent Skills allowed set and the documented command and agent key
  sets, hand-rolled because the vendor validator was measured not to cover it.
- SC-3 / SC-4: P5 proves every shipped component and asset reference resolves and the hook script is
  executable, so the new capabilities are structurally verified rather than assumed.
- D-13: every one of P3, P4, and P5 was seen failing before it was trusted.
- No test framework, linter, or formatter introduced; the guard stays hermetic and zero-dependency.
</success_criteria>

<output>
Create `.planning/phases/06-plugin-skills-enhancement/06-06-SUMMARY.md` when done, including all five
verbatim red-before-green captures with their exit statuses.
</output>
