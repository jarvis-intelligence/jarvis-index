---
phase: 06-plugin-skills-enhancement
plan: 08
type: execute
wave: 5
depends_on: ["06-07"]
files_modified: []
autonomous: false
requirements: [D-04, D-15, D-16, SC-2, SC-3, SC-5]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-15 / SC-5: the `v0.9.1` tag exists on the remote and points at the merge commit that carries all three `0.9.1` manifests, the byte-identical `jarvis-mcp>=0.9.1` floors, and the two `blob/v0.9.1/` policy URLs."
    - "D-04 / SC-2: both `.codex-plugin/plugin.json` policy URLs return HTTP 200 after the tag is pushed — the 404 window opened by plan 06-07 is closed, and closed by construction rather than by hope."
    - "D-15 / SC-4: `node scripts/check-plugin.mjs --release` exits 0 after the tag is pushed, which is the first moment it can, and every always-on dimension P1, P2a, P3, P4, P5 is green alongside it."
    - "D-15 / SC-5: the Cursor submission checklist passes — unique lowercase kebab-case `name` identical across `.cursor-plugin/marketplace.json` and `plugin/.cursor-plugin/plugin.json`, every path field relative with no parent-directory segment, the referenced logo committed, and `plugin/README.md` documenting usage."
    - "SC-3: the manual client verifications the validation strategy names as unprovable in CI are either performed and recorded, or recorded as outstanding with the reason — never silently skipped."
  artifacts: []
  key_links:
    - "The tag must point at the merge commit, not at a branch head: the commit carrying the `blob/v0.9.1/` URLs is the commit those URLs must resolve against, and that is the only ordering in which they ever return 200."
    - "`node scripts/check-plugin.mjs --release` is the single command that re-checks the ref half and the tag half together. Run it after the push, never before — and never from CI, where a pull request cannot make its own future tag exist."
    - "Research measured the failure mode directly: the identical URL shape returned 404 for a tag that did not exist and 200 for one that did. The 404s observed between merge and tag push are expected, not a defect."
    - "A pushed tag is not cleanly retractable — it is already fetched by anyone who pulled. That is why Task 1 is a decision checkpoint rather than an automatic step."
---

<objective>
Close the phase: push the `v0.9.1` tag at the merge commit, prove the two in-manifest policy URLs now
resolve, run the release-gated guard for the first time in the only state where it can pass, and take
the Cursor submission checklist plus the manual client verifications that CI structurally cannot do.

Purpose: every earlier plan produced a shippable tree; none of them could make a tag exist. Until the
tag is pushed, `.codex-plugin/plugin.json` carries two URLs that 404 — the phase's own D-04 violation,
deliberately created and deliberately scheduled to close here.

Output: a pushed `v0.9.1` tag, a green `--release` run, two HTTP 200 probes, and a recorded verdict for
each manual verification.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-plugin-skills-enhancement/06-CONTEXT.md
@.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md
@.planning/phases/06-plugin-skills-enhancement/06-VALIDATION.md
@.planning/phases/06-plugin-skills-enhancement/06-07-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decision — push the v0.9.1 tag, which cannot be cleanly retracted</name>
  <read_first>
    - `.planning/phases/06-plugin-skills-enhancement/06-07-SUMMARY.md` — confirmation that all three manifests declare `0.9.1`, that both MCP configs carry the byte-identical floor, and that both policy URLs already reference the `v0.9.1` tag and therefore 404 right now.
    - `.planning/phases/06-plugin-skills-enhancement/06-CONTEXT.md` D-15 and D-16 — the synchronized-release definition of done and the user's resolution of the version target, including the accepted semver cost and the accepted coupling to the server's release train.
    - `.planning/ROADMAP.md` Phase 4 plan 04-02 — the prior synchronized-release precedent, where the tag was created and pushed only after validation passed.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *Target 5* — the four-step sequence and the verified 404-versus-200 evidence behind it.
  </read_first>
  <decision>Create the annotated tag `v0.9.1` at the merge commit on `main` and push it to the remote — the phase's one irreversible act.</decision>
  <context>
`gate="blocking-human"` is deliberate: `workflow.auto_advance` is `true` in this project, and a
`blocking` decision checkpoint would auto-select the first option. Auto-selecting a tag push is exactly
the mistake this gate exists to prevent, so it must stop for a human in every mode.

Why it is one-way: once pushed, the tag is fetchable by anyone and is resolved by the marketplace
surfaces that follow `blob/v0.9.1/` paths. Deleting a published tag does not un-publish it; the
correction path is a new version, not a revert.

What the push buys: it is the only thing that makes the two `.codex-plugin/plugin.json` policy URLs
resolve. Both return 404 right now, by construction — plan 06-07 wrote them pointing at a tag that does
not exist yet, because the commit carrying those URLs is necessarily the commit the tag must point at.
Research verified the mechanism directly: the identical URL shape returns 404 for a missing ref and 200
for an existing one. So leaving the tag unpushed does not preserve optionality; it leaves two dead links
on a marketplace-facing manifest and keeps D-04 open.

Four preconditions the operator must confirm before approving: the phase branch is merged to `main`;
the merge commit contains all three manifests at `0.9.1`, both MCP configs byte-identical at the
`jarvis-mcp>=0.9.1` floor, and both policy URLs at the `blob/v0.9.1/` ref; `node
scripts/check-manifests.mjs` and `node scripts/check-plugin.mjs` both exit 0 at that commit; and no tag
named `v0.9.1` already exists on the remote — the automated probe below asserts the last one.

Changing the version number is not a variation on this decision. It re-opens D-16, which the user
already resolved to `0.9.1`, and requires re-running plan 06-07 with the new number, because the URLs,
the floors, and dimension P2a all key off it.

No tag is created and nothing is pushed in this task.
  </context>
  <options>
    <option id="push">
      <name>Approve — create and push `v0.9.1` at the merge commit</name>
      <pros>Closes D-04: both policy URLs start returning 200 and Task 2 can prove it. Satisfies D-15's definition of done, that the tag the manifests reference is created and pushed. Makes the release-gated dimension P2b passable for the first time. Matches the Phase 4 precedent, where the tag was pushed only after validation passed.</pros>
      <cons>Irreversible: the tag is immediately fetchable and cannot be meaningfully withdrawn. Publishes `0.9.1` as the plugin version, accepting the semver cost D-16 named — three new capability surfaces shipping under a patch number — and the coupling of the plugin release train to the server's.</cons>
    </option>
    <option id="hold">
      <name>Decline — stop the phase before tagging</name>
      <pros>Nothing is published; the version number stays re-openable at zero cost. Appropriate if any of the four preconditions is unconfirmed, or if the operator wants to revisit D-16.</pros>
      <cons>D-04 stays open with two 404 URLs shipped on the Codex manifest, which is worse than the pre-phase state where they pointed at a different non-existent tag. SC-2 and SC-5 both stay unmet and the phase cannot be called complete. Tasks 2 and 3 do not run.</cons>
    </option>
  </options>
  <resume-signal>Select: push or hold</resume-signal>
  <verify>
    <automated>git log --oneline -1 main</automated>
    <fails_when>the printed commit is not the merge commit carrying this phase's work</fails_when>
    <automated>git ls-remote --tags origin refs/tags/v0.9.1</automated>
    <fails_when>any output is printed — a tag with this name already exists on the remote, so the release must be re-planned rather than pushed over</fails_when>
    <human-check>Operator has confirmed the branch is merged, that the merge commit carries all three 0.9.1 manifests plus the byte-identical floors plus both tag-referencing policy URLs, that both Node guards exit 0 at that commit, and has explicitly approved or declined pushing the irreversible v0.9.1 tag.</human-check>
  </verify>
  <acceptance_criteria>
    - The operator has been shown the irreversibility, the benefit, the four preconditions, and the alternative, and has given an explicit approve-or-decline answer.
    - `git ls-remote --tags origin refs/tags/v0.9.1` prints nothing, confirming the tag name is free.
    - No tag was created and nothing was pushed during this task.
    - If the operator declined, the SUMMARY records the decline, states that D-04 remains open with two dead URLs on the Codex manifest, and Tasks 2 and 3 do not run.
  </acceptance_criteria>
  <done>The operator has explicitly approved or declined the tag push, with all four preconditions confirmed and the tag name verified free.</done>
  <reversibility rating="one-way">Pushing a git tag publishes an immutable reference that third parties and marketplace surfaces can already have fetched. Deletion does not undo distribution; the correction path is a new version. This checkpoint exists because of exactly that.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Create and push v0.9.1, then prove the release gate and both URLs</name>
  <files></files>
  <precondition>Task 1's decision checkpoint returned an approval, the phase branch is merged to `main`, and `git ls-remote --tags origin refs/tags/v0.9.1` printed nothing.</precondition>
  <read_first>
    - `.planning/phases/06-plugin-skills-enhancement/06-07-SUMMARY.md` — the exact merge-commit contents the tag must point at, and the recorded P2b red capture this task turns green.
    - `.codex-plugin/plugin.json` — the two policy URLs to probe, read verbatim so the probe uses the shipped strings rather than reconstructed ones.
    - `scripts/check-plugin.mjs` — the release flag and environment override P2b reads, and the summary format a release-flagged run prints.
    - `.planning/phases/06-plugin-skills-enhancement/06-VALIDATION.md` § *Per-Task Verification Map → Release synchronization* — the three conditions that together define a verified release: the manifest guard green, the tag present in the remote tag list, and both URL probes returning 200.
    - `.planning/STATE.md` § *Blockers/Concerns* — the Phase 1 finding that a workflow dispatched before a push runs against a stale remote ref, and the note that `gh` needs the admin account for this repository. Both bear on ordering here: push first, then verify.
  </read_first>
  <action>
Create an annotated tag named `v0.9.1` pointing at the merge commit on `main` — not at a branch head and
not at a local commit that has not been pushed. Give it a conventional message naming the release
contents in one line: the 10-tool surface realignment, the new commands, hook, and navigator agent, the
five guard dimensions, and the `jarvis-mcp>=0.9.1` floor. Push the tag to the remote.

Then, and only then, run the release gate: `node scripts/check-plugin.mjs --release`. This is the first
moment it can pass. P2b's ref half was already satisfied by plan 06-07's edits; its tag half becomes
satisfiable the instant the push lands. Capture the green output as the counterpart to plan 06-07's red
capture — together they are the D-13 evidence for P2b.

Probe both `.codex-plugin/plugin.json` policy URLs for HTTP 200, following redirects, reading the URL
strings out of the manifest rather than retyping them. Both returned 404 from the moment plan 06-07
committed them; both must return 200 now. If either does not, do not paper over it — the likely causes
are a tag pointing at the wrong commit or a path that does not exist at that tag, and both are real
defects in the release rather than probe flakiness.

Re-run the always-on guards at the tagged commit to confirm nothing regressed: `node
scripts/check-manifests.mjs`, `node scripts/check-plugin.mjs`, and both `claude plugin validate
--strict` invocations.

Edit no file in this task. If a probe fails, stop and report rather than adjusting a URL — a URL edit
would need a new commit, which would no longer be the commit the tag points at, which is the whole
ordering problem this plan exists to respect.
  </action>
  <verify>
    <automated>git ls-remote --tags origin refs/tags/v0.9.1</automated>
    <fails_when>no output is printed — the tag is absent from the remote</fails_when>
    <automated>node scripts/check-plugin.mjs --release</automated>
    <fails_when>non-zero exit, or any line matching `^P[0-9]` printed to stderr</fails_when>
    <automated>node scripts/check-plugin.mjs --release | grep -c -F 'P2b'</automated>
    <fails_when>the printed count is 0 — the release run must report P2b explicitly, not silently skip it</fails_when>
    <automated>curl -o /dev/null -s -w '%{http_code}' -L https://github.com/jarvis-intelligence/jarvis-index/blob/v0.9.1/plugin/README.md</automated>
    <fails_when>the printed status code is not 200</fails_when>
    <automated>curl -o /dev/null -s -w '%{http_code}' -L https://github.com/jarvis-intelligence/jarvis-index/blob/v0.9.1/plugin/LICENSE</automated>
    <fails_when>the printed status code is not 200</fails_when>
    <automated>node scripts/check-manifests.mjs &amp;&amp; node scripts/check-plugin.mjs</automated>
    <fails_when>non-zero exit, or any line matching `^P[0-9]` printed to stderr</fails_when>
    <automated>claude plugin validate ./plugin --strict &amp;&amp; claude plugin validate . --strict</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>cmp plugin/mcp.json plugin/.mcp.json</automated>
    <fails_when>non-zero exit, or any `differ` output</fails_when>
    <automated>git status --porcelain</automated>
    <fails_when>any output is printed — this task edits no file</fails_when>
  </verify>
  <acceptance_criteria>
    - An annotated tag `v0.9.1` exists on the remote and resolves to the merge commit on `main` carrying this phase's work.
    - `node scripts/check-plugin.mjs --release` exits 0 and its output names P2b among the reported dimensions.
    - Both `.codex-plugin/plugin.json` policy URLs return HTTP 200, probed using the strings read out of the manifest.
    - `node scripts/check-manifests.mjs`, `node scripts/check-plugin.mjs`, and both `claude plugin validate --strict` runs all exit 0 at the tagged commit.
    - `cmp plugin/mcp.json plugin/.mcp.json` exits 0.
    - `git status --porcelain` prints nothing — no file was edited.
    - The SUMMARY records the green `--release` output beside plan 06-07's red capture, and both HTTP 200 results with their probed URLs.
  </acceptance_criteria>
  <done>The `v0.9.1` tag is pushed at the merge commit, the release gate passes for the first time, both previously-404 policy URLs return 200, and every always-on guard is green at the tagged commit.</done>
  <reversibility rating="one-way">The tag push itself; gated by Task 1's checkpoint. Everything else in this task is read-only verification.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Cursor submission checklist and the client verifications CI cannot do</name>
  <files></files>
  <precondition>Task 2 completed: the `v0.9.1` tag is present on the remote and `node scripts/check-plugin.mjs --release` exits 0.</precondition>
  <read_first>
    - `.planning/phases/06-plugin-skills-enhancement/06-VALIDATION.md` § *Manual-Only Verifications* — the seven rows that define this task's scope, each with its reason for being manual and its test instructions.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *Validation Architecture → "What CANNOT be validated in CI"* — the same seven items with their measured justifications, including that `codex plugin` has no validate subcommand, that no Cursor IDE is installed on this machine, and that OpenAI publishes no schema for the `openai.yaml` `interface:` block.
    - `.planning/phases/06-plugin-skills-enhancement/06-CONTEXT.md` D-15 — the Cursor submission checklist as the release definition of done.
    - `.cursor-plugin/marketplace.json` and `plugin/.cursor-plugin/plugin.json` — the two files whose `name` must be identical lowercase kebab-case, and the path fields that must all be relative with no parent-directory segment.
    - `plugin/assets/app-icon.png` — the committed logo the Cursor manifest references.
    - `plugin/README.md` — the usage documentation the checklist requires, now including the hook subsection and its three caveats.
  </read_first>
  <action>
Walk the Cursor submission checklist and record a verdict for each item: the plugin `name` is unique,
lowercase kebab-case, and identical between `.cursor-plugin/marketplace.json` and
`plugin/.cursor-plugin/plugin.json`; every path field in the Cursor manifest — logo, skills, mcpServers,
hooks — is relative, contains no parent-directory segment, and resolves; the referenced logo is
committed rather than generated or ignored; and `plugin/README.md` documents usage. Dimension P3 and
dimension P5 already assert the mechanical halves, so this pass is the human confirmation plus the two
items no guard can reach: uniqueness on the marketplace, and whether the README reads as usable
documentation to a reviewer.

Then present the six client verifications that are structurally unprovable in CI, and record for each
either a performed result or an explicit outstanding status with its blocker. Do not mark any of them
passed on inference.

First, whether `/jarvis:index` and `/jarvis:status` actually appear and run in Claude Code's command
surface and in Cursor's, after a real marketplace install. CI can prove the frontmatter parses; it
cannot prove a client renders the entry.

Second, whether the SessionStart hook actually injects its context into a live session: install, open a
fresh session in a repository with a stale index, confirm the staleness note appears, then repeat in a
repository with no index at all and confirm silence.

Third, Codex end to end. `codex plugin` has no validate subcommand, so nothing in CI touches the Codex
overlay. The open question is whether a marketplace install sourced at the plugin directory resolves the
plugin root there — in which case the hook file is discovered — or resolves it to the repository root,
in which case it is not. Codex additionally skips plugin-bundled hooks until a user reviews and trusts
them, so even a live run needs a human trust action. Record the answer if a live install is available;
otherwise record it as outstanding, and confirm `plugin/README.md`'s caveat still matches the state of
knowledge.

Fourth, any Cursor claim at all. No Cursor IDE is installed on this machine and no Cursor plugin
validator CLI exists; submission is manual review. Route this to a human on a Cursor machine or record
it as outstanding.

Fifth, whether the navigator subagent produces useful multi-hop answers. Structural load is already
guarded; answer quality is human judgement. Ask an architecture question spanning definition, then call
hierarchy, then blast radius against an indexed repository, and record the verdict.

Sixth, whether the `interface:` nesting in `plugin/skills/*/agents/openai.yaml:1-4` is a real OpenAI
skill-manifest shape or inert decoration. This is research open question 6, carried forward rather
than resolved: OpenAI's docs show `dependencies.tools` in `agents/openai.yaml` but never the
`interface.display_name` / `short_description` / `default_prompt` block these three files ship, and
OpenAI publishes no schema and no validator for it — so neither CI, nor `claude plugin validate`, nor
a local run can decide it. The stubs ship unchanged this phase per D-08 and no P-dimension reads them.
Record it as outstanding with that blocker unless a live OpenAI skills install is available to answer
it; do not mark it passed because the files parse as YAML.

Write every verdict into the plan SUMMARY as a table — item, verdict, evidence or blocker — so the phase
closes with an honest record rather than an implied pass. An item recorded as outstanding with its
blocker is an acceptable outcome; an item silently omitted is not. `workflow.human_verify_mode` is
`end-of-phase` in this project, so the human-facing half of this task is carried in the `<human-check>`
block below rather than as a mid-flight halt: the verifier harvests it at end-of-phase into the phase
UAT record, which is where all six client verifications belong anyway — they need a real install, not
a paused executor.
  </action>
  <verify>
    <automated>node --input-type=module -e "import{readFileSync}from'node:fs';const a=JSON.parse(readFileSync('.cursor-plugin/marketplace.json','utf8'));const b=JSON.parse(readFileSync('plugin/.cursor-plugin/plugin.json','utf8'));if(a.name!==b.name)process.exit(1);if(!/^[a-z0-9]+(-[a-z0-9]+)*$/.test(a.name))process.exit(1)"</automated>
    <fails_when>non-zero exit — the two Cursor `name` values differ, or the value is not lowercase kebab-case</fails_when>
    <automated>git ls-files --error-unmatch plugin/assets/app-icon.png</automated>
    <fails_when>non-zero exit — the referenced logo is not committed</fails_when>
    <automated>grep -c -F '..' plugin/.cursor-plugin/plugin.json</automated>
    <fails_when>the printed count is not 0 — no Cursor path field may contain a parent-directory segment</fails_when>
    <automated>node scripts/check-plugin.mjs --release</automated>
    <fails_when>non-zero exit, or any line matching `^P[0-9]` printed to stderr</fails_when>
    <human-check>Every Cursor submission-checklist item has a recorded verdict, and each of the six CI-unprovable client verifications — commands appearing in Claude Code and Cursor, the hook injecting context and staying silent when unindexed, Codex end-to-end plugin-root resolution and hook trust, any Cursor claim, navigator answer quality, and whether the `openai.yaml` `interface:` nesting is a real OpenAI shape — is recorded as either performed with its result or outstanding with its named blocker. No item is omitted and none is marked passed on inference.</human-check>
  </verify>
  <acceptance_criteria>
    - The Cursor `name` is identical in `.cursor-plugin/marketplace.json` and `plugin/.cursor-plugin/plugin.json` and matches lowercase kebab-case.
    - `plugin/assets/app-icon.png` is tracked by git.
    - `plugin/.cursor-plugin/plugin.json` contains no parent-directory segment in any value.
    - `node scripts/check-plugin.mjs --release` exits 0.
    - The SUMMARY carries a verdict table with one row per Cursor checklist item and one row per CI-unprovable client verification, each row naming either the evidence or the blocker.
    - No row is marked passed without evidence, and no row is absent.
    - If the Codex or Cursor rows are outstanding, the SUMMARY confirms that `plugin/README.md`'s caveats still match the state of knowledge.
    - The `openai.yaml` `interface:` row (research open question 6) is present and, unless a live OpenAI skills install answered it, is recorded as outstanding with the no-published-schema/no-validator blocker.
  </acceptance_criteria>
  <done>The Cursor submission checklist has a per-item verdict, and all six CI-unprovable client verifications are recorded as performed-with-result or outstanding-with-blocker — none inferred, none omitted.</done>
  <reversibility rating="reversible">A verification and recording pass; it changes no artifact.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repository → public remote | pushing a tag publishes an immutable reference that third parties and marketplace surfaces resolve |
| published tag → marketplace reviewers and users | `blob/v0.9.1/` policy URLs are the links a reviewer and a user follow to read the terms they accept |
| remote refs → release verdict | the release gate asks the remote for its tag list and probes two public URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-43 | Tampering | a tag pushed at the wrong commit | high | mitigate | Task 1 requires the operator to confirm the merge commit's contents before approving; Task 2 tags the merge commit explicitly and then proves the choice by probing both URLs at that tag for HTTP 200 — a tag on the wrong commit fails that probe |
| T-06-44 | Spoofing | published policy URLs that do not resolve | high | mitigate | Both URLs are probed for 200 after the push, using the strings read out of the manifest rather than retyped, and a failing probe halts the release instead of being patched around |
| T-06-45 | Repudiation | overwriting an existing `v0.9.1` tag | high | mitigate | Task 1 asserts the remote has no ref by that name before approval; a pre-existing tag forces a re-plan rather than a force-push |
| T-06-46 | Repudiation | recording an unprovable client behaviour as verified | medium | mitigate | Task 3 requires a per-item verdict table where an outstanding item must name its blocker, and forbids marking any item passed on inference |
| T-06-47 | Tampering | a file edited between merge and tag so the tag no longer matches the reviewed tree | medium | mitigate | Task 2 edits no file and asserts a clean working tree; a failing URL probe is reported rather than fixed in place, precisely because a fix would move the tree off the tagged commit |
| T-06-48 | Information Disclosure | the release tag message | low | accept | It names shipped capability surfaces already public in the repository |
</threat_model>

## Artifacts this phase produces

Symbols and paths created by Phase 6. None exists before this phase, so source-grounding must not
flag them as unresolved references.

| Artifact | Kind | Created by |
|---|---|---|
| `scripts/check-plugin.mjs` | guard script | 06-01 |
| `P1` dimension — roster set-equality + roster-derived snake_case scan | guard dimension | 06-01 |
| `check:plugin` npm alias | `package.json` script | 06-01 |
| `Check plugin surface` CI step | `checks.yml` step | 06-01 |
| `plugin/skills/jarvis-setup/references/troubleshooting.md` | skill reference | 06-02 |
| `plugin/hooks/jarvis-index-status.sh` | POSIX probe script | 06-03 |
| `plugin/hooks/hooks.json` | hook config (Claude + Codex) | 06-03 |
| `plugin/hooks/cursor.json` | hook config (Cursor) | 06-03 |
| Cursor manifest `hooks` path field | manifest key | 06-03 |
| `plugin/commands/index.md` — `/jarvis:index` | slash command | 06-04 |
| `plugin/commands/status.md` — `/jarvis:status` | slash command | 06-04 |
| `plugin/agents/jarvis-navigator.md` | navigator subagent | 06-04 |
| `$schema` key on `plugin/.claude-plugin/plugin.json` | manifest key | 06-04 |
| `Install Claude Code CLI` CI step | `checks.yml` step | 06-05 |
| `Validate plugin + marketplace` CI step | `checks.yml` step | 06-05 |
| `P3` dimension — both `marketplace.json` valid | guard dimension | 06-06 |
| `P4` dimension — skill/command/agent frontmatter allowed-keys | guard dimension | 06-06 |
| `P5` dimension — referenced assets/components exist, hook executable | guard dimension | 06-06 |
| `P2a` dimension — manifest version ↔ `jarvis-mcp>=` floor | guard dimension | 06-07 |
| `P2b` dimension — repository URL ref ↔ `v<version>` + tag existence | guard dimension | 06-07 |
| `--release` flag and its environment override on `scripts/check-plugin.mjs` | CLI flag | 06-07 |
| `v0.9.1` git tag | release tag | 06-08 |

<verification>
1. `git ls-remote --tags origin refs/tags/v0.9.1` prints exactly one ref, resolving to the merge commit.
2. `node scripts/check-plugin.mjs --release` exits 0 and reports P2b.
3. Both `.codex-plugin/plugin.json` policy URLs return HTTP 200.
4. `node scripts/check-manifests.mjs && node scripts/check-plugin.mjs` exits 0 at the tagged commit.
5. `claude plugin validate ./plugin --strict && claude plugin validate . --strict` exits 0.
6. `cmp plugin/mcp.json plugin/.mcp.json` exits 0.
7. `git status --porcelain` prints nothing.
8. The SUMMARY carries the manual-verification verdict table with no omitted and no inferred rows.
</verification>

<success_criteria>
- D-15 / SC-5: one synchronized release — three manifests agreeing, both MCP configs byte-identical,
  the Cursor checklist walked, and the referenced tag created and pushed.
- D-04 / SC-2: both in-manifest policy URLs return 200; no shipped URL 404s.
- D-16: the release ships as `v0.9.1`, mirroring the server version, per the user's resolution.
- SC-3: the manual client verifications are recorded with evidence or with a named blocker.
- No file is edited in this plan; no test framework, linter, or formatter introduced; `setup.sh` and
  the `../jarvis` repo untouched.
</success_criteria>

<output>
Create `.planning/phases/06-plugin-skills-enhancement/06-08-SUMMARY.md` when done, including the green
`--release` output, both HTTP 200 probe results, and the manual-verification verdict table.
</output>
