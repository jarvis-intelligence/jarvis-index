---
phase: 06-plugin-skills-enhancement
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - plugin/skills/jarvis-setup/SKILL.md
  - plugin/skills/jarvis-setup/references/troubleshooting.md
  - plugin/skills/jarvis-use/SKILL.md
  - plugin/skills/jarvis-issues/SKILL.md
  - .claude/CLAUDE.md
  - docs/code-standards.md
autonomous: true
requirements: [D-02, D-03, D-05, D-10, D-11, D-18, SC-1, SC-2]
estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-11: each of the three `SKILL.md` frontmatter blocks contains exactly the keys `name` and `description` — a subset of the Agent Skills allowed set `{name, description, license, compatibility, metadata, allowed-tools}` — and no key was relocated under `metadata`."
    - "D-11 / research C-8: neither `.claude/CLAUDE.md` nor `docs/code-standards.md` still instructs a contributor to add the off-spec third frontmatter key, so the convention docs and the new guard agree."
    - "D-02 / SC-1: `jarvis-setup` names `indexRepo` as the recovery tool a missing-index error returns, and documents `universal-ctags` as an index-time prerequisite for `sym:` queries whose recovery command uses the flag value `ctags`."
    - "D-02 / research C-6: `jarvis-use` states that of the four symbol-taking navigation tools only `goToDefinition` accepts a `syntax:` identifier, and that `documentSymbols` takes `(repo, path)` and produces such identifiers rather than consuming them."
    - "D-10: `jarvis-setup`'s troubleshooting table lives in `plugin/skills/jarvis-setup/references/troubleshooting.md` and `SKILL.md` reaches it through one exact on-demand retrieval command in the `plugin/skills/jarvis-use/SKILL.md:41` style."
    - "D-03 / SC-2: every `jarvis-mcp` prose pin inside the three skills and the new reference file states the `0.9.1` floor as a `>=` floor."
    - "D-05: the installer command at `plugin/skills/jarvis-setup/SKILL.md:22` still points at `raw.githubusercontent.com/.../main/setup.sh` — unchanged, by design, per the Phase 4 precedent."
    - "D-18: each of `jarvis-issues`' five documented limitations has been re-read against 0.9.1 server source and either survives verbatim or is corrected, with the settled-decision-plus-rationale shape preserved."
  artifacts:
    - plugin/skills/jarvis-setup/SKILL.md
    - plugin/skills/jarvis-setup/references/troubleshooting.md
    - plugin/skills/jarvis-use/SKILL.md
    - plugin/skills/jarvis-issues/SKILL.md
    - .claude/CLAUDE.md
    - docs/code-standards.md
  key_links:
    - "`plugin/skills/jarvis-use/SKILL.md:41` is the retrieval-command pattern `.claude/CLAUDE.md:141` mandates; the new `references/troubleshooting.md` link must mirror its exact shape, and :41 itself must stay unchanged."
    - "Plan 06-06's dimension P4 rejects any frontmatter key outside the Agent Skills allowed set. If the convention docs keep mandating the removed key, the next contributor re-adds exactly what P4 fails on (research C-8)."
    - "`plugin/skills/jarvis-use/SKILL.md:60` and `references/tool-roster.md:24` cite upstream `scip` `v0.9.0` — a third-party version, not a jarvis pin. Rewriting it would be wrong and would not satisfy any decision."
---

<objective>
Make the three shipped skills true to jarvis 0.9.1 and conformant to the Agent Skills specification:
strip the off-spec frontmatter key, fix the two convention docs that mandate it, move `jarvis-setup`'s
troubleshooting table behind an on-demand reference, teach the 0.9.1 behaviours the 0.9.0-era copy
predates, bump the prose `jarvis-mcp` floors, and re-verify `jarvis-issues`' documented limitations
against current server source.

Purpose: a skill is instruction text an agent executes. Stale limitations make `jarvis-issues` file
false bug reports; a missing `indexRepo` mention leaves `jarvis-setup` with no recovery path for the
error the server actually returns; an off-spec frontmatter key is a hard error on claude.ai upload.

Output: three spec-conformant, 0.9.1-true SKILL.md files, one new on-demand reference, and two
convention docs that no longer contradict the guard landing in plan 06-06.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-plugin-skills-enhancement/06-CONTEXT.md
@.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md
@.planning/phases/06-plugin-skills-enhancement/06-PATTERNS.md
@.planning/phases/06-plugin-skills-enhancement/06-01-SUMMARY.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip the off-spec frontmatter key from all three skills and fix the two convention docs that mandate it</name>
  <files>plugin/skills/jarvis-setup/SKILL.md, plugin/skills/jarvis-use/SKILL.md, plugin/skills/jarvis-issues/SKILL.md, .claude/CLAUDE.md, docs/code-standards.md</files>
  <read_first>
    - `plugin/skills/jarvis-setup/SKILL.md` lines 1-5 — the frontmatter block; line 4 is the subject and line 5 is the closing `---`.
    - `plugin/skills/jarvis-use/SKILL.md` lines 1-5 — same shape; note `description` at :3 is double-quoted because it contains a colon (`.claude/CLAUDE.md:130`), and that quoting must survive.
    - `plugin/skills/jarvis-issues/SKILL.md` lines 1-5 — same shape, unquoted `description`.
    - `.claude/CLAUDE.md` lines 127-147 — the `## Manifest & Skill Conventions` and `## Markdown & Docs Conventions` sections. Line 129 is the skill-file-shape bullet, line 136 the frontmatter bullet. Line 131 legitimately discusses the manifest delivery mechanism and must not be touched.
    - `.claude/CLAUDE.md` lines 263-273 — the `## Key Abstractions` block; line 269 is the skill pattern line.
    - `docs/code-standards.md` lines 116-150 — `### Cursor-specific constraints` (line 123 is already correct and needs no edit) and `## Skills → ### File shape`, whose paragraph at :137 is the source both `.claude/CLAUDE.md` bullets cite.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *Target 4 — SKILL.md frontmatter (D-11)* for the authoritative allowed set and the verbatim claude.ai rejection error, and § *C-8* for why the convention docs are in scope.
  </read_first>
  <action>
Delete line 4 of each of the three `SKILL.md` files — the off-spec frontmatter key whose value is the
string `0.1.0`. Do not relocate it under `metadata`: that would mint a fourth version string with no
consumer, which plan 06-06's dimension P4 and plan 06-07's dimension P2a would then have to track for
nothing (D-11 as amended). After the edit each file's frontmatter block must open on line 1 with
`---`, carry exactly the two keys `name` and `description`, and close on line 4 with `---`. Preserve
`jarvis-use`'s double-quoted `description` byte-for-byte.

In `docs/code-standards.md` § *Skills → File shape*, rewrite the paragraph that currently mandates a
third frontmatter key so it instead states the authoritative Agent Skills allowed set — `name`,
`description`, `license`, `compatibility`, `metadata`, `allowed-tools` — notes that `name` and
`description` are the two required members and the two jarvis skills use, and keeps the existing
sentence that `description` is the trigger surface because it is the string the agent matches against.
Add one clause recording why the off-spec key was removed rather than relocated: claude.ai upload and
the Skills API hard-error on an unexpected frontmatter key, emitting `Unexpected key(s) in SKILL.md
frontmatter`. Leave the ASCII file-shape tree at `:130-135` and the already-correct Cursor bullet at
`:123` untouched.

In `.claude/CLAUDE.md`, bring the three affected lines into agreement with that paragraph:
the skill-file-shape bullet (currently line 129), the docs/skills frontmatter bullet (currently line
136), and the `## Key Abstractions` skill pattern line (currently line 269). Each must name only the
keys the spec allows. Change nothing else in the file — in particular leave line 131's manifest
delivery rule and line 132's MCP-config rule alone, and do not attempt a wider refresh: the stale
`>=0.6.0` floor lines and the stale nine-tool line at `:204` are explicitly out of scope because
`.claude/**` is not in the `checks.yml` path filter and no guard reads them (research § *Project
Constraints*).
  </action>
  <verify>
    <automated>grep -c '^version' plugin/skills/jarvis-setup/SKILL.md plugin/skills/jarvis-use/SKILL.md plugin/skills/jarvis-issues/SKILL.md</automated>
    <fails_when>any printed line ends in a count other than 0</fails_when>
    <automated>head -4 plugin/skills/jarvis-use/SKILL.md | tail -1</automated>
    <fails_when>the printed line is not exactly `---`</fails_when>
    <automated>grep -c -F 'Frontmatter carries `name`, `description`, and `version`.' docs/code-standards.md</automated>
    <fails_when>the printed count is not 0</fails_when>
    <automated>grep -c -F 'allowed-tools' docs/code-standards.md</automated>
    <fails_when>the printed count is 0</fails_when>
    <automated>grep -c -F '`name` + `description` + `version`' .claude/CLAUDE.md</automated>
    <fails_when>the printed count is not 0</fails_when>
    <automated>grep -c -F 'skills carry `name`/`description`/`version`' .claude/CLAUDE.md</automated>
    <fails_when>the printed count is not 0</fails_when>
    <automated>grep -c -F '`description` trigger surface, `version`' .claude/CLAUDE.md</automated>
    <fails_when>the printed count is not 0</fails_when>
    <automated>grep -c -F 'Version bump is the delivery mechanism' .claude/CLAUDE.md</automated>
    <fails_when>the printed count is not 1 — the manifest delivery rule must survive untouched</fails_when>
    <automated>node scripts/check-plugin.mjs</automated>
    <fails_when>non-zero exit, or any line matching `^P1: ` printed to stderr</fails_when>
    <automated>claude plugin validate ./plugin --strict</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - Each of the three `SKILL.md` files has `---` on line 1, `name:` on line 2, `description` on line 3, and `---` on line 4 — a four-line frontmatter block with exactly two keys.
    - No `metadata` key was added to any SKILL.md frontmatter.
    - `plugin/skills/jarvis-use/SKILL.md:3` still begins `description: "` and ends with `"` — the quoting required for a colon-bearing description is intact.
    - `docs/code-standards.md` names all six Agent Skills allowed keys including `allowed-tools`, and states that claude.ai upload and the Skills API hard-error on an unexpected frontmatter key.
    - `docs/code-standards.md:123`'s Cursor bullet and the file-shape ASCII tree are byte-unchanged.
    - `.claude/CLAUDE.md` no longer contains any of the three stale three-key literals; `Version bump is the delivery mechanism` still appears exactly once.
    - `.claude/CLAUDE.md:204`'s nine-tool line and the `>=0.6.0` floor lines are byte-unchanged — out of scope by decision.
    - `node scripts/check-plugin.mjs` exits 0.
  </acceptance_criteria>
  <done>All three skills carry a two-key, spec-conformant frontmatter block; both convention docs describe the Agent Skills allowed set instead of mandating the removed key; P1 and the vendor validator stay green.</done>
  <reversibility rating="reversible">Frontmatter and prose edits; the removed key had no consumer, so no client behaviour changes and reverting is a clean diff.</reversibility>
</task>

<task type="auto">
  <name>Task 2: jarvis-setup — progressive-disclosure move, indexRepo recovery path, universal-ctags prerequisite, 0.9.1 floor</name>
  <files>plugin/skills/jarvis-setup/SKILL.md, plugin/skills/jarvis-setup/references/troubleshooting.md</files>
  <read_first>
    - `plugin/skills/jarvis-setup/SKILL.md` in full (96 lines) — the numbered H2 structure `## 1. Check prerequisites` through `## 7. Next` (`.claude/CLAUDE.md:138`), the installer command at :22, the `--only` documentation at :25, the already-correct 17-selections/16-distributions sentence at :29, the `--no-scip` line at :66, and the `## 6. Troubleshooting` heading plus its eight-row table at :81-92. Row :85 carries a `jarvis-mcp` prose pin; row :87 documents the zoekt flag-value/name mismatch and is the row to imitate for ctags.
    - `plugin/skills/jarvis-use/references/tool-roster.md` lines 1-5 and 31-34 — the reference-file opening shape (title, one-paragraph scope statement, then the `## ` heading the retrieval command greps for) and `indexRepo`'s authoritative signature and semantics.
    - `plugin/skills/jarvis-use/SKILL.md` line 41 — the verbatim on-demand retrieval line the new link must mirror in shape, and line 48 — the phrasing for `indexRepo` as the recovery tool, including `recoveryToolArgs`.
    - `../jarvis/src/jarvis/server.py` lines 340-390 — the capability/miss error payload, `recoveryTool = "indexRepo"` and `recoveryToolArgs = {"path": ...}` at :384-385, emitted whether or not a registry row exists.
    - `setup.sh` lines 610-660 — the ctags block: zoekt auto-discovers `universal-ctags` on `PATH` (or `$CTAGS_COMMAND`) at index time, shards built without it carry no symbol sections so `sym:` queries silently return nothing, absence is a warn not a failure, shards indexed before install stay symbol-less until `jarvis reindex <slug>`, and the bare-`ctags` symlink at :616-631. Read only — never edit this file.
    - `setup.sh` lines 950-1050 — the `--only` target list naming the value `ctags` and the `should_run ctags` dispatch.
    - `.claude/CLAUDE.md` lines 138 and 141 — the numbered-H2 convention and the on-demand-reference mandate.
  </read_first>
  <action>
Create `plugin/skills/jarvis-setup/references/troubleshooting.md` in the shape of
`plugin/skills/jarvis-use/references/tool-roster.md`: an H1 title, a one-paragraph scope statement
naming what the file covers, then the single H2 heading `## Symptoms and fixes` followed by the
symptom/fix table moved out of `SKILL.md:83-92` with its `| Symptom | Fix |` header preserved and all
eight existing rows byte-identical except for the pin edit below.

In the moved `command not found: jarvis` row, state the one-off command floor as
`jarvis-mcp>=0.9.1` (D-03). Keep it a `>=` floor; do not exact-pin it.

Add one new row to that table for the `universal-ctags` prerequisite, written in the register of the
existing zoekt row so the flag-value/name mismatch reads as the same class of gotcha. Its symptom is
that zoekt `sym:` queries return nothing; its fix states all four facts: zoekt auto-discovers
`universal-ctags` on `PATH` (or `$CTAGS_COMMAND`) at **index time**, so its absence degrades `sym:`
only and is a warn rather than a failure; the recovery command is `sh setup.sh --only ctags` where the
flag value is `ctags`, not `universal-ctags`; zoekt looks up the literal binary name
`universal-ctags`, so a bare `ctags` on `PATH` is invisible to it and setup.sh symlinks it; and a
shard indexed before the install stays symbol-less until `jarvis reindex <slug>`.

In `plugin/skills/jarvis-setup/SKILL.md`, keep `## 6. Troubleshooting` as a section heading — do not
renumber `## 7. Next` — and replace its table body with one on-demand retrieval line mirroring
`plugin/skills/jarvis-use/SKILL.md:41` exactly in shape, naming the command
`grep -nA20 "## Symptoms and fixes" references/troubleshooting.md` and marking it as loaded on
demand. Add one preceding sentence naming what the reference covers so an agent can decide whether to
load it.

In `## 5. Verify`, add the `indexRepo` recovery path (D-02): any jarvis tool that misses on an
unindexed or unknown repo returns an error payload naming `indexRepo` as its `recoveryTool` with
`recoveryToolArgs.path` set to the repo's local git working directory, so an agent can build the index
itself instead of asking the user to run the CLI; poll `getIndexStatus` to completion afterwards.
Match the phrasing register of `plugin/skills/jarvis-use/SKILL.md:48`.

Leave `SKILL.md:22` exactly as it is — the installer command URL stays on `main` while reading links
pin to tags, which is the Phase 4 precedent and a decision of record (D-05), not an oversight. Leave
`:29` and `:66` untouched; both are already correct.
  </action>
  <verify>
    <automated>test -f plugin/skills/jarvis-setup/references/troubleshooting.md</automated>
    <fails_when>non-zero exit — the reference file does not exist</fails_when>
    <automated>grep -c '^|' plugin/skills/jarvis-setup/references/troubleshooting.md</automated>
    <fails_when>the printed count is not 11 — one header row, one delimiter row, and nine symptom rows. Measured on the pre-move tree: `grep -c '^|' plugin/skills/jarvis-setup/SKILL.md` prints 10 (header :83, delimiter :84, eight data rows :85-92), so the move plus the new `universal-ctags` row yields 11. The pattern is `^|` and not `^| ` because the delimiter row this task preserves is `|---|---|`, with no space after the pipe.</fails_when>
    <automated>grep -c -F 'grep -nA20 "## Symptoms and fixes" references/troubleshooting.md' plugin/skills/jarvis-setup/SKILL.md</automated>
    <fails_when>the printed count is not 1</fails_when>
    <automated>grep -c '^|' plugin/skills/jarvis-setup/SKILL.md</automated>
    <fails_when>the printed count is not 0 — the table must have left SKILL.md entirely, delimiter row included. Measured on the pre-move tree: 10.</fails_when>
    <automated>grep -c -F 'setup.sh --only ctags' plugin/skills/jarvis-setup/references/troubleshooting.md</automated>
    <fails_when>the printed count is 0</fails_when>
    <automated>grep -c -F 'jarvis-mcp>=0.9.1' plugin/skills/jarvis-setup/references/troubleshooting.md</automated>
    <fails_when>the printed count is 0</fails_when>
    <automated>grep -c -F 'jarvis-mcp>=0.9.0' plugin/skills/jarvis-setup/SKILL.md plugin/skills/jarvis-setup/references/troubleshooting.md</automated>
    <fails_when>any printed line ends in a count other than 0</fails_when>
    <automated>grep -c -F 'indexRepo' plugin/skills/jarvis-setup/SKILL.md</automated>
    <fails_when>the printed count is 0</fails_when>
    <automated>grep -c -F 'raw.githubusercontent.com/jarvis-intelligence/jarvis-index/main/setup.sh' plugin/skills/jarvis-setup/SKILL.md</automated>
    <fails_when>the printed count is not 1 — D-05 requires the installer URL to stay on `main`</fails_when>
    <automated>grep -c '^## 7. Next' plugin/skills/jarvis-setup/SKILL.md</automated>
    <fails_when>the printed count is not 1 — the numbered-H2 sequence must not be renumbered</fails_when>
    <automated>node scripts/check-plugin.mjs</automated>
    <fails_when>non-zero exit, or any line matching `^P1: ` printed to stderr</fails_when>
    <automated>git diff --stat -- setup.sh</automated>
    <fails_when>any output is printed</fails_when>
  </verify>
  <acceptance_criteria>
    - `plugin/skills/jarvis-setup/references/troubleshooting.md` exists, opens with an H1 and a scope paragraph, and contains exactly one H2 heading, `## Symptoms and fixes`.
    - Its table has nine symptom rows: the eight moved from `SKILL.md:85-92` plus the new `universal-ctags` row.
    - The `universal-ctags` row contains the literal `sh setup.sh --only ctags`, states that the flag value is `ctags` and not `universal-ctags`, states that zoekt discovers the binary at index time, and states that an existing shard stays symbol-less until `jarvis reindex`.
    - The moved `command not found: jarvis` row states `jarvis-mcp>=0.9.1`; no file in this task contains the `0.9.0` floor form.
    - `plugin/skills/jarvis-setup/SKILL.md` contains zero table rows, retains `## 6. Troubleshooting` and `## 7. Next` with their original numbers, and reaches the reference through the single literal retrieval command `grep -nA20 "## Symptoms and fixes" references/troubleshooting.md`.
    - `plugin/skills/jarvis-setup/SKILL.md` `## 5. Verify` names `indexRepo` as the `recoveryTool` and `recoveryToolArgs.path` as the argument shape.
    - `plugin/skills/jarvis-setup/SKILL.md:22` still targets `.../main/setup.sh`; lines 29 and 66 are byte-unchanged.
    - `setup.sh` has no diff against `HEAD`.
    - Both files are under the 500-line soft cap (`.claude/CLAUDE.md:145`).
  </acceptance_criteria>
  <done>`jarvis-setup` carries an on-demand troubleshooting reference with a new `universal-ctags` row and a 0.9.1 floor, names `indexRepo` as the miss-error recovery tool, and keeps its installer URL on `main`.</done>
  <reversibility rating="reversible">A content move plus additions inside the plugin tree; no published artifact changes until the release plan tags.</reversibility>
</task>

<task type="auto">
  <name>Task 3: jarvis-use 0.9.1 nuances and floors; jarvis-issues limitation re-verification</name>
  <files>plugin/skills/jarvis-use/SKILL.md, plugin/skills/jarvis-issues/SKILL.md</files>
  <read_first>
    - `plugin/skills/jarvis-use/SKILL.md` lines 28-42 — `## Symbol format` and the retrieval line at :41 (which must stay unchanged); lines 55-71 — the `## Gotchas` list, where :57 already states the always-on syntax baseline correctly, :58 states the removed-options rule, and :60 cites upstream `scip` `v0.9.0`; lines 63-68 — the semantic-extra block whose two commands at :65-66 carry the prose pins.
    - `plugin/skills/jarvis-issues/SKILL.md` lines 23-33 — `## 2. Classify — and check known limitations` and the five documented limitations at :27-31, plus the settled-decision-with-rationale row shape `.claude/CLAUDE.md:142` mandates.
    - `../jarvis/src/jarvis/server.py` lines 416-420 and 439-446 — `documentSymbols(repo, path)` takes no symbol, and `goToDefinition` accepts an opaque `syntax:` identifier; lines 461-467, 483-489, 505-515 — `findReferences`, `callHierarchy`, and `typeHierarchy` each reject one with a capability error.
    - `../jarvis/src/jarvis/index_cli.py` lines 1950-2010 — `_REMOVED_OPTIONS`, `--search-only`'s replacement string, and `_reject_removed_options` printing `error: {flag} was removed — use {replacement}` then `raise SystemExit(2)` in `main` before argparse, so it fires for every subcommand.
    - `../jarvis/src/jarvis/config.py` lines 20-26 — the single-tenant `PROJECT` / `BRANCH` pins behind limitation 2.
    - `../jarvis/src/jarvis/server.py` lines 788-806 — `blastRadius`' docstring stating the graph has no per-node timestamp so freshness is always `unknown`, behind limitation 4.
    - `../jarvis/pyproject.toml` and `../jarvis/src/jarvis/syntax.py` — for the 0.9.1 version and platform/grammar facts behind limitations 1, 3, and 5.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *Target 8* (all sub-sections) for the verified 0.9.1 behaviour set and § *C-6* for the `syntax:` nuance wording, and *Open questions* item 8 for why D-18 is in scope.
  </read_first>
  <action>
In `plugin/skills/jarvis-use/SKILL.md`, correct the `syntax:` rule to the accurate form research C-6
verified: of the four symbol-taking navigation tools, only `goToDefinition` accepts an opaque
`syntax:` identifier — `findReferences`, `callHierarchy`, and `typeHierarchy` each reject one with a
capability error — while `documentSymbols` takes `(repo, path)` and no symbol at all, so it produces
`syntax:` identifiers rather than consuming them and is outside the accept/reject dichotomy. Place it
where the skill already discusses symbol inputs (`## Symbol format`) rather than adding a new section.

Extend the removed-options gotcha at `:58` with the two enforcement facts the skill currently omits:
the rejection exits with status **2**, and it happens in `main` before argparse, so it fires for every
subcommand rather than only for `index`. Keep the existing sentence that a missing or failed SCIP
stage publishes the syntax baseline with `status: degraded` and an actionable recovery; present no
search-only "mode" anywhere (D-02).

Update both prose pins in the semantic-extra block at `:65-66` to the `jarvis-mcp[semantic]>=0.9.1`
floor (D-03), keeping them `>=` floors and keeping the `--python ">=3.12" jarvis-server` tail
byte-identical. Leave `:60`'s upstream `scip` `v0.9.0` citation alone — it is a third-party version
string, not a jarvis pin, and rewriting it would make the sentence false.

Leave `:41` and `:75-76` byte-unchanged; `:75` was already corrected in plan 06-01.

In `plugin/skills/jarvis-issues/SKILL.md`, re-verify each of the five documented limitations at
`:27-31` against 0.9.1 server source using the files named in this task's read-first list, then for each one either
leave it byte-identical or correct it so it states what 0.9.1 actually does. Record the verification
verdict per limitation in the plan SUMMARY with the `path:line` that settles it — a skill that files
issues against limitations that no longer hold generates false bug reports, which is the same truth
failure this phase exists to close. Preserve the existing shape: a settled decision with the rationale
inline and, where applicable, the explicit DO-file-a-bug-if condition, never an unresolved-work marker
(`.claude/CLAUDE.md:142`). Do not add a sixth limitation and do not soften the Windows-unsupported
statement unless server source contradicts it.
  </action>
  <verify>
    <automated>grep -c -F 'jarvis-mcp[semantic]>=0.9.1' plugin/skills/jarvis-use/SKILL.md</automated>
    <fails_when>the printed count is not 2</fails_when>
    <automated>grep -c -F 'jarvis-mcp[semantic]>=0.9.0' plugin/skills/jarvis-use/SKILL.md</automated>
    <fails_when>the printed count is not 0</fails_when>
    <automated>grep -c -F 'goToDefinition' plugin/skills/jarvis-use/SKILL.md</automated>
    <fails_when>the printed count is 0</fails_when>
    <automated>grep -c -F 'documentSymbols' plugin/skills/jarvis-use/SKILL.md</automated>
    <fails_when>the printed count is 0</fails_when>
    <automated>grep -c -F 'grep -nA20 "## Tool detail" references/tool-roster.md' plugin/skills/jarvis-use/SKILL.md</automated>
    <fails_when>the printed count is not 1 — line 41 must survive unchanged</fails_when>
    <automated>grep -c -F '`scip expt-convert` through v0.9.0' plugin/skills/jarvis-use/SKILL.md</automated>
    <fails_when>the printed count is not 1 — the upstream scip citation must not be rewritten. Measured against the unmodified file: this literal prints 1, because the shipped text is "Upstream `scip expt-convert` through v0.9.0 never populated relationships" and the closing backtick sits between the command name and " through". The literal is matched by content rather than by line number because Task 1 deletes a frontmatter line from this same file and shifts the citation up by one.</fails_when>
    <automated>grep -c '^- ' plugin/skills/jarvis-issues/SKILL.md</automated>
    <fails_when>the printed count is less than 5 — the limitation list must still enumerate five entries plus the context bullets</fails_when>
    <automated>grep -c -i -F 'TODO' plugin/skills/jarvis-issues/SKILL.md</automated>
    <fails_when>the printed count is not 0</fails_when>
    <automated>node scripts/check-manifests.mjs &amp;&amp; node scripts/check-plugin.mjs</automated>
    <fails_when>non-zero exit, or any line matching `^P1: ` printed to stderr</fails_when>
    <automated>claude plugin validate ./plugin --strict</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `plugin/skills/jarvis-use/SKILL.md` states that only `goToDefinition` among the four symbol-taking navigation tools accepts a `syntax:` identifier, and that `documentSymbols` takes `(repo, path)` and produces rather than consumes them.
    - The removed-options gotcha states exit status `2` and that the rejection happens before argparse so it applies to every subcommand.
    - No text in any of the three skills presents a search-only mode.
    - `plugin/skills/jarvis-use/SKILL.md` contains the `jarvis-mcp[semantic]>=0.9.1` floor exactly twice and the `0.9.0` form zero times; line 41 and the trigger-example lines are byte-unchanged.
    - `plugin/skills/jarvis-use/SKILL.md`'s upstream `scip expt-convert` citation (the bullet at `:60` in the pre-Task-1 file, `:59` after Task 1 removes the frontmatter line) is byte-unchanged, so the guard literal above still prints 1.
    - Each of `jarvis-issues`' five limitations is either byte-unchanged or corrected, with a per-limitation verdict and settling `path:line` recorded in the plan SUMMARY; no limitation is phrased as a TODO and no sixth limitation is added.
    - `node scripts/check-manifests.mjs && node scripts/check-plugin.mjs` exits 0 and `claude plugin validate ./plugin --strict` exits 0.
  </acceptance_criteria>
  <done>`jarvis-use` teaches the accurate `syntax:` rule, the exit-2 removed-option contract, and the 0.9.1 floors; `jarvis-issues`' five limitations have each been checked against 0.9.1 source with the verdict recorded.</done>
  <reversibility rating="reversible">Prose corrections traceable to server source; every claim is re-derivable from the cited `path:line`.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill text → agent execution | all three `SKILL.md` bodies and the new `references/troubleshooting.md` are instructions an installed agent follows, including shell commands it may run on the user's machine |
| skill text → GitHub issue tracker | `jarvis-issues` drives `gh issue create` against a public repository |
| server source → skill claims | `../jarvis` is read-only ground truth; a stale transcription becomes a false instruction |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering | `plugin/skills/jarvis-setup/references/troubleshooting.md` recovery commands | medium | mitigate | Every command in the moved and new rows is transcribed from `setup.sh` read-only and re-verified by the `--only ctags` and floor greps; no command is invented |
| T-06-07 | Spoofing | `jarvis-issues` filing against stale limitations | medium | mitigate | Task 3 re-reads all five limitations against 0.9.1 server source and records a per-limitation verdict, so the skill cannot advise filing a duplicate of a limitation that no longer exists |
| T-06-08 | Tampering | `jarvis-mcp` prose pins | high | mitigate | Every pin stays a `>=` floor at `0.9.1`, never exact-pinned and never lowered (`.claude/CLAUDE.md:132`); the greps assert the new floor is present and the old one absent in every touched file |
| T-06-09 | Repudiation | convention docs vs guard disagreement | medium | mitigate | Task 1 removes the mandate for the key plan 06-06's P4 rejects, so the documented convention and the enforced convention are the same rule |
| T-06-10 | Information Disclosure | `jarvis-issues` context-gathering instructions | low | accept | The gathered context is command lines, slugs, error payloads, and `uname` output; the skill already requires explicit user approval before filing (`:63-71`) |
| T-06-11 | Elevation of Privilege | `curl … | sh` installer command at `SKILL.md:22` | medium | accept | Unchanged by decision (D-05) — the URL stays on `main` per the Phase 4 precedent; `setup.sh` is sync-owned from the private repo and out of this repo's edit surface |
</threat_model>

## Artifacts this phase produces

Symbols and paths created by Phase 6. None exists before this phase, so source-grounding must not
flag them as unresolved references.

| Artifact | Kind | Created by |
|---|---|---|
| `scripts/check-plugin.mjs` | guard script | 06-01 |
| `P1` dimension — roster set-equality + roster-derived snake_case scan | guard dimension | 06-01 |
| `check:plugin` npm alias | `package.json` script | 06-01 |
| `Check plugin surface` CI step | `checks.yml` step | 06-01 |
| `plugin/skills/jarvis-setup/references/troubleshooting.md` | skill reference | 06-02 |
| `## Symptoms and fixes` retrieval heading | reference anchor | 06-02 |
| `plugin/hooks/jarvis-index-status.sh` | POSIX probe script | 06-03 |
| `plugin/hooks/hooks.json` | hook config (Claude + Codex) | 06-03 |
| `plugin/hooks/cursor.json` | hook config (Cursor) | 06-03 |
| Cursor manifest `hooks` path field | manifest key | 06-03 |
| `plugin/commands/index.md` — `/jarvis:index` | slash command | 06-04 |
| `plugin/commands/status.md` — `/jarvis:status` | slash command | 06-04 |
| `plugin/agents/jarvis-navigator.md` | navigator subagent | 06-04 |
| `$schema` key on `plugin/.claude-plugin/plugin.json` | manifest key | 06-04 |
| `Install Claude Code CLI` CI step | `checks.yml` step | 06-05 |
| `Validate plugin + marketplace` CI step | `checks.yml` step | 06-05 |
| `P3` dimension — both `marketplace.json` valid | guard dimension | 06-06 |
| `P4` dimension — skill/command/agent frontmatter allowed-keys | guard dimension | 06-06 |
| `P5` dimension — referenced assets/components exist, hook executable | guard dimension | 06-06 |
| `P2a` dimension — manifest version ↔ `jarvis-mcp>=` floor | guard dimension | 06-07 |
| `P2b` dimension — `blob/<ref>` ↔ `v<version>` + tag existence | guard dimension | 06-07 |
| `--release` flag on `scripts/check-plugin.mjs` | CLI flag | 06-07 |
| `v0.9.1` git tag | release tag | 06-08 |

<verification>
1. `node scripts/check-manifests.mjs && node scripts/check-plugin.mjs` exits 0 from the project root.
2. `claude plugin validate ./plugin --strict` exits 0.
3. Each of the three SKILL.md frontmatter blocks is four lines with exactly `name` and `description`.
4. `plugin/skills/jarvis-setup/SKILL.md` contains zero table rows and one literal retrieval command;
   `plugin/skills/jarvis-setup/references/troubleshooting.md` contains nine symptom rows.
5. No file in this plan contains a `jarvis-mcp` floor at `0.9.0`.
6. `git diff --stat -- setup.sh` prints nothing, and nothing under
   `/Users/ddphuong/Projects/jarvis-ai/jarvis` was modified.
7. The SUMMARY records a per-limitation D-18 verdict with the settling `path:line` for all five
   `jarvis-issues` limitations.
8. The SUMMARY records the deliberate `.claude/CLAUDE.md` exclusion: `:204`'s nine-tool line and the
   `>=0.6.0` floor lines at `:17`, `:132`, `:198`, `:298` (plus `:68`'s "currently 0.7.2") stay stale
   after this phase, because `.claude/**` is outside the `checks.yml` path filter and no P-dimension
   reads it — so no guard will ever catch them and a future reader needs the exclusion written down.
</verification>

<success_criteria>
- D-11 satisfied and C-8 closed: the off-spec key is gone from all three skills and neither
  convention doc still mandates it.
- D-02 / SC-1: `jarvis-setup` names `indexRepo` and the `universal-ctags` / `sym:` prerequisite;
  `jarvis-use` states the accurate `syntax:` rule and the exit-2 removed-option contract.
- D-10: the troubleshooting table is behind an exact on-demand retrieval command.
- D-03 / SC-2: every prose `jarvis-mcp` pin in the skills states a `>=0.9.1` floor.
- D-05: the installer URL is unchanged and still on `main`.
- D-18: all five `jarvis-issues` limitations re-verified against 0.9.1 with recorded verdicts.
- No test framework, linter, or formatter introduced; `setup.sh` and the `../jarvis` repo untouched.
</success_criteria>

<output>
Create `.planning/phases/06-plugin-skills-enhancement/06-02-SUMMARY.md` when done, including the
five-row D-18 verification table (limitation → verdict → settling `path:line`) and the one-line
`.claude/CLAUDE.md` exclusion note required by verification item 8.
</output>
