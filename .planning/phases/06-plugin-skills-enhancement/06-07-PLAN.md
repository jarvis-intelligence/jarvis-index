---
phase: 06-plugin-skills-enhancement
plan: 07
type: execute
wave: 4
depends_on: ["06-06"]
files_modified:
  - plugin/.claude-plugin/plugin.json
  - plugin/.cursor-plugin/plugin.json
  - .codex-plugin/plugin.json
  - plugin/mcp.json
  - plugin/.mcp.json
  - scripts/check-plugin.mjs
autonomous: true
requirements: [D-03, D-04, D-05, D-12, D-13, D-15, D-16, SC-2, SC-5]
estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-15 / D-16 / SC-5: all three `plugin.json` files declare `0.9.1` in the same commit, mirroring the released server version so the plugin number and the `jarvis-mcp` floor it pins read as one fact."
    - "D-04 / SC-2: `.codex-plugin/plugin.json`'s `privacyPolicyURL` and `termsOfServiceURL` reference the `v0.9.1` tag, landing in the same commit the tag will point at — the only ordering in which those URLs can ever resolve."
    - "D-03 / SC-2: `plugin/mcp.json` and `plugin/.mcp.json` both declare the floor `jarvis-mcp>=0.9.1`, stay byte-identical, stay a `>=` floor, are never exact-pinned, and never gain the semantic extra."
    - "D-12(b) / SC-4: dimension P2a asserts, offline and at PR time, that the floor parsed from the MCP config is a `>=` floor carrying no extra, is not below `0.6.0`, and equals the version the three manifests agree on."
    - "D-12(b) / D-04 / D-05 / SC-4: dimension P2b, gated behind `--release`, asserts that every tag-bearing GitHub URL on the shipped surface references `v<manifestVersion>` — with `main` permitted only for the installer URL — and that the tag exists on the remote; with no remote reachable it prints a skip note and does not fail."
    - "D-13: P2a was demonstrated red between the manifest bump and the floor bump, and P2b was demonstrated red while the `v0.9.1` tag did not yet exist."
    - "P2b never runs at PR time: GitHub does not serve a `blob/<tag>` path before the tag is pushed, so a pull request cannot satisfy an assertion about its own future tag."
  artifacts:
    - plugin/.claude-plugin/plugin.json
    - plugin/.cursor-plugin/plugin.json
    - .codex-plugin/plugin.json
    - plugin/mcp.json
    - plugin/.mcp.json
    - scripts/check-plugin.mjs
  key_links:
    - "`scripts/check-manifests.mjs:43-53` already asserts three-way `version` equality and `:55-63` already asserts MCP-config byte-identity. P2a must assert only the new relation — floor shape and floor-to-version identity — and never duplicate those two."
    - "`plugin/hooks/cursor.json` carries a top-level numeric `version` that is Cursor's hook-file schema version, not a plugin version. P2a must read versions only from the three `plugin.json` files."
    - "`plugin/skills/jarvis-use/SKILL.md:60` and `plugin/skills/jarvis-use/references/tool-roster.md:24` cite upstream `scip` `v0.9.0`, a third-party version string. Neither P2a nor P2b may flag it, and neither may rewrite it."
    - "The installer URL at `plugin/skills/jarvis-setup/SKILL.md:22` stays on `main` by decision (D-05, the Phase 4 precedent). P2b's allowance for `main` exists exactly and only for that URL."
    - "Research verified the ordering empirically: `blob/v0.9.0/...` returns 404 while `blob/v0.8.0/...` returns 200, because GitHub resolves a ref against refs that exist. The URL-resolution probe is therefore a post-tag release gate, never a PR check."
---

<objective>
Land the synchronized release edit — three manifests, two MCP configs, two URLs, all at `0.9.1` in one
commit — and guard the relation between them with dimension P2a offline plus dimension P2b behind a
`--release` flag.

Purpose: D-16 resolved the version target to `0.9.1` so that every version string on the shipped
surface becomes one number, which makes guard P2a a literal equality instead of a two-number relation.
The URL rewrite must land in the same commit the tag will point at, because GitHub will not serve a
`blob/<tag>` path until that tag exists — that is a hard ordering constraint, not a preference.

Output: the synchronized bump, the 0.9.1 floors, the retargeted URLs, and two new guard dimensions —
one that runs on every pull request and one that can only run after the tag is pushed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-plugin-skills-enhancement/06-CONTEXT.md
@.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md
@.planning/phases/06-plugin-skills-enhancement/06-PATTERNS.md
@.planning/phases/06-plugin-skills-enhancement/06-06-SUMMARY.md
@scripts/check-manifests.mjs
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synchronized manifest bump to 0.9.1 and the tag-targeted Codex URLs</name>
  <files>plugin/.claude-plugin/plugin.json, plugin/.cursor-plugin/plugin.json, .codex-plugin/plugin.json</files>
  <read_first>
    - `plugin/.claude-plugin/plugin.json` in full as it stands after plan 06-04 — the `$schema` declaration is now the first key and `version` follows `name`; 2-space indent.
    - `plugin/.cursor-plugin/plugin.json` in full as it stands after plan 06-03 — `version` is the fourth key and a `hooks` path field is now the file's tail.
    - `.codex-plugin/plugin.json` in full as it stands after plan 06-01 — `version` at the third key, the corrected `interface.longDescription`, and the two tag-bearing URLs in the `interface` block.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *Target 5 — Tag and version strategy* — the measured state table, the verified 404-versus-200 probe pair proving GitHub does not serve a ref before it exists, the four-step release sequence, and the 0.9.1-versus-0.10.0 tradeoff.
    - `.planning/phases/06-plugin-skills-enhancement/06-CONTEXT.md` D-16 — the user's resolution of the version target and the three mechanical consequences that bind this task.
    - `scripts/check-manifests.mjs` lines 43-53 — the three-way equality assertion that must go green immediately after all three edits, and red if any one is missed.
    - `.claude/CLAUDE.md` line 131 — all three `version` fields move to the same value in the same commit.
  </read_first>
  <action>
Set `version` to `0.9.1` in all three manifests — `plugin/.claude-plugin/plugin.json`,
`plugin/.cursor-plugin/plugin.json`, and `.codex-plugin/plugin.json` — in this one task, so the
three-way equality assertion at `scripts/check-manifests.mjs:43-53` is never left failing between
commits. Change no other key in any of the three files: the `$schema` declaration, the Cursor `hooks`
path field, the corrected `longDescription`, `keywords`, `author`, and the whole remaining `interface`
block all stay byte-identical.

Rewrite the two tag-bearing URLs in `.codex-plugin/plugin.json`'s `interface` block so both reference
the `v0.9.1` tag: `privacyPolicyURL` targets the `plugin/README.md` privacy anchor at that tag, and
`termsOfServiceURL` targets `plugin/LICENSE` at that tag. Keep the owner and repository segments and
the anchor fragment exactly as they are; only the ref segment changes.

Understand and accept the window this creates. Both URLs will return HTTP 404 from the moment they are
committed until the `v0.9.1` tag is pushed at the merge commit, because GitHub resolves a ref against
refs that exist — research verified this directly, with the identical URL shape returning 404 for a
missing tag and 200 for an existing one. There is no ordering that avoids the window: the commit
carrying the URLs is the commit the tag must point at. Do not attempt to close it here, do not add a
probe to PR-time CI, and do not soften the URLs to `main` as a workaround — a `main` reading link would
contradict the Phase 4 precedent that reading links pin to tags. Plan 06-08 closes it by pushing the
tag and then probing.

Leave the installer command URL at `plugin/skills/jarvis-setup/SKILL.md:22` on `main` (D-05). Leave the
upstream `scip` `v0.9.0` citations in `plugin/skills/jarvis-use/SKILL.md` and
`plugin/skills/jarvis-use/references/tool-roster.md` untouched — they name a third-party tool's version,
not a jarvis pin.

Do not touch `plugin/mcp.json` or `plugin/.mcp.json` in this task. Leaving the floors behind is
deliberate: it is the state that makes dimension P2a fail in Task 2, which is how the guard earns
trust.
  </action>
  <verify>
    <automated>node --input-type=module -e "import{readFileSync}from'node:fs';const p=['plugin/.claude-plugin/plugin.json','plugin/.cursor-plugin/plugin.json','.codex-plugin/plugin.json'].map(f=>JSON.parse(readFileSync(f,'utf8')).version);if(p.some(v=>v!=='0.9.1'))process.exit(1)"</automated>
    <fails_when>non-zero exit — at least one of the three manifests does not declare exactly `0.9.1`</fails_when>
    <automated>node scripts/check-manifests.mjs</automated>
    <fails_when>non-zero exit, or output containing `version mismatch`</fails_when>
    <automated>grep -c -F 'blob/v0.9.1/' .codex-plugin/plugin.json</automated>
    <fails_when>the printed count is not 2</fails_when>
    <automated>grep -c -F 'blob/v0.9.0/' .codex-plugin/plugin.json</automated>
    <fails_when>the printed count is not 0</fails_when>
    <automated>grep -c -F 'through ten MCP tools.' .codex-plugin/plugin.json</automated>
    <fails_when>the printed count is not 1 — plan 06-01's longDescription correction must survive this edit</fails_when>
    <automated>grep -c -F './hooks/cursor.json' plugin/.cursor-plugin/plugin.json</automated>
    <fails_when>the printed count is not 1 — plan 06-03's Cursor hooks declaration must survive this edit</fails_when>
    <automated>grep -c -F 'json.schemastore.org/claude-code-plugin-manifest.json' plugin/.claude-plugin/plugin.json</automated>
    <fails_when>the printed count is not 1 — plan 06-04's schema declaration must survive this edit</fails_when>
    <automated>grep -c -F 'raw.githubusercontent.com/jarvis-intelligence/jarvis-index/main/setup.sh' plugin/skills/jarvis-setup/SKILL.md</automated>
    <fails_when>the printed count is not 1 — D-05 requires the installer URL to stay on `main`</fails_when>
    <automated>git diff --stat -- plugin/mcp.json plugin/.mcp.json</automated>
    <fails_when>any output is printed — the floors must still be behind, so P2a has something to fail on in Task 2</fails_when>
    <automated>claude plugin validate ./plugin --strict &amp;&amp; claude plugin validate . --strict</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - All three `plugin.json` files declare `version` exactly `0.9.1`, and `node scripts/check-manifests.mjs` exits 0.
    - `.codex-plugin/plugin.json` contains the `blob/v0.9.1/` ref segment exactly twice and the `v0.9.0` ref segment zero times; the owner, repository, path, and anchor segments of both URLs are otherwise unchanged.
    - The corrected `longDescription`, the Cursor `hooks` declaration, and the Claude `$schema` declaration all survive unchanged.
    - `plugin/mcp.json` and `plugin/.mcp.json` have no diff against `HEAD`.
    - `plugin/skills/jarvis-setup/SKILL.md:22` still targets the `main` installer URL, and the upstream `scip` citations in `jarvis-use` are unchanged.
    - Both `claude plugin validate --strict` runs exit 0.
    - The SUMMARY records that both Codex URLs are expected to 404 until plan 06-08 pushes the tag, with the verified 404-versus-200 evidence cited as the reason no earlier ordering exists.
  </acceptance_criteria>
  <done>All three manifests agree at `0.9.1` in one commit, both Codex policy URLs target the `v0.9.1` tag, the MCP floors are deliberately still behind, and every prior plan's manifest edit survived.</done>
  <reversibility rating="costly">These are marketplace-facing manifests. In-repo the bump is a clean diff, but once plan 06-08 pushes the tag the version is published and is corrected by a new release, not a revert. D-16 resolved the number and its coupling cost explicitly.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dimension P2a red, then the byte-identical 0.9.1 floor bump green</name>
  <files>scripts/check-plugin.mjs, plugin/mcp.json, plugin/.mcp.json</files>
  <read_first>
    - `scripts/check-plugin.mjs` as it stands after plan 06-06 — the header block with P2a's reserved contract line, the `fail(dimension, detail)` collector, and the `process.cwd()` seam.
    - `scripts/check-manifests.mjs` lines 16 and 43-63 — the MCP config path pair, the three-way version equality, and the byte-identity comparison. P2a must duplicate neither of the latter two.
    - `plugin/mcp.json` in full (14 lines) — the `mcpServers.jarvis.args` array whose second element carries the floor; note the exact surrounding elements `--from`, `--python`, the Python floor, and `jarvis-server`.
    - `plugin/.mcp.json` in full — the byte-identical twin.
    - `.claude/CLAUDE.md` lines 19 and 132, and `docs/code-standards.md` lines 104-110 — the pin rules: `>=` floor only, never exact-pinned, never below `0.6.0`, never the semantic extra, and edit both files or neither.
    - `plugin/hooks/cursor.json` — the file whose top-level numeric `version` P2a must not read as a plugin version.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *Target 6 → P2* — P2a's exact scope and the instruction not to duplicate what `check-manifests.mjs` already owns.
  </read_first>
  <behavior>
    - Parsing `plugin/mcp.json`'s `mcpServers.jarvis.args` yields exactly one element matching a `jarvis-mcp` requirement string; zero or more than one fails P2a.
    - A floor expressed with an exact-pin operator rather than a greater-or-equal operator fails P2a, naming the offending string.
    - A requirement string carrying the semantic extra fails P2a, because the default plugin registration must never install it.
    - A floor version below `0.6.0` fails P2a.
    - A floor version that differs from the version the three `plugin.json` files agree on fails P2a, naming both values — this is the state Task 1 deliberately leaves behind.
    - `plugin/hooks/cursor.json`'s top-level numeric version is never read as a plugin version; adding or changing it does not affect P2a.
    - After the floor bump, P2a emits no failure and the run stays offline.
  </behavior>
  <action>
Add dimension P2a to `scripts/check-plugin.mjs`, filling in the contract line reserved for it in plan
06-01's header block. Keep it offline and pure filesystem so it runs on every pull request.

Read the version the three `plugin.json` files agree on, taking it only from those three files — never
from `plugin/hooks/cursor.json`, whose top-level numeric version is Cursor's hook-file schema version.
If the three disagree, do not report it: `scripts/check-manifests.mjs:43-53` already owns that
assertion, and duplicating it would give two scripts the same contract. Note that deliberate skip in a
comment.

Parse the `jarvis-mcp` requirement string out of `plugin/mcp.json`'s `mcpServers.jarvis.args` array by
searching the elements for the one naming the distribution, rather than indexing a fixed position — an
element inserted before it must not silently break the guard. Then assert four things, each through
`fail('P2a', …)` with the offending value named: the operator is greater-or-equal and not an exact pin;
the requirement carries no extra in brackets; the floor version is not below `0.6.0`; and the floor
version equals the manifest version.

Record in P2a's header entry that the fourth assertion is an identity rather than a relation because
D-16 chose to mirror the server version, and that if a future release decouples the two numbers this
becomes a `floor <= serverVersion` relation instead. Also record that P2a deliberately does not
re-assert three-way equality or byte-identity.

Now demonstrate red. Task 1 left the manifests at `0.9.1` and both MCP configs at the previous floor,
so run the guard against that state and capture the `P2a:` failure line naming both versions, with the
exit-1 status, as the D-13 evidence. This is a natural red state produced by the plan's own ordering —
no scratch mutation is needed.

Then go green: set the floor in `plugin/mcp.json` to `jarvis-mcp>=0.9.1` and make the identical edit in
`plugin/.mcp.json`, so the two files stay byte-identical. Keep it a `>=` floor, do not exact-pin it, do
not add the semantic extra, and change nothing else in either file — the `--python` floor, the
`jarvis-server` entry point, key order, and 2-space indent all stay byte-identical.
  </action>
  <verify>
    <automated>node scripts/check-plugin.mjs</automated>
    <fails_when>non-zero exit, or any line matching `^P2a: ` printed to stderr</fails_when>
    <automated>node scripts/check-plugin.mjs | grep -c -F 'P2a'</automated>
    <fails_when>the printed count is 0 — the success summary must name P2a among the green dimensions</fails_when>
    <automated>grep -c -F 'jarvis-mcp>=0.9.1' plugin/mcp.json plugin/.mcp.json</automated>
    <fails_when>any printed line ends in a count other than 1</fails_when>
    <automated>grep -c -F 'jarvis-mcp[semantic]' plugin/mcp.json plugin/.mcp.json</automated>
    <fails_when>any printed line ends in a count other than 0 — the default registration must never carry the semantic extra</fails_when>
    <automated>grep -c -F 'jarvis-mcp==' plugin/mcp.json plugin/.mcp.json</automated>
    <fails_when>any printed line ends in a count other than 0 — the pin must stay a floor, never an exact pin</fails_when>
    <automated>cmp plugin/mcp.json plugin/.mcp.json</automated>
    <fails_when>non-zero exit, or any `differ` output — the two MCP configs must stay byte-identical</fails_when>
    <automated>node scripts/check-manifests.mjs</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>grep -c -F 'fetch(' scripts/check-plugin.mjs</automated>
    <fails_when>the printed count is not 0 — P2a must stay offline, and P2b uses `git ls-remote` rather than HTTP</fails_when>
  </verify>
  <acceptance_criteria>
    - P2a reads the manifest version only from the three `plugin.json` files, and a comment records that `plugin/hooks/cursor.json`'s numeric version is a hook-file schema version and is deliberately not read.
    - P2a locates the `jarvis-mcp` requirement string by searching the `args` elements, not by fixed index.
    - P2a asserts all four properties — greater-or-equal operator, no bracketed extra, floor not below `0.6.0`, floor equal to the manifest version — each with its own `P2a: ` failure naming the offending value.
    - P2a does not re-assert three-way version equality or MCP-config byte-identity, and a comment records that `scripts/check-manifests.mjs` owns both.
    - P2a's header entry states that the floor-to-version check is an identity because D-16 mirrored the server version, and what it would become if the two numbers are ever decoupled.
    - The red capture is recorded in the SUMMARY: the verbatim `P2a: ` line naming `0.9.1` against the earlier floor, with the exit-1 status, produced by Task 1's deliberate ordering rather than a scratch mutation.
    - Both `plugin/mcp.json` and `plugin/.mcp.json` contain `jarvis-mcp>=0.9.1` exactly once, contain no bracketed extra and no exact pin, and are byte-identical to each other.
    - Nothing else changed in either MCP config: the `--python` floor, the `jarvis-server` entry point, key order, and 2-space indent are unchanged.
    - `node scripts/check-manifests.mjs && node scripts/check-plugin.mjs` exits 0 and the `ok:` summary names P2a.
  </acceptance_criteria>
  <done>P2a was seen failing on the real mid-plan tree, both MCP configs now declare the byte-identical `jarvis-mcp>=0.9.1` floor, and P2a is green and offline.</done>
  <reversibility rating="costly">The MCP floor is what every plugin install resolves at launch. Raising it excludes users pinned below `0.9.1`; lowering it again is a new release. It stays a `>=` floor so no user is exact-pinned.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dimension P2b — tag-referencing URLs and tag existence, gated behind --release</name>
  <files>scripts/check-plugin.mjs</files>
  <read_first>
    - `scripts/check-plugin.mjs` as it stands after Task 2 — the header block with P2b's reserved contract line, and the argv-reading style to follow.
    - `scripts/verify-build.mjs` lines 29-30 — the in-repo precedent for reading `process.argv` positionally with defaults and no argument parser; P2b needs a flag rather than a positional, but the no-dependency rule is the same.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *Target 6 → P2* — P2b's exact scope, the instruction to use `git ls-remote --tags origin` rather than an HTTPS fetch, the verified success of that command from this working copy, and the skip-with-a-note-never-fail rule for an offline or remote-less run.
    - `.planning/phases/06-plugin-skills-enhancement/06-RESEARCH.md` § *Target 5* — the four-step release sequence and the reason P2b can never be a PR-time check.
    - `.codex-plugin/plugin.json` — the two tag-bearing URLs Task 1 retargeted.
    - `plugin/skills/jarvis-setup/SKILL.md` line 22 — the one URL permitted to reference `main`, per D-05.
    - `plugin/skills/jarvis-use/SKILL.md` line 60 and `plugin/skills/jarvis-use/references/tool-roster.md` line 24 — the upstream `scip` version citations P2b must not mistake for jarvis refs; they are not GitHub URLs into this repository.
    - `plugin/README.md` and root `README.md` — the other two prose surfaces P2b scans for repository URLs.
  </read_first>
  <behavior>
    - With no flag and no environment override, P2b does not run at all and the script performs no network access; the default run stays hermetic and PR-safe.
    - With the release flag, every URL into this repository found in `.codex-plugin/plugin.json`, the three `SKILL.md`, `plugin/README.md`, and root `README.md` has its ref segment checked: a ref equal to the tag form of the manifest version passes.
    - A ref equal to `main` passes only for the installer URL in `plugin/skills/jarvis-setup/SKILL.md`; the same ref anywhere else fails P2b, naming the file and the URL.
    - A ref that is neither `main` nor the tag form of the manifest version fails P2b, naming the file, the URL, and the expected ref.
    - A URL that does not point into this repository — including an upstream project's version mentioned in prose — is not examined.
    - With the release flag and a reachable remote, a tag list that does not contain the tag form of the manifest version fails P2b naming the missing tag.
    - With the release flag and no reachable remote, P2b prints a skip note naming the reason and does not fail the run.
    - Run with the release flag before the tag is pushed, P2b fails on tag existence.
  </behavior>
  <action>
Add dimension P2b to `scripts/check-plugin.mjs`, filling in the contract line reserved for it in plan
06-01's header block. P2b is the script's only network dimension and must be gated: it runs only when
the release flag is present in `process.argv` or the release environment override is set. Read argv in
the dependency-free style of `scripts/verify-build.mjs:29-30` — a flag test, not an argument parser.
Record the flag and the override name in P2b's header entry so the release step is discoverable from
the script itself.

First half, ref correctness. Scan the six subject files for URLs into this repository — both the
`blob/<ref>/` form on the code host and the raw-content `<owner>/<repo>/<ref>/` form — and extract each
ref segment. Assert every ref equals the tag form of the version the three manifests agree on, with one
exception: a `main` ref is permitted only in `plugin/skills/jarvis-setup/SKILL.md`, because the
installer command deliberately stays on `main` while reading links pin to tags (D-05, the Phase 4
precedent). A `main` ref in any other file, or any ref that is neither `main` nor the expected tag,
fails through `fail('P2b', …)` naming the file, the URL, and the expected ref. Match only URLs whose
owner and repository segments are this project's, so an upstream project's version mentioned in prose
is never examined.

Second half, tag existence. Ask the remote for its tag list with `git ls-remote --tags origin` — one
round trip, no token needed for a public repository, and no HTTP client in the script. Assert the tag
form of the manifest version appears in the result. If the command fails for any reason — no remote
configured, no network, a sandbox without outbound access — print a skip note naming the reason and do
**not** fail. Record the rule as a comment in the same words the research uses: a guard that cannot run
must not become a guard that blocks.

Record the ordering constraint in P2b's header entry, because it is the single most surprising thing
about this dimension: GitHub does not serve a `blob/<ref>` path before the ref exists, verified by the
identical URL shape returning 404 for a missing tag and 200 for an existing one, so the commit carrying
the tag-bearing URLs is necessarily the commit the tag points at. A pull request cannot make its own
future tag exist. P2b therefore belongs to the post-tag release step and must never be added to the
PR-time CI job.

Finish the success summary so the `ok:` line names every green dimension, and so a release-flagged run
reports P2b separately from the always-on dimensions.

Demonstrate red: run the script with the release flag now, while the tag does not yet exist, and capture
the `P2b:` tag-existence failure and the exit-1 status as the D-13 evidence. Also capture a second red
by pointing one subject URL's ref at a stale value in a scratch copy and confirming the ref half fires.
Delete the scratch copy. Do not create the tag in this task — plan 06-08 owns that, behind a decision
checkpoint.
  </action>
  <verify>
    <automated>node scripts/check-plugin.mjs</automated>
    <fails_when>non-zero exit, or any line matching `^P2b` printed at all — the default run must not execute the network dimension</fails_when>
    <automated>node scripts/check-plugin.mjs --release; test $? -eq 1</automated>
    <fails_when>non-zero exit from this pipeline — the release run exited 0 even though the `v0.9.1` tag does not exist yet, meaning P2b's tag-existence half did not fire</fails_when>
    <automated>node scripts/check-plugin.mjs --release 2>&amp;1 | grep -c -F 'P2b'</automated>
    <fails_when>the printed count is 0</fails_when>
    <automated>grep -c -F 'fetch(' scripts/check-plugin.mjs</automated>
    <fails_when>the printed count is not 0 — tag existence must come from `git ls-remote`, never from an HTTP client in the script</fails_when>
    <automated>grep -c -F 'ls-remote' scripts/check-plugin.mjs</automated>
    <fails_when>the printed count is not 1</fails_when>
    <automated>git status --porcelain -- .github/workflows/checks.yml</automated>
    <fails_when>any output is printed — this task must not touch the workflow at all, which is the strongest form of "the release gate is not wired into PR-time CI"</fails_when>
    <automated>node --input-type=module -e "import{readFileSync}from'node:fs';const y=readFileSync('.github/workflows/checks.yml','utf8');if(/P2b|--release|CHECK_PLUGIN_RELEASE/.test(y))process.exit(1)"</automated>
    <fails_when>non-zero exit — the workflow names the release dimension, the release flag, or its environment override; a pull request cannot make its own future tag exist, so any of those would fail every PR</fails_when>
    <automated>node scripts/check-manifests.mjs &amp;&amp; node scripts/check-plugin.mjs</automated>
    <fails_when>non-zero exit, or any line matching `^P[0-9]` printed to stderr</fails_when>
    <automated>claude plugin validate ./plugin --strict &amp;&amp; claude plugin validate . --strict</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-plugin.mjs` with no flag exits 0, prints nothing mentioning P2b, and performs no network access.
    - `node scripts/check-plugin.mjs --release` exits 1 while the `v0.9.1` tag does not exist, with a `P2b` failure naming the missing tag.
    - The ref half scans `.codex-plugin/plugin.json`, all three `SKILL.md`, `plugin/README.md`, and root `README.md`, and examines only URLs whose owner and repository segments are this project's.
    - A `main` ref is accepted only in `plugin/skills/jarvis-setup/SKILL.md` and rejected elsewhere; the SUMMARY records the scratch-copy red proving the ref half fires on a stale ref.
    - Tag existence comes from `git ls-remote --tags origin`, appearing exactly once in the script; the script contains no HTTP client.
    - An unreachable remote produces a printed skip note and does not fail the run, with the never-block rule recorded as a comment.
    - P2b's header entry records the release flag, the environment override, and the verified reason a pull request can never satisfy it.
    - `.github/workflows/checks.yml` contains no occurrence of P2b and no release flag.
    - The `ok:` summary names every green dimension, and a release-flagged run reports P2b separately.
    - Both red captures are in the SUMMARY with their verbatim lines and exit statuses; no scratch directory remains in `git status --porcelain`; no tag was created by this task.
  </acceptance_criteria>
  <done>P2b exists behind the release flag, was demonstrated red on both halves, skips rather than blocks without a remote, and is deliberately absent from the PR-time workflow.</done>
  <reversibility rating="reversible">A gated guard dimension; the default PR-time behaviour is unchanged and removing the block restores it exactly.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest version → every client install | the three `plugin.json` `version` fields are what clients use as the update cache key and display metadata |
| MCP floor → package resolution at launch | the `jarvis-mcp>=` floor in both MCP configs is resolved by `uvx` on every server start on a user's machine |
| in-manifest policy URLs → marketplace reviewers and users | `privacyPolicyURL` and `termsOfServiceURL` are the links a reviewer and a user follow to read the terms they are accepting |
| remote git refs → release verdict | P2b asks the configured remote for its tag list |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-36 | Spoofing | `privacyPolicyURL` / `termsOfServiceURL` resolving to nothing | high | mitigate | Task 1 targets both at the exact tag the release will create; P2b's ref half asserts the ref equals the manifest version, and plan 06-08's post-tag probe asserts both return 200 before the release is called done |
| T-06-37 | Tampering | an exact-pinned or extra-bearing MCP requirement reaching users | high | mitigate | P2a fails on an exact pin, on any bracketed extra, and on a floor below `0.6.0`; the greps assert all three on both files, and `cmp` asserts the pair stays byte-identical |
| T-06-38 | Tampering | a reading link pointing at a mutable ref | medium | mitigate | P2b rejects a `main` ref everywhere except the one installer URL D-05 exempts, so a reading link cannot silently drift to a moving target |
| T-06-39 | Denial of Service | P2b blocking a release in a sandbox with no outbound access | medium | mitigate | An unreachable remote prints a skip note and does not fail; the rule is recorded as a comment so it is not "fixed" into a hard failure later |
| T-06-40 | Denial of Service | P2b accidentally wired into PR-time CI, failing every pull request | high | mitigate | P2b runs only behind the release flag; the verify step asserts the workflow contains neither the dimension name nor the flag, and P2b's header entry records why a pull request can never satisfy it |
| T-06-41 | Repudiation | a green default run mistaken for release readiness | medium | mitigate | The release-flagged run reports P2b separately from the always-on dimensions, so the two verdicts cannot be confused |
| T-06-42 | Elevation of Privilege | spawning `git` from a guard script | low | accept | A fixed argument vector with no interpolated input, reading only public ref names, and only when the release flag is passed by a human |
</threat_model>

## Artifacts this phase produces

Symbols and paths created by Phase 6. None exists before this phase, so source-grounding must not
flag them as unresolved references.

| Artifact | Kind | Created by |
|---|---|---|
| `scripts/check-plugin.mjs` | guard script | 06-01 |
| `P1` dimension — roster set-equality + roster-derived snake_case scan | guard dimension | 06-01 |
| `check:plugin` npm alias | `package.json` script | 06-01 |
| `Check plugin surface` CI step | `checks.yml` step | 06-01 |
| `plugin/skills/jarvis-setup/references/troubleshooting.md` | skill reference | 06-02 |
| `plugin/hooks/jarvis-index-status.sh` | POSIX probe script | 06-03 |
| `plugin/hooks/hooks.json` | hook config (Claude + Codex) | 06-03 |
| `plugin/hooks/cursor.json` | hook config (Cursor) | 06-03 |
| Cursor manifest `hooks` path field | manifest key | 06-03 |
| `plugin/commands/index.md` — `/jarvis:index` | slash command | 06-04 |
| `plugin/commands/status.md` — `/jarvis:status` | slash command | 06-04 |
| `plugin/agents/jarvis-navigator.md` | navigator subagent | 06-04 |
| `$schema` key on `plugin/.claude-plugin/plugin.json` | manifest key | 06-04 |
| `Install Claude Code CLI` CI step | `checks.yml` step | 06-05 |
| `Validate plugin + marketplace` CI step | `checks.yml` step | 06-05 |
| `P3` dimension — both `marketplace.json` valid | guard dimension | 06-06 |
| `P4` dimension — skill/command/agent frontmatter allowed-keys | guard dimension | 06-06 |
| `P5` dimension — referenced assets/components exist, hook executable | guard dimension | 06-06 |
| `P2a` dimension — manifest version ↔ `jarvis-mcp>=` floor | guard dimension | 06-07 |
| `P2b` dimension — repository URL ref ↔ `v<version>` + tag existence | guard dimension | 06-07 |
| `--release` flag and its environment override on `scripts/check-plugin.mjs` | CLI flag | 06-07 |
| `v0.9.1` git tag | release tag | 06-08 |

<verification>
1. `node scripts/check-manifests.mjs && node scripts/check-plugin.mjs` exits 0 from the project root,
   and the `ok:` line names P1, P2a, P3, P4, and P5.
2. `node scripts/check-plugin.mjs --release` exits 1 on tag existence, because the tag does not exist
   yet — that is the expected state at the end of this plan.
3. `cmp plugin/mcp.json plugin/.mcp.json` exits 0.
4. All three `plugin.json` files declare `0.9.1`; `.codex-plugin/plugin.json` carries the
   `blob/v0.9.1/` ref exactly twice.
5. `claude plugin validate ./plugin --strict && claude plugin validate . --strict` exits 0.
6. `.github/workflows/checks.yml` contains neither P2b nor the release flag.
7. Three red captures are in the SUMMARY: one for P2a and two for P2b.
</verification>

<success_criteria>
- D-15 / D-16 / SC-5: one synchronized bump — three manifests at `0.9.1`, both MCP configs
  byte-identical at the matching floor.
- D-03 / SC-2: the floor is `jarvis-mcp>=0.9.1`, a `>=` floor with no extra, in both files.
- D-04 / SC-2: both Codex policy URLs reference the `v0.9.1` tag, in the commit the tag will point at.
- D-05: the installer URL still references `main`, and P2b permits that ref in exactly one file.
- D-12(b) / SC-4: P2a guards the version-to-floor identity offline at PR time; P2b guards ref
  correctness and tag existence behind the release flag, skipping rather than blocking when offline.
- D-13: P2a and P2b were each seen failing before being trusted.
- No test framework, linter, or formatter introduced; no tag created; `setup.sh` and the `../jarvis`
  repo untouched.
</success_criteria>

<output>
Create `.planning/phases/06-plugin-skills-enhancement/06-07-SUMMARY.md` when done, including the three
verbatim red captures and an explicit note that both Codex policy URLs return 404 until plan 06-08
pushes the tag.
</output>