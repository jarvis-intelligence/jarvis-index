---
wave: 1
depends_on: []
files_modified:
  - design/tokens.css
  - src/styles/landing.css
  - src/components/landing/InstallWidget.astro
  - src/components/landing/Badges.astro
  - src/pages/index.astro
  - src/content/docs/docs/quickstart.mdx
autonomous: true
requirements:
  - LNDG-01
  - LNDG-05
  - LNDG-08
  - LNDG-10
---

# 03-01: Token Refinement, Hero with Install Widget, Badges + Footer Scaffold

## Objective

Refine the `--jv-*` token sheet in place (never fork), tokenize the three remaining hardcoded literals in `landing.css`, rebuild the page skeleton with new section order (Hero → Demo placeholder → Tools placeholder → Languages placeholder → Privacy placeholder → Architecture placeholder → Badges+CTA → Footer), land the tracer: the hero section with the tabbed install widget (syncing with quickstart), copy-to-clipboard on all command chips, sticky header with new nav anchors, slim footer, and the badges+CTA closing section. After this plan a visitor sees the hero, copies the install command, and navigates to docs; the widget syncs across surfaces; `npm run build` and `npm run verify` pass.

## must_haves

### truths

- `design/tokens.css` defines `--jv-muted-soft` in both light and dark `:root`/`[data-theme='dark']` blocks, and `--jv-mask-opaque: #000` as a theme-invariant token
- `landing.css` contains zero instances of the literal `#9aa4ac` — all three former occurrences now reference `var(--jv-muted-soft)`
- `landing.css` contains zero instances of the literal `#000` — the hero-art mask gradient stops now reference `var(--jv-mask-opaque)`
- The hero section contains a one-line value proposition in an h1 and a compact tabbed install widget defaulting to the Installer+uv tab showing the curl command as a copy-to-clipboard `.mono-box` chip
- Every `.mono-box[data-copy]` on the page copies its `data-copy` attribute to clipboard on click and shows the "✓ copied" state for ~2 seconds (D-03 copy affordance, LNDG-08)
- The install widget's three tab labels are exactly "Installer + uv", "Plugin marketplace", "Manual uvx" and the widget reads/writes `starlight-synced-tabs__channel` in localStorage (LNDG-10, per D-02)
- The quickstart page (`quickstart.mdx`) has a `<Tabs syncKey="channel">` block above Step 1 with the same three label strings — the two surfaces share the syncKey (LNDG-10, per D-02)
- The sticky header nav contains anchor links to: `#demo`, `#tools`, `#languages`, `#privacy`, `#architecture`, plus a "Docs" CTA button linking to `/jarvis-index/docs/`
- The footer contains links to: MIT, GitHub (repo), PyPI (package), MCP Registry (`io.github.jarvis-intelligence/jarvis`), Discussions, Docs — per D-01 footer contract
- The badges+CTA closing section contains 4 custom inline SVG badges (GitHub, PyPI, MIT, MCP Registry) using `var(--jv-*)` colors — zero third-party requests (LNDG-05, per D-06)
- The anti-FOUC head script and the theme toggle script survive the rebuild byte-identical (per CONTEXT.md code_context)
- `npm run build` exits 0 and `node scripts/verify-build.mjs` passes V1–V10
- Zero `#9aa4ac` or `#000` hardcoded hex literals remain in `landing.css`
- The page has zero hardcoded hex fills/strokes in any inline SVG — all colors reference `var(--jv-*)` or CSS class rules
- Static server-rendered HTML — no dynamic loads; every section's content is present at first paint (UI-SPEC loading consideration)
- No runtime fetches or submits on the landing — error states cannot occur (UI-SPEC error consideration)
- Every `.mono-box` chip and `.exchange-body` code block uses `overflow-x: auto` and `white-space: pre` for contained horizontal scroll (UI-SPEC overflow consideration)
- Command chips never wrap (`pre` + horizontal scroll inside the chip) (UI-SPEC long-text consideration)
- Badges are fixed authored content (4 badges) — empty, partial, and zero-one-many states are structurally impossible (UI-SPEC empty/partial/populated/zero-one-many considerations)
- The page is static HTML with no user-driven data collections — loading states, empty states, partial states, and zero-one-many pluralization are not applicable (UI-SPEC considerations: loading, error, empty, populated, partial, zero-one-many)

## Tasks

<task type="tracer">
  <name>Tracer: Token refinement + hero with install widget + badges/footer scaffold</name>
  <files>design/tokens.css, src/styles/landing.css, src/components/landing/InstallWidget.astro, src/components/landing/Badges.astro, src/pages/index.astro, src/content/docs/docs/quickstart.mdx</files>
  <read_first>
    - design/tokens.css (token definitions, light+dark blocks)
    - src/styles/landing.css (all existing classes, non-tokenized literals at lines 240-241, 374, 403, 414)
    - src/pages/index.astro (current structure: head/anti-FOUC script, header, hero, sections, footer, copy-to-clipboard script, theme toggle script)
    - .planning/phases/03-landing-page-rebuild/03-UI-SPEC.md (sections 1/7/8, interaction contracts, token refinements, copywriting contract for labels)
    - .planning/phases/03-landing-page-rebuild/03-CONTEXT.md (D-01 page architecture, D-02 hero & install widget, D-03 copy affordance, D-05 badges, D-06 mobile, D-07 demo panel deferred to 03-02)
    - .planning/phases/03-landing-page-rebuild/03-PATTERNS.md (InstallWidget pattern, Badges pattern, header/footer patterns, shared patterns section)
    - .planning/phases/03-landing-page-rebuild/03-RESEARCH.md (Starlight Tabs syncKey mechanics section 2, copy-to-clipboard section 3, badges section 6, token refinement section 9)
    - src/content/docs/docs/quickstart.mdx (existing Step 4 Tabs syncKey="client" at lines 120-152; mount point for new channel widget above Step 1)
    - scripts/verify-build.mjs (V1–V10 assertions that must keep passing)
  </read_first>
  <action>
    1. **Refine `design/tokens.css` in place** (per D-04, CONTEXT.md code_context "refine existing --jv-* values in place; Phase 3 refines the same variables, never forks them"):
       - Add `--jv-muted-soft` to `:root` with value `#9aa4ac`; add `--jv-muted-soft` to `[data-theme='dark']` with a value darker than `#5a6168` that passes 4.5:1 contrast against `--jv-chip-bg` dark (`#0a0e11`). Compute the exact value to pass.
       - Add `--jv-mask-opaque: #000` to `:root` only (theme-invariant — the mask is structurally invisible per UI-SPEC token refinements item 2).
       - Dark-mode contrast pass: verify `--jv-muted` dark (`#8c9399`) on `--jv-bg` dark (`#040506`) meets 4.5:1 AA for normal text. If not, bump toward `#969da3` (per UI-SPEC token refinements item 3).
       - Accent punch evaluation: if `--jv-accent-strong` dark (`#7cafe4`) appears too soft for CTA energy, increase saturation (per UI-SPEC token refinements item 4, Claude's Discretion).

    2. **Tokenize literals in `landing.css`**: replace the three `#9aa4ac` occurrences (lines 374, 403, 414 — `.mono-box .copy` idle, `.exchange-head`, `.exchange-body .ex-out`) with `var(--jv-muted-soft)`; replace `#000` at lines 240-241 (hero-art mask gradient stops) with `var(--jv-mask-opaque)`. Add new CSS classes needed by subsequent plans: `.demo-panel`, `.demo-step`, `.demo-nav` (stepper layout extending `.exchange`), `.gate-chip` (extends `.surface-chip` with warning tone using `color-mix` of `--jv-accent-soft` or a warm amber), `.badge-row`, `.badge` (badge layout). Keep existing classes intact.

    3. **Create `src/components/landing/InstallWidget.astro`**: an Astro component rendering three tab buttons (labels: "Installer + uv", "Plugin marketplace", "Manual uvx") and tab panels each containing the appropriate command as a `.mono-box[data-copy]` chip. The "Installer + uv" panel shows the curl setup.sh command (the primary install per D-02). The "Plugin marketplace" panel shows three client commands (Claude Code `/plugin marketplace add...`, Cursor deeplink note, Codex `codex plugin marketplace add...`). The "Manual uvx" panel shows `uv tool install jarvis-mcp`. Each command chip is a `.mono-box tap` with `data-copy` attribute. The component includes an inline script that: reads/writes `localStorage['starlight-synced-tabs__channel']` using the label string as value; renders `role="tablist"`/`role="tab"`/`aria-selected` attributes; dispatches a CustomEvent on tab change; on page load reads localStorage and activates the matching tab. Labels MUST match the quickstart's Starlight Tabs exactly (per RESEARCH section 2 syncKey mechanics). The component reuses the copy-to-clipboard delegation pattern from the existing `index.astro` script (delegate on `.mono-box[data-copy]`). Default tab is "Installer + uv".

    4. **Create `src/components/landing/Badges.astro`**: an Astro component rendering 4 inline SVG badges (GitHub, PyPI, MIT License, MCP Registry) in a `.badge-row` flex container. Each badge: self-contained SVG with `viewBox`, `fill`/`stroke` referencing `var(--jv-*)` tokens, pill shape using `var(--jv-radius)`, icon + text. GitHub badge links to the repo URL, PyPI to the package URL, MCP Registry to `io.github.jarvis-intelligence/jarvis` (per UI-SPEC integration points). Zero external image fetches (per D-06, preserving the verified zero-third-party-requests property).

    5. **Rewrite `src/pages/index.astro`**: replace the body contents with the new section order. Preserve: the front matter imports (tokens.css + landing.css), the entire `<head>` block (meta, favicon, anti-FOUC script), the `<html lang="en">` + `<body>` wrapper. Build the new structure:
       - **Header**: sticky header with brand SVG mark, condensed nav below 1060px (existing pattern), new anchor links: `#demo`, `#tools`, `#languages`, `#privacy`, `#architecture`, plus a "Docs" CTA `.primary-action tap` button linking to `/jarvis-index/docs/`. Keep the theme toggle button unchanged.
       - **Hero** (id="hero"): two-column `.hero-frame` grid on desktop (existing pattern). Left column: h1 (one-line value prop — Claude's Discretion wording per CONTEXT.md, consistent with Phase-2 docs voice using "structural code intelligence", "locally on your machine", "9 tools"), tagline (1-2 sentence expansion), the `InstallWidget` component. Right column: existing hero-art decorative SVG (retokenized to use `var(--jv-mask-opaque)` for the mask). Below hero content: `.hero-meta` line (MIT, macOS/Linux, open source — existing pattern).
       - **Demo placeholder** (id="demo"): empty `.frame section` with section-head (eyebrow + h2 + lede) — populated by plan 03-02.
       - **Tools placeholder** (id="tools"): empty `.frame section` with section-head — populated by plan 03-02.
       - **Languages placeholder** (id="languages"): empty `.frame section` with section-head — populated by plan 03-02.
       - **Privacy placeholder** (id="privacy"): empty `.frame section` with section-head — populated by plan 03-02.
       - **Architecture placeholder** (id="architecture"): empty `.frame section` with section-head — populated by plan 03-03.
       - **Badges + CTA** (closing section): `.closing` grid (existing 2-column desktop pattern). Left: h2 final CTA heading + `.text-link` to docs. Right: `Badges` component.
       - **Footer**: `.site-footer` / `.footer-inner` with updated links per D-01: MIT, GitHub, PyPI, MCP Registry, Discussions, Docs.
       - **Scripts**: preserve the existing copy-to-clipboard delegation script and theme toggle script byte-identical.

    6. **Mount channel widget on quickstart** (`src/content/docs/docs/quickstart.mdx`): insert a `<Tabs syncKey="channel">` block with three `TabItem` components ABOVE "## Step 1" (before line 16). Tab labels: "Installer + uv" (contains the curl command + note about `setup.sh`), "Plugin marketplace" (contains the three client install commands), "Manual uvx" (contains `uv tool install jarvis-mcp`). Keep the existing Step 4 `Tabs syncKey="client"` untouched — different concern (per D-02). Labels must match the landing widget exactly.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/jarvis-ai/jarvis-index && npm run build 2>&1 | tail -5 && node scripts/verify-build.mjs 2>&1</automated>
  </verify>
  <done>
    - `npm run build` exits 0
    - `node scripts/verify-build.mjs` passes all V1–V10 dimensions
    - `grep -c '#9aa4ac' src/styles/landing.css` returns 0 (all tokenized)
    - `grep -c '#000' src/styles/landing.css` returns 0 (all tokenized; use `grep -v 'rgba' | grep -c '#000'` to exclude rgba-zero)
    - `grep -c 'starlight-synced-tabs__channel' src/components/landing/InstallWidget.astro` returns >= 1 (syncKey wired)
    - `grep -c 'syncKey="channel"' src/content/docs/docs/quickstart.mdx` returns 1 (quickstart widget present)
    - `grep -rc 'Installer + uv' src/components/landing/InstallWidget.astro src/content/docs/docs/quickstart.mdx` returns 2 (label parity across surfaces)
    - `grep -c 'shields.io' dist/index.html` returns 0 (no external badge fetches — run after build)
    - `grep -c 'data-copy' dist/index.html` returns >= 3 (copy chips present — Installer, Plugin commands, Manual uvx)
    - Built `dist/index.html` contains the anti-FOUC script (`starlight-theme` localStorage read) and the theme toggle script
    - Built `dist/index.html` contains anchor links to `#demo`, `#tools`, `#languages`, `#privacy`, `#architecture`
  </done>
  <reversibility rating="reversible">Token value changes and CSS class additions are additive; the page rewrite is the main commit but everything is in git.</reversibility>
</task>

## Artifacts This Phase Produces

- `src/components/landing/InstallWidget.astro` — tabbed install widget component with Starlight syncKey sync
- `src/components/landing/Badges.astro` — 4 inline SVG badges component
- `design/tokens.css` — refined with `--jv-muted-soft`, `--jv-mask-opaque`, dark contrast adjustments
- `src/styles/landing.css` — tokenized (zero `#9aa4ac`/`#000` literals), new `.demo-panel`/`.demo-step`/`.demo-nav`/`.gate-chip`/`.badge-row`/`.badge` class stubs
- `src/pages/index.astro` — rewritten with new section order, hero+widget, badges+CTA, updated header/footer
- `src/content/docs/docs/quickstart.mdx` — channel widget mounted above Step 1

## Verification

```xml
<verify>
  <automated>cd /Users/ddphuong/Projects/jarvis-ai/jarvis-index && npm run build 2>&1 | tail -5 && node scripts/verify-build.mjs 2>&1</automated>
</verify>
```

Structural assertions (grep gates, run after build):
- Zero `#9aa4ac` in `src/styles/landing.css` (tokenized)
- Zero bare `#000` in `src/styles/landing.css` (tokenized; exclude `rgba`)
- `starlight-synced-tabs__channel` present in `InstallWidget.astro`
- `syncKey="channel"` present in `quickstart.mdx`
- Label string "Installer + uv" appears in both `InstallWidget.astro` and `quickstart.mdx` (exact parity)
- Zero `shields.io` references in `dist/index.html`
- At least 3 `data-copy` attributes in `dist/index.html`
- Anti-FOUC script present in `dist/index.html`
- Section anchors `#demo`, `#tools`, `#languages`, `#privacy`, `#architecture` in `dist/index.html`

## Source Coverage Audit

| Source | Item | Covered By | Status |
|--------|------|------------|--------|
| GOAL | Hero with copyable install | 03-01 T1 | ✅ |
| GOAL | Honestly tiered showcase | 03-02 T1 | ✅ |
| GOAL | Compact language matrix | 03-02 T2 | ✅ |
| GOAL | Local-first privacy | 03-02 T2 | ✅ |
| GOAL | Badges | 03-01 T1 | ✅ |
| GOAL | Simulated demo panel | 03-02 T1 | ✅ |
| GOAL | Architecture diagrams dark-mode-safe | 03-03 T1 | ✅ |
| GOAL | Tabbed install widget hero+quickstart | 03-01 T1 | ✅ |
| GOAL | Mobile responsive | 03-04 T1 | ✅ |
| REQ LNDG-01 | Hero value prop + copyable install | 03-01 T1 | ✅ |
| REQ LNDG-02 | Tiered 9-tool showcase | 03-02 T1 | ✅ |
| REQ LNDG-03 | Language matrix | 03-02 T2 | ✅ |
| REQ LNDG-04 | Privacy section | 03-02 T2 | ✅ |
| REQ LNDG-05 | Badges | 03-01 T1 | ✅ |
| REQ LNDG-06 | Mobile responsive | 03-04 T1 | ✅ |
| REQ LNDG-07 | Demo panel | 03-02 T1 | ✅ |
| REQ LNDG-08 | Copy-to-clipboard | 03-01 T1 | ✅ |
| REQ LNDG-09 | Architecture diagrams | 03-03 T1 | ✅ |
| REQ LNDG-10 | Tabbed install widget | 03-01 T1 | ✅ |
| RESEARCH | Astro componentization | 03-01 T1 (components created) | ✅ |
| RESEARCH | Starlight Tabs syncKey | 03-01 T1 (syncKey wired) | ✅ |
| RESEARCH | Copy-to-clipboard | 03-01 T1 (reuse pattern) | ✅ |
| RESEARCH | Demo data JSON | 03-02 T1 (created) | ✅ |
| RESEARCH | Badges inline SVG | 03-01 T1 (Badges.astro) | ✅ |
| RESEARCH | Mobile containment | 03-04 T1 | ✅ |
| RESEARCH | Token refinement | 03-01 T1 (tokens.css) | ✅ |
| RESEARCH | Build/verify impact | 03-01 T1 (verify-build V1-V10) | ✅ |
| CONTEXT D-01 | Section order | 03-01 T1 (page skeleton) | ✅ |
| CONTEXT D-02 | Install widget tabs | 03-01 T1 (InstallWidget) | ✅ |
| CONTEXT D-03 | Copy affordance | 03-01 T1 (data-copy chips) | ✅ |
| CONTEXT D-04 | Token refinement | 03-01 T1 (tokens.css) | ✅ |
| CONTEXT D-05 | Badges | 03-01 T1 (Badges.astro) | ✅ |
| CONTEXT D-06 | Mobile | 03-04 T1 | ✅ |
| CONTEXT D-07 | Demo panel | 03-02 T1 | ✅ |

## Threat Model

<threat_model>
**ASVS Level 1, blocking threshold: high**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| External request surface | Low | All assets are inline SVG or self-hosted. Zero third-party font/CDN/badge requests. Verified by `verify-build.mjs` V4 (font-origin scan). No new external domains introduced. |
| XSS via user-derived content | N/A | All content is static authored HTML — no user input, no DOM injection, no `innerHTML` assignment. The only runtime JS is clipboard API access and tab visibility toggling. |
| Clipboard API misuse | Low | `navigator.clipboard.writeText` only writes the static `data-copy` attribute value — no user-supplied strings. Fallback path (`execCommand`) is read-only of the same attribute. |
</threat_model>

## Estimate

`estimate` tokens=14000 raw_tokens=14000 confidence=no-calibration-data (first phase-3 plan; Phase 2 averaged 12 min/plan for similar static-content tasks)
