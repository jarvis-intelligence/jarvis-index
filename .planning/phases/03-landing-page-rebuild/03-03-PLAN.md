---
wave: 3
depends_on:
  - 03-01-PLAN.md
  - 03-02-PLAN.md
files_modified:
  - src/components/landing/Diagrams.astro
  - src/pages/index.astro
  - src/styles/landing.css
autonomous: true
requirements:
  - LNDG-06
  - LNDG-09
---

# 03-03: Architecture Diagrams, Mobile Responsiveness, Structural Verification

## Objective

Fill the architecture section with two dark-mode-safe inline SVG diagrams hand-adapted from the `.dot` sources (jarvis-layers 7-layer architecture, jarvis-index-pipeline with failure semantics), then run a full mobile-responsiveness pass across every section at 390px, and perform structural verification: label-sync assertion, token coverage, zero hardcoded hex, accessibility attributes, and build+verify-build green.

## must_haves

### truths

- The architecture section contains two inline SVGs: jarvis-layers (7-layer architecture with runtime half, storage seam, indexing half) and jarvis-index-pipeline (preflight → full/search-only → terminal states OK/SO/FAIL)
- Both SVGs use CSS class references (`.diagram .box`, `.diagram .t`, `.diagram .flow`, etc.) or `var(--jv-*)` attributes for ALL fill/stroke/color values — zero hardcoded hex colors in the SVG markup
- Both SVGs render correctly in light and dark themes without CSS `filter` hacks — colors resolve via the `.diagram` CSS class rules in `landing.css` that reference `--jv-*` tokens
- The pipeline diagram's FAIL terminal node uses a warm red tone (via `.diagram .node-fail` or `var(--jv-diagram-red)` if the token was added)
- The layers diagram's runtime cluster, indexing cluster, and storage seam are visually distinct (runtime = accent blue family, indexing = green tone, storage = warm neutral)
- Both SVGs have `role="img"`, `<title>`, and `<desc>` elements for accessibility
- Both SVGs scale via `max-width: 100%; height: auto` inside `.diagram-wrap` (per UI-SPEC unclassified → resolved consideration)
- The page has no horizontal scroll at 390px viewport width — verified by structural assertions on container classes
- The language matrix region scrolls horizontally within its `.table-wrap` container at 390px (not page-level)
- The sticky header is condensed (nav hidden) below 1060px (existing media query preserved)
- All interactive elements have `role`, `aria-selected`, `aria-label` attributes where needed (install widget tabs, demo stepper, copy chips)
- `npm run build` exits 0 and `node scripts/verify-build.mjs` passes V1–V10
- The three install widget channel labels are identical in `dist/index.html` and `dist/docs/quickstart/index.html` (label-sync assertion)

## Tasks

<task type="auto">
  <name>Task: Dark-mode-safe architecture diagrams from .dot sources</name>
  <files>src/components/landing/Diagrams.astro, src/pages/index.astro, src/styles/landing.css</files>
  <read_first>
    - .planning/phases/03-landing-page-rebuild/03-UI-SPEC.md (section 6: Architecture Diagrams — node color mapping, caption, no hardcoded hex)
    - .planning/phases/03-landing-page-rebuild/03-CONTEXT.md (D-07 diagrams — hand-adapt from .dot, CSS variable driven, no filter hack)
    - .planning/phases/03-landing-page-rebuild/03-PATTERNS.md (Diagrams.astro pattern, diagram CSS classes, .dot source geometry reference)
    - .planning/phases/03-landing-page-rebuild/03-RESEARCH.md (section 4: diagrams — dot available but output has hardcoded fills; hand-adapt approach; .dot as geometry reference)
    - ../jarvis/docs/assets/jarvis-layers.dot (7-layer structure, cluster groupings, edge labels)
    - ../jarvis/docs/assets/jarvis-index-pipeline.dot (preflight → full/search-only → terminal states, failure semantics, MODE diamond)
    - src/styles/landing.css (existing `.diagram`, `.box`, `.box-accent`, `.t`, `.s`, `.flow`, `.flow-cap`, `.machine`, `.m-label` CSS classes at lines 449-470)
    - src/pages/index.astro (architecture placeholder section)
  </read_first>
  <action>
    1. **Add diagram semantic color classes to `landing.css`**: extend the existing `.diagram` CSS class block with new classes for the two diagrams' semantic groups:
       - `.diagram .cluster-runtime` — background fill using `color-mix(in oklch, var(--jv-accent) 8%, transparent)`, border `color-mix(in oklch, var(--jv-accent) 30%, transparent)` (matches existing `.box-accent` tone — runtime is accent-blue per .dot source)
       - `.diagram .cluster-index` — background fill `color-mix(in oklch, var(--jv-diagram-green) 12%, transparent)`, border `color-mix(in oklch, var(--jv-diagram-green) 35%, transparent)`. Visually distinct from runtime-accent.
       - `.diagram .cluster-storage` — background fill `color-mix(in oklch, var(--jv-diagram-amber) 10%, transparent)`, border `color-mix(in oklch, var(--jv-diagram-amber) 30%, transparent)` — warm neutral for the storage seam / MODE diamond.
       - `.diagram .node-fail` — fill `color-mix(in oklch, var(--jv-diagram-red) 15%, transparent)`, border `color-mix(in oklch, var(--jv-diagram-red) 60%, transparent)`. Used for the FAIL terminal in the pipeline diagram.
       - `.diagram .node-ok` — fill `color-mix(in oklch, var(--jv-diagram-green) 18%, transparent)`, border `color-mix(in oklch, var(--jv-diagram-green) 45%, transparent)`
       - **New tokens** added to `design/tokens.css` (both `[data-theme]` variants tuned for contrast): `--jv-diagram-green: #16a34a` (light) / `#4ade80`-family (dark), `--jv-diagram-red: #b91c1c` (light) / `#f87171`-family (dark), `--jv-diagram-amber: #b45309` (light) / `#fbbf24`-family (dark). NO raw hex appears in `landing.css` or the component — only via these tokens.
       - All new classes inherit `font-family: var(--jv-mono)` and appropriate sizing from the existing `.t` / `.s` patterns.

    2. **Create `src/components/landing/Diagrams.astro`**: render two `.diagram-wrap` containers, each with an inline `<svg>` and a `.caption` paragraph below.

       **Diagram 1 — jarvis-layers**: hand-adapt the 7-layer architecture from `jarvis-layers.dot`. Structure: two `subgraph` groups (runtime half with L1/L2/L3, indexing half with L5/L6/L7) surrounding L4 (storage seam). Use `<rect>` elements with the semantic CSS classes (`.cluster-runtime` for the runtime group background, `.cluster-index` for the indexing group background, `.cluster-storage` for L4). Individual nodes use `.box` or `.box-accent` classes. Text uses `.t` (main label) and `.s` (sub-label). Edges use `.flow` + `.flow-cap`. The runtime cluster label and indexing cluster label use `.m-label`. A comment at the top of the SVG notes the source: `<!-- Hand-adapted from ../jarvis/docs/assets/jarvis-layers.dot -->`. The SVG must have `role="img"`, `<title id="layers-title">`, `<desc id="layers-desc">`.

       **Diagram 2 — jarvis-index-pipeline**: hand-adapt the index pipeline from `jarvis-index-pipeline.dot`. Structure: preflight cluster (P1–P4), MODE diamond, full pipeline cluster (F1–F6), search-only cluster (S1–S2), three terminal nodes (OK, SO, FAIL). Use semantic classes: `.cluster-runtime` for the full pipeline, `.cluster-index` for search-only, `.cluster-storage` for the MODE diamond, `.node-ok` for OK/SO, `.node-fail` for FAIL, `.box` for preflight nodes. Edges: `.flow` for normal flow; error/fallback edges use a dedicated `.flow-fail` class whose stroke is `var(--jv-diagram-red)` — NO inline `color="#…"` attribute anywhere. The diamond sh…

       Both SVGs set `background: transparent` on the top-level `<svg>` (or rely on the parent container). Neither uses `currentColor` as a fill proxy (use explicit class references for clarity). The SVG `viewBox` dimensions come from the graphviz `dot` output geometry (run `dot -Tsvg` locally if needed for reference, but the final SVG is hand-written to use CSS classes, not the graphviz output directly).

    3. **Fill the `#architecture` section** in `index.astro`: replace placeholder with the `Diagrams` component inside `.frame.section` skeleton. Section eyebrow e.g. "Architecture", heading e.g. "Seven layers, two halves, one seam", lede describing the runtime/indexing split and the storage seam.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/jarvis-ai/jarvis-index && npm run build 2>&1 | tail -5 && node scripts/verify-build.mjs 2>&1</automated>
  </verify>
  <done>
    - `grep -c 'cluster-runtime' src/components/landing/Diagrams.astro` returns >= 1
    - `grep -c 'node-fail' src/components/landing/Diagrams.astro` returns >= 1
    - `grep -c 'role="img"' src/components/landing/Diagrams.astro` returns 2
    - `grep -rc 'fill="#' src/components/landing/Diagrams.astro` returns 0 (zero hardcoded hex fills — use `grep -c 'fill="#[0-9a-fA-F]'` to be precise)
    - `grep -c 'jarvis-layers.dot' src/components/landing/Diagrams.astro` returns 1 (source attribution comment)
    - `grep -c 'jarvis-index-pipeline.dot' src/components/landing/Diagrams.astro` returns 1 (source attribution comment)
    - `grep -c 'filter' src/components/landing/Diagrams.astro` returns 0 (no CSS filter hacks)
    - `npm run build` exits 0, `verify-build` V1–V10 pass
  </done>
  <reversibility rating="reversible">New component; page section swap in git.</reversibility>
</task>

<task type="checkpoint:structural">
  <name>Task: Mobile responsiveness pass + structural verification</name>
  <files>src/pages/index.astro, src/styles/landing.css</files>
  <read_first>
    - .planning/phases/03-landing-page-rebuild/03-UI-SPEC.md (responsive breakpoints table, mobile minimum 390px, overflow consideration, interaction contracts)
    - .planning/phases/03-landing-page-rebuild/03-CONTEXT.md (D-06 mobile — single-column collapse, matrix contained scroll, condensed header, no page-level horizontal scroll at 390px)
    - .planning/phases/03-landing-page-rebuild/03-RESEARCH.md (section 7: mobile — matrix containment, header condenses, tabs a11y, reduced-motion)
    - .planning/phases/03-landing-page-rebuild/03-PATTERNS.md (shared patterns: reduced-motion, zero third-party, base path)
    - src/styles/landing.css (all media queries, `.table-wrap`, `.header-nav` display breakpoints, `.hero-frame` grid, `.card-grid` column rules)
    - src/pages/index.astro (all sections)
    - src/components/landing/InstallWidget.astro (tab a11y attributes)
    - src/components/landing/DemoPanel.astro (stepper a11y)
  </read_first>
  <action>
    1. **Mobile responsiveness audit and fix**: review every section at the 390px minimum breakpoint. Ensure:
       - Card grids collapse to single-column below 760px (existing `.card-grid` media query — verify it applies to the new ToolShowcase cards)
       - The language matrix `.table-wrap` has `overflow-x: auto` and `-webkit-overflow-scrolling: touch` (existing pattern — verify it wraps the new LanguageMatrix table)
       - No section content overflows horizontally at 390px — add `max-width: 100%` to any new container that might need it (diagram SVGs, demo JSON blocks, install widget tabs)
       - The sticky header is condensed (nav hidden, only brand + theme toggle + mobile menu if needed) below 1060px — the existing `.header-nav { display: none }` / `display: flex` breakpoint at 1060px must apply to the new nav items
       - The hero frame collapses to single-column on mobile (existing `.hero-frame` media query)
       - The `.closing` badges grid collapses to single-column on mobile (existing `.closing` media query at 900px)
       - The footer `.footer-inner` adapts to full-width on mobile
       - Install widget tabs are horizontally scrollable if they overflow on narrow screens, or wrap appropriately — ensure `role="tablist"` container does not cause page-level overflow

    2. **Accessibility attributes audit**: verify every interactive element:
       - Install widget: `role="tablist"` on the tab container, `role="tab"` with `aria-selected` on each tab button, `role="tabpanel"` with `aria-labelledby` on each panel, arrow-key roving tabindex (left/right arrows move focus between tabs, Home/End jump to first/last)
       - Demo stepper: step indicators are buttons with `aria-label` (e.g. "Step 1: Check index status"), prev/next buttons have `aria-label`, active step is indicated via `aria-current="step"` or equivalent
       - Copy chips: `.mono-box` buttons have `aria-label` describing the copy action (e.g. "Copy install command to clipboard")
       - Diagrams: `role="img"`, `<title>`, `<desc>` present (verified in 03-03 T1 done criteria)
       - Nav links: the sticky header anchor links have appropriate text content for screen readers

    3. **Label-sync structural assertion**: verify the three channel labels are identical across the landing and quickstart surfaces. The executor should add a grep-based check in the done criteria.

    4. **Token coverage assertion**: verify no hardcoded hex colors exist in any new component or in the diagram CSS classes of `landing.css` (only acceptable exception: `rgba(0,0,0,...)` transparency and pre-existing literals elsewhere in landing.css). Diagram semantics flow exclusively through `--jv-diagram-green` / `--jv-diagram-red` / `--jv-diagram-amber` tokens defined in `design/tokens.css`. Add grep gate: `! grep -qE '#[0-9a-fA-F]{3,8}' src/components/landing/Diagrams.astro` and `grep -c -- '--jv-diagram-' design/tokens.css` returns >= 6 (3 tokens × 2 themes).

    5. **Reduced-motion verification**: confirm the demo stepper's transitions are gated by `@media (prefers-reduced-motion: reduce)` (added in 03-02 T1) and that no new transitions were added without this gate.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/jarvis-ai/jarvis-index && npm run build 2>&1 | tail -5 && node scripts/verify-build.mjs 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and `node scripts/verify-build.mjs` passes V1–V10
    - `grep -c 'role="tablist"' dist/index.html` returns >= 1 (install widget a11y)
    - `grep -c 'role="tabpanel"' dist/index.html` returns >= 1
    - `grep -c 'aria-selected' dist/index.html` returns >= 3 (three tabs)
    - `grep -c 'aria-label' dist/index.html` returns >= 5 (multiple a11y labels)
    - `grep -c 'prefers-reduced-motion' src/styles/landing.css` returns >= 1 (reduced-motion gate present)
    - The string "Installer + uv" appears in both `dist/index.html` and `dist/docs/quickstart/index.html` with identical content (label-sync — extract and compare via grep)
    - Zero new hardcoded hex fill/stroke in `src/components/landing/Diagrams.astro` (verified in 03-03 T1 but re-confirm after any mobile fixes)
    - `grep -rc 'overflow-x.*auto' src/styles/landing.css` returns >= 1 (contained scroll regions present)
  </acceptance_criteria>
  <done>
    All acceptance criteria pass. Mobile-responsiveness and structural verification complete.
  </done>
  <reversibility rating="reversible">CSS tweaks and a11y attribute additions; all in git.</reversibility>
</task>

## Artifacts This Phase Produces

- `src/components/landing/Diagrams.astro` — 2 dark-mode-safe inline SVG diagrams (jarvis-layers, jarvis-index-pipeline)
- `src/styles/landing.css` — new diagram semantic color classes, mobile responsiveness fixes
- `src/pages/index.astro` — architecture section filled, mobile pass applied

## Verification

```xml
<verify>
  <automated>cd /Users/ddphuong/Projects/jarvis-ai/jarvis-index && npm run build 2>&1 | tail -5 && node scripts/verify-build.mjs 2>&1</automated>
</verify>
```

Structural assertions (grep gates, run after build):
- `Diagrams.astro` has zero `fill="#` hardcoded hex (excluding acceptable rgba)
- Both SVGs have `role="img"`, `<title>`, `<desc>`
- `.cluster-runtime`, `.node-fail` semantic classes present in built output
- `role="tablist"` and `aria-selected` in built landing (install widget a11y)
- Channel label "Installer + uv" identical in `dist/index.html` and `dist/docs/quickstart/index.html`
- `prefers-reduced-motion` gate in `landing.css`
- `npm run build` + `verify-build` V1–V10 pass

## Threat Model

<threat_model>
**ASVS Level 1, blocking threshold: high**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| External request surface | Low | Diagram SVGs are inline — no external SVG fetches. No new network requests. |
| XSS | N/A | SVGs are static authored markup with no user-controlled attributes. |
| Accessibility regression | Medium | A11y attributes (role, aria-*) verified by grep gates. Manual Chrome audit deferred to orchestrator verification pass. |
</threat_model>

## Estimate

`estimate` tokens=20000 raw_tokens=20000 confidence=no-calibration-data
