---
phase: 05-launch-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/asciinema/asciinema-player.min.js
  - public/assets/asciinema/asciinema-player.css
  - src/content/docs/docs/troubleshooting/index.md
  - docs/system-architecture.md
  - docs/deployment-guide.md
  - docs/code-standards.md
  - scripts/crawl-urls.sh
  - scripts/claims-audit.py
autonomous: true
requirements: [VRFY-02, VRFY-03, VRFY-04, COMM-01]

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - Every factual claim on the landing and quickstart is traceable to a source artifact (README, CHANGELOG, setup.sh, manifests, or ../jarvis source)
    - The crawl script exists and can check all 39 URLs from url-contract.json against the live site
    - The sitemap-equality check is scriptable and covers all contract URLs
    - The three maintainer docs describe the new Astro + Starlight site structure (not the old VitePress/site/ split)
    - The docs troubleshooting page links to GitHub Discussions as the support surface
    - Discussions are confirmed enabled (already verified by research)
    - The asciinema-player static assets are in public/assets/asciinema/ (self-hosted, zero third-party requests)
  artifacts:
    - scripts/crawl-urls.sh
    - scripts/claims-audit.py
    - docs/claims-audit-table.md
    - public/assets/asciinema/asciinema-player.min.js
    - public/assets/asciinema/asciinema-player.css
  key_links:
    - Troubleshooting Discussions link → https://github.com/jarvis-intelligence/jarvis-index/discussions
    - url-contract.json → crawl script reads it directly
    - asciinema-player assets → quickstart embed in 05-02 references them
---

<objective>
Prepare all site content, tooling, and maintainer docs for the launch verification sequence: self-host asciinema-player assets, add the Discussions link to troubleshooting, update maintainer docs to the new Astro + Starlight site structure, author the claims-audit and crawl scripts, and produce the claims audit table. This plan produces everything the orchestrator needs for 05-02's live verification sequence.

Purpose: All content work that can be done BEFORE merge/deploy — no live site needed.
Output: Self-hosted player assets, updated maintainer docs, crawl/audit tooling, claims audit table, Discussions link in troubleshooting.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-launch-verification/05-CONTEXT.md
@.planning/phases/05-launch-verification/05-RESEARCH.md
@scripts/verify-build.mjs
@design/url-contract.json
@docs/system-architecture.md
@docs/deployment-guide.md
@docs/code-standards.md
@src/content/docs/docs/troubleshooting/index.md
@public/assets/
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Self-host asciinema-player assets + Discussions link in troubleshooting</name>
  <files>
    public/assets/asciinema/asciinema-player.min.js
    public/assets/asciinema/asciinema-player.css
    src/content/docs/docs/troubleshooting/index.md
  </files>
  <read_first>
    - Verify `npm view asciinema-player version` is 3.17.0 (per 05-RESEARCH.md)
    - Verify Discussions already enabled: `gh api repos/jarvis-intelligence/jarvis-index --jq '.has_discussions'` → `true`
    - Read `src/content/docs/docs/troubleshooting/index.md` — insert point is before the "## Reference pages" section (line 61-66), adding a "Getting help" paragraph with a link to Discussions
    - Landing footer at `src/pages/index.astro:210` already links to Discussions — no change needed there
  </read_first>
  <action>
    Download and self-host the asciinema-player static assets (per D-02 — zero third-party requests). Run `npm pack asciinema-player@3.17.0`, extract the tarball, copy `package/dist/bundle/asciinema-player.min.js` and `package/dist/bundle/asciinema-player.css` into `public/assets/asciinema/`. These are `public/` passthrough files — Astro copies them to `dist/` unhashed. Clean up the temporary tarball and package directory after extraction.

    Add a "Getting help" paragraph to `src/content/docs/docs/troubleshooting/index.md` between the last troubleshooting entry (line 60) and the `---` horizontal rule before "## Reference pages" (line 61). The paragraph links to `https://github.com/jarvis-intelligence/jarvis-index/discussions` as the support surface. Keep it one short sentence matching the troubleshooting page's existing voice. Per D-05 (COMM-01): Discussions are already enabled, so only the link edit is needed — no API call.
  </action>
  <verify>
    <automated>
      test -f public/assets/asciinema/asciinema-player.min.js && test -f public/assets/asciinema/asciinema-player.css && echo 'OK: assets exist'
      grep -F 'github.com/jarvis-intelligence/jarvis-index/discussions' src/content/docs/docs/troubleshooting/index.md > /dev/null && echo 'OK: discussions link'
    </automated>
  </verify>
  <done>
    asciinema-player.min.js (~185 KB) and asciinema-player.css (~19 KB) exist in public/assets/asciinema/. The troubleshooting page links to GitHub Discussions. The landing footer already links there — no change. Zero third-party requests preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author crawl script + claims-audit script + execute claims audit table</name>
  <files>
    scripts/crawl-urls.sh
    scripts/claims-audit.py
    docs/claims-audit-table.md
  </files>
  <read_first>
    - Read `design/url-contract.json` for the 39 canonical URLs (origin + path list)
    - Read `scripts/verify-build.mjs` V9/V10 for the sitemap-equality pattern already used in CI
    - Read the landing page `src/pages/index.astro` for factual claims (hero value prop, 9-tool count, language matrix, privacy section, badges, architecture diagrams)
    - Read `src/content/docs/docs/quickstart.mdx` for command/output claims
    - Read `src/content/docs/docs/guide/requirements.md` for the language-support matrix claims
    - Read `plugin/skills/jarvis-use/references/tool-roster.md` for the 9-tool evidence
    - Read `../jarvis/README.md` and `../jarvis/CHANGELOG.md` as evidence sources
  </read_first>
  <action>
    **Crawl script** (`scripts/crawl-urls.sh`): Write a POSIX-sh script with TWO URL sets: (a) the 39 canonical URLs read from `design/url-contract.json`, and (b) the 34 pre-rebuild URLs from the Phase-2 classification (02-01-SUMMARY.md's keep-list — embed them as a here-doc list; they cover the roadmap SC-3 wording "every retired URL serves content or a redirect"). Curl each against the origin; canonical set expects 200; retired set expects 200 or 301/302 (redirect targets must themselves land 200 — follow with `-L` and require a final 200). Append a sitemap-equality check: fetch live `sitemap-0.xml`, extract `<loc>` URLs, diff against the contract-derived list. Report per-URL OK/FAIL, exit non-zero on any failure. Optional `ORIGIN` env var defaulting to the production URL. Designed for the orchestrator to run against the LIVE site in 05-02.

    **Claims audit script** (`scripts/claims-audit.py`): Write a Python script that reads landing + quickstart + requirements markdown, extracts sentences containing factual claims (heuristic: sentences with numbers, tool names, version references, or capability statements), and emits a CSV with columns: surface_file, section, claim_excerpt, evidence_artifact, evidence_locator. This is a tool for the orchestrator — the human fills the evidence columns.

    **Claims audit table** (`docs/claims-audit-table.md`): Produce the VRFY-02 audit table per RESEARCH.md's schema. Cover these surfaces with concrete claims:
    - Landing hero (value prop, install command validity)
    - Landing tools section (9 tools, honest tiering — semanticSearch extra, typeHierarchy fork)
    - Landing language matrix (4 nav families, 10 search-only)
    - Landing privacy ("Nothing leaves the machine")
    - Quickstart Steps 1-5 (commands, expected output shapes, error shapes)
    - Requirements page (platform requirements, language caveats)
    Every row MUST have an evidence artifact path and a specific locator (section, line range, or commit). Leave Status as `Traced` for rows with clear evidence, `Needs review` for any that require judgment. Per D-03 (VRFY-02): trace every factual sentence on landing + quickstart + requirements to README / CHANGELOG / synced setup.sh / manifests.
  </action>
  <verify>
    <automated>
      bash -n scripts/crawl-urls.sh && echo 'OK: crawl script parseable'
      python3 -c 'import ast; ast.parse(open("scripts/claims-audit.py").read())' && echo 'OK: audit script parseable'
      grep -c '|' docs/claims-audit-table.md | head -1
    </automated>
  </verify>
  <done>
    `scripts/crawl-urls.sh` is a POSIX-sh script that crawls all 39 contract URLs and checks sitemap equality — runnable against a live origin. `scripts/claims-audit.py` is a Python 3 script that extracts claims to CSV. `docs/claims-audit-table.md` contains the completed audit table with every landing + quickstart + requirements claim traced to its evidence artifact.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update maintainer docs to the new Astro + Starlight site structure</name>
  <files>
    docs/system-architecture.md
    docs/deployment-guide.md
    docs/code-standards.md
  </files>
  <read_first>
    - `docs/system-architecture.md` — currently describes only plugin/setup.sh/binary distribution; needs the docs/landing site as a fifth outbound channel
    - `docs/deployment-guide.md` — currently has 4 channels; needs Channel 5 for the docs/landing site
    - `docs/code-standards.md` — currently has no mention of src/ content structure, design/tokens.css, or public/ conventions
    - Read `astro.config.mjs` for the current build config (base path, integrations)
    - Read `src/content/docs/` structure for the MDX content organization
  </read_first>
  <action>
    **system-architecture.md**: Add a "## Docs & Landing site (Channel 5)" section after the indexing pipeline section. Document: the site is Astro 5 + Starlight, landing at `/` (`src/pages/index.astro`), docs at `/docs/` (`src/content/docs/docs/`), design tokens in `design/tokens.css`, self-hosted fonts in `public/fonts/`, passthrough files in `public/` (llms.txt, asciinema assets, brand-logo.html), built by `npm run build`, deployed to GitHub Pages via `deploy-pages.yml` on push to `main`. Add `design/tokens.css` and `public/` to the workspace context table or as a new paragraph. Per D-04 (VRFY-04): update where they reference the old site structure.

    **deployment-guide.md**: Add a "## Channel 5 — Docs & Landing site" section after Channel 4. Document: the site builds with Astro + Starlight on Node 22, `deploy-pages.yml` triggers on `src/**`, `public/**`, `design/**`, `astro.config.*`, `package*.json`, the workflow runs `npm ci`, `npm run build` (which includes `scripts/verify-build.mjs` V1–V10), then copies `dist/` as the Pages artifact. Note the post-deploy smoke probe (curl `/` and `/docs/` → 200 + marker check). Per D-04.

    **code-standards.md**: Add a "## Site (Astro + Starlight)" section after the Documentation section. Document: MDX content lives in `src/content/docs/docs/` (kebab-case directories, `index.md` or `index.mdx` per page), Astro components in `src/components/`, design tokens sourced from `design/tokens.css`, static assets in `public/` (passthrough, not hashed), and the `src/pages/` directory for non-docs pages. Per D-04.
  </action>
  <verify>
    <automated>
      grep -c 'Astro' docs/system-architecture.md
      grep -c 'Astro' docs/deployment-guide.md
      grep -c 'src/content/docs' docs/code-standards.md
    </automated>
  </verify>
  <done>
    All three maintainer docs describe the new Astro + Starlight site structure. system-architecture.md has Channel 5. deployment-guide.md has Channel 5 with the build/deploy flow. code-standards.md has the site content conventions. No stale VitePress or `site/index.html` references exist (confirmed clean by RESEARCH.md grep).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → public/assets/ | Static JS/CSS downloaded from npm and committed to repo | 

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | asciinema-player npm package | medium | accept | Static assets only (no runtime execution context — loaded as `<script>` in browser, no Node.js). The package is Apache-2.0 with 500+ GitHub stars and regular releases. The `<script>` loads `is:inline` in Astro, not bundled, so no supply-chain code execution on the build server. |
| T-05-02 | Tampering | crawl-urls.sh against arbitrary origin | low | accept | Script takes ORIGIN env var — orchestrator passes the production URL. No user input reaches it. |
| T-05-03 | Information disclosure | claims-audit-table.md | low | accept | Audit table is a maintainer artifact in `docs/`, excluded from the Starlight site via `srcExclude` in astro.config.mjs. Not user-facing. |
</threat_model>

<verification>
1. asciinema-player assets exist in `public/assets/asciinema/` and are loadable (non-zero file sizes)
2. Troubleshooting page links to Discussions
3. `scripts/crawl-urls.sh` parses as valid POSIX sh and reads url-contract.json
4. `scripts/claims-audit.py` parses as valid Python 3
5. `docs/claims-audit-table.md` has ≥15 claim rows with evidence artifacts
6. All three maintainer docs mention the Astro + Starlight site structure
7. `npm run build` succeeds with the new assets and docs edits (no regressions before merge)
</verification>

<success_criteria>
- Self-hosted asciinema-player assets ready for embed in 05-02
- Discussions link added to troubleshooting (landing footer already done)
- Crawl script and claims-audit script authored and parseable
- Claims audit table covers landing + quickstart + requirements with evidence pointers
- Maintainer docs describe the new site structure (no stale VitePress references introduced)
- `npm run build` passes (no regressions from new assets or docs edits)
</success_criteria>

<output>
Create `.planning/phases/05-launch-verification/05-01-SUMMARY.md` when done
</output>
