---
phase: 01-site-foundation-identity
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - src/pages/index.astro
autonomous: true
requirements:
  - SITE-08
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "One storage key and one DOM attribute across both surfaces: the landing theme script reads/writes the Starlight theme storage key and sets the same data-theme attribute Starlight uses — a theme chosen on / holds on /docs/ and survives reloads in both directions"
    - "The landing applies the stored theme before first paint (inline anti-FOUC script in the head), mirroring Starlight's ThemeProvider semantics including the prefers-color-scheme fallback"
    - "Local search works across all docs pages: dist/docs/ contains the Pagefind index assets (js + json) and docs pages carry the search UI markup; typing a query returns hits spanning multiple pages"
    - "Toggling the theme twice returns both surfaces to the initial state (idempotent toggle — no drift, no stuck state); concurrent theme changes on / and /docs/ converge on the single storage key with last-write-wins and no per-surface divergence after reload"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "landing theme script unified with Starlight: shared storage key, data-theme attribute, inline pre-paint application"
      contains: "starlight-theme"
  key_links:
    - from: "src/pages/index.astro theme script"
      to: "Starlight ThemeProvider storage"
      via: "both surfaces read/write the same localStorage key and the same data-theme attribute — the cross-surface persistence link"
      pattern: "starlight-theme"
    - from: "Starlight build (pagefind default-on)"
      to: "dist/docs/pagefind assets"
      via: "Starlight runs Pagefind post-build over the emitted docs HTML; zero runtime service"
      pattern: "pagefind"
  prohibitions:
    - requirement_id: SITE-08
      category: privacy
      statement: "MUST NOT route search through any hosted service or add query/analytics telemetry — Pagefind is fully local (static index, in-browser execution); hosted search and analytics are hard out-of-scope fences for this project"
    - requirement_id: SITE-08
      category: values
      statement: "MUST NOT ship a dark mode that flashes the wrong theme on load — the inline pre-paint theme script is part of the requirement, not polish to defer; a toggle without anti-FOUC is a broken toggle"
---

<objective>
Complete SITE-08 on the unified shell: dark mode that persists across visits and across the / ↔ /docs/ boundary (one storage key, one attribute, pre-paint application on the landing), and Pagefind local search verified working across all docs pages (Starlight default, zero configuration, fully local).

Purpose: Starlight already ships both mechanisms (verified in package source: ThemeProvider/ThemeSelect with persistence + anti-FOUC; Pagefind default-on) — this plan's real work is unifying the LANDING's hand-rolled toggle onto Starlight's key/attribute semantics so the two surfaces behave as one site, then proving both features against the built artifact.

Output: Re-keyed landing theme script + verified search assets and interactions.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-site-foundation-identity/01-RESEARCH.md
@.planning/phases/01-site-foundation-identity/01-PATTERNS.md

Key anchors:
- 01-RESEARCH.md § Pattern 4 (dark mode = Starlight built-in + landing key unification; decision: standardize on ONE key and ONE attribute; FOUC avoidance requires the theme script inline in head), § Standard Stack pagefind row (default-on, disable flag exists), § Validation V5/V6
- 01-PATTERNS.md § Shared Patterns "Theme persistence — ONE storage key, ONE attribute" (the legacy landing script quoted with the key it uses today and the is:inline guidance)
- Source: src/pages/index.astro (the transplanted theme toggle script near the end of the file)
- Standing instrument note: the permanent pagefind-presence assertion (V5) is added to scripts/verify-build.mjs by 01-05 (same wave, different files); this plan verifies it directly against dist/
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Unify the landing theme script onto Starlight's key and attribute</name>
  <!-- planner-discipline-allow: jarvis-theme -->
  <read_first>
  - src/pages/index.astro (the theme toggle script at the end of the body and the head — the script moves inline into head as part of this task)
  - 01-PATTERNS.md § Shared Patterns "Theme persistence — ONE storage key, ONE attribute" (the legacy script quoted verbatim with its storage key; the replacement semantics: Starlight's key, data-theme attribute, is:inline, prefers-color-scheme fallback)
  - 01-RESEARCH.md § Pattern 4 (ThemeProvider.astro/ThemeSelect.astro verified behavior: localStorage key starlight-theme, document.documentElement.dataset.theme, inline anti-FOUC, system-preference fallback)
  - design/tokens.css (dark overrides keyed on [data-theme='dark'] — the attribute the script sets must match)
  </read_first>
  <files>src/pages/index.astro</files>
  <action>
  Replace the landing's theme script with a copy of Starlight's semantics: an inline script in the head (marked is:inline so Astro emits it verbatim, pre-paint) that reads the shared theme storage key starlight-theme from localStorage (wrapped in try/catch like the legacy script), falls back to prefers-color-scheme dark, and sets the data-theme attribute on the document root BEFORE first paint; the toggle control writes the same key with values light/dark/auto matching Starlight's ThemeSelect values. Remove every reference to the legacy landing storage key jarvis-theme — after this task exactly one key exists on both surfaces. Do not fork the storage model: if the landing needs different toggle visuals later, that is a ThemeSelect component override, never a second key (RESEARCH Pattern 4 decision). Keep the tokens' dark selector ([data-theme='dark']) as-is — it already matches.
  </action>
  <verify>
    <automated>npm run build && grep -q 'starlight-theme' dist/index.html && test "$(grep -c 'jarvis-theme' dist/index.html | tr -d ' ')" = "0" && grep -q 'data-theme' dist/index.html && grep -q 'prefers-color-scheme' dist/index.html</automated>
    <human-check>
    With npm run preview serving dist/: 1) open the landing, click the theme toggle to dark, reload — still dark (no flash); 2) navigate to /docs/ — dark there too; 3) toggle to light on /docs/, navigate back to / — light; 4) reload both — state held in both directions. Record result in SUMMARY (V6).
    </human-check>
  </verify>
  <acceptance_criteria>
  - Source assertion: src/pages/index.astro contains the shared key starlight-theme and the prefers-color-scheme fallback, and contains zero occurrences of the legacy key (grep count 0 on the built page).
  - Source assertion: the theme script is emitted inline in the head of dist/index.html (pre-paint — no external script fetch before the attribute is set).
  - Behavior assertion: dist/index.html sets the data-theme attribute on the document root.
  - Manual (recorded in SUMMARY): theme persists across reload and across the / ↔ /docs/ boundary in both directions; no wrong-theme flash on load.
  - CLI: npm run build exits 0.
  </acceptance_criteria>
  <done>Both surfaces share one theme storage key and one attribute; persistence verified manually across surfaces; no FOUC.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Pagefind local search across docs pages</name>
  <read_first>
  - 01-RESEARCH.md § Standard Stack pagefind row (Starlight bundles and runs it at build time; default-on; the disable flag exists — do NOT set it) and § Don't Hand-Roll docs-search row
  - 01-VALIDATION.md § Manual-Only Verifications (the interactive search check instructions)
  - astro.config.mjs (confirm no pagefind:false setting — search must stay default-on)
  </read_first>
  <files>src/pages/index.astro</files>
  <action>
  Search itself needs no implementation — Starlight's default wiring builds the Pagefind index over the emitted docs HTML. This task PROVES it on the built artifact: build, then assert the pagefind assets exist under dist/docs/ (the pagefind directory containing the index javascript and json entries) and that docs pages carry the search UI markup (the search dialog/button elements Starlight injects). If any assertion fails, the fix is configuration archaeology (a stray pagefind:false, a custom Head component dropping the markup) — not hand-rolling a search UI (Don't Hand-Roll table forbids it). The file scope is src/pages/index.astro only in the sense that NO file should need changing when green; treat any required change as a fix-forward with the reason recorded.
  </action>
  <verify>
    <automated>npm run build && test -d dist/docs/pagefind && test -n "$(find dist/docs/pagefind -name '*.js')" && test -n "$(find dist/docs/pagefind -name '*.json')" && grep -qi 'search' dist/docs/index.html && ! grep -q 'pagefind: false' astro.config.mjs</automated>
    <human-check>
    With preview serving dist/docs/: open the docs home, activate search, type a two-term query that exists on different pages (e.g. reindex zoekt) and confirm hits from multiple pages; click one hit and land on the page with the term highlighted. Record in SUMMARY (V5 interactive half).
    </human-check>
  </verify>
  <acceptance_criteria>
  - Behavior assertion: dist/docs/pagefind/ exists and contains at least one .js and one .json index file.
  - Behavior assertion: the built docs home HTML contains search-related markup (case-insensitive match).
  - Source assertion: astro.config.mjs contains no pagefind:false directive.
  - Manual (recorded in SUMMARY): a typed query returns hits across multiple docs pages; clicking a hit navigates correctly.
  - CLI: npm run build exits 0.
  </acceptance_criteria>
  <done>Local search proven working across the docs corpus with zero external services.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → same-origin static assets | Search and theme state never leave the browser: Pagefind executes in-page against local index files; theme persists to localStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Information Disclosure | search queries | medium | mitigate | Pagefind is fully client-side against bundled static index files; the plan adds no network calls, and the prohibition blocks any hosted-search/analytics drift |
| T-01-10 | Tampering | inline theme script | low | mitigate | Script is small, repo-authored, is:inline verbatim — diff-reviewable; no external script injection surface introduced |
</threat_model>

<verification>
- Quick command: npm run build plus Task 1/2 automated grep sets.
- Manual instruments (recorded in SUMMARY per 01-VALIDATION.md Manual-Only table): V6 theme persistence both directions; V5 interactive search query.
</verification>

<success_criteria>
- One key, one attribute, no FOUC, persistence proven across surfaces.
- Pagefind index present and interactively returning multi-page hits.
</success_criteria>

<output>
Create `.planning/phases/01-site-foundation-identity/01-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces (01-04)

- Modified: `src/pages/index.astro` — head inline theme script (storage key starlight-theme, values light/dark/auto, prefers-color-scheme fallback, data-theme pre-paint application); legacy landing storage key removed
- Build outputs (Starlight-provided, not repo files): dist/docs/pagefind/ index assets; search UI markup on docs pages
- npm deps: none new (Pagefind ships via Starlight's bundled pagefind dependency; no standalone install)
- Script commands / workflow jobs: none new
