---
phase: 01-site-foundation-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/.vitepress/config.ts
  - .github/workflows/deploy-pages.yml
autonomous: true
requirements:
  - SITE-01
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The deployed docs at https://jarvis-intelligence.github.io/jarvis-index/docs/ return 200 fully styled — every hashed asset URL the page references returns 200"
    - "The deployed sitemap.xml lists every docs URL with the /docs path segment, and a sitemap-listed deep URL (e.g. /docs/cli/forget) returns 200 live"
    - "The deploy workflow fails loudly when / or /docs/ is not 200, lacks the jarvis marker, or a referenced hashed asset 404s (post-deploy smoke probe)"
    - "The hotfix ships as exactly two config-line changes plus the smoke-probe workflow step — no unrelated edits ride along"
  artifacts:
    - path: "docs/.vitepress/config.ts"
      provides: "correct asset prefix + sitemap hostname for the Pages project path"
      contains: "base: '/jarvis-index/docs/'"
    - path: ".github/workflows/deploy-pages.yml"
      provides: "post-deploy smoke probe step running after deploy-pages@v4"
      contains: "Smoke probe"
  key_links:
    - from: "docs/.vitepress/config.ts (base)"
      to: "built HTML asset hrefs in docs/.vitepress/dist/"
      via: "VitePress derives absolute asset URLs from base at build time"
      pattern: "/jarvis-index/docs/assets/"
    - from: "deploy-pages.yml smoke probe step"
      to: "live GitHub Pages origin"
      via: "curl after the deploy-pages@v4 step in the same job"
      pattern: "curl .*jarvis-intelligence\\.github\\.io"
  prohibitions:
    - requirement_id: SITE-01
      category: transparency
      statement: "MUST NOT weaken the smoke probe into an always-pass step — a non-200 response, a missing jarvis marker, or a 404 hashed asset must fail the workflow loudly; a probe made tolerant of failure defeats the requirement's purpose (silence becomes failure)"
---

<objective>
Stop the bleeding on the live site today: fix the verified base-path asset 404 and the sitemap trailing-slash rot in the current VitePress config (two lines), and land the post-deploy smoke probe that proves the fix live — stack-independent of the Astro migration in 01-02.

Purpose: SITE-01. The live docs are unstyled right now (verified 2026-08-21: domain-root /docs/assets/*.css → 404, /jarvis-index/docs/assets/*.css → 200; sitemap loc URLs missing the /docs segment). The probe is the feedback loop that makes this class of breakage loud instead of silent.

Output: 2-line config hotfix + smoke-probe workflow step, proven by a dispatched deploy and live curls.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-site-foundation-identity/01-RESEARCH.md
@.planning/phases/01-site-foundation-identity/01-PATTERNS.md

Key research anchors (executor: read these sections, not the whole file):
- 01-RESEARCH.md § Pattern 1 (root cause, both bugs, verified probe evidence) and § Code Examples "The VitePress hotfix (plan 01-01 — verified minimal)"
- 01-RESEARCH.md § Pattern 5 (smoke probe step, exact failure mode observed 2026-08-21)
- 01-PATTERNS.md § "`docs/.vitepress/config.ts` (config, build-time — plan 01-01 two-line hotfix)" (current lines quoted verbatim)
- Source files: docs/.vitepress/config.ts (135 lines), .github/workflows/deploy-pages.yml (59 lines)
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Two-line VitePress hotfix — base prefix + sitemap trailing slash</name>
  <read_first>
  - docs/.vitepress/config.ts (entire file — the two changed lines sit in context of cleanUrls/ignoreDeadLinks/lastUpdated/srcExclude)
  - .planning/phases/01-site-foundation-identity/01-RESEARCH.md (§ Pattern 1 "One origin constant" + § Code Examples "The VitePress hotfix")
  - .planning/phases/01-site-foundation-identity/01-PATTERNS.md (§ "`docs/.vitepress/config.ts` … two-line hotfix" — quotes current lines 8 and 14)
  </read_first>
  <files>docs/.vitepress/config.ts</files>
  <action>
  Change exactly two values in docs/.vitepress/config.ts. (1) Line 8: replace the base value '/docs/' with '/jarvis-index/docs/' — the Pages project site serves under /jarvis-index/, so the base must carry the project prefix plus the docs segment. (2) Line 14: append the missing trailing slash to the sitemap hostname so it reads 'https://jarvis-intelligence.github.io/jarvis-index/docs/' — without it the bundled sitemap lib treats docs as a replaceable path segment and emits loc URLs without /docs. Touch NOTHING else: cleanUrls, ignoreDeadLinks, lastUpdated, srcExclude, head, themeConfig stay byte-identical (stack swap is 01-02's job — do not start it here). Then run the local build to prove the emission: npm run docs:build.
  </action>
  <verify>
    <automated>npm run docs:build && grep -q 'href="/jarvis-index/docs/assets/' docs/.vitepress/dist/index.html && S="$(grep -c 'jarvis-intelligence.github.io/jarvis-index/docs/' docs/.vitepress/dist/sitemap.xml)" && test "$S" -ge 30 && test "$S" = "$(grep -c 'jarvis-intelligence.github.io/jarvis-index/' docs/.vitepress/dist/sitemap.xml)"</automated>
  </verify>
  <acceptance_criteria>
  - Source assertion: docs/.vitepress/config.ts contains the exact string base: '/jarvis-index/docs/' and the sitemap hostname string ends in /docs/' (trailing slash present).
  - Source assertion: git diff for this task touches only those two values in docs/.vitepress/config.ts (no other file changes).
  - Behavior assertion: docs/.vitepress/dist/index.html references at least one hashed asset under /jarvis-index/docs/assets/.
  - Behavior assertion: every sitemap loc entry in docs/.vitepress/dist/sitemap.xml carries the segment jarvis-intelligence.github.io/jarvis-index/docs/ — the count of URL lines containing it equals the total count of URL lines containing the origin, and that count is at least 30 (the equality cannot pass vacuously on an empty or truncated sitemap).
  </acceptance_criteria>
  <done>Local build emits project-prefixed asset URLs and a fully /docs-segmented sitemap; two-line diff committed.</done>
</task>

<task type="auto">
  <name>Task 2: Post-deploy smoke probe in deploy-pages.yml + live proof via dispatched deploy</name>
  <precondition>gh auth status exits 0 (workflow dispatch + run watch require an authenticated GitHub CLI)</precondition>
  <read_first>
  - .github/workflows/deploy-pages.yml (entire file — the probe appends after the final deploy-pages@v4 step; permissions/concurrency/job blocks stay verbatim per 01-PATTERNS.md "Keep-verbatim block")
  - .planning/phases/01-site-foundation-identity/01-RESEARCH.md (§ Pattern 5 "Post-deploy smoke probe" — copy its step body, including the hashed-asset extraction)
  - .planning/phases/01-site-foundation-identity/01-PATTERNS.md (§ "`.github/workflows/deploy-pages.yml`")
  </read_first>
  <files>.github/workflows/deploy-pages.yml</files>
  <action>
  Append one step named "Smoke probe" to the deploy job, after the deploy-pages@v4 step (steps after the deploy action in the same job DO run — research Open Question 6 settled on same-job). Copy the probe body from RESEARCH § Pattern 5: set -e; BASE literal https://jarvis-intelligence.github.io/jarvis-index; for each of "/" and "/docs/" curl the page, assert HTTP 200 and that the body contains the marker string jarvis; then extract the first hashed _astro-style asset href from the /docs/ page HTML (the grep pattern in Pattern 5 targets the built assets directory), resolve it relative to BASE or /docs/ as Pattern 5's case-statement does, and assert it returns 200 — this is the exact failure mode observed live on 2026-08-21. Additionally curl one sitemap-listed deep URL (https://jarvis-intelligence.github.io/jarvis-index/docs/cli/forget) and assert 200 with the marker, covering the sitemap half of the fix. Change nothing else in the workflow.
  </action>
  <verify>
    <automated>gh workflow run deploy-pages.yml --ref "$(git branch --show-current)" && sleep 5 && gh run watch "$(gh run list --workflow=deploy-pages.yml --limit 1 --json databaseId --jq '.[0].databaseId')" --exit-status && curl -fsS -o /tmp/live-docs.html -w "%{http_code}\n" https://jarvis-intelligence.github.io/jarvis-index/docs/ | grep -qx 200 && grep -q jarvis /tmp/live-docs.html && curl -fsS -o /dev/null -w "%{http_code}\n" https://jarvis-intelligence.github.io/jarvis-index/docs/cli/forget | grep -qx 200</automated>
  </verify>
  <acceptance_criteria>
  - Source assertion: .github/workflows/deploy-pages.yml contains a step named "Smoke probe" positioned after the deploy-pages@v4 step, with set -e semantics so any failed check fails the job.
  - Behavior assertion: the dispatched workflow run (gh run watch --exit-status) concludes success — meaning the in-workflow probe saw 200 + marker on / and /docs/ and a live 200 on one hashed asset.
  - Behavior assertion: local curl of the live /docs/ page returns 200 containing the marker; the live deep URL /docs/cli/forget returns 200.
  - CLI: gh run list --workflow=deploy-pages.yml --limit 1 shows conclusion success for the dispatched run.
  </acceptance_criteria>
  <done>Live docs fully styled (page + hashed assets 200), sitemap deep URL live-200, and the workflow now fails loudly on any future regression of this class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → GitHub Pages | The workflow holds pages:write + id-token:write and publishes public content |
| Pages → visitor browser | Visitors execute HTML/CSS/JS served from the artifact this pipeline publishes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | .github/workflows/deploy-pages.yml | medium | mitigate | Probe step is append-only; permissions/concurrency blocks stay verbatim (01-PATTERNS keep-verbatim block); diff review catches privilege drift |
| T-01-02 | Denial of Service (silent) | deployed site | medium | mitigate | Smoke probe converts silent breakage into a red workflow run (set -e; 200 + marker + hashed-asset assertions) |
| T-01-03 | Information Disclosure | smoke probe output | low | accept | Probe prints status codes and page markers only — no secrets exist in this repo to leak |
</threat_model>

<verification>
- Quick command: npm run docs:build, then the Task 1 grep assertions (seconds).
- Live proof: dispatched deploy-pages.yml run green including the in-workflow smoke probe (Task 2 automated verify).
- Instrument mapping (01-VALIDATION.md): this plan delivers V8's smoke-probe step; task 1-01-01's http-probe is the Task 2 verify command.
</verification>

<success_criteria>
- Live /docs/ page 200, styled, marker present; live hashed asset 200; live /docs/cli/forget 200.
- Sitemap loc URLs all carry the /docs segment.
- Workflow fails loudly on regression (probe step committed and exercised in a green run).
</success_criteria>

<output>
Create `.planning/phases/01-site-foundation-identity/01-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (01-01)

- Modified config: `docs/.vitepress/config.ts` — keys `base` (value `/jarvis-index/docs/`) and `sitemap.hostname` (value `https://jarvis-intelligence.github.io/jarvis-index/docs/`)
- Modified workflow: `.github/workflows/deploy-pages.yml` — new step `Smoke probe` (post-deploy curl assertions on `/`, `/docs/`, one hashed asset, one deep URL)
- No new files, no npm deps, no script commands

## Flagged Assumptions (spec-less probe fallback — unclassified, unresolved)

| Requirement | Probe status | Flagged assumption | Resolution path |
|-------------|--------------|--------------------|-----------------|
| SITE-01 | unclassified / unresolved | The hotfix targets the two verified failure modes (asset prefix, sitemap trailing slash) and the probe asserts ONE hashed asset per page. If additional asset classes (JS chunks, font files) were also mis-prefixed, they are covered only by the same origin-prefix mechanism, not individually probed. Verify-work should confirm the deployed /docs/ page is interactive (JS loads), not merely CSS-styled. | Surface at /gsd-verify-work; one manual browser check of /docs/ interactivity closes it |
