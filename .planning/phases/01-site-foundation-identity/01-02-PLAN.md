---
phase: 01-site-foundation-identity
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - package.json
  - package-lock.json
  - astro.config.mjs
  - src/content.config.ts
  - src/content/docs/docs/
  - src/pages/index.astro
  - public/assets/
  - public/brand-logo.html
  - .github/workflows/deploy-pages.yml
  - .gitignore
  - docs/.vitepress/
  - site/index.html
  - site/assets/
autonomous: false
requirements:
  - SITE-02
  - SITE-05
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 4
  confidence: low
must_haves:
  truths:
    - "One command, one artifact: npm ci && npm run build on Node 22 emits a single dist/ tree with the landing at dist/index.html and all 32 docs pages under dist/docs/ — deployed as one Pages artifact"
    - "Every existing public URL keeps its exact shape: extensionless paths (e.g. /docs/cli/forget) serve 200 live via build.format 'file', verified by live spot curls after deploy"
    - "The eight maintainer-only files (brand-spec.md, code-standards.md, codebase-summary.md, deployment-guide.md, project-overview-pdr.md, project-roadmap.md, system-architecture.md, superpowers/**) are absent from dist/ — exclusion is structural (they never leave docs/ at repo root)"
    - "package.json declares engines node >=22 and exact-pins the stack (astro 5.18.2, @astrojs/starlight 0.37.7, @astrojs/mdx 4.3.14, @fontsource-variable/geist 5.3.0, @fontsource-variable/geist-mono 5.3.0, @fontsource/rajdhani 5.3.0)"
    - "The deploy-pages.yml diff (paths src/** + design/** + astro.config.*, node 22, npm run build, artifact path dist, Assemble step deleted) ships in the SAME commit as the package.json/package-lock.json stack change, and a dispatched deploy passes the 01-01 smoke probe against the new artifact"
  artifacts:
    - path: "astro.config.mjs"
      provides: "single origin constant (site + base), build format, Starlight integration with mirrored sidebar"
      contains: "base: '/jarvis-index'"
    - path: "src/content.config.ts"
      provides: "Starlight docs content collection"
      contains: "docsLoader"
    - path: "src/pages/index.astro"
      provides: "landing page at / (mechanical transplant of site/index.html)"
      contains: "assets/jarvis-mark.svg"
    - path: "package.json"
      provides: "Node engines floor + pinned unified stack"
      contains: "astro"
  key_links:
    - from: "astro.config.mjs (site + base)"
      to: "every emitted URL (pages, assets, sitemap)"
      via: "Astro/Starlight derive all absolute paths from the single origin constant — the one-origin rule that fixes the 01-01 bug class"
      pattern: "site: 'https://jarvis-intelligence\\.github\\.io'"
    - from: "src/content/docs/docs/**"
      to: "dist/docs/** URLs"
      via: "Starlight's fixed content root plus one nested directory = /docs/ URL prefix (code-verified, no routePrefix exists)"
      pattern: "src/content/docs/docs"
    - from: "deploy-pages.yml build step"
      to: "upload-pages-artifact path dist"
      via: "npm run build output is the whole artifact — no assembly step"
      pattern: "path: dist"
  prohibitions:
    - requirement_id: SITE-02
      category: privacy
      statement: "MUST NOT publish the eight maintainer-only documents the old srcExclude kept private — structural exclusion (they stay in docs/ at repo root, outside src/content/docs/) is the mechanism, and any sweep that moves them into the content tree is wrong even if a deny-list could patch it after"
    - requirement_id: SITE-02
      category: values
      statement: "MUST NOT redesign the landing in this plan — the transplant is mechanical (same sections, same copy, same inline styles carried over); claims, tiers, badges, and copy are Phase 3 scope"
    - requirement_id: SITE-05
      category: values
      statement: "MUST NOT ship the stack change without its deploy-pages.yml diff in the same commit — a half-migrated workflow (old paths, old node, old assemble step) deploys nothing or deploys a broken artifact, which is silent staleness"
---

<objective>
Replace the two-artifact assembly (hand-copied site/ + VitePress docs build) with ONE Astro 5 + Starlight project: landing at /, docs at /docs/, built as one dist/ tree, deployed as one Pages artifact on Node 22 with an engines field — the workflow diff landing in the same commit as the stack change (SITE-02 + SITE-05).

Purpose: This is the infrastructure layer every later phase consumes. The one-origin constant (site + base in astro.config.mjs) kills the base-path bug family structurally; the nested content directory (src/content/docs/docs/) mounts Starlight at /docs/ inside the single project (code-verified — no routePrefix exists in any Starlight version through 0.41.7).

Output: New Astro project (config, content collection, migrated pages, transplanted landing), rewritten deploy workflow, regenerated lockfile — proven by local build/preview and a live dispatched deploy.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-site-foundation-identity/01-RESEARCH.md
@.planning/phases/01-site-foundation-identity/01-PATTERNS.md

Key anchors (executor: read these sections):
- 01-RESEARCH.md § Standard Stack (verified npm pins + peer matrix — Pitfall 3: exact pins are mandatory), § Architecture Patterns "Recommended Project Structure", § Pattern 2 (Starlight at /docs/ — code-verified mechanics), § Code Examples "Unified astro.config.mjs", "Content collection", "Deploy workflow delta"
- 01-PATTERNS.md § astro.config.mjs (field-by-field translation table + sidebar excerpt), § package.json (current file + delta), § src/content.config.ts, § src/content/docs/docs/**.md (migration rules — which files do NOT move), § src/pages/index.astro (transplant map), § deploy-pages.yml (keep-verbatim block + lines to change)
- Source: docs/.vitepress/config.ts (sidebar 42-116, srcExclude 17-26), site/index.html, .github/workflows/deploy-pages.yml, package.json

Decision context (locked): SITE-02 mandates the unified Astro 5 + Starlight build; version pins are the mandate-compliant pair verified on npm 2026-08-21 (astro@5.18.2 is the last 5.x; starlight@0.37.7 is the last line peering astro ^5.5.0; the Astro 6 line is forbidden — 0.40.0 ships a typo'd peer dep). Astro 7.x was considered and NOT chosen: the roadmap mandate says Astro 5; swapping majors later is a lockfile-level change (costly, not one-way). Custom domain: stays OFF for v1 — the /jarvis-index prefix is carried by the single origin constant, so a later custom domain is a two-line change in astro.config.mjs (recorded decision per STATE.md blocker; flagged assumption below).
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Package legitimacy gate — six npm packages, all [ASSUMED]</name>
  <what-built>
  Nothing yet — this checkpoint precedes the install task. RESEARCH.md has no "Package Legitimacy Audit" section, so per the fallback policy all six packages are treated as [ASSUMED] and require human verification before any npm install runs. Research verified versions and behavior (npm view + throwaway installs + source reads, 2026-08-21) but did not audit publisher legitimacy.
  </what-built>
  <how-to-verify>
  Open each package page and confirm the publisher and download profile are the expected well-known projects (not typosquats), then approve:
  1. astro 5.18.2 — https://www.npmjs.com/package/astro (publisher: the Astro team)
  2. @astrojs/starlight 0.37.7 — https://www.npmjs.com/package/@astrojs/starlight (official Astro org scope)
  3. @astrojs/mdx 4.3.14 — https://www.npmjs.com/package/@astrojs/mdx (official Astro org scope)
  4. @fontsource-variable/geist 5.3.0 — https://www.npmjs.com/package/@fontsource-variable/geist (Fontsource org)
  5. @fontsource-variable/geist-mono 5.3.0 — https://www.npmjs.com/package/@fontsource-variable/geist-mono (Fontsource org)
  6. @fontsource/rajdhani 5.3.0 — https://www.npmjs.com/package/@fontsource/rajdhani (Fontsource org)
  </how-to-verify>
  <resume-signal>Approve all six, or name the package(s) to reject</resume-signal>
  <read_first>
  - .planning/phases/01-site-foundation-identity/01-RESEARCH.md (§ Standard Stack — the verified version/purpose table for the six packages; § Sources — the npm verification method used on 2026-08-21)
  </read_first>
  <acceptance_criteria>
  - Behavior assertion: each of the six npmjs.com package pages confirms the expected publisher (astro + @astrojs/* from the Astro team; the three Fontsource packages from the Fontsource org) and a substantial download profile.
  - Behavior assertion: the checkpoint records an explicit approve (or names rejected packages) before Task 1 runs any npm install.
  </acceptance_criteria>
</task>

<task type="tracer">
  <name>Task 1: Atomic stack swap — pinned Astro project, engines, lockfile, workflow diff in one commit</name>
  <precondition>Task 0 checkpoint approved (all six packages verified legitimate)</precondition>
  <reversibility rating="costly">Undoing the stack swap means another dependency-major change plus a lockfile regen and workflow revert — coordinated across package.json, lockfile, and CI; no data migration, no broken public contract, so costly rather than one-way.</reversibility>
  <read_first>
  - package.json (current file, 14 lines — quoted in 01-PATTERNS.md § package.json)
  - .github/workflows/deploy-pages.yml (all 59 lines; 01-PATTERNS.md "Keep-verbatim block" + "Lines to change")
  - .planning/phases/01-site-foundation-identity/01-RESEARCH.md (§ Standard Stack + § Code Examples "Unified astro.config.mjs", "Content collection", "Deploy workflow delta" — copy these verbatim)
  - .planning/phases/01-site-foundation-identity/01-PATTERNS.md (§ astro.config.mjs translation table, § package.json delta)
  - docs/index.md and site/index.html (the two source files being moved/transplanted)
  </read_first>
  <files>package.json, package-lock.json, astro.config.mjs, src/content.config.ts, src/content/docs/docs/index.md, src/pages/index.astro, .github/workflows/deploy-pages.yml, .gitignore</files>
  <action>
  ONE commit carries all of this (SITE-05 atomicity). package.json: remove vitepress from devDependencies; add exact pins (no carets — Pitfall 3) astro 5.18.2, @astrojs/starlight 0.37.7, @astrojs/mdx 4.3.14, @fontsource-variable/geist 5.3.0, @fontsource-variable/geist-mono 5.3.0, @fontsource/rajdhani 5.3.0 (fonts install now, consumed by 01-03); add engines field node >=22; replace scripts docs:dev/docs:build/docs:preview with dev/build/preview running astro dev / astro build / astro preview; keep name, version, private, type module. Create astro.config.mjs per the RESEARCH Code Example: site 'https://jarvis-intelligence.github.io', base '/jarvis-index', build format 'file', starlight integration with title jarvis, the current description, a minimal sidebar containing one group with the entry for the moved docs home (label Docs Home, link docs/), and NO customCss yet (01-03 adds it — the referenced files do not exist yet). Create src/content.config.ts verbatim from the RESEARCH example (defineCollection + docsLoader from @astrojs/starlight/loaders). git mv docs/index.md to src/content/docs/docs/index.md (ONE page only — the remaining ~31 move in Task 2). Create src/pages/index.astro as a mechanical transplant of site/index.html: carry the head metas, favicon path assets/jarvis-mark.svg, the inline style block, all body sections, and the theme toggle script verbatim (wrap the inline script with is:inline so Astro emits it unchanged); do NOT edit copy, sections, or styles (Pitfall 6 — the landing redesign is Phase 3). Rewrite deploy-pages.yml per the RESEARCH "Deploy workflow delta": paths add src/**, design/**, astro.config.*; node-version 20 to 22; build step becomes npm run build; DELETE the Assemble step; upload-pages-artifact path becomes dist; keep the 01-01 Smoke probe step (its / and /docs/ assertions now probe the new artifact — verify the probe's asset-extraction grep still matches Astro's _astro asset paths, adjusting the pattern if Astro emits a different hashed-asset directory). Update .gitignore: node_modules/ stays; replace the two docs/.vitepress entries with dist/ and .astro/. Regenerate the lockfile exactly once on Node 22 (node --version must start with v22): npm install, then confirm package-lock.json lockfileVersion 3.
  </action>
  <verify>
    <automated>npm ci --silent && npm run build && test -f dist/index.html && test -f dist/docs/index.html && (npm run preview -- --port 4322 & PV=$!; sleep 8; curl -fsS -o /tmp/pv-landing.html http://localhost:4322/jarvis-index/; curl -fsS -o /tmp/pv-docs.html http://localhost:4322/jarvis-index/docs/index.html; kill $PV) && grep -q 'assets/jarvis-mark.svg' /tmp/pv-landing.html && grep -q jarvis /tmp/pv-docs.html && node -e "console.log(require('./package.json').engines.node)" | grep -qx '>=22'</automated>
  </verify>
  <acceptance_criteria>
  - Source assertion: package.json devDependencies contains the six exact pins (astro 5.18.2, @astrojs/starlight 0.37.7, @astrojs/mdx 4.3.14, @fontsource-variable/geist 5.3.0, @fontsource-variable/geist-mono 5.3.0, @fontsource/rajdhani 5.3.0) and does NOT contain vitepress; engines.node equals '>=22'; scripts contain dev, build, preview.
  - Source assertion: astro.config.mjs contains site 'https://jarvis-intelligence.github.io', base '/jarvis-index', build format 'file', and the starlight integration.
  - Source assertion: deploy-pages.yml sets node-version '22', runs npm run build, uploads path dist, contains no Assemble step, and still contains the Smoke probe step; permissions/concurrency blocks byte-identical to before.
  - Behavior assertion: npm run build emits dist/index.html AND dist/docs/index.html — proving the static landing route beats Starlight's catch-all at / (research Pattern 2, the mount's load-bearing assumption).
  - Behavior assertion: preview serves the landing at /jarvis-index/ containing the favicon reference and the docs home at /jarvis-index/docs/index.html containing the marker.
  - CLI: npm ci exits 0 on Node 22; one atomic commit contains package.json + package-lock.json + astro.config.mjs + src/content.config.ts + the moved index.md + src/pages/index.astro + deploy-pages.yml + .gitignore (git show --stat on the commit lists exactly these).
  </acceptance_criteria>
  <done>Unified pinned stack builds locally: landing and docs home both live in one dist/ tree served under the base path, in a single atomic stack commit.</done>
</task>

<task type="auto">
  <name>Task 2: Full content migration — 31 pages git mv, sidebar mirror, structural exclusion, asset move</name>
  <read_first>
  - docs/.vitepress/config.ts lines 42-116 (the six sidebar groups to mirror) and 17-26 (the srcExclude list — these files DO NOT move)
  - 01-PATTERNS.md § "src/content/docs/docs/**.md" (migration rules: frontmatter preserved, root-relative clean links keep working, the eight excluded files stay put — any wholesale git mv docs/*.md is WRONG, Pitfall 5)
  - 01-RESEARCH.md § Pattern 7 (the verified 32-URL enumeration — the migration must reproduce exactly this set) and § Pitfall 4 (URL-shape: build.format file + cleanUrls equivalence)
  - site/assets/ directory (five files to move) and site/brand-logo.html (live URL to preserve)
  </read_first>
  <files>src/content/docs/docs/, astro.config.mjs, public/assets/, public/brand-logo.html, docs/.vitepress/, site/</files>
  <action>
  Use git mv (history-preserving — Phase 2's DOCS-09 classification keeps blame) to move every PUBLIC docs page from docs/ into the mirrored path under src/content/docs/docs/: quickstart.md, guide/install.md, tools/index.md + the 9 tool pages, cli/index.md + the 6 command pages (index-cmd, list, status, reindex, forget, watch), the 5 concepts pages, integrations/index.md + 3 client pages, troubleshooting/index.md + 2 pages — 32 files total including the Task-1 home page. The eight maintainer files (brand-spec.md, code-standards.md, codebase-summary.md, deployment-guide.md, project-overview-pdr.md, project-roadmap.md, system-architecture.md, superpowers/**) DO NOT MOVE — they stay in docs/ at repo root; exclusion is structural (Pitfall 5). Preserve each page's frontmatter and body verbatim; internal links are root-relative clean URLs (/quickstart, /concepts/scip) and keep working under the new mount because URL shape is preserved. In astro.config.mjs, mirror the six VitePress sidebar groups from docs/.vitepress/config.ts:42-116 into the starlight sidebar as labeled groups, converting each link by dropping the leading slash and prefixing docs/ (e.g. /tools/go-to-definition becomes docs/tools/go-to-definition; section index links /tools/ become docs/tools/). If Starlight warns about missing page titles, add title frontmatter derived from each page's first heading — URLs must not change. Move the five brand assets (jarvis-mark.svg, jarvis-avatar.svg, jarvis-mark-inverse.svg, jarvis-avatar-512.png, jarvis-avatar-1024.png) from site/assets/ to public/assets/ (same relative paths — the landing favicon reference keeps resolving), and move site/brand-logo.html to public/brand-logo.html (it is served live today at /brand-logo.html and its URL must survive). Delete the now-empty site/ directory and the docs/.vitepress/ directory (VitePress config, theme, cache). Then build and count: the dist docs page set must equal the 32-page enumeration.
  </action>
  <verify>
    <automated>npm run build && test "$(find dist/docs -name '*.html' | wc -l | tr -d ' ')" = "32" && test -f dist/docs/tools/go-to-definition.html && test -f dist/docs/cli/forget.html && test -f dist/brand-logo.html && test ! -e dist/docs/brand-spec.html && test ! -e dist/docs/code-standards.html && test ! -d dist/docs/superpowers && test ! -e docs/.vitepress/config.ts</automated>
  </verify>
  <acceptance_criteria>
  - Behavior assertion: dist/docs contains exactly 32 .html pages (the verified enumeration: docs home, quickstart, guide/install, tools index + 9, cli index + 6, 5 concepts, integrations index + 3, troubleshooting index + 2).
  - Behavior assertion: none of the eight maintainer files appear anywhere under dist/ (leak guard — structural exclusion proven).
  - Behavior assertion: dist/brand-logo.html exists (live URL preserved); public/assets/ holds the five brand files; site/ and docs/.vitepress/ no longer exist in the repo.
  - Source assertion: astro.config.mjs sidebar has six labeled groups mirroring the VitePress structure with docs/-prefixed links; every sidebar link resolves to a built page (spot-check three: docs/tools/go-to-definition, docs/concepts/scip, docs/troubleshooting/).
  - CLI: git log shows the 32 moves as renames (git diff --stat -M reports renames, not add+delete pairs).
  </acceptance_criteria>
  <done>All 32 public pages live under the unified build at their exact current URLs, maintainer docs structurally excluded, brand assets and brand-logo.html preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Live deploy of the unified artifact + full live URL spot-check</name>
  <precondition>gh auth status exits 0; Tasks 1-2 committed (the dispatched deploy builds the committed ref)</precondition>
  <read_first>
  - .github/workflows/deploy-pages.yml (as rewritten in Task 1 — this task observes it running, editing only if the probe needs a fix-forward)
  - 01-RESEARCH.md § Pattern 5 (probe semantics) and § Validation Architecture V8
  </read_first>
  <files>.github/workflows/deploy-pages.yml</files>
  <action>
  Dispatch the rewritten workflow on the current ref (gh workflow run deploy-pages.yml --ref followed by the current branch name) and watch the run to completion. The run itself proves: npm ci on Node 22, npm run build, artifact upload from dist, deploy, and the smoke probe green against the NEW unified artifact. Then spot-check live URLs beyond the probe: three deep docs pages in extensionless form (https://jarvis-intelligence.github.io/jarvis-index/docs/tools/go-to-definition, /docs/cli/forget, /docs/concepts/scip) expecting 200, plus /brand-logo.html expecting 200, plus one hashed _astro asset extracted from the live /docs/ page. If the probe step fails because Astro emits different asset path shapes than VitePress did, fix the probe's extraction grep in deploy-pages.yml (fix-forward commit) and re-dispatch until green. Verify the live sitemap: fetch sitemap-0.xml (the @astrojs/sitemap chunk that carries the page <loc> URLs — sitemap-index.xml only references the chunk file, it holds no page URLs), and assert its docs URLs carry the /docs segment.
  </action>
  <verify>
    <automated>gh workflow run deploy-pages.yml --ref "$(git branch --show-current)" && sleep 5 && gh run watch "$(gh run list --workflow=deploy-pages.yml --limit 1 --json databaseId --jq '.[0].databaseId')" --exit-status && for u in docs/tools/go-to-definition docs/cli/forget docs/concepts/scip brand-logo.html; do curl -fsS -o /dev/null -w "%{http_code} $u\n" "https://jarvis-intelligence.github.io/jarvis-index/$u" | grep -qx "200 $u" || exit 1; done && curl -fsS "https://jarvis-intelligence.github.io/jarvis-index/sitemap-0.xml" | grep -q 'jarvis-index/docs/'</automated>
  </verify>
  <acceptance_criteria>
  - Behavior assertion: the dispatched deploy-pages.yml run concludes success on Node 22 building the Astro artifact (gh run watch --exit-status exits 0), including the in-workflow smoke probe.
  - Behavior assertion: the three deep docs URLs return 200 live in extensionless form (the cleanUrls-equivalence claim of build.format file, proven on Pages).
  - Behavior assertion: /brand-logo.html returns 200 live; the live sitemap chunk sitemap-0.xml references docs URLs carrying the /docs segment (@astrojs/sitemap puts page <loc> URLs in the chunk file, not in sitemap-index.xml).
  - CLI: gh run list --workflow=deploy-pages.yml --limit 1 shows the unified-artifact run as success.
  </acceptance_criteria>
  <done>The unified single-artifact site is live: every spot-checked URL (3 deep docs pages extensionless, brand-logo, hashed asset, sitemap) returns 200, and the standing smoke probe is green against the new stack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Unverified third-party code enters the build via npm install (six packages + transitives) |
| repo content → public artifact | Everything under src/content/docs/ and public/ is published verbatim; maintainer docs must never cross |
| CI → GitHub Pages | The workflow holds pages:write + id-token:write and publishes the artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (astro, starlight, mdx, 3x fontsource) | high | mitigate | Package-legitimacy gate: blocking-human checkpoint (Task 0) verifies all six [ASSUMED] packages on npmjs.com before install; exact pins + lockfile v3 + npm ci make the installed tree deterministic |
| T-01-04 | Information Disclosure | content migration (Pitfall 5) | high | mitigate | Structural exclusion: the eight maintainer files never enter src/content/docs/; Task 2 acceptance asserts their absence from dist/; 01-05's verify-build later mechanizes the equality check |
| T-01-01 | Tampering | deploy-pages.yml rewrite | medium | mitigate | Permissions/concurrency blocks stay byte-identical; smoke probe preserved; workflow diff rides the atomic stack commit for review visibility |
| T-01-05 | Spoofing | landing transplant | low | accept | Mechanical transplant of existing content introduces no new untrusted input; all content is repo-authored |
</threat_model>

<verification>
- Quick command (per task commit): npm ci --silent && npm run build (V1) plus the Task 1/2 acceptance greps.
- Full command (plan close): quick + npm run preview + curl of /, /docs/, and three deep .html paths through the preview server, then the Task 3 live dispatch + probes (V8 rehearsed end-to-end).
- Instrument mapping (01-VALIDATION.md): this plan delivers V1 (build integrity on Node 22) and the deploy half of V8; tasks 1-02-01's build check is Task 1's automated verify.
</verification>

<success_criteria>
- One project, one build, one artifact, one origin constant — VitePress and the site/ assembly fully removed.
- 32 docs pages + landing + brand-logo.html live at their exact current URLs; zero maintainer-doc leakage.
- Stack commit is atomic (deps + engines + lockfile + workflow diff together) and proven by a green dispatched deploy.
</success_criteria>

<output>
Create `.planning/phases/01-site-foundation-identity/01-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (01-02)

- New files: `astro.config.mjs` (config keys: site, base, build.format, integrations.starlight.{title,description,sidebar}), `src/content.config.ts` (docs collection via docsLoader), `src/pages/index.astro` (landing), `src/content/docs/docs/**` (32 migrated pages), `public/assets/*` (5 brand assets), `public/brand-logo.html`
- Deleted: `docs/.vitepress/**` (config.ts, theme/), `site/index.html`, `site/assets/*`, `site/` directory
- package.json: devDependencies exactly {astro@5.18.2, @astrojs/starlight@0.37.7, @astrojs/mdx@4.3.14, @fontsource-variable/geist@5.3.0, @fontsource-variable/geist-mono@5.3.0, @fontsource/rajdhani@5.3.0}; engines.node ">=22"; scripts dev/build/preview
- Workflow: deploy-pages.yml — triggers add src/**, design/**, astro.config.*; node 22; build via npm run build; artifact path dist; Assemble step removed; Smoke probe step retained
- .gitignore: dist/, .astro/ replace docs/.vitepress entries
- pagefind 1.5.2 ships transitively via Starlight (no direct dependency, per research — the standalone package is not installed)

## Flagged Assumptions (spec-less probe fallback — unclassified, unresolved)

| Requirement | Probe status | Flagged assumption | Resolution path |
|-------------|--------------|--------------------|-----------------|
| SITE-02 | unclassified / unresolved | Astro route precedence (static src/pages/index.astro beats Starlight's [...slug] catch-all at /) is MEDIUM-HIGH confidence from source reading, not yet live-proven. Task 1's assertion dist/index.html exists guards it locally; if the live landing ever serves Starlight's home instead, the fallback is the documented two-Astro-builds merge (STACK.md escape hatch). | Task 3 live curl of / asserting the landing favicon reference closes it |
| SITE-05 | unclassified / unresolved | engines floor assumes '>=22' (astro 5.18.2 range) suffices for the full transitive tree on CI's Node 22 runner; if a transitive dep demands a newer minor, the floor must be raised (e.g. >=22.12). Lockfile regenerated exactly once on the local Node 22 — npm-major drift between local and CI is assumed absent. | Watch the Task 3 CI run's npm ci step; raise the floor if it warns |
