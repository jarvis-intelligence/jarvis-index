---
phase: 02-docs-rebuild-tutorial-first-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/brand-logo.html
  - public/fonts/geist-latin-wght-normal.woff2
  - public/fonts/geist-mono-latin-wght-normal.woff2
  - public/fonts/rajdhani-latin-600-normal.woff2
  - public/fonts/rajdhani-latin-700-normal.woff2
  - scripts/verify-build.mjs
  - design/url-contract.json
  - src/content/docs/404.md
  - src/content/docs/docs/tools/index.md
  - src/content/docs/docs/concepts/architecture.md
  - src/content/docs/docs/concepts/scip.md
  - src/content/docs/docs/concepts/zoekt.md
  - src/content/docs/docs/concepts/semantic-search.md
  - src/content/docs/docs/concepts/blast-radius.md
  - .planning/WINDOWS.md
autonomous: true
requirements:
  - DOCS-09
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Built dist/brand-logo.html contains zero third-party font CDN references and verify-build V4 scans it — the passthrough exclusion is gone (D-01, closes WINDOWS.md id 4)"
    - "public/fonts/ carries four stable-named woff2 files and brand-logo.html declares Geist / Geist Mono / Rajdhani @font-face against them via relative fonts/ URLs (D-01)"
    - "Every one of the 34 url-contract URLs is classified keep / merge-with-redirect / retire in a table committed with the plan summary; zero retires are executed without explicit user confirmation (D-02, D-03); V2 set-equality stays green"
    - "The custom 404 page emits at dist/404.html (root file, never a /404/ directory) and renders the documented zero-retire empty-state copy plus links into the six sidebar groups (D-04)"
    - "The tools overview page links findReferences at the kebab-case trailing-slash target — the WR-01 live 404 is fixed"
    - "All five concept pages are deepened in place with unchanged URLs, no duplicate body H1, and every capability claim traceable to ../jarvis sources (D-02, D-05)"
  artifacts:
    - path: "public/fonts/geist-latin-wght-normal.woff2"
      provides: "Self-hosted variable Geist for the brand-logo passthrough page (plus 3 sibling woff2 files)"
    - path: "src/content/docs/404.md"
      provides: "D-04 lookup-table 404 route (template: splash), excluded from V2 by design"
      contains: "template: splash"
    - path: "scripts/verify-build.mjs"
      provides: "V4 font-origin scan now covering every built HTML/CSS file with no exclusions"
  key_links:
    - from: "public/brand-logo.html"
      to: "public/fonts/*.woff2"
      via: "inline @font-face src relative URLs"
      pattern: "url\\(fonts/"
    - from: "scripts/verify-build.mjs"
      to: "dist/brand-logo.html"
      via: "V4 CDN-pattern scan with the exclusion set removed"
    - from: "src/content/docs/404.md"
      to: "the six sidebar groups"
      via: "link list into Guide/Concepts/MCP Tools/CLI/Integrations/Troubleshooting"
  prohibitions:
    - statement: "No page is retired, merged, or dropped from the URL contract without explicit user confirmation — classification FLAGS a retire, it never executes one (D-03)"
      status: flagged
    - statement: "No invented facts or capability claims in concept pages — every statement traces to ../jarvis README / system-architecture / source, per D-05's no-invented-content rule"
      status: flagged
    - statement: "setup.sh and plugin/** are not modified anywhere in this phase (ownership fences; plugin realignment is Phase 4)"
      status: flagged
---

<objective>
Execute the additive-restructure mechanics of the docs rebuild (DOCS-09): classify every existing public URL keep / merge-with-redirect / retire (D-02, D-03), ship the lookup-table 404 (D-04), self-host brand-logo.html's fonts and remove verify-build's V4 exclusion (D-01, closing WINDOWS.md id 4), fix the WR-01 live 404 link, and deepen the five concept pages in place (D-02's deepen-in-place stance for pages no other requirement owns).

Purpose: this plan settles the page-set ground truth (what exists, what stays, what the 404 catches) so every later content plan can add pages against a clean contract. It also closes the last zero-third-party-fonts gap so the site-wide claim finally holds.
Output: self-hosted brand-logo fonts + tightened V4, `src/content/docs/404.md`, the 34-row classification table in the SUMMARY, fixed tools overview link, five deepened concept pages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-CONTEXT.md
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-RESEARCH.md
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-PATTERNS.md
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-UI-SPEC.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end brand-logo font self-hosting — copy fonts, rewrite the page, tighten V4 (D-01)</name>
  <files>public/brand-logo.html, public/fonts/geist-latin-wght-normal.woff2, public/fonts/geist-mono-latin-wght-normal.woff2, public/fonts/rajdhani-latin-600-normal.woff2, public/fonts/rajdhani-latin-700-normal.woff2, scripts/verify-build.mjs, design/url-contract.json, .planning/WINDOWS.md</files>
  <read_first>public/brand-logo.html (lines 1-35: the three CDN lines at 9-11 and the --sans/--mono/--logo stacks at 28-30), scripts/verify-build.mjs (header comment lines 13-17 and the V4 section lines 152-170), 02-RESEARCH.md Pattern 6 + Code Example 5, design/url-contract.json (the generated_from note), .planning/WINDOWS.md (id 4)</read_first>
  <action>
This is the plan's tracer: one page wired end-to-end through the font copy, the HTML rewrite, and the CI assertion, proven by a green build before anything else in the phase.

1. Run `npm run build` to materialize `dist/_astro/`. IMPORTANT: this machine's scout-block hook denies both Read and Bash access to `node_modules/**`, so do NOT copy from the Fontsource packages directly. Astro's hashed asset names preserve the original base filenames, so `dist/_astro/` is a byte-identical source.
2. `mkdir -p public/fonts` and copy with stable un-hashed names (glob matches exactly one hashed file each):
   - `cp dist/_astro/geist-latin-wght-normal.*.woff2 public/fonts/geist-latin-wght-normal.woff2`
   - `cp dist/_astro/geist-mono-latin-wght-normal.*.woff2 public/fonts/geist-mono-latin-wght-normal.woff2`
   - `cp dist/_astro/rajdhani-latin-600-normal.*.woff2 public/fonts/rajdhani-latin-600-normal.woff2`
   - `cp dist/_astro/rajdhani-latin-700-normal.*.woff2 public/fonts/rajdhani-latin-700-normal.woff2`
3. In `public/brand-logo.html`, delete lines 9-11 (the two Google-Fonts preconnect links and the css2 stylesheet link) and insert in their place this style block (02-RESEARCH Code Example 5, verbatim):
   `<style>`
   `@font-face { font-family: "Geist"; src: url(fonts/geist-latin-wght-normal.woff2) format("woff2-variations"); font-weight: 100 900; font-style: normal; font-display: swap; }`
   `@font-face { font-family: "Geist Mono"; src: url(fonts/geist-mono-latin-wght-normal.woff2) format("woff2-variations"); font-weight: 100 900; font-style: normal; font-display: swap; }`
   `@font-face { font-family: "Rajdhani"; src: url(fonts/rajdhani-latin-600-normal.woff2) format("woff2"); font-weight: 600; font-style: normal; font-display: swap; }`
   `@font-face { font-family: "Rajdhani"; src: url(fonts/rajdhani-latin-700-normal.woff2) format("woff2"); font-weight: 700; font-style: normal; font-display: swap; }`
   `</style>`
   The family names match the page's existing `--sans` / `--mono` / `--logo` stacks at lines 28-30 — make NO other CSS change on this page. Relative `fonts/...` URLs are correct: the page sits at the artifact root, so they resolve under `${BASE}/` on Pages.
4. In `scripts/verify-build.mjs`, delete the passthrough exclusion: remove the exclusion Set declaration and its two comment lines (currently lines 156-158) and simplify the font-scan filter (lines 160-162) to `allFiles.filter((f) => f.endsWith('.html') || f.endsWith('.css'))`. Also rewrite the V4 header-comment (lines 12-17) to drop the sentence about the excluded passthrough page — V4 now scans everything. Comment policy: why-comments only, no removed-code references.
5. In `design/url-contract.json`, update the `generated_from` note: replace the final sentence ("Phase 2's DOCS-09 classification decides /brand-logo.html's keep/retire fate and updates this contract in the same commit.") with: "Phase 2 classification (plan 02-01, D-01): /brand-logo.html is KEEP — fonts self-hosted from public/fonts/, no longer excluded from verify-build V4." The `urls` array is unchanged.
6. Mark WINDOWS.md id 4 fixed: run the gsd-tools shim `windows fixed 4` subcommand if available, otherwise edit `.planning/WINDOWS.md` directly to set id 4's status to fixed with a one-line note pointing at this plan.
7. Run the full gate: `npm run build && npm run verify` — V4 must be green while scanning `dist/brand-logo.html`.
<!-- planner-discipline-allow: V4_EXCLUDED -->
  </action>
  <verify>
    <automated>npm run build && npm run verify && test -f dist/fonts/geist-latin-wght-normal.woff2 && ! grep -qE 'fonts\.(googleapis|gstatic)\.com' public/brand-logo.html</automated>
  </verify>
  <acceptance_criteria>
- `npm run build && npm run verify` exits 0 (V4 now scans dist/brand-logo.html — no exclusion)
- `! grep -qE 'fonts\.(googleapis|gstatic)\.com' public/brand-logo.html`
- `! grep -q 'V4_EXCLUDED' scripts/verify-build.mjs`
- `test -f public/fonts/geist-latin-wght-normal.woff2 && test -f public/fonts/geist-mono-latin-wght-normal.woff2 && test -f public/fonts/rajdhani-latin-600-normal.woff2 && test -f public/fonts/rajdhani-latin-700-normal.woff2`
- `grep -c 'url(fonts/' public/brand-logo.html` outputs 4
- `test -f dist/fonts/geist-latin-wght-normal.woff2` (public/ passthrough lands in dist)
- `grep -q 'KEEP' design/url-contract.json`
  </acceptance_criteria>
  <done>brand-logo.html self-hosts all three families from public/fonts/, verify-build V4 scans it green with the exclusion deleted, the contract note records the keep classification, and WINDOWS.md id 4 is closed (D-01).</done>
</task>

<task type="auto">
  <name>Task 2: Lookup-table 404 page (D-04), WR-01 link fix, and the 34-URL classification audit (D-02, D-03)</name>
  <reversibility rating="costly">D-02 keep-all/deepen-in-place: reorganizing the IA later means new redirects, url-contract edits, and inbound-link risk after Phase 5's crawl baseline.</reversibility>
  <files>src/content/docs/404.md, src/content/docs/docs/tools/index.md</files>
  <read_first>02-UI-SPEC.md (Page Layout Contract 3 + the Copywriting Contract "Empty state" row), 02-RESEARCH.md Pattern 3 / Pitfall 5 / Code Example 3, design/url-contract.json (all 34 URLs), astro.config.mjs (the six sidebar group labels, lines 54-116), src/content/docs/docs/tools/index.md (the camelCase link target on line 15), 02-PATTERNS.md ("404" and "Internal link form" sections)</read_first>
  <action>
1. Create `src/content/docs/404.md` with frontmatter exactly: `title: '404'`, `template: splash`, `editUrl: false`, and hero `title: 'Page not found'` with tagline `This URL moved during the docs rebuild — find its new home below.` (02-RESEARCH Code Example 3). Body, in order:
   - A `| Old URL | New location |` markdown table. With zero retires (the expected outcome per D-03), the table has one placeholder row and is preceded by the exact empty-state line from the UI-SPEC Copywriting Contract: "No URLs have been retired yet — the page you wanted may have moved; find it in the sections below."
   - A link list into the six sidebar groups (the "nav" half of D-04), authored docs-root-relative with trailing slashes so `rebaseDocsLinks()` rebases them: Guide → `/quickstart/`, Concepts → `/concepts/scip/`, MCP Tools → `/tools/`, CLI → `/cli/`, Integrations → `/integrations/`, Troubleshooting → `/troubleshooting/`.
   - Do NOT add this page to `design/url-contract.json` — verify-build V2 excludes exactly `404.html` from its walk (verify-build.mjs:76).
   - Fallback ONLY if the build proves `template: splash` cannot render the table+nav body: `src/pages/404.astro` + `disable404Route: true` in the Starlight config, with identical content (UI-SPEC contract 3 authorizes this fallback).
2. Fix WR-01 in `src/content/docs/docs/tools/index.md` line 15: the link target uses a camelCase slug that 404s live; change the target to `/tools/find-references/` (kebab-case, trailing slash). The link label stays camelCase `findReferences` — labels are camelCase, targets are kebab-case (UI-SPEC typography rules).
3. Classification audit (D-02, D-03): walk all 34 URLs in `design/url-contract.json` against the built page set and 02-PATTERNS.md's file classification. Produce a table `URL | Classification | Rationale` covering every URL. Expected outcome: keep × 34 (D-02 default stance; `/brand-logo.html` keep already executed by Task 1). If any page looks genuinely redundant, classify it `flagged — merge/retire candidate, awaiting user confirmation` and do NOT execute the retire: no redirects entry, no contract drop, no file deletion. The full table MUST be included in this plan's SUMMARY under a `## Classification` heading, and any flagged rows must be listed as open questions for the user.
4. Run `npm run build && npm run verify` — the page set is unchanged for V2 (404.html is excluded), so V2 must stay green.
  </action>
  <verify>
    <automated>npm run build && npm run verify && test -f dist/404.html && test ! -d dist/404 && grep -q 'No URLs have been retired yet' src/content/docs/404.md && grep -q '/tools/find-references/' src/content/docs/docs/tools/index.md</automated>
    <human-check>End-of-phase preview pass: `npm run preview`, visit a bogus URL under /jarvis-index/docs/ — the lookup table, empty-state copy, and six group links render inside the Starlight shell (VALIDATION.md manual item).</human-check>
  </verify>
  <acceptance_criteria>
- `test -f dist/404.html && test ! -d dist/404` (Pitfall 5: root 404.html, never a /404/ page)
- `npm run build && npm run verify` exits 0 with no V2 delta
- `grep -q 'template: splash' src/content/docs/404.md` (or the documented 404.astro fallback exists and `disable404Route: true` is set)
- `grep -q 'No URLs have been retired yet' src/content/docs/404.md`
- `grep -q '/tools/find-references/' src/content/docs/docs/tools/index.md` and `! grep -q 'findReferences)' src/content/docs/docs/tools/index.md`
- SUMMARY contains a `## Classification` table with 34 rows and zero executed retires
  </acceptance_criteria>
  <done>dist/404.html serves the D-04 lookup table with the zero-retire empty state and six-group nav; WR-01 is fixed; every contract URL is classified with retires flagged-only per D-03.</done>
</task>

<task type="auto">
  <name>Task 3: Deepen the five concept pages in place (D-02)</name>
  <precondition>../jarvis/docs/system-architecture.md and ../jarvis/README.md exist and are readable (private-repo checkout at ../jarvis, jarvis-mcp 0.6.2)</precondition>
  <files>src/content/docs/docs/concepts/scip.md, src/content/docs/docs/concepts/zoekt.md, src/content/docs/docs/concepts/semantic-search.md, src/content/docs/docs/concepts/blast-radius.md, src/content/docs/docs/concepts/architecture.md</files>
  <read_first>All five existing concept stubs (they are sound skeletons — deepen, don't replace structure), ../jarvis/docs/system-architecture.md, ../jarvis/README.md, 02-RESEARCH.md Product Truth §1 (flat freshness), §3 (version gate), §4 (language families), §5 (issue #9), 02-UI-SPEC.md (aside budget, typography, link rules), CLAUDE.md docs conventions (tables for decision surfaces, settled-decision framing, 500-line soft cap)</read_first>
  <action>
Deepen each concept page in place — same URL, same frontmatter contract (`title` + `description` only), body H1 dropped (the frontmatter title renders the H1). Source every claim from ../jarvis/docs/system-architecture.md and ../jarvis/README.md; where they disagree with shipped code, code/setup.sh wins (RESEARCH anti-pattern list). Per page:
- `concepts/scip.md`: what SCIP is and why jarvis uses it; the index pipeline (language indexer → SCIP → SQLite publish, atomic swap); the scip version gate — versions below 0.9.0 silently write a schema-valid database with zero chunks, which is why `jarvis index` refuses them (index_cli.py MIN_SCIP_VERSION rationale); one-language-per-repo detection (git-tracked file plurality, `--language` override).
- `concepts/zoekt.md`: trigram-based code search; jarvis indexes from the git tree with the git-flavored zoekt indexer binary that setup.sh installs; repo-name pinning via `git config zoekt.name <slug>` and why collisions happen without it; how hits surface through `searchCode` as `{repo, path, lineNumber, lineText}` entries.
- `concepts/semantic-search.md`: vector + lexical fusion; requires the `[semantic]` extra AND a second MCP-server registration — present this as a settled decision with rationale, consistent with D-08's framing on the tool page; chunking is code-symbol-scoped, so prose-only repos gain nothing from `--semantic-include` (issue #9 — settled limitation, not a bug); result entry keys per Product Truth §1.
- `concepts/blast-radius.md`: the cross-repo dependency graph; dependents returned with hop counts; freshness is always reported "unknown" by design — the graph has no per-node timestamp (keep the existing settled framing).
- `concepts/architecture.md`: component map as prose + a table (indexers, SCIP converter, SQLite store, Zoekt, the stdio MCP server — one process per connected client), data flow from `jarvis index` to a tool response, distilled from system-architecture.md.
Rules for all five: internal links docs-root-relative in trailing-slash form; JSON snippets (if any) use only Product Truth §1 shapes with the five freshness fields spread flat at the top level, never as a nested object; no new asides beyond the UI-SPEC budget; no TODO framing — limitations are settled decisions with rationale; each page stays under the 500-line soft cap. No contract or sidebar edits (no URLs change).
  </action>
  <verify>
    <automated>npm run build && npm run verify && ! grep -rq '"freshness": {' src/content/docs/docs/concepts/ && ! grep -rqE '^# [^#]' src/content/docs/docs/concepts/</automated>
  </verify>
  <acceptance_criteria>
- `npm run build && npm run verify` exits 0 with the page set unchanged (V2 green, no contract edit)
- `! grep -rq '"freshness": {' src/content/docs/docs/concepts/` (flat freshness only)
- `! grep -rqE '^# [^#]' src/content/docs/docs/concepts/` (no duplicate body H1)
- `! grep -rnE '\]\(/[^)#]*[^/#]\)' src/content/docs/docs/concepts/` (anchor-free internal links end with a trailing slash)
- Each of the five pages is meaningfully deepened (line count grows vs the stub) and ≤ 500 lines: `wc -l src/content/docs/docs/concepts/*.md`
  </acceptance_criteria>
  <done>All five concept pages are deepened in place with unchanged URLs, claims traced to ../jarvis sources, flat-freshness-only JSON, and settled-decision framing for every limitation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader browser → Pages CDN | fonts previously crossed to a third-party CDN; after D-01 all font bytes come from the site origin |
| reader terminal ← docs commands | concept pages may show commands readers paste into shells |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information disclosure | public/brand-logo.html font loading | low | mitigate | Task 1 deletes the last third-party font CDN reference; verify-build V4 then enforces zero-CDN site-wide on every build |
| T-02-02 | Tampering | commands quoted in concept pages | low | mitigate | Commands quoted verbatim from ../jarvis README/setup.sh only; no user-interpolated unquoted shell vars |
| T-02-SC | Tampering | package installs | low | accept | This phase installs zero packages (02-RESEARCH Package Legitimacy Audit: none); no install tasks exist in this plan |
</threat_model>

<verification>
- `npm run build && npm run verify` green after every task commit (V2 set-equality, V3 assets, V4 fonts with no exclusion, V5 pagefind, V9 sitemap)
- `! grep -qE 'fonts\.(googleapis|gstatic)\.com' public/brand-logo.html`
- `test -f dist/404.html && test ! -d dist/404`
- Classification table (34 rows) present in the SUMMARY, zero executed retires
- Human check at phase gate: 404 page renders lookup table + nav in preview
</verification>

<success_criteria>
- D-01 fully landed: self-hosted brand-logo fonts, V4 exclusion removed, WINDOWS.md id 4 fixed
- D-04 fully landed: lookup-table 404 with empty-state copy and six-group nav
- D-02/D-03 executed: 34-URL classification committed, retires flagged-only
- WR-01 fixed; five concept pages deepened in place with V2 unchanged
</success_criteria>

## Artifacts this phase produces (plan 02-01)

| Artifact | Path / URL |
|----------|------------|
| Self-hosted brand-logo fonts | `public/fonts/geist-latin-wght-normal.woff2`, `public/fonts/geist-mono-latin-wght-normal.woff2`, `public/fonts/rajdhani-latin-600-normal.woff2`, `public/fonts/rajdhani-latin-700-normal.woff2` |
| Lookup-table 404 route | `src/content/docs/404.md` → served at every unknown URL (`dist/404.html`) |
| Tightened CI assertion | `scripts/verify-build.mjs` V4 with no exclusion (command: `npm run verify`) |
| Classification table | `## Classification` section of `02-01-SUMMARY.md` (34 rows) |
| Deepened concept pages | `/docs/concepts/scip/`, `/docs/concepts/zoekt/`, `/docs/concepts/semantic-search/`, `/docs/concepts/blast-radius/`, `/docs/concepts/architecture/` (URLs unchanged) |

<output>
Create `.planning/phases/02-docs-rebuild-tutorial-first-content/02-01-SUMMARY.md` when done. It MUST contain the `## Classification` table (34 rows: URL | Classification | Rationale) and list any flagged merge/retire candidates as open questions for the user (D-03).
</output>
