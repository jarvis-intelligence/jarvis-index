---
phase: 02-docs-rebuild-tutorial-first-content
plan: 05
type: execute
wave: 4
depends_on:
  - 02-04
files_modified:
  - src/content/docs/docs/integrations/generic-stdio.md
  - src/content/docs/docs/integrations/claude-code.md
  - src/content/docs/docs/integrations/cursor.md
  - src/content/docs/docs/integrations/codex-cli.md
  - src/content/docs/docs/integrations/index.md
  - src/content/docs/docs/guide/install.md
  - design/url-contract.json
  - astro.config.mjs
autonomous: true
requirements:
  - DOCS-02
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All four client guides — Claude Code, Cursor, Codex CLI, and the generic stdio page — exist on equal footing, each ending with a Verify step that names getIndexStatus (D-11, DOCS-02)"
    - "/docs/integrations/generic-stdio/ ships as a 3-file atomic commit with sidebar label 'Any stdio client' after Codex CLI"
    - "Every registration JSON on every guide keeps the jarvis-mcp>=0.6.0 floor and none carries the semantic extra (hard project rules)"
    - "The Cursor guide carries the real cursor:// install deeplink; Claude Code and Codex CLI present command equivalents with an honest no-URL-scheme note — never a fabricated link"
    - "The install guide documents the PyPI channel alternatives (uv tool install primary, pip, ad-hoc uvx) consistent with the quickstart's setup.sh-first path (D-09 alignment)"
  artifacts:
    - path: "src/content/docs/docs/integrations/generic-stdio.md"
      provides: "D-11 standalone generic stdio guide with the verbatim mcp.json block"
      contains: "jarvis-mcp>=0.6.0"
    - path: "src/content/docs/docs/integrations/cursor.md"
      provides: "Cursor guide with real deeplink + manual JSON"
      contains: "cursor://anysphere.cursor-deeplink/mcp/install"
    - path: "design/url-contract.json"
      provides: "Contract grown by exactly one URL: /docs/integrations/generic-stdio/"
      contains: "/docs/integrations/generic-stdio/"
  key_links:
    - from: "src/content/docs/docs/integrations/*.md"
      to: "plugin/.mcp.json"
      via: "the registration JSON quoted verbatim (uvx --from floor form)"
      pattern: "jarvis-mcp>=0.6.0"
    - from: "src/content/docs/docs/integrations/index.md"
      to: "the four client guides"
      via: "one-click decision table"
      pattern: "/integrations/[a-z-]+/"
  prohibitions:
    - statement: "No registration config anywhere on these pages adds the semantic extra or lowers/pins the >=0.6.0 floor — the semantic workaround lives on the quickstart extras tip and the troubleshooting entry, not in client registration JSON (hard rules; D-08 consistency)"
      status: flagged
    - statement: "No fabricated deep links — clients without a URL scheme get command equivalents with the honest note (RESEARCH A1)"
      status: flagged
    - statement: "plugin/** is read as the JSON source of truth but never modified (Phase 4 owns plugin changes)"
      status: flagged
---

<objective>
Deliver DOCS-02: per-client install guides for Claude Code, Cursor, Codex CLI, and a NEW standalone generic stdio JSON guide on equal footing (D-11). Each guide gives a working registration path (plugin and/or manual), quotes the registration JSON verbatim from plugin/.mcp.json with the >=0.6.0 floor, and closes with a Verify step. The install guide page is deepened to document the PyPI channel alternatives consistent with the setup.sh-first quickstart (D-09).

Purpose: however a visitor's client is shaped, there is a copy-paste registration path that works.
Output: 1 new page (3-file atomic), 5 deepened pages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-RESEARCH.md
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-PATTERNS.md
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-UI-SPEC.md
</context>

## Canonical registration JSON (verbatim from plugin/.mcp.json — the only registration shape any page may show)

```json
{
  "mcpServers": {
    "jarvis": {
      "command": "uvx",
      "args": ["--from", "jarvis-mcp>=0.6.0", "jarvis-server"]
    }
  }
}
```

<tasks>

<task type="tracer">
  <name>Task 1: Generic stdio guide end-to-end — new page through contract and sidebar (D-11)</name>
  <files>src/content/docs/docs/integrations/generic-stdio.md, design/url-contract.json, astro.config.mjs</files>
  <read_first>src/content/docs/docs/integrations/cursor.md (the skeleton analog, lines 1-36), 02-UI-SPEC.md Page Layout Contract 4, 02-PATTERNS.md generic-stdio section, plugin/.mcp.json (verify the JSON above is still byte-accurate — READ ONLY), astro.config.mjs Integrations sidebar group</read_first>
  <action>
This tracer proves the plan's ADD machinery and creates the guide the other three link to as the fallback for any unlisted client.

1. Create `src/content/docs/docs/integrations/generic-stdio.md` per UI-SPEC Layout Contract 4:
   - Frontmatter `title: Any stdio client` + description; one intro sentence: any MCP client that can launch a stdio server can run jarvis with this one block.
   - The canonical registration JSON above in a ```json title="mcp.json"``` block, byte-identical to plugin/.mcp.json's server entry shape. Keep the version floor exactly as shown; do not add extras to the args.
   - One sentence on where the block goes: the client's MCP config file (name varies by client — link the three sibling guides).
   - `## Verify` — after registering, ask the agent to call `getIndexStatus` with an indexed repo slug; a JSON response (even an error object) proves the server is wired.
   - Cross-link the two sibling-guide conventions: first body line names the three client guides so a mis-landed reader can redirect.
2. Add `"/docs/integrations/generic-stdio/"` to `design/url-contract.json`.
3. Add `{ label: 'Any stdio client', link: 'docs/integrations/generic-stdio' },` to the Integrations sidebar group immediately after Codex CLI.
4. `npm run build && npm run verify`.
  </action>
  <verify>
    <automated>npm run build && npm run verify && grep -q 'jarvis-mcp>=0.6.0' src/content/docs/docs/integrations/generic-stdio.md && grep -q 'title="mcp.json"' src/content/docs/docs/integrations/generic-stdio.md && ! grep -q 'jarvis-mcp\[semantic\]' src/content/docs/docs/integrations/generic-stdio.md</automated>
  </verify>
  <acceptance_criteria>
- `npm run build && npm run verify` exits 0; `test -f dist/docs/integrations/generic-stdio/index.html`
- `grep -q 'jarvis-mcp>=0.6.0' src/content/docs/docs/integrations/generic-stdio.md` (floor intact)
- `grep -q 'title="mcp.json"' src/content/docs/docs/integrations/generic-stdio.md` (titled config block per UI-SPEC)
- `grep -q 'getIndexStatus' src/content/docs/docs/integrations/generic-stdio.md` (Verify step)
- Contract + sidebar edits in the same commit; sidebar label is exactly `Any stdio client`
  </acceptance_criteria>
  <done>The generic stdio guide exists on equal footing, 3-file atomic, floor intact, verify step present.</done>
</task>

<task type="auto">
  <name>Task 2: Deepen the three client guides, the integrations overview, and the install guide</name>
  <precondition>../jarvis/README.md readable (plugin install commands, README:29-37)</precondition>
  <files>src/content/docs/docs/integrations/claude-code.md, src/content/docs/docs/integrations/cursor.md, src/content/docs/docs/integrations/codex-cli.md, src/content/docs/docs/integrations/index.md, src/content/docs/docs/guide/install.md</files>
  <read_first>The four existing pages + guide/install.md, 02-RESEARCH.md Product Truth §6 (channel commands + deep-link table + A1), ../jarvis/README.md:20-37, 02-UI-SPEC.md (code-block titling, tables), the completed generic-stdio.md (Task 1)</read_first>
  <action>
Deepen each in place. Every guide follows the same shape (equal footing): intro sentence → registration path(s) → `## Verify` closing step (ask the agent to call `getIndexStatus`). Registration JSON, where shown, is the canonical block from this plan's header — floor intact, no extras.
- `claude-code.md`: two paths as `###` subsections. Plugin (recommended — registers the server for you): `/plugin marketplace add jarvis-intelligence/jarvis-index` then `/plugin install jarvis@jarvis` (README:29-32 verbatim). Manual: `claude mcp add jarvis --scope user -- jarvis-server` (README:37 verbatim — requires jarvis-server on PATH; for GUI-launched sessions use the absolute path from `which jarvis-server`, linking the troubleshooting PATH entry at /troubleshooting/common-failures/#jarvis-or-jarvis-server-not-found-path).
- `cursor.md`: plugin/marketplace path where applicable, the REAL install deeplink `cursor://anysphere.cursor-deeplink/mcp/install?name=jarvis&config=BASE64` where BASE64 is computed (echo -n the single-server config object `{"command":"uvx","args":["--from","jarvis-mcp>=0.6.0","jarvis-server"]}` | base64) — compute it, paste the literal result, and show the manual `mcp.json` block as the alternative (titled code block).
- `codex-cli.md`: plugin path via the marketplace (the repo-root Codex manifest points into plugin/), and the manual path — mirror the canonical JSON into the Codex MCP config under ~/.codex; state honestly that Codex has no install URL scheme (RESEARCH A1) so the command/config IS the path.
- `integrations/index.md`: rewrite as a 4-row decision table `Client | Path | Guide` (Claude Code, Cursor, Codex CLI, Any stdio client) with one-click trailing-slash links; one sentence pointing readers who want channel comparison at the install-channels matrix page (authored next wave at /guide/install-matrix/ — link it now; it resolves before the phase gate).
- `guide/install.md`: deepen the PyPI-channel detail consistent with D-09 (the quickstart leads with setup.sh; this page covers the package alternatives): `uv tool install jarvis-mcp` (primary — also the verified uvx cold-start fix), `pip install jarvis-mcp`, ad-hoc `uvx --from jarvis-mcp jarvis-server`; what each puts on PATH; link the requirements page before any install command ("check your language is supported" → /guide/requirements/). Do NOT inline the semantic-extra install command on integration pages — the quickstart optional-extras tip and the troubleshooting entry own that workaround (keeps registration configs clean and greppable).
Rules: labels camelCase where they are tool names, targets kebab-case trailing-slash; no body H1; terse second-person voice; no asides beyond any that already exist on these pages.
  </action>
  <verify>
    <automated>npm run build && npm run verify && grep -q 'cursor://anysphere.cursor-deeplink/mcp/install' src/content/docs/docs/integrations/cursor.md && grep -q 'claude mcp add jarvis' src/content/docs/docs/integrations/claude-code.md && ! grep -rq 'jarvis-mcp\[semantic\]' src/content/docs/docs/integrations/</automated>
  </verify>
  <acceptance_criteria>
- Each of claude-code.md, cursor.md, codex-cli.md, generic-stdio.md contains a `## Verify` section naming `getIndexStatus` (`grep -l 'getIndexStatus' src/content/docs/docs/integrations/*.md` lists all four guide pages)
- `grep -q '/plugin marketplace add jarvis-intelligence/jarvis-index' src/content/docs/docs/integrations/claude-code.md`
- `grep -q 'cursor://anysphere.cursor-deeplink/mcp/install' src/content/docs/docs/integrations/cursor.md` and the BASE64 payload decodes to the canonical single-server config (`grep -o 'config=[A-Za-z0-9+/=]*' src/content/docs/docs/integrations/cursor.md | cut -d= -f2- | base64 -d | grep -q 'jarvis-mcp>=0.6.0'`)
- codex-cli.md contains an honest no-URL-scheme statement (`grep -qi 'no.*URL scheme' src/content/docs/docs/integrations/codex-cli.md`)
- `grep -rq 'jarvis-mcp>=0.6.0' src/content/docs/docs/integrations/cursor.md src/content/docs/docs/integrations/codex-cli.md` (floor everywhere a config appears)
- `! grep -rq 'jarvis-mcp\[semantic\]' src/content/docs/docs/integrations/` (no extras in any registration)
- integrations/index.md has the 4-row table incl. `Any stdio client`; guide/install.md links `/guide/requirements/`
- `npm run build && npm run verify` exits 0
  </acceptance_criteria>
  <done>All four client guides give working, floor-correct, verify-terminated paths; the overview reaches each in one click; install.md documents the PyPI alternatives consistent with D-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader terminal / client config ← registration JSON | readers paste these configs into MCP clients that will execute the launch command |
| reader browser ← cursor:// deeplink | the deeplink carries a base64-encoded launch config |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Tampering | registration JSON blocks | medium | mitigate | Single canonical block quoted from plugin/.mcp.json; floor-intact grep + no-extras grep in acceptance criteria; base64 payload decode-verified against the same block |
| T-02-09 | Spoofing | cursor:// deeplink | medium | mitigate | Deeplink format cited from cursor.com official docs; payload is decode-verified; no fabricated links for clients without schemes (A1) |
| T-02-SC | Tampering | package installs | low | accept | Zero packages installed by this plan |
</threat_model>

<verification>
- `npm run build && npm run verify` green after every task commit (V2 grows by exactly one URL)
- Floor + no-extras greps across the integrations directory
- Deeplink base64 decode check (acceptance criteria Task 2)
</verification>

<success_criteria>
- DOCS-02 complete: four equal-footing guides, each with a working path and a Verify step
- Generic stdio ADD was 3-file atomic; all registration JSON floor-correct with no extras
</success_criteria>

## Flagged Assumptions (edge-probe unresolved — surfaced, not dropped)

- DOCS-02 unclassified by the edge engine: assumed the four named clients plus the generic stdio fallback exhaust the client surface (any fifth client is served by generic-stdio) — flagged, not silently dropped.
- RESEARCH A1: Codex CLI has no MCP-install URL scheme — its path is presented as command/config, trivially upgradable to a link if one ships later.
- Note for the seal-time api-coverage detector: this plan integrates NO external API — it authors static documentation describing MCP clients; the `cursor://` string is a client-side OS URL scheme, not an API call.

## Artifacts this phase produces (plan 02-05)

| Artifact | Path / URL |
|----------|------------|
| NEW generic stdio guide | `src/content/docs/docs/integrations/generic-stdio.md` → `/docs/integrations/generic-stdio/` |
| Contract entry | `"/docs/integrations/generic-stdio/"` in `design/url-contract.json` |
| Sidebar entry | `Any stdio client` in the Integrations group of `astro.config.mjs` |
| Deepened guides | `/docs/integrations/claude-code/`, `/docs/integrations/cursor/`, `/docs/integrations/codex-cli/`, `/docs/integrations/`, `/docs/guide/install/` |
| Cursor install deeplink (literal, decode-verified) | inside cursor.md |

<output>
Create `.planning/phases/02-docs-rebuild-tutorial-first-content/02-05-SUMMARY.md` when done. Record the computed base64 deeplink payload and its decoded config for the Phase 5 claims audit.
</output>
