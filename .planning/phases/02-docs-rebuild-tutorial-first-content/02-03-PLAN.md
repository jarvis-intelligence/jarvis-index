---
phase: 02-docs-rebuild-tutorial-first-content
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/content/docs/docs/cli/jarvis-server.md
  - src/content/docs/docs/cli/index-cmd.md
  - src/content/docs/docs/cli/watch.md
  - src/content/docs/docs/cli/list.md
  - src/content/docs/docs/cli/status.md
  - src/content/docs/docs/cli/reindex.md
  - src/content/docs/docs/cli/forget.md
  - src/content/docs/docs/cli/index.md
  - design/url-contract.json
  - astro.config.mjs
autonomous: true
requirements:
  - DOCS-05
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "All 7 commands (6 jarvis subcommands + jarvis-server) each have a reference page reachable from the CLI sidebar group in one click"
    - "The new /docs/cli/jarvis-server/ page ships as a 3-file atomic commit: content + url-contract URL + sidebar entry after jarvis watch (Pattern 1)"
    - "jarvis index documents all five flags verbatim from index_cli.py:1118-1177 (slug, scheme, repeatable semantic-include, language choices java/python/swift/typescript, search-only); jarvis watch documents debounce with its 5.0 default"
    - "jarvis status documents the indexed / failed / partial status values; jarvis forget states irreversibility as a settled fact in one sentence"
    - "The CLI overview is a 7-row one-click table including jarvis-server"
  artifacts:
    - path: "src/content/docs/docs/cli/jarvis-server.md"
      provides: "Reference page for the MCP-server entry point every client config names (RESEARCH Open Question 1 resolved: dedicated page)"
    - path: "design/url-contract.json"
      provides: "Contract grown by exactly one URL: /docs/cli/jarvis-server/"
      contains: "/docs/cli/jarvis-server/"
    - path: "astro.config.mjs"
      provides: "CLI sidebar group with the jarvis-server entry after jarvis watch"
      contains: "docs/cli/jarvis-server"
  key_links:
    - from: "src/content/docs/docs/cli/jarvis-server.md"
      to: "design/url-contract.json + astro.config.mjs"
      via: "3-file atomic ADD (V2 set-equality forces same-commit)"
      pattern: "jarvis-server"
    - from: "src/content/docs/docs/cli/index.md"
      to: "the 7 command pages"
      via: "kebab-case trailing-slash links"
      pattern: "/cli/[a-z-]+/"
  prohibitions:
    - statement: "No flag, default, or choice list is documented that does not appear verbatim in index_cli.py:1118-1177 or pyproject.toml scripts — no remembered or invented CLI surface (D-05 discipline applied to flags)"
      status: flagged
    - statement: "No TODO-framing — the version gate and partial status are settled behaviors with rationale inline"
      status: flagged
---

<objective>
Deepen the CLI reference to full 7-command coverage (DOCS-05): the 6 `jarvis` subcommands deepened in place plus a NEW dedicated `jarvis-server` page (RESEARCH Open Question 1's recommendation — it is the command every MCP config names). All flags, defaults, and choices verbatim from `../jarvis/src/jarvis/index_cli.py:1118-1177` and `pyproject.toml:86-88`. Terse reference voice (D-06).

Purpose: a user or agent can look up any command's exact flags and behavior in one click.
Output: 7 command pages + rewritten CLI overview; contract and sidebar grown by exactly one URL.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-RESEARCH.md
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-PATTERNS.md
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-UI-SPEC.md
</context>

## Verified CLI surface (from 02-RESEARCH Product Truth §3 — executors re-verify against index_cli.py before writing)

| Command | Flags (verbatim from argparse) |
|---------|-------------------------------|
| `jarvis index <path>` | `--slug` (override the auto-derived slug), `--scheme` (Xcode scheme for multi-scheme Swift repos), `--semantic-include PATH` (repeatable; force-include a path prefix the generated-file filter would skip; persisted and reused by reindex/watch), `--language` (choices: java, python, swift, typescript), `--search-only` (skip SCIP indexing; publish only Zoekt + semantic search; persisted) |
| `jarvis list` | none |
| `jarvis status <slug>` | none |
| `jarvis reindex <slug>` | none (reuses persisted options) |
| `jarvis forget <slug>` | none |
| `jarvis watch <path>` | `--slug`, `--scheme`, `--debounce` (float, default 5.0, quiet-period seconds), `--language` |
| `jarvis-server` | no args; stdio MCP server (`jarvis.server:main`) |

Status values: `indexed`, `failed`, plus `partial` (published real symbols but no navigable positions). Version gate: `jarvis index` refuses scip below 0.9.0 — older versions silently write a schema-valid database with zero chunks and zero mentions.

<tasks>

<task type="tracer">
  <name>Task 1: New jarvis-server page end-to-end — 3-file atomic ADD through contract and sidebar</name>
  <precondition>../jarvis/pyproject.toml readable (entry-point verification) </precondition>
  <files>src/content/docs/docs/cli/jarvis-server.md, design/url-contract.json, astro.config.mjs</files>
  <read_first>src/content/docs/docs/cli/index-cmd.md (the structure analog — its skeleton AND flag data are correct), 02-PATTERNS.md CLI section + "3-file atomic ADD" shared pattern, astro.config.mjs (CLI sidebar group, lines 87-98; entry shape `{ label, link }` with docs/-prefixed, no leading/trailing slash), design/url-contract.json (trailing-slash URL form), ../jarvis/pyproject.toml:86-88, ../jarvis/src/jarvis/server.py:389-390</read_first>
  <action>
This tracer proves the plan's page-ADD machinery end-to-end (content + contract + sidebar in one commit, V2 green) before the in-place deepens.

1. Create `src/content/docs/docs/cli/jarvis-server.md` following the index-cmd.md skeleton: frontmatter `title: jarvis-server` + description; `## Usage` with fenced `jarvis-server` (no args); `## Behavior` — it is the stdio MCP server entry point installed by the package (`[project.scripts]` maps jarvis-server to jarvis.server:main), serves the 9 MCP tools over stdio, one process per connected client; clients normally launch it as `uvx --from jarvis-mcp>=0.6.0 jarvis-server` (the exact string registered in the MCP configs); `## Verify` — after registering, ask the agent to call `getIndexStatus` with an indexed slug. Link the integrations overview (`/integrations/`) for per-client registration. No duplicate body H1.
2. Add `"/docs/cli/jarvis-server/"` to the `urls` array in `design/url-contract.json`, placed with the other `/docs/cli/` URLs (trailing-slash form).
3. Add `{ label: 'jarvis-server', link: 'docs/cli/jarvis-server' },` to the CLI sidebar group in `astro.config.mjs`, immediately after the `jarvis watch` entry (UI-SPEC sidebar placement).
4. `npm run build && npm run verify` — V2 must pass with the grown set (proves the 3-file atomic discipline).
  </action>
  <verify>
    <automated>npm run build && npm run verify && grep -q '"/docs/cli/jarvis-server/"' design/url-contract.json && grep -q "docs/cli/jarvis-server" astro.config.mjs</automated>
  </verify>
  <acceptance_criteria>
- `npm run build && npm run verify` exits 0 (V2 set-equality green with 35 contract URLs)
- `test -f dist/docs/cli/jarvis-server/index.html`
- `grep -q 'jarvis-mcp>=0.6.0' src/content/docs/docs/cli/jarvis-server.md` (the exact launch string, floor intact)
- `grep -q 'getIndexStatus' src/content/docs/docs/cli/jarvis-server.md`
- All three files changed in the same commit: `git show --name-only HEAD` lists the content file, design/url-contract.json, and astro.config.mjs
  </acceptance_criteria>
  <done>/docs/cli/jarvis-server/ exists, is in the contract and the sidebar in one commit, and the build+verify gate is green — the ADD machinery is proven for the phase.</done>
</task>

<task type="auto">
  <name>Task 2: Deepen the flag-bearing pages — jarvis index, jarvis watch — and the CLI overview</name>
  <precondition>../jarvis/src/jarvis/index_cli.py readable (flag verification)</precondition>
  <files>src/content/docs/docs/cli/index-cmd.md, src/content/docs/docs/cli/watch.md, src/content/docs/docs/cli/index.md</files>
  <read_first>The verified CLI surface table above, ../jarvis/src/jarvis/index_cli.py:1118-1177 (re-verify every flag string), ../jarvis/src/jarvis/index_cli.py:34-49 (extension→indexer map + priority) and :61-66 (version gate), the existing three stubs, 02-UI-SPEC.md (tables, code blocks)</read_first>
  <action>
- `index-cmd.md`: keep the existing skeleton (Usage / Arguments / Options / Behavior — this stub's data is already correct); deepen the Options table to all five flags with the verbatim help text meanings, and the Behavior section to cover: language detection (git-tracked file plurality; extension→indexer families ts/tsx→typescript, py→python, java/kt→java, swift→swift; tie-break priority ts, tsx, py, java, kt, swift; `--language` overrides), the scip version gate (refuses scip below 0.9.0 because older versions silently produce an empty-but-valid database — settled behavior with rationale), search-only publishing, persisted options (`--search-only` and `--semantic-include` are stored and reused by reindex/watch), and atomic publish. One example invocation with expected `jarvis status` follow-up.
- `watch.md`: Usage, Options (all four flags; `--debounce` float with the 5.0-second default), Behavior: requires the watch extra (link the quickstart optional-extras tip); reuses the persisted index options; quiet-period semantics. One example.
- `cli/index.md`: rewrite as the section overview — one intro sentence distinguishing `jarvis` (indexer CLI) from `jarvis-server` (the MCP server clients launch), then a 7-row table `Command | What it does | Reference` linking all seven pages in kebab-case trailing-slash form (`/cli/index-cmd/`, `/cli/list/`, `/cli/status/`, `/cli/reindex/`, `/cli/forget/`, `/cli/watch/`, `/cli/jarvis-server/`).
Rules: terse voice (D-06); no asides; links trailing-slash; no body H1; every flag string re-verified against index_cli.py before writing.
  </action>
  <verify>
    <automated>npm run build && npm run verify && grep -q -- '--semantic-include' src/content/docs/docs/cli/index-cmd.md && grep -q -- '--debounce' src/content/docs/docs/cli/watch.md && grep -q '/cli/jarvis-server/' src/content/docs/docs/cli/index.md</automated>
  </verify>
  <acceptance_criteria>
- index-cmd.md contains all five flags: `grep -q -- '--slug'`, `--scheme`, `--semantic-include`, `--language`, `--search-only`
- `grep -q 'typescript' src/content/docs/docs/cli/index-cmd.md` (language choices listed)
- `grep -q '0.9.0' src/content/docs/docs/cli/index-cmd.md` (version gate documented)
- watch.md contains `--debounce` and `5.0`
- cli/index.md links all seven command pages (7 matches of `/cli/.*/` targets)
- `npm run build && npm run verify` exits 0
  </acceptance_criteria>
  <done>The two flag-bearing commands are fully documented from source, and the CLI overview reaches all 7 commands in one click.</done>
</task>

<task type="auto">
  <name>Task 3: Deepen the four no-flag command pages — list, status, reindex, forget</name>
  <files>src/content/docs/docs/cli/list.md, src/content/docs/docs/cli/status.md, src/content/docs/docs/cli/reindex.md, src/content/docs/docs/cli/forget.md</files>
  <read_first>The four existing stubs, the verified CLI surface table above, 02-RESEARCH.md Product Truth §3 (status values), 02-UI-SPEC.md Copywriting Contract (the jarvis forget destructive-action row)</read_first>
  <action>
Deepen each in place with the index-cmd.md skeleton minus the Options section (each states "No flags." explicitly):
- `list.md`: enumerates registered repos with their slugs and statuses; one example with realistic output columns.
- `status.md`: per-slug status; document the three values — `indexed`, `failed`, `partial` (real symbols published but no navigable positions) — and staleness (published commit differs from current HEAD → reindex). Cross-link `getIndexStatus` (`/tools/get-index-status/`) as the MCP-side equivalent.
- `reindex.md`: rebuilds the index reusing the persisted options recorded at index time (search-only, semantic-include, scheme); when to run it (stale status, after setup.sh upgrades an indexer, after the typeHierarchy fork-scip fix).
- `forget.md`: per the UI-SPEC copy contract, state irreversibility as fact in one sentence: it "removes the index permanently; re-run `jarvis index` to rebuild" — no warning box (a static site has no confirmation UI; the sentence is the contract).
Rules: terse voice; no asides; trailing-slash links; no body H1.
  </action>
  <verify>
    <automated>npm run build && npm run verify && grep -q 'removes the index permanently' src/content/docs/docs/cli/forget.md && grep -q 'partial' src/content/docs/docs/cli/status.md</automated>
  </verify>
  <acceptance_criteria>
- `grep -q 'removes the index permanently' src/content/docs/docs/cli/forget.md`
- `grep -q 'partial' src/content/docs/docs/cli/status.md` and `grep -q 'indexed' src/content/docs/docs/cli/status.md`
- `grep -q 'persisted' src/content/docs/docs/cli/reindex.md`
- Each of the 4 pages states "No flags." (`grep -l 'No flags.' ...` lists all four)
- `! grep -q ':::' ` on all four pages; `npm run build && npm run verify` exits 0
  </acceptance_criteria>
  <done>All four no-flag commands documented with settled-decision framing; the full 7-command surface is complete and one click from the nav.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader terminal ← CLI examples | every example is a command a reader pastes into a shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | CLI example commands | low | mitigate | Commands and flags verbatim from index_cli.py/pyproject; no user-interpolated unquoted vars; launch string keeps the >=0.6.0 floor |
| T-02-SC | Tampering | package installs | low | accept | Zero packages installed by this plan |
</threat_model>

<verification>
- `npm run build && npm run verify` green after every task commit (V2 grows by exactly one URL in Task 1, then stays constant)
- Per-page flag greps as listed in acceptance criteria
- Manual phase-gate item: diff each Options table against index_cli.py:1118-1177
</verification>

<success_criteria>
- 7 command pages complete and one-click reachable; jarvis-server ADD was 3-file atomic
- All flags/defaults/choices source-verified; forget states irreversibility; status documents partial
</success_criteria>

## Flagged Assumptions (edge-probe unresolved — surfaced, not dropped)

- DOCS-05 unclassified by the edge engine: assumed to have no edge cases beyond flag-accuracy against index_cli.py. The "7 commands" counting follows RESEARCH Open Question 1's resolution (6 subcommands + jarvis-server as a dedicated page) — flagged, not silently dropped.

## Artifacts this phase produces (plan 02-03)

| Artifact | Path / URL |
|----------|------------|
| NEW jarvis-server reference page | `src/content/docs/docs/cli/jarvis-server.md` → `/docs/cli/jarvis-server/` |
| Contract entry | `"/docs/cli/jarvis-server/"` in `design/url-contract.json` |
| Sidebar entry | `jarvis-server` in the CLI group of `astro.config.mjs` |
| 6 deepened command pages + overview | `/docs/cli/index-cmd/`, `/docs/cli/watch/`, `/docs/cli/list/`, `/docs/cli/status/`, `/docs/cli/reindex/`, `/docs/cli/forget/`, `/docs/cli/` |

<output>
Create `.planning/phases/02-docs-rebuild-tutorial-first-content/02-03-SUMMARY.md` when done.
</output>
