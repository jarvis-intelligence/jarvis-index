---
phase: 02-docs-rebuild-tutorial-first-content
plan: 06
type: execute
wave: 5
depends_on:
  - 02-02
  - 02-04
  - 02-05
files_modified:
  - src/content/docs/docs/quickstart.md
  - src/content/docs/docs/quickstart.mdx
  - src/content/docs/docs/guide/install-matrix.mdx
  - src/content/docs/docs/index.md
  - design/url-contract.json
  - astro.config.mjs
autonomous: true
requirements:
  - DOCS-01
  - DOCS-03
  - DOCS-10
  - DOCS-12
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A cold visitor's quickstart runs why → setup.sh → jarvis index → register per client → first tool call, with a bold 'You'll know it works when…' and 'You'll know it broke when…' pair at every step, every shape a real transcript (DOCS-01, DOCS-10, D-05, D-09)"
    - "The quickstart states what happens on re-run of each step: setup.sh is idempotent (skips installed deps) and re-indexing republishes safely — re-run behavior is never left undefined"
    - "The requirements link with anchor text 'check your language is supported' sits immediately before the install step's command block (D-14)"
    - "The install-matrix page carries the four-channel decision table and the MCP Registry deep-link table with the live registry entry name io.github.jarvis-intelligence/jarvis (D-10, DOCS-03, DOCS-12)"
    - "Every tab group uses the canonical TabItem set in order — Claude Code, Cursor, Codex CLI, Any stdio client — with syncKey client, so a reader's choice persists across groups and pages (UI-SPEC)"
    - "Guide sidebar order is Quickstart → Requirements & Limits → Install → Install Channels (UI-SPEC sidebar placement; DOCS-09 nav-ordering edge)"
    - "Every quickstart failure shape links a troubleshooting entry using the anchors locked by plan 02-04"
    - statement: "Concurrent-use caveats (two clients registering simultaneously) are either documented in the quickstart or explicitly confirmed out of scope on the page"
      verification: backstop
  artifacts:
    - path: "src/content/docs/docs/quickstart.mdx"
      provides: "The tutorial-first quickstart (renamed .md→.mdx for Tabs; URL unchanged)"
      contains: "syncKey=\"client\""
    - path: "src/content/docs/docs/guide/install-matrix.mdx"
      provides: "Four-channel matrix + MCP Registry deep-link table (D-10, DOCS-12)"
      contains: "io.github.jarvis-intelligence/jarvis"
    - path: "design/url-contract.json"
      provides: "Contract grown by exactly one URL: /docs/guide/install-matrix/"
      contains: "/docs/guide/install-matrix/"
  key_links:
    - from: "src/content/docs/docs/quickstart.mdx"
      to: "src/content/docs/docs/guide/requirements.md"
      via: "the D-14 pre-install link"
      pattern: "check your language is supported"
    - from: "src/content/docs/docs/quickstart.mdx"
      to: "troubleshooting anchors locked by 02-04"
      via: "'You'll know it broke when…' links"
      pattern: "/troubleshooting/common-failures/#"
    - from: "src/content/docs/docs/guide/install-matrix.mdx"
      to: "@astrojs/starlight/components"
      via: "Tabs/TabItem import with syncKey"
      pattern: "syncKey=\"client\""
  prohibitions:
    - statement: "The setup.sh command uses ONLY the canonical raw.githubusercontent.com/jarvis-intelligence/jarvis-index/main/setup.sh URL — never a variant host, path, or shortened URL (D-09; supply-chain row in 02-RESEARCH Security Domain)"
      status: flagged
    - statement: "No invented transcripts — every 'works'/'broke' shape traces to Product Truth §1-2, the test fixtures, or setup.sh's own output format (D-05)"
      status: flagged
    - statement: "No fake deep links in the registry table — clients without URL schemes get command equivalents with the honest note (A1)"
      status: flagged
---

<objective>
Deliver the journey capstone: the tutorial-first quickstart rewritten to the DOCS-01 shape (why → setup.sh → `jarvis index` → register per client → first tool call) with inline "You'll know it works when… / You'll know it broke when…" pairs at every step (DOCS-10, D-05, D-07: quickstart-only), the NEW install-channels matrix page carrying the four-channel table and the MCP Registry deep-link table (D-10, DOCS-03, DOCS-12), and the docs home rewritten as the journey entry point. Runs at wave 5 so every link target (requirements page, troubleshooting anchors, tool pages, client guides) already exists.

Purpose: this is the page set that carries the project's core value — a cold visitor reaches a self-verifiable first tool call using only these pages.
Output: quickstart.mdx (renamed, URL unchanged), install-matrix.mdx (3-file atomic), rewritten docs home.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-RESEARCH.md
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-PATTERNS.md
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-UI-SPEC.md
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-04-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Install-channels matrix page end-to-end — Tabs + tables through contract and sidebar (D-10, DOCS-03, DOCS-12)</name>
  <files>src/content/docs/docs/guide/install-matrix.mdx, design/url-contract.json, astro.config.mjs</files>
  <read_first>02-UI-SPEC.md Page Layout Contract 1 (implement exactly) + Starlight Component Conventions (canonical TabItem set), 02-RESEARCH.md Product Truth §6 (channel commands, registry entry, deep-link table rows) + Code Example 4 (Tabs), 02-PATTERNS.md install-matrix section, astro.config.mjs Guide sidebar group, the completed integrations pages from 02-05 (link targets)</read_first>
  <action>
This tracer proves the phase's only untested rendering machinery — an .mdx page importing Starlight Tabs — end-to-end through the contract before the quickstart reuses it.

1. Create `src/content/docs/docs/guide/install-matrix.mdx` per UI-SPEC Layout Contract 1, in this exact order:
   a. Frontmatter (`title: Install Channels` + description) and one framing sentence: four ways in, all converge on the same `jarvis-server`.
   b. **Four-channel decision table** — columns `Channel | Command | Best when`; rows verbatim from RESEARCH §6: PyPI direct (`uv tool install jarvis-mcp`), Claude Code plugin (`/plugin marketplace add jarvis-intelligence/jarvis-index` then `/plugin install jarvis@jarvis`), Codex plugin (same marketplace source via the repo-root Codex manifest), MCP Registry (entry `io.github.jarvis-intelligence/jarvis`, pypi package jarvis-mcp, stdio transport).
   c. `import { Tabs, TabItem } from '@astrojs/starlight/components';` and a `<Tabs syncKey="client">` walkthrough with the canonical label set IN ORDER: `Claude Code`, `Cursor`, `Codex CLI`, `Any stdio client` — each TabItem gives the channel detail for that client and links its full guide (`/integrations/claude-code/`, `/integrations/cursor/`, `/integrations/codex-cli/`, `/integrations/generic-stdio/`).
   d. **MCP Registry deep-link table** — columns `Client | Deep link / command`: Cursor row carries the real `cursor://` deeplink (copy the literal from 02-05's cursor.md so both pages stay identical); Claude Code row carries `claude mcp add jarvis --scope user -- jarvis-server` with the honest "no URL scheme" note; Codex CLI row carries the plugin/manual-config command with the same note; a final row points generic clients at the JSON guide. Never a fabricated link (A1).
2. Add `"/docs/guide/install-matrix/"` to `design/url-contract.json`.
3. Add `{ label: 'Install Channels', link: 'docs/guide/install-matrix' },` to the Guide sidebar group AFTER Install — final Guide order: Quickstart → Requirements & Limits → Install → Install Channels.
4. `npm run build && npm run verify`.
  </action>
  <verify>
    <automated>npm run build && npm run verify && grep -q 'syncKey="client"' src/content/docs/docs/guide/install-matrix.mdx && grep -q 'io.github.jarvis-intelligence/jarvis' src/content/docs/docs/guide/install-matrix.mdx && grep -q 'label="Any stdio client"' src/content/docs/docs/guide/install-matrix.mdx</automated>
    <human-check>End-of-phase preview pass: tabs switch, and a tab chosen here is pre-selected on the quickstart's register step (syncKey persistence) — VALIDATION.md interactive item.</human-check>
  </verify>
  <acceptance_criteria>
- `npm run build && npm run verify` exits 0; `test -f dist/docs/guide/install-matrix/index.html`
- All four canonical TabItem labels present: `grep -q 'label="Claude Code"' src/content/docs/docs/guide/install-matrix.mdx && grep -q 'label="Cursor"' src/content/docs/docs/guide/install-matrix.mdx && grep -q 'label="Codex CLI"' src/content/docs/docs/guide/install-matrix.mdx && grep -q 'label="Any stdio client"' src/content/docs/docs/guide/install-matrix.mdx` exits 0
- `grep -q '| Channel | Command | Best when |' src/content/docs/docs/guide/install-matrix.mdx`
- `grep -q 'cursor://anysphere.cursor-deeplink/mcp/install' src/content/docs/docs/guide/install-matrix.mdx` and `grep -qi 'no.*URL scheme' src/content/docs/docs/guide/install-matrix.mdx`
- Registry entry live (network probe, non-blocking if offline): `curl -s "https://registry.modelcontextprotocol.io/v0/servers?search=io.github.jarvis-intelligence/jarvis" | grep -q '"status":"active"'`
- Contract + sidebar in the same commit; Guide group order is Quickstart, Requirements & Limits, Install, Install Channels (verify by reading astro.config.mjs)
  </acceptance_criteria>
  <done>/docs/guide/install-matrix/ renders the four-channel table, the synced Tabs walkthrough, and the honest registry deep-link table — MDX+Tabs machinery proven for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Quickstart rewrite — .md→.mdx, five steps, works/broke pairs, D-14 link (DOCS-01, DOCS-10)</name>
  <precondition>Plan 02-04's locked troubleshooting anchors exist in the built site (grep 'id="first-mcp-connect-times-out-uvx-cold-start"' dist/docs/troubleshooting/common-failures/index.html)</precondition>
  <files>src/content/docs/docs/quickstart.md, src/content/docs/docs/quickstart.mdx</files>
  <read_first>src/content/docs/docs/quickstart.md (the voice baseline — its tone survives, its JSON examples are verified WRONG and die), 02-UI-SPEC.md Quickstart Step Pattern (the rendering contract — implement exactly) + Copywriting Contract, 02-RESEARCH.md Product Truth §1-2 (shapes) + Code Example 1 (goToDefinition) + §5 (failure modes), 02-04-SUMMARY.md (shipped anchor list), setup.sh print_summary section (~lines 703-710, READ ONLY — the real step-1 success output format), ../jarvis/tests/test_server_tools.py + fixtures (transcript values)</read_first>
  <action>
Rename `quickstart.md` → `quickstart.mdx` (git mv; the URL /docs/quickstart/ is unchanged — extension is not part of the slug, so NO contract edit; Pitfall 7). Rewrite to the DOCS-01 shape. Keep the existing direct second-person voice. Structure:

- Intro ("why", no step number): 2-3 sentences — structural code intelligence for coding agents, local-first, what the reader will have at the end. Then the D-14 line with anchor text exactly `check your language is supported` linking `/guide/requirements/`, placed IMMEDIATELY BEFORE Step 1's command block.
- `## Step 1: Install the external binaries` — the canonical curl|sh command verbatim (D-09): `curl -fsSL https://raw.githubusercontent.com/jarvis-intelligence/jarvis-index/main/setup.sh | sh`. State re-run behavior: setup.sh is idempotent — re-running skips what is already installed (`--force` overrides). Works: the end-of-run summary block listing each dependency with its ok status (lift the real format from setup.sh's summary printer — do not invent lines). Broke: a FAILED row in that summary → link the PATH entry `/troubleshooting/common-failures/#jarvis-or-jarvis-server-not-found-path` for path issues, and note `--only <name>` re-runs one dependency.
- `## Step 2: Install the CLI and MCP server` — `uv tool install jarvis-mcp` (also the verified cold-start prevention). Keep the existing `:::tip[Optional extras]` aside (semantic/watch extras) verbatim. Works: `jarvis --help` prints usage and `command -v jarvis-server` prints a path. Broke: command not found → same PATH anchor.
- `## Step 3: Index a repo` — `jarvis index /path/to/your/repo`. Re-run safety: re-running reindexes and republishes atomically — safe. Works: `jarvis status <slug>` prints `indexed`. Broke: the version-gate refusal (scip too old) → `/troubleshooting/common-failures/#jarvis-index-refuses-an-old-scip-version-gate`.
- `## Step 4: Register jarvis with your client` — `import { Tabs, TabItem } from '@astrojs/starlight/components';` and `<Tabs syncKey="client">` with the canonical four labels IN ORDER; each TabItem: the one registration command/JSON for that client (floor-intact canonical forms from the 02-05 pages) + a link to its full guide. After the tabs, client-agnostic works/broke: Works — your client lists the jarvis tools (9 tools) after connecting. Broke — first connect times out → `/troubleshooting/common-failures/#first-mcp-connect-times-out-uvx-cold-start` (the uv tool install fix). Add one sentence on concurrent use: registering jarvis in several clients at once is safe — stdio transport means each client launches its own `jarvis-server` process.
- `## Step 5: Make your first tool call` — getIndexStatus call `{ "repo": "<slug>" }` and its REAL response shape (repo, indexed, status, the five flat freshness fields, searchCoverage — Product Truth §1; values from fixtures/derived, never the old stub's invented nested form), then goToDefinition per RESEARCH Code Example 1 with fixture-grounded values. Works: a JSON response with `"freshness": "fresh"` and definitions carrying nested range objects. Broke: the ambiguous-symbol candidates response (quote the verbatim 2-candidate transcript or link the goToDefinition Errors section) → retry with a qualified name; anything else → the decision tree `/troubleshooting/`.
- `## Next steps` — links: tools overview, CLI overview, install channels matrix, concepts.

Every step renders per the UI-SPEC step pattern: bold literal labels **You'll know it works when…** and **You'll know it broke when…** (same phrases every step, greppable), code blocks tagged sh/json, NO new asides (the existing optional-extras tip is the only one).
  </action>
  <verify>
    <automated>npm run build && npm run verify && test -f src/content/docs/docs/quickstart.mdx && test ! -f src/content/docs/docs/quickstart.md && test "$(grep -c "You'll know it works when" src/content/docs/docs/quickstart.mdx)" -ge 5 && test "$(grep -c "You'll know it broke when" src/content/docs/docs/quickstart.mdx)" -ge 5</automated>
  </verify>
  <acceptance_criteria>
- `npm run build && npm run verify` exits 0 and V2 is unchanged (`test -f dist/docs/quickstart/index.html` — Pitfall 7 rename check)
- `grep -c '^## Step' src/content/docs/docs/quickstart.mdx` equals 5
- Both bold phrases appear ≥ 5 times each (one pair per step)
- `grep -q 'check your language is supported' src/content/docs/docs/quickstart.mdx` and it precedes the Step 1 command: `awk '/check your language is supported/{f=1} /curl -fsSL/{exit !(f)}' src/content/docs/docs/quickstart.mdx`
- `grep -q 'raw.githubusercontent.com/jarvis-intelligence/jarvis-index/main/setup.sh' src/content/docs/docs/quickstart.mdx` (canonical URL only)
- `grep -q 'syncKey="client"' src/content/docs/docs/quickstart.mdx` with all four canonical labels
- `grep -c '/troubleshooting/common-failures/#' src/content/docs/docs/quickstart.mdx` ≥ 3 (failure links hit 02-04's locked anchors)
- `grep -q 'idempotent' src/content/docs/docs/quickstart.mdx` (re-run behavior stated)
- `! grep -q '"freshness": {' src/content/docs/docs/quickstart.mdx` and `! grep -q 'startLine' src/content/docs/docs/quickstart.mdx` (the old stub's invented shapes are gone)
  </acceptance_criteria>
  <done>The quickstart is a five-step, self-verifiable cold-visitor journey with real shapes, re-run semantics, the D-14 pre-install link, synced client tabs, and anchor-true failure links.</done>
</task>

<task type="auto">
  <name>Task 3: Docs home rewrite — the journey entry point</name>
  <files>src/content/docs/docs/index.md</files>
  <read_first>src/content/docs/docs/index.md (current stub), 02-UI-SPEC.md Copywriting Contract (voice), the shipped Guide sidebar order (astro.config.mjs after Task 1), ../jarvis/README.md (one-line positioning)</read_first>
  <action>
Rewrite the docs home as the journey entry: one honest positioning paragraph (local-first code intelligence MCP server; 9 tools; your code never leaves the machine — claims traceable to README), then a "Start here" ordering that mirrors the Guide sidebar reading order: 1. [Quickstart](/quickstart/) 2. [check your language is supported](/guide/requirements/) 3. [Install](/guide/install/) / [Install Channels](/guide/install-matrix/) — then a six-group section map table `Section | What's there` (Guide, Concepts, MCP Tools, CLI, Integrations, Troubleshooting) plus the Changelog link (/changelog/). Trailing-slash targets throughout; no body H1; no asides.
  </action>
  <verify>
    <automated>npm run build && npm run verify && grep -q '/quickstart/' src/content/docs/docs/index.md && grep -q '/guide/requirements/' src/content/docs/docs/index.md && grep -q '/guide/install-matrix/' src/content/docs/docs/index.md</automated>
  </verify>
  <acceptance_criteria>
- Home links quickstart, requirements, install-matrix, and changelog with trailing-slash targets
- `! grep -qE '^# [^#]' src/content/docs/docs/index.md`; `npm run build && npm run verify` exits 0
  </acceptance_criteria>
  <done>The docs home routes a cold visitor into the journey in reading order.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader terminal ← quickstart step 1 | curl piped to sh — the single highest-trust command on the site |
| reader client config ← register-step tabs | registration JSON/commands pasted into MCP clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering | curl-pipe-sh install command | high | mitigate | ONLY the canonical raw.githubusercontent URL, grep-enforced in acceptance criteria; setup.sh itself SHA256-verifies every artifact it downloads (setup.sh:238-248); prohibition row forbids variant URLs |
| T-02-11 | Tampering | register-step configs | medium | mitigate | Canonical floor-intact forms copied from the 02-05 pages; no extras; Tabs content links the full guides rather than duplicating divergent JSON |
| T-02-12 | Spoofing | registry deep-link table | medium | mitigate | Cursor link decode-verified in 02-05 and copied literally; command equivalents with honest no-scheme notes for the rest (A1) |
| T-02-SC | Tampering | package installs | low | accept | Zero packages installed by this plan |
</threat_model>

<verification>
- `npm run build && npm run verify` green after every task commit (V2 grows by exactly one URL in Task 1)
- Quickstart greps: 5 steps, ≥5 works/broke pairs, D-14 link position, canonical setup.sh URL, anchor-true failure links
- Human checks at phase gate (end-of-phase mode): read the built quickstart end-to-end as a stranger (DOCS-01/DOCS-10 editorial item); tabs switch and sync across install-matrix ↔ quickstart; Pagefind finds the new pages
</verification>

<success_criteria>
- DOCS-01 + DOCS-10: five-step self-verifiable quickstart with inline real success/failure shapes
- DOCS-03 + DOCS-12: four-channel matrix + registry deep-link table live on /docs/guide/install-matrix/
- D-09 (setup.sh-first), D-10 (matrix own page), D-14 (pre-install link), D-07 (shapes quickstart-only) all honored
- Guide sidebar reading order final; V2 green at 39 contract URLs
</success_criteria>

## Flagged Assumptions (edge-probe unresolved — surfaced, not dropped)

- DOCS-03 unclassified: assumed the four-channel matrix has no edge cases beyond the UI-SPEC layout contract — flagged, not silently dropped.
- DOCS-10 unclassified: assumed works/broke shapes are required on the quickstart ONLY (D-07 scopes them there; tool pages' failure shape is the error contract) — flagged.
- DOCS-12 unclassified: assumed the honest deep-link table (one real link + command equivalents) satisfies "per-client deep links"; RESEARCH A1 records the no-scheme reality for Claude Code/Codex — flagged.
- Note for the seal-time api-coverage detector: no external API integration exists in this plan — the MCP Registry is described in static prose and probed read-only by a verification curl; nothing in the shipped site calls any API.

## Artifacts this phase produces (plan 02-06)

| Artifact | Path / URL |
|----------|------------|
| NEW install-channels matrix page | `src/content/docs/docs/guide/install-matrix.mdx` → `/docs/guide/install-matrix/` |
| Contract entry | `"/docs/guide/install-matrix/"` in `design/url-contract.json` |
| Sidebar entry | `Install Channels` in the Guide group of `astro.config.mjs` |
| Rewritten quickstart | `src/content/docs/docs/quickstart.mdx` → `/docs/quickstart/` (URL unchanged) |
| Rewritten docs home | `/docs/` |

<output>
Create `.planning/phases/02-docs-rebuild-tutorial-first-content/02-06-SUMMARY.md` when done. List the transcript sources used for each works/broke shape (Phase 5 claims-audit input).
</output>
