---
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - src/content/docs/docs/guide/requirements.md
  - src/data/demo-scenarios.json
  - src/components/landing/DemoPanel.astro
  - src/components/landing/ToolShowcase.astro
  - src/components/landing/LanguageMatrix.astro
  - src/pages/index.astro
  - src/styles/landing.css
autonomous: true
requirements:
  - LNDG-02
  - LNDG-03
  - LNDG-04
  - LNDG-07
---

# 03-02: Demo Panel, Tiered 9-Tool Showcase, Language Matrix, Privacy Section

## Objective

Fill the four content sections left as placeholders by 03-01: the simulated demo panel with real recorded JSON shapes (3 scenarios from Phase-2 fixtures), the honestly tiered 9-tool showcase with gate chips on semanticSearch and typeHierarchy, the compact language-support matrix mirroring requirements.md exactly, and the local-first privacy section. After this plan a visitor sees the full content of the page minus diagrams and the final polish pass.

## must_haves

### truths

- `src/data/demo-scenarios.json` exists and contains exactly 3 scenario objects with `id`, `question`, `toolCall`, and `result` fields; the `result` shapes match verbatim the Phase-2 fixture JSON from quickstart.mdx (getIndexStatus, goToDefinition) and the findReferences tool reference page
- The demo panel renders all 3 scenarios as a click-through stepper; each step shows a natural-language question, the MCP tool call JSON, and the recorded result JSON in `.exchange` terminal-chrome styling; the stepper does not auto-advance
- The demo stepper transitions collapse under `prefers-reduced-motion: reduce` (per UI-SPEC interaction contracts)
- 9 tool cards render in a `.card-grid.cols-3` on desktop (3-col), 2-col tablet, 1-col mobile
- 7 core tools (getIndexStatus, goToDefinition, findReferences, callHierarchy, documentSymbols, searchCode, blastRadius) render as plain `.card` elements with `.card-kicker`, h3, and description
- `semanticSearch` card has a `.gate-chip` containing the text "requires `[semantic]` extra + reindex" with a deep-link to `/jarvis-index/docs/guide/requirements/#semanticsearch-requires-the-semantic-extra` (anchor created by this plan's requirements.md addition — see task action step 0)
- `typeHierarchy` card has a `.gate-chip` containing the text "requires fork-built scip + reindex" with a deep-link to `/jarvis-index/docs/guide/requirements/#swift-version-floor-and-code-signed-targets`
- The `.gate-chip` has a visually distinct warning tone (amber/warm, not accent blue) per UI-SPEC section 3
- The language matrix table has exactly 14 data rows matching `guide/requirements.md` verbatim: TypeScript/TSX, Python, Java/Kotlin, Swift, Go, Ruby, Rust, C, C++, C#, PHP, Scala, shell, SQL
- Matrix navigation column shows ✅ Yes for 4 families and — for 10 search-only languages
- Matrix caveat column links to the same `###` anchors in requirements.md as the docs page
- The matrix is wrapped in `.table-wrap` with `overflow-x: auto` — no page-level horizontal scroll at 390px
- The privacy section conveys "nothing leaves your machine" with zero-network positioning
- Static authored content — empty/partial data states are structurally impossible (UI-SPEC empty/partial/populated considerations)
- `npm run build` exits 0 and `node scripts/verify-build.mjs` passes V1–V10

## Tasks

<task type="auto">
  <name>Task: Simulated demo panel with real recorded JSON shapes</name>
  <files>src/data/demo-scenarios.json, src/components/landing/DemoPanel.astro, src/pages/index.astro</files>
  <read_first>
    - .planning/phases/03-landing-page-rebuild/03-UI-SPEC.md (section 2: Demo Panel, interaction contracts)
    - .planning/phases/03-landing-page-rebuild/03-CONTEXT.md (D-07 demo panel decision — 3 preset scenarios, click-through stepper, deliberately not live)
    - .planning/phases/03-landing-page-rebuild/03-PATTERNS.md (DemoPanel pattern, exchange terminal pattern, demo data source with fixture JSON shapes)
    - .planning/phases/03-landing-page-rebuild/03-RESEARCH.md (section 5: demo data — quickstart lines 178-200, 209-224, find-references.md lines 44-72)
    - src/content/docs/docs/quickstart.mdx (lines 172-239 for getIndexStatus and goToDefinition fixture JSON blocks)
    - src/content/docs/docs/tools/find-references.md (lines 44-72 for findReferences fixture JSON)
    - src/pages/index.astro (current demo placeholder section)
    - src/styles/landing.css (`.exchange`, `.exchange-head`, `.exchange-body`, `.ex-you`, `.ex-call`, `.ex-out` classes)
  </read_first>
  <action>
    1. **Create `src/data/demo-scenarios.json`**: an array of 3 scenario objects. Each object has `id` (string: "index-status", "go-to-definition", "find-references"), `question` (a natural-language question a developer might ask), `toolCall` (the MCP request JSON object), `result` (the recorded response JSON object). Copy the JSON shapes verbatim from the Phase-2 fixtures: getIndexStatus from quickstart.mdx lines 178-200 (full shape including `last_index_run`, `capabilities`, `searchCoverage`), goToDefinition from quickstart.mdx lines 209-224 (including `resolvedSymbol`, `definitions` array), findReferences from the find-references tool reference page (including `references` array with 3 entries). This is the single source of truth for demo data — one source of truth per RESEARCH section 5.

    2. **Create `src/components/landing/DemoPanel.astro`**: import the scenarios from `../data/demo-scenarios.json` in the front matter. Render a stepper UI with 3 step indicators (clickable circles/labels) and prev/next navigation buttons. Each step renders: (a) the question text in a heading, (b) the tool call JSON in a `<pre>` inside `.exchange-body` with `.ex-call` styling, (c) the result JSON in a `<pre>` inside `.exchange-body` with `.ex-out` styling. All three steps are server-rendered in the HTML; the inline script toggles visibility via a `data-active` attribute or CSS class on the step containers. The stepper does NOT auto-advance (per D-07). The `.demo-panel` container reuses the `.exchange` / `.exchange-head` visual pattern for terminal-chrome styling. Add `@media (prefers-reduced-motion: reduce)` to collapse any transition on step switches.

    3. **Mount DemoPanel in `src/pages/index.astro`**: replace the `#demo` placeholder section's content with the `DemoPanel` component. The section uses the standard `.frame.section` + `.section-head` skeleton (eyebrow: e.g. "See it in action", h2: e.g. "Try before you install", lede: explaining these are recorded fixtures not live calls).
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/jarvis-ai/jarvis-index && npm run build 2>&1 | tail -5 && node scripts/verify-build.mjs 2>&1</automated>
  </verify>
  <done>
    - `src/data/demo-scenarios.json` exists and is valid JSON with 3 array entries
    - `grep -c '"index-status"' src/data/demo-scenarios.json` returns 1
    - `grep -c '"go-to-definition"' src/data/demo-scenarios.json` returns 1
    - `grep -c '"find-references"' src/data/demo-scenarios.json` returns 1
    - `grep -c 'demo-panel' dist/index.html` returns >= 1 (section rendered)
    - `grep -c 'data-active' dist/index.html` returns >= 1 (stepper state mechanism present)
    - `grep -c 'last_index_run' dist/index.html` returns >= 1 (getIndexStatus shape present in built output)
    - `grep -c 'resolvedSymbol' dist/index.html` returns >= 2 (goToDefinition + findReferences shapes)
    - `npm run build` exits 0, `verify-build` V1–V10 pass
  </done>
  <reversibility rating="reversible">New component + data file; page section swap is in git.</reversibility>
</task>

<task type="auto">
  <name>Task: Tiered 9-tool showcase, language matrix, privacy section</name>
  <files>src/components/landing/ToolShowcase.astro, src/components/landing/LanguageMatrix.astro, src/pages/index.astro, src/styles/landing.css</files>
  <read_first>
    - .planning/phases/03-landing-page-rebuild/03-UI-SPEC.md (sections 3/4/5: tool showcase tier treatment, language matrix spec, privacy section)
    - .planning/phases/03-landing-page-rebuild/03-CONTEXT.md (D-07 honesty tiers — semanticSearch gate, typeHierarchy gate; language matrix mirrors requirements.md; privacy nothing-leaves positioning)
    - .planning/phases/03-landing-page-rebuild/03-PATTERNS.md (ToolShowcase card-grid pattern, LanguageMatrix table pattern, gate-chip extension of surface-chip)
    - src/content/docs/docs/guide/requirements.md (lines 8-23 for exact matrix rows and anchor IDs)
    - src/styles/landing.css (`.card`, `.card-grid`, `.cols-3`, `.card-kicker`, `.surface-chip`, `.table-wrap`, `.table-matrix`, `.note` classes)
  </read_first>
  <action>
    0. **Add the semanticSearch anchor section to `src/content/docs/docs/guide/requirements.md`**: under `## Language caveats` (after the Swift/Kotlin/Java/MacOS subsections, before `### Language detection: one language per repo`), insert a new subsection `### semanticSearch: requires the semantic extra` with 2-3 sentences: semanticSearch is not installed by default — it needs `uv tool install "jarvis-mcp[semantic]"` plus a re-index of each repo (`jarvis reindex <slug>`); without the extra the tool returns its unavailable-reason payload. This creates the anchor `#semanticsearch-requires-the-semantic-extra` for the landing gate chip and gives the gating claim a docs home on the requirements page (mirroring how typeHierarchy's gate resolves to the Swift anchor). Verify the built page exposes the anchor: `grep -c 'id="semanticsearch-requires-the-semantic-extra"' dist/docs/guide/requirements/index.html` returns 1.
    1. **Create `src/components/landing/ToolShowcase.astro`**: render 9 `.card` elements inside `.card-grid.cols-3`. Each card has: `.card-kicker` (mono, uppercase — the MCP tool name like "getIndexStatus"), an h3 (human-readable name like "Index status"), a `<p>` (one-line description of what the tool does). For `semanticSearch`: append a `.gate-chip` (extending `.surface-chip`) below the `<p>`, containing the text "requires `[semantic]` extra + reindex" with a link to `/jarvis-index/docs/guide/requirements/#semanticsearch-requires-the-semantic-extra` (link text styled via `.text-link`). For `typeHierarchy`: append a `.gate-chip` containing "requires fork-built scip + reindex" with a link to `/jarvis-index/docs/guide/requirements/#swift-version-floor-and-code-signed-targets`. Gate chip styling: warm…

    2. **Create `src/components/landing/LanguageMatrix.astro`**: render a `.table-wrap` containing a `<table class="table-matrix">` with 3 columns (Language, Navigation, Caveat) and 14 data rows matching `guide/requirements.md` lines 10-23 verbatim. Navigation column: "✅ Yes" for TypeScript/TSX, Python, Java/Kotlin, Swift; "—" for the other 10. Caveat column: for Java/Kotlin, render links to the three requirements.md anchors (`#androidgradle-no-scip-support-auto-degrades-to-search-only`, `#kotlin-exact-version-match-required-auto-degrades-to-search-only`, `#java-maven-on-macos-requires-bash--44`); for Swift, render link to `#swift-version-floor-and-code-signed-targets`; for all others, "—". All links use `/jarvis-index/docs/guide/requirements/` as the base path. The table uses `min-width: 640px` (existing `.table-matrix` pattern) and the `.table-wrap` provides `overflow-x: auto` containment.

    3. **Fill the `#tools` section** in `index.astro`: replace placeholder with `ToolShowcase` component inside the `.frame.section` skeleton. Section eyebrow e.g. "Tools", heading e.g. "9 tools for structural code intelligence", lede describing the honest tier approach.

    4. **Fill the `#languages` section** in `index.astro`: replace placeholder with `LanguageMatrix` component inside the `.frame.section` skeleton.

    5. **Fill the `#privacy` section** in `index.astro`: replace placeholder with a single-column prose section using `.section-head` (eyebrow e.g. "Privacy", heading e.g. "Local-first. Nothing leaves your machine.", lede expanding on zero telemetry / zero network / zero cloud — all computation local). May use the `.note` pattern for a highlighted callout box if it fits the content. No inline SVG icons needed unless they add clarity (Claude's Discretion per D-07).
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/jarvis-ai/jarvis-index && npm run build 2>&1 | tail -5 && node scripts/verify-build.mjs 2>&1</automated>
  </verify>
  <done>
    - `grep -c 'gate-chip' dist/index.html` returns 2 (semanticSearch + typeHierarchy gates)
    - `grep -c 'semantic' dist/index.html` returns >= 1 (semanticSearch gate text)
    - `grep -c 'fork-built scip' dist/index.html` returns >= 1 (typeHierarchy gate text)
    - `grep -c 'TypeScript / TSX' dist/index.html` returns >= 1 (matrix first row)
    - `grep -c 'SQL' dist/index.html` returns >= 1 (matrix last row, 14 rows total)
    - `grep -c 'androidgradle-no-scip-support' dist/index.html` returns >= 1 (matrix caveat deep-link)
    - `grep -c 'blastRadius' dist/index.html` returns >= 1 (9th tool card)
    - `grep -c 'Nothing leaves' dist/index.html` returns >= 1 (privacy section)
    - `npm run build` exits 0, `verify-build` V1–V10 pass
  </done>
  <reversibility rating="reversible">New components + section content; all in git.</reversibility>
</task>

## Artifacts This Phase Produces

- `src/data/demo-scenarios.json` — 3 demo scenario objects with verbatim Phase-2 fixture JSON
- `src/components/landing/DemoPanel.astro` — click-through stepper with terminal-chrome JSON display
- `src/components/landing/ToolShowcase.astro` — 9-card grid with 2 gate chips
- `src/components/landing/LanguageMatrix.astro` — 14-row compact table mirroring requirements.md
- `src/pages/index.astro` — filled demo/tools/languages/privacy sections

## Verification

```xml
<verify>
  <automated>cd /Users/ddphuong/Projects/jarvis-ai/jarvis-index && npm run build 2>&1 | tail -5 && node scripts/verify-build.mjs 2>&1</automated>
</verify>
```

Structural assertions (grep gates, run after build):
- `demo-scenarios.json` has 3 entries with correct IDs
- `dist/index.html` contains `last_index_run` and `resolvedSymbol` (real fixture data rendered)
- 2 `.gate-chip` instances in built output (semanticSearch + typeHierarchy)
- 14 language matrix rows (TypeScript/TSX through SQL)
- Matrix caveat deep-links present (androidgradle, kotlin, maven, swift anchors)
- Privacy section with "Nothing leaves" text
- `npm run build` + `verify-build` V1–V10 pass

## Threat Model

<threat_model>
**ASVS Level 1, blocking threshold: high**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| External request surface | Low | Demo JSON is imported at build time (Astro front matter), not fetched at runtime. All content is static. |
| XSS | N/A | JSON rendered inside `<pre>` blocks (text content, not innerHTML). No user input reaches any DOM manipulation. |
</threat_model>

## Estimate

`estimate` tokens=18000 raw_tokens=18000 confidence=no-calibration-data
