---
phase: 02-docs-rebuild-tutorial-first-content
plan: 04
type: execute
wave: 3
depends_on:
  - 02-03
files_modified:
  - src/content/docs/docs/guide/requirements.md
  - src/content/docs/docs/troubleshooting/index.md
  - src/content/docs/docs/troubleshooting/common-failures.md
  - src/content/docs/docs/troubleshooting/upstream-issues.md
  - src/content/docs/docs/changelog.md
  - design/url-contract.json
  - astro.config.mjs
autonomous: true
requirements:
  - DOCS-06
  - DOCS-07
  - DOCS-08
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The requirements page leads with the language-support matrix (4 nav families / 10 search-only languages / per-language caveats) before any install content, and names the search-indexer binary exactly as setup.sh installs it — never the private README's stale name (D-14, DOCS-06)"
    - "The troubleshooting decision tree leads with the three named failure modes (uvx cold-start, PATH, scip version-gate), and entries grounded in issues #4, #9, #10, and jarvis#14 exist with their verified fixes; the two-page Common Failures / Upstream Issues split is preserved (D-12, D-13)"
    - "The four new common-failures entry headings match the anchor contract verbatim so plan 02-06's quickstart failure links resolve"
    - "The changelog page body is byte-identical to ../jarvis/CHANGELOG.md minus its H1 line — verbatim, in full, no editorial layer (D-15, DOCS-08)"
    - "Both new pages ship as 3-file atomic commits, and the Guide sidebar reading order is Quickstart → Requirements & Limits → Install"
  artifacts:
    - path: "src/content/docs/docs/guide/requirements.md"
      provides: "D-14 Requirements & Limits page, language matrix first"
      contains: "| Language | Navigation | Search | Caveat |"
    - path: "src/content/docs/docs/changelog.md"
      provides: "D-15 verbatim mirror of ../jarvis/CHANGELOG.md"
      contains: "0.6.2"
    - path: "src/content/docs/docs/troubleshooting/common-failures.md"
      provides: "Symptom → Diagnosis → Fix entries incl. the three named modes"
      contains: "uv tool install jarvis-mcp"
  key_links:
    - from: "src/content/docs/docs/troubleshooting/index.md"
      to: "common-failures.md entries"
      via: "anchor links derived from the locked H2 heading set"
      pattern: "/troubleshooting/common-failures/#"
    - from: "src/content/docs/docs/changelog.md"
      to: "../jarvis/CHANGELOG.md"
      via: "verbatim body (diff-verifiable)"
    - from: "src/content/docs/docs/guide/requirements.md"
      to: "design/url-contract.json + astro.config.mjs"
      via: "3-file atomic ADD"
      pattern: "/docs/guide/requirements/"
  prohibitions:
    - statement: "No editorial trimming, curation, or summarizing of the changelog — the body is the source file verbatim minus only its H1 (D-15)"
      status: flagged
    - statement: "No guessed troubleshooting entries — every failure mode traces to the three named modes or the mined issues #4/#9/#10/jarvis#14 with their verified workarounds (D-12)"
      status: flagged
    - statement: "setup.sh is read as the binary-name source of truth but never edited (ownership fence)"
      status: flagged
---

<objective>
Ship the guardrail content: the NEW Requirements & Limits page led by the language-support matrix (D-14, DOCS-06), the troubleshooting decision tree deepened with the three named failure modes plus the issue-mined entries under the preserved two-page split (D-12, D-13, DOCS-07), and the NEW changelog page mirroring ../jarvis/CHANGELOG.md verbatim (D-15, DOCS-08). This plan also LOCKS the troubleshooting anchor contract that plan 02-06's quickstart failure links consume.

Purpose: a visitor can verify their language/platform is supported before installing, and a stuck user goes symptom → diagnosis → fix without leaving the docs.
Output: 2 new pages (3-file atomic each), 3 deepened troubleshooting pages, contract +2 URLs, sidebar +2 entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-RESEARCH.md
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-PATTERNS.md
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-UI-SPEC.md
</context>

## Anchor contract (LOCKED — plan 02-06 links against these; do not deviate)

New H2 headings added to `troubleshooting/common-failures.md`, verbatim:

| H2 heading (verbatim) | Derived anchor |
|---|---|
| `## First MCP connect times out (uvx cold start)` | `#first-mcp-connect-times-out-uvx-cold-start` |
| `## jarvis or jarvis-server not found (PATH)` | `#jarvis-or-jarvis-server-not-found-path` |
| `## jarvis index refuses an old scip (version gate)` | `#jarvis-index-refuses-an-old-scip-version-gate` |
| `## semanticSearch server fails to connect (Python version)` | `#semanticsearch-server-fails-to-connect-python-version` |

Existing entries KEEP their headings (and anchors) except `## nav-empty-but-search-works`, which is renamed to `## Navigation empty but search works (search-only)` — its two inbound links in `troubleshooting/index.md` are updated in the same commit.

<tasks>

<task type="tracer">
  <name>Task 1: Requirements & Limits page end-to-end — matrix-first content through contract and sidebar (D-14)</name>
  <precondition>../jarvis/README.md and setup.sh (this repo's synced copy) readable — matrix and binary-name sources</precondition>
  <files>src/content/docs/docs/guide/requirements.md, design/url-contract.json, astro.config.mjs</files>
  <read_first>02-UI-SPEC.md Page Layout Contract 2 (implement exactly), 02-RESEARCH.md Product Truth §4 (matrix rows, caveats, the binary-name correction) and §3 (version gate), ../jarvis/README.md:93-120 and :183-219 and :293-312, setup.sh:492-500 (zoekt binary names — READ ONLY), astro.config.mjs Guide sidebar group, design/url-contract.json</read_first>
  <action>
This tracer proves the plan's ADD machinery and lands the page every other journey page links to.

1. Create `src/content/docs/docs/guide/requirements.md` per UI-SPEC Layout Contract 2, in this exact order:
   a. Frontmatter (`title: Requirements & Limits` + description) and ONE intro sentence.
   b. **Language-support matrix FIRST** — table `| Language | Navigation | Search | Caveat |`: 4 navigation families (TypeScript/TSX, Python, Java/Kotlin, Swift — from the extension→indexer map) and the 10 search-only languages (Go, Ruby, Rust, C, C++, C#, PHP, Scala, shell, SQL — search tools only, no navigation). One row per language; caveat cells link the `###` detail subsections.
   c. Platform requirements: macOS/Linux only (no Windows); Python 3.12+ via uv; git; the external-binaries table sourced from ../jarvis/README.md:108-120 BUT with the search-indexer binary names exactly as setup.sh installs them: `zoekt-git-index` and `zoekt-webserver` (setup.sh:492-500). The private README's shorter zoekt indexer name is stale — never copy it (RESEARCH §4 binary-name correction).
   d. Per-language caveat details as `###` subsections, each framed as a settled decision with rationale: Swift needs scip ≥ 0.9.0 and scip-swift ≥ 0.1.2 for code-signed app-extension targets (upgrade note: `--only scip-swift --force`); Android/Gradle cannot be SCIP-indexed → auto-degrades to search-only; Kotlin requires the exact matching Kotlin version → auto-degrades; Maven-built Java on macOS needs bash ≥ 4.4 — fails with a remedy rather than degrading; language detection counts git-tracked files only, one language per repo, `--language` overrides. Use `:::caution` asides ONLY for the Kotlin and Android auto-degrade notes (UI-SPEC aside assignment).
2. Add `"/docs/guide/requirements/"` to `design/url-contract.json`.
3. Add `{ label: 'Requirements & Limits', link: 'docs/guide/requirements' },` to the Guide sidebar group in `astro.config.mjs`, between Quickstart and Install (pre-install reading order per UI-SPEC).
4. `npm run build && npm run verify`.
  </action>
  <verify>
    <automated>npm run build && npm run verify && grep -q '| Language | Navigation | Search | Caveat |' src/content/docs/docs/guide/requirements.md && grep -q 'zoekt-git-index' src/content/docs/docs/guide/requirements.md && ! grep -q 'zoekt-index' src/content/docs/docs/guide/requirements.md</automated>
  </verify>
  <acceptance_criteria>
- `npm run build && npm run verify` exits 0; `test -f dist/docs/guide/requirements/index.html`
- The matrix table is the first content element: it appears before any `###` heading (`awk '/^###/{exit 1} /\| Language \| Navigation \| Search \| Caveat \|/{found=1; exit} END{exit !found}' src/content/docs/docs/guide/requirements.md`)
- All 10 search-only languages present: `grep -q 'Scala' && grep -q 'PHP' && grep -q 'SQL'` on the page
- `grep -q 'zoekt-git-index' src/content/docs/docs/guide/requirements.md` and `! grep -q 'zoekt-index' src/content/docs/docs/guide/requirements.md` (stale name absent; note: the correct hyphenated name does not match this pattern)
- `grep -c ':::caution' src/content/docs/docs/guide/requirements.md` is 1 or 2 (Kotlin/Android degrade notes only)
- Contract and sidebar edits in the same commit as the page
  </acceptance_criteria>
  <done>/docs/guide/requirements/ exists matrix-first with source-true binary names, in contract and sidebar (Guide order: Quickstart → Requirements & Limits → Install), build green.</done>
</task>

<task type="auto">
  <name>Task 2: Deepen the troubleshooting tree — three named modes lead, issue-mined entries, anchor contract locked (D-12, D-13)</name>
  <precondition>../jarvis/README.md readable; the mined issue findings are already captured in 02-RESEARCH.md Product Truth §5 (no gh calls needed)</precondition>
  <files>src/content/docs/docs/troubleshooting/index.md, src/content/docs/docs/troubleshooting/common-failures.md, src/content/docs/docs/troubleshooting/upstream-issues.md</files>
  <read_first>The three existing troubleshooting pages (sound skeletons — extend, don't replace), 02-RESEARCH.md Product Truth §5 (the four issues + verified fixes) and §3 (version gate), the Anchor contract table above, 02-UI-SPEC.md Copywriting Contract (Symptom → Diagnosis → Fix structure), ../jarvis/README.md:56-58 (PATH guidance)</read_first>
  <action>
Keep the two-page split (D-13) and the existing entries; extend:

1. `common-failures.md` — add four entries using the LOCKED verbatim H2 headings from the anchor contract, each in the Symptom → Diagnosis → Fix structure (bold labels, fenced sh fix blocks):
   - uvx cold start (issue #4, closed): Symptom — first MCP connect exceeds the client's 30s timeout; Diagnosis — on a cold cache uvx builds the package's Rust extension from sdist (5+ minutes); Fix (verified) — `uv tool install jarvis-mcp` builds and caches the wheel once; subsequent `uvx --from jarvis-mcp jarvis-server` cold starts in seconds.
   - PATH: Symptom — shell reports jarvis or jarvis-server not found, or a GUI client cannot spawn the server; Diagnosis — `~/.jarvis/bin` (or the uv tool bin dir) is not on PATH; GUI apps do not inherit shell PATH; Fix — add to PATH for shells; for GUI clients use the absolute path from `which jarvis-server` in the registration.
   - scip version gate: Symptom — `jarvis index` refuses to run naming the scip version; Diagnosis — scip below 0.9.0 silently writes a schema-valid database with zero chunks, so jarvis refuses it; Fix — re-run setup.sh (installs the pinned fixed build), confirm with `scip --version`, reindex.
   - semanticSearch server connect failure (issue #10, open): Symptom — the second (semantic) server registration fails with a closed-connection error; Diagnosis — ambient uv Python below 3.12 (semantic wheels ship for cp312+); Fix (verified) — pin explicitly: `uvx --python 3.13 --from "jarvis-mcp[semantic]" jarvis-server`.
   Also rename `## nav-empty-but-search-works` to `## Navigation empty but search works (search-only)` and keep its body (it already covers the jarvis#14 Android/Gradle degrade path — extend its Cause with the Gradle multi-module case).
2. `troubleshooting/index.md` — add a new FIRST section `## Start here: the three most common failures` immediately after the intro line: a 3-row table `Symptom | Entry` linking the uvx cold-start, PATH, and version-gate anchors (trailing-slash + fragment form, e.g. `/troubleshooting/common-failures/#first-mcp-connect-times-out-uvx-cold-start`). Keep the existing tool-branch decision tree below it; update the two links that pointed at the renamed nav-empty anchor; normalize all internal links on the page to trailing-slash-before-fragment form.
3. `upstream-issues.md` — add one entry: `--semantic-include` has no effect on prose-only repos (issue #9, open) — semantic chunking is code-symbol-scoped, so markdown-only repos gain nothing; settled limitation, not a bug (copy the existing "This is by design." framing). Keep all existing entries.
Rules: every entry traces to §5 or the named modes (D-12 — no guessed entries); settled-decision framing; no new pages, no contract edits in this task.
  </action>
  <verify>
    <automated>npm run build && npm run verify && grep -q '## First MCP connect times out (uvx cold start)' src/content/docs/docs/troubleshooting/common-failures.md && grep -q 'uv tool install jarvis-mcp' src/content/docs/docs/troubleshooting/common-failures.md && grep -q -- '--python 3.13' src/content/docs/docs/troubleshooting/common-failures.md</automated>
  </verify>
  <acceptance_criteria>
- All four LOCKED H2 headings present verbatim (grep each full heading string)
- `grep -q 'which jarvis-server' src/content/docs/docs/troubleshooting/common-failures.md` (GUI PATH fix)
- `grep -q '## Start here' src/content/docs/docs/troubleshooting/index.md` and it links all three named-mode anchors (`grep -c '/troubleshooting/common-failures/#' src/content/docs/docs/troubleshooting/index.md` ≥ 3)
- `! grep -q 'nav-empty-but-search-works' src/content/docs/docs/troubleshooting/index.md` (renamed anchor fully updated)
- `grep -q 'semantic-include' src/content/docs/docs/troubleshooting/upstream-issues.md` (issue #9 entry)
- `npm run build && npm run verify` exits 0 (page set unchanged)
  </acceptance_criteria>
  <done>The tree leads with the three named modes; all mined-issue entries exist with verified fixes; the anchor contract is live for 02-06; the two-page split is intact.</done>
</task>

<task type="auto">
  <name>Task 3: Changelog page — verbatim mirror, 3-file atomic ADD (D-15)</name>
  <precondition>../jarvis/CHANGELOG.md exists, is readable, and its first line is a markdown H1 (the title line the copy drops)</precondition>
  <files>src/content/docs/docs/changelog.md, design/url-contract.json, astro.config.mjs</files>
  <read_first>../jarvis/CHANGELOG.md (head + wc: ~400 lines, top entry 0.6.2), 02-RESEARCH.md Product Truth §7 + Pitfall 8, 02-PATTERNS.md changelog section, astro.config.mjs sidebar (top-level entry shape)</read_first>
  <action>
1. Create `src/content/docs/docs/changelog.md` with EXACTLY this head structure so the verbatim diff is deterministic:
   - Line 1: `---`
   - Line 2: `title: Changelog`
   - Line 3: `description: "jarvis release history, mirrored verbatim from the jarvis repo."`
   - Line 4: `---`
   - Line 5 onward: the ENTIRE body of ../jarvis/CHANGELOG.md starting from its line 2 (drop ONLY the source's H1 line; keep everything else byte-for-byte — no trimming, no reformatting, no link rewriting; D-15).
2. Add `"/docs/changelog/"` to `design/url-contract.json`.
3. Add a top-level sidebar entry `{ label: 'Changelog', link: 'docs/changelog' },` in `astro.config.mjs` AFTER the Troubleshooting group (UI-SPEC placement).
4. `npm run build && npm run verify` — Starlight's on-page TOC handles the length; the frontmatter title is mandatory (a missing title silently drops the page — Pitfall 8).
  </action>
  <verify>
    <automated>npm run build && npm run verify && diff <(tail -n +5 src/content/docs/docs/changelog.md) <(tail -n +2 ../jarvis/CHANGELOG.md)</automated>
  </verify>
  <acceptance_criteria>
- `diff <(tail -n +5 src/content/docs/docs/changelog.md) <(tail -n +2 ../jarvis/CHANGELOG.md)` exits 0 (verbatim body)
- `grep -q '\[0.6.2\]' src/content/docs/docs/changelog.md` (top entry present)
- `test -f dist/docs/changelog/index.html`; `grep -q '"/docs/changelog/"' design/url-contract.json`; `grep -q "docs/changelog" astro.config.mjs`
- All three files in the same commit
  </acceptance_criteria>
  <done>/docs/changelog/ mirrors the source verbatim (diff-proven), in contract and sidebar, build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader terminal ← troubleshooting fix commands | fixes are commands pasted into shells under failure stress |
| docs ← private-repo content | changelog and matrix content crosses from ../jarvis into the public surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Tampering | fix commands in troubleshooting entries | low | mitigate | Every fix command quoted verbatim from the verified issue workaround or README; no interpolated unquoted vars |
| T-02-07 | Information disclosure | changelog verbatim import | low | mitigate | Source is the already-public product changelog; verbatim diff check prevents accidental inclusion drift in either direction |
| T-02-SC | Tampering | package installs | low | accept | Zero packages installed by this plan |
</threat_model>

<verification>
- `npm run build && npm run verify` green after every task commit (V2 grows by exactly two URLs)
- Verbatim changelog diff exits 0 (this is the DOCS-08 Wave-0 check riding with the page, per 02-VALIDATION.md)
- All four locked anchors resolve in the built HTML: `grep -q 'id="first-mcp-connect-times-out-uvx-cold-start"' dist/docs/troubleshooting/common-failures/index.html`
</verification>

<success_criteria>
- DOCS-06: requirements/limits stated before install, matrix-first, source-true binary names
- DOCS-07: tree leads with the three named modes; issues #4/#9/#10/#14 covered with verified fixes; two-page split preserved
- DOCS-08: changelog verbatim-mirrored and diff-proven
- Anchor contract live; Guide sidebar reading order correct
</success_criteria>

## Flagged Assumptions (edge-probe unresolved — surfaced, not dropped)

- DOCS-06 unclassified: assumed no edge cases beyond the UI-SPEC layout contract and the matrix's row completeness (4 + 10 languages) — flagged.
- DOCS-07 unclassified: assumed the mined issue set (4 issues total across both repos, RESEARCH §5) is exhaustive as of 2026-08-21; new issues after that date are out of this plan's scope — flagged.
- DOCS-08 unclassified: assumed a one-time verbatim copy satisfies the requirement (the project's no-sync-mechanism decision); staleness after future jarvis releases is accepted upkeep — flagged.

## Artifacts this phase produces (plan 02-04)

| Artifact | Path / URL |
|----------|------------|
| NEW Requirements & Limits page | `src/content/docs/docs/guide/requirements.md` → `/docs/guide/requirements/` |
| NEW Changelog page | `src/content/docs/docs/changelog.md` → `/docs/changelog/` |
| Contract entries | `"/docs/guide/requirements/"`, `"/docs/changelog/"` in `design/url-contract.json` |
| Sidebar entries | `Requirements & Limits` (Guide group), `Changelog` (top-level) in `astro.config.mjs` |
| Deepened troubleshooting pages | `/docs/troubleshooting/`, `/docs/troubleshooting/common-failures/`, `/docs/troubleshooting/upstream-issues/` |
| Anchor contract | the four locked common-failures anchors (consumed by plan 02-06) |
| Verification command | `diff <(tail -n +5 src/content/docs/docs/changelog.md) <(tail -n +2 ../jarvis/CHANGELOG.md)` |

<output>
Create `.planning/phases/02-docs-rebuild-tutorial-first-content/02-04-SUMMARY.md` when done. Record the final anchor list actually shipped (02-06 depends on it).
</output>
