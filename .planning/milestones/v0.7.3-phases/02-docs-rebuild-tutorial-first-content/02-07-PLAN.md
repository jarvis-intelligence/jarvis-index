---
phase: 02-docs-rebuild-tutorial-first-content
plan: 07
type: tdd
wave: 6
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
  - 02-05
  - 02-06
files_modified:
  - public/llms.txt
  - scripts/verify-build.mjs
autonomous: true
requirements:
  - DOCS-11
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "dist/llms.txt exists, opens with an H1 line followed by a blockquote summary, and every markdown link in it resolves to a contract-enumerated docs URL on the site origin (llmstxt.org shape; link-poisoning guard)"
    - "verify-build.mjs carries a llms.txt dimension (existence, format, link-origin-and-path ⊆ contract) that was committed FAILING before the file existed — RED before GREEN"
    - "llms.txt ships only after plans 02-01 through 02-06 are complete — the DOCS-11 strictly-last sequencing constraint, enforced by this plan's wave and depends_on"
    - "llms.txt is NOT in design/url-contract.json and the contract file is untouched by this plan (Pitfall 4: V2 walks only HTML — adding it would fail every future build)"
  artifacts:
    - path: "public/llms.txt"
      provides: "Agent-consumable docs index per llmstxt.org (H1, blockquote, link sections)"
    - path: "scripts/verify-build.mjs"
      provides: "New llms.txt assertion dimension alongside V2/V3/V4/V5/V9"
      contains: "llms.txt"
  key_links:
    - from: "public/llms.txt"
      to: "design/url-contract.json"
      via: "every link's origin+path derives from the contract (assertion-enforced)"
      pattern: "https://jarvis-intelligence.github.io/jarvis-index"
    - from: "scripts/verify-build.mjs"
      to: "dist/llms.txt"
      via: "the new dimension reads the built file every run"
  prohibitions:
    - statement: "llms.txt is never added to design/url-contract.json — V2's page walk covers only HTML files and a non-HTML contract entry breaks every subsequent build (Pitfall 4)"
      status: flagged
    - statement: "No third-party llms.txt generator plugin and zero new packages — a hand-authored static file (or the repo's zero-dep script convention) only; auto-regeneration contradicts the project's no-sync-mechanism decision"
      status: flagged
    - statement: "No link in llms.txt points anywhere except the site's own contract-enumerated docs URLs (spoofing/poisoning guard)"
      status: flagged
---

<objective>
Generate the agent-consumable `llms.txt` index (DOCS-11) — sequenced STRICTLY LAST, after all content plans have stabilized the page set — with TDD discipline exactly as Phase 1's 01-05 applied to the assertion harness: the verify-build dimension lands RED (failing, no file yet), then the file lands GREEN. Format per llmstxt.org (CONTEXT grants discretion on format/generation; only the sequencing is locked).

Purpose: agents fetching one URL get a faithful, poison-proof map of the stabilized docs.
Output: `public/llms.txt` (→ `dist/llms.txt`) + a permanent verify-build dimension guarding its existence, shape, and link origins.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-RESEARCH.md
@.planning/phases/02-docs-rebuild-tutorial-first-content/02-PATTERNS.md
@scripts/verify-build.mjs
@design/url-contract.json
</context>

<feature>
  <name>llms.txt + its standing build assertion</name>
  <files>public/llms.txt, scripts/verify-build.mjs</files>
  <behavior>
    - dist/llms.txt exists after `npm run build`
    - Line 1 matches `^# ` (H1 project name); a `^> ` blockquote line appears within the first 4 lines
    - Every markdown link target in the file starts with the contract's `origin` value and, after stripping the origin, its path is a member of the contract's `urls` array
    - The assertion FAILS the verify run when any of the above is false (proven RED before the file exists)
  </behavior>
  <implementation>Zero-dep additions to verify-build.mjs (matching its existing style); hand-authored public/llms.txt listing every /docs/ contract URL with its page description</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Add the llms.txt dimension to verify-build.mjs and commit it failing</name>
  <precondition>Plans 02-01..02-06 are complete: design/url-contract.json contains 39 URLs (34 baseline + jarvis-server, requirements, changelog, generic-stdio, install-matrix) — check with `grep -c '"/' design/url-contract.json`</precondition>
  <files>scripts/verify-build.mjs</files>
  <read_first>scripts/verify-build.mjs in full (zero-dep style, fail() helper, the header comment's dimension list), design/url-contract.json (origin field + urls array), 02-RESEARCH.md Pattern 5 + Pitfall 4</read_first>
  <behavior>
    - New dimension (name it V10 in the header comment and fail() labels — V2..V9 are taken by history) placed after the V9 block:
    - Fails "V10: dist/llms.txt not found" when the file is absent
    - Fails "V10: first line is not an H1" when line 1 does not start with `# `
    - Fails "V10: no blockquote summary in the first 4 lines" when no early line starts with `> `
    - For every regex match of a markdown link `[label](url)` in the file: fails "V10: link off-origin" when the url does not start with `contract.origin`, and fails "V10: link path not in contract" when the origin-stripped path is not in the contract urls set
    - Appends V10 to the final ok summary line
  </behavior>
  <action>
Implement the V10 dimension in scripts/verify-build.mjs exactly per the behavior block, reusing the existing fail() helper, the already-parsed contract object, and node:fs primitives (zero-dep, matching file style; why-comments only). Derive the origin from `contract.origin` — never hardcode a hostname. Then run `npm run build && npm run verify`: it MUST fail with the V10 file-absent message (the file does not exist yet — this is the RED gate; if it passes, STOP and investigate per TDD fail-fast rules). Commit the failing state as `test(02-07): add llms.txt build assertion (red)`.
  </action>
  <verify>
    <automated>npm run build; node scripts/verify-build.mjs 2>&1 | grep -q 'V10' && ! node scripts/verify-build.mjs</automated>
  </verify>
  <acceptance_criteria>
- `node scripts/verify-build.mjs` exits non-zero and its stderr names V10 and llms.txt
- V2/V3/V4/V5/V9 still pass (the ONLY failure is the new dimension): the error output contains no other dimension label
- The commit message follows `test(02-07): ...` (RED gate commit)
- design/url-contract.json is untouched (`git diff HEAD~1 --name-only` does not list it)
  </acceptance_criteria>
  <done>The assertion exists, fails for exactly the right reason (missing file), and is committed RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Author public/llms.txt; verify goes green; optional refactor</name>
  <files>public/llms.txt</files>
  <read_first>llmstxt.org format per 02-RESEARCH Pattern 5 (H1 → blockquote → `##` sections of `[Name](URL): description` link lists; `## Optional` marks skippable entries), design/url-contract.json (the final URL set), the `description` frontmatter of the shipped docs pages (source for link descriptions)</read_first>
  <behavior>
    - `npm run build && npm run verify` exits 0 with V10 in the ok line
    - Every /docs/ URL in the contract appears exactly once as an absolute link
  </behavior>
  <action>
Hand-author `public/llms.txt`:
- Line 1: `# jarvis`
- Line 2: blank; Line 3: `> Local-first code intelligence for coding agents — an MCP server answering structural questions (definitions, references, call hierarchy, search) over your own indexed repos.` (align wording with the shipped docs home; every claim traceable)
- Then `##` sections mirroring the sidebar groups — Getting started (quickstart, requirements, install, install-matrix), MCP Tools (overview + 9 tools), CLI (overview + 7 commands), Concepts (5), Integrations (overview + 4 guides), Troubleshooting (3), Changelog — each entry `[Page title](absolute URL): one-line description` where the URL is `contract.origin` + the contract path (trailing-slash form) and the description comes from that page's frontmatter description. Cover every `/docs/` URL in the contract exactly once; do NOT include `/`, `/brand-logo.html`, or any external URL. Optionally close with `## Optional` for the changelog entry if placing it there reads better.
- Do NOT touch design/url-contract.json (Pitfall 4).
Run `npm run build && npm run verify` — all dimensions green. Commit `feat(02-07): add llms.txt agent index`. REFACTOR gate (optional): only if the V10 implementation wants cleanup; tests must stay green; commit `refactor(02-07): ...` only if changes were made.
  </action>
  <verify>
    <automated>npm run build && npm run verify && test -f dist/llms.txt && head -1 dist/llms.txt | grep -q '^# ' && sed -n '1,4p' dist/llms.txt | grep -q '^> '</automated>
  </verify>
  <acceptance_criteria>
- `npm run build && npm run verify` exits 0 and the ok line includes V10
- Every `/docs/` contract URL appears in llms.txt: for each such URL, `grep -qF "$url)" public/llms.txt` (loop over `grep -o '"/docs/[^"]*"' design/url-contract.json`)
- `! grep -q 'llms' design/url-contract.json` (contract untouched)
- Git log shows the gate sequence: a `test(02-07)` commit precedes the `feat(02-07)` commit
  </acceptance_criteria>
  <done>dist/llms.txt ships per llmstxt.org with contract-derived links only; the RED→GREEN gate sequence is in the log; the assertion guards it permanently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM agents ← llms.txt | agents follow these links sight-unseen; a poisoned link redirects agent traffic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Spoofing | llms.txt link targets | medium | mitigate | V10 asserts every link is on the contract origin AND its path is contract-enumerated — poisoning fails the build |
| T-02-SC | Tampering | package installs | low | accept | Zero packages; the generator-plugin alternative was explicitly rejected (RESEARCH Alternatives) |
</threat_model>

<verification>
- RED gate: verify fails naming only V10 before the file exists; GREEN gate: full verify green including V10
- Every /docs/ contract URL present exactly once; no off-origin links (assertion-enforced forever)
- `test(02-07)` commit precedes `feat(02-07)` in the log (TDD gate validation)
</verification>

<success_criteria>
- DOCS-11 shipped strictly last: dist/llms.txt live, llmstxt.org-shaped, contract-derived
- Permanent V10 assertion in verify-build.mjs; RED→GREEN commits present; contract untouched
</success_criteria>

## Flagged Assumptions (edge-probe unresolved — surfaced, not dropped)

- DOCS-11 unclassified by the edge engine: the llmstxt.org format is websearch-cited (MEDIUM confidence in RESEARCH); the output is a small reviewable static file and the V10 assertion pins the structural contract — flagged, not silently dropped.
- RESEARCH Open Question 4 resolved as recommended: `llms.txt` only; a full-content companion file is out of scope for DOCS-11 (the requirement names one file).

## Artifacts this phase produces (plan 02-07)

| Artifact | Path / URL |
|----------|------------|
| Agent index | `public/llms.txt` → served at `/jarvis-index/llms.txt` (`dist/llms.txt`) |
| Standing assertion | V10 dimension in `scripts/verify-build.mjs` (runs in `npm run verify`) |

<output>
Create `.planning/phases/02-docs-rebuild-tutorial-first-content/02-07-SUMMARY.md` when done, with the RED/GREEN(/REFACTOR) commit list per the TDD summary contract.
</output>
