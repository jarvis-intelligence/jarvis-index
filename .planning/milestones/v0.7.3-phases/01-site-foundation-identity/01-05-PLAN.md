---
phase: 01-site-foundation-identity
plan: 05
type: tdd
wave: 4
depends_on:
  - 01-02
  - 01-03
files_modified:
  - scripts/check-manifests.mjs
  - scripts/verify-build.mjs
  - design/url-contract.json
  - .github/workflows/checks.yml
  - .github/workflows/deploy-pages.yml
  - package.json
  - astro.config.mjs
autonomous: true
requirements:
  - SITE-06
  - SITE-07
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "design/url-contract.json enumerates the complete public URL set (34 entries: landing /, brand-logo.html, 32 docs URLs) and scripts/verify-build.mjs asserts the built dist/ page set EQUALS it — a vanished page OR an unexpected extra page both fail the build"
    - "node scripts/check-manifests.mjs exits 0 on the current repo, and each of its three failure modes is proven to exit 1 with a file-plus-reason message: (a) manifest JSON parse failure, (b) three-manifest version disagreement, (c) the two MCP config files differing by any byte"
    - ".github/workflows/checks.yml runs the manifest checks on push AND pull_request against main, path-filtered to the manifest trees — a PR that edits only a plugin manifest gets check feedback the push-only deploy workflow cannot give"
    - "deploy-pages.yml runs node scripts/verify-build.mjs between build and artifact upload — the URL contract, asset-resolution, font-origin, search-asset, and sitemap dimensions gate every deploy"
    - "The redirect mechanism is decided and proven: the Astro redirects map in astro.config.mjs emits meta-refresh stub HTML (demonstrated on a scratch entry, then removed), Starlight's 404 route yields dist/404.html, and the contract discipline (add a page = edit the contract in the same commit; retire a URL = add a redirects entry) is documented at the map"
  artifacts:
    - path: "scripts/check-manifests.mjs"
      provides: "SITE-07: JSON parse + version agreement + MCP-config byte equality, zero-dependency ESM"
      contains: "plugin/.claude-plugin/plugin.json"
    - path: "scripts/verify-build.mjs"
      provides: "V2 URL-contract equality, V3 asset resolution, V4 font origin, V5 pagefind presence, V9 sitemap mapping — zero-dependency ESM, optional dist-path argument for scratch testing"
      contains: "url-contract.json"
    - path: "design/url-contract.json"
      provides: "the committed enumeration of every public URL"
      contains: "/docs/tools/go-to-definition"
    - path: ".github/workflows/checks.yml"
      provides: "manifest checks on push/PR, contents:read only"
      contains: "check-manifests"
  key_links:
    - from: "scripts/verify-build.mjs"
      to: "deploy-pages.yml Verify build step"
      via: "npm-independent node invocation between build and upload-pages-artifact — the pre-deploy gate"
      pattern: "node scripts/verify-build.mjs"
    - from: "design/url-contract.json"
      to: "scripts/verify-build.mjs V2 dimension"
      via: "set equality between contract urls and the walked dist page set (404.html excluded)"
      pattern: "urls"
    - from: "astro.config.mjs redirects map"
      to: "dist meta-refresh stubs"
      via: "Astro static output renders redirect entries as refresh stub HTML (verified in astro dist source)"
      pattern: "redirects"
  prohibitions:
    - requirement_id: SITE-06
      category: values
      statement: "MUST NOT let a public URL retire without a redirects entry — no-link-rot is the core promise; the verify-build equality assertion mechanizes it (a removed page fails the build until a redirect + contract edit ship together)"
    - requirement_id: SITE-07
      category: transparency
      statement: "MUST NOT ship checks that cannot fail — each of the three manifest assertions is proven to go red on a deliberately broken scratch copy before it is trusted green (a check that cannot fail is theater, and the baseline is only meaningful because the break was demonstrated)"
---

<objective>
Decide and mechanize the URL contract (SITE-06): the complete public URL enumeration committed as data, the old→new redirect mechanism (Astro redirects map + Starlight 404) proven and documented, and verify-build.mjs as the standing dist-vs-contract assertion. Land the three manifest CI checks (SITE-07) as a separate path-triggered workflow so manifest-only PRs get PR-time signaling — both scripts zero-dependency, test-the-test proven.

Purpose: These are the guardrails every later phase depends on: Phase 2's restructure executes against the contract; Phase 4's release is protected by the manifest checks; the deploy pipeline stops shipping silent breakage.

Output: Two assertion scripts, the contract data file, checks.yml, the deploy-workflow gate, the redirects scaffold.

TDD discipline (plan type: tdd): the fail-first scratch proofs are the RED gates, the clean-repo runs are GREEN — land them as separate test(01-05)/feat(01-05) commits per the TDD gate. Task 3 (redirects scaffold) is the configuration-only exception: scratch-entry proof, no tdd cycle.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-site-foundation-identity/01-RESEARCH.md
@.planning/phases/01-site-foundation-identity/01-PATTERNS.md

Key anchors:
- 01-RESEARCH.md § Pattern 6 (the three assertions verbatim + why a separate path-triggered workflow; verified baseline: all three manifests 0.7.2, MCP pair byte-identical), § Pattern 7 (the 32-URL verified enumeration + contract-as-data design), § Anti-Patterns (two origin sources; hand-placed 404), § Don't Hand-Roll (manifest checks as one script), § Validation Architecture V2/V3/V4/V5/V7/V9
- 01-PATTERNS.md § scripts/check-manifests.mjs (conventions: ESM, node:fs, console.error + exit 1; assertion subjects), § scripts/verify-build.mjs (dimension-by-dimension logic anchors), § design/url-contract.json (shape: manifest-style plain JSON + transcribe the RESEARCH Pattern 7 list), § checks.yml (trigger/permissions/steps)
- Source: .github/workflows/deploy-pages.yml (as rewritten by 01-02 — insert the Verify build step after Build site), astro.config.mjs (redirects scaffold), package.json (add script entries; no dependency changes)
- Repo is "type": "module" — plain .mjs ESM, node:fs/node:path only, NO test framework (VALIDATION: none needed)
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: check-manifests.mjs — three assertions, fail-first proven, wired to npm + checks.yml</name>
  <behavior>
  - Case parse: a scratch copy of the repo with one manifest containing malformed JSON → script exits 1 naming that file and the parse error.
  - Case version: a scratch copy with one manifest bumped to a different version string → script exits 1 listing every file with its version value.
  - Case bytes: a scratch copy where one MCP config file differs by a single byte → script exits 1 naming both files.
  - Case clean: the real repo (baseline verified 2026-08-21: three manifests at 0.7.2, MCP pair byte-identical) → exit 0.
  </behavior>
  <read_first>
  - plugin/.claude-plugin/plugin.json, plugin/.cursor-plugin/plugin.json, .codex-plugin/plugin.json (the three version carriers)
  - plugin/.mcp.json and plugin/mcp.json (the byte-equality pair)
  - 01-RESEARCH.md § Pattern 6 (assertion logic + workflow rationale) and § Validation V7 (test-the-test requirement)
  - 01-PATTERNS.md § scripts/check-manifests.mjs (conventions + subjects) and § checks.yml (workflow skeleton derived from deploy-pages.yml)
  </read_first>
  <files>scripts/check-manifests.mjs, .github/workflows/checks.yml, package.json</files>
  <action>
  Create scripts/check-manifests.mjs: zero-dependency ESM (node:fs, node:path, node:url). Resolve all subject paths relative to process.cwd() so the script can run against a scratch copy by changing working directory — this is the testability seam for the fail-first proof. Three assertions in order: (1) parse each of the three manifests as JSON, failing with the file path plus the parse error on stderr and exit 1; (2) require all three version fields identical, failing with a line per file showing its value; (3) require the two MCP config files byte-identical (compare Buffer contents), failing naming both files. Any success path prints a one-line ok summary. Then prove each assertion fails-first (RED before trusting GREEN): build a scratch copy under a mktemp dir (copy the five subject files), break it one way at a time — malformed JSON in one manifest, a version mismatch, a one-byte config diff — run the script from inside the scratch dir and capture exit 1 for each of the three cases; then run against the real repo for the clean exit 0. Record the three RED outputs in the SUMMARY. Wire package.json script check:manifests to run it, and create .github/workflows/checks.yml per the PATTERNS skeleton: name Manifest checks; on push to main AND pull_request targeting main, paths filtered to plugin/**, .codex-plugin/**, .claude-plugin/**, .cursor-plugin/**, scripts/check-manifests.mjs, and the workflow file itself; permissions contents: read only (no pages, no id-token — this workflow never deploys); single job: checkout@v4, setup-node@v4 on 22 (no npm cache, no install — the script is dependency-free), run node scripts/check-manifests.mjs.
  </action>
  <verify>
    <automated>node scripts/check-manifests.mjs && SCRATCH=$(mktemp -d) && mkdir -p "$SCRATCH/plugin/.claude-plugin" "$SCRATCH/plugin/.cursor-plugin" "$SCRATCH/.codex-plugin" && cp plugin/.claude-plugin/plugin.json "$SCRATCH/plugin/.claude-plugin/plugin.json" && cp plugin/.cursor-plugin/plugin.json "$SCRATCH/plugin/.cursor-plugin/plugin.json" && cp .codex-plugin/plugin.json "$SCRATCH/.codex-plugin/plugin.json" && cp plugin/.mcp.json plugin/mcp.json "$SCRATCH/plugin/" && (cd "$SCRATCH" && node "$OLDPWD/scripts/check-manifests.mjs") && node -e "const p=require('./package.json');process.exit(p.scripts['check:manifests']?0:1)" && grep -q 'pull_request' .github/workflows/checks.yml && grep -q 'contents: read' .github/workflows/checks.yml</automated>
  </verify>
  <acceptance_criteria>
  - Behavior assertion: node scripts/check-manifests.mjs exits 0 on the repo (green baseline).
  - Behavior assertion: the three RED proofs (scratch parse-failure, version mismatch, byte diff) each produced exit 1 with a file-plus-reason stderr line — captured in SUMMARY (a check that never failed is not trusted).
  - Source assertion: scripts/check-manifests.mjs contains the three manifest paths and both MCP config paths; no imports beyond node builtins.
  - Source assertion: package.json scripts contain check:manifests.
  - Source assertion: checks.yml declares pull_request and push triggers with the manifest path filters, permissions contents: read only, and runs node scripts/check-manifests.mjs on node 22.
  - CLI: the scratch-copy run in the verify command exits 0 (clean copy passes), demonstrating the cwd-seam works.
  </acceptance_criteria>
  <done>All three manifest checks green on the repo, each proven failable on a broken scratch copy, wired to npm and to a PR-visible workflow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: url-contract.json + verify-build.mjs — five dist assertions, fail-first proven, gating CI</name>
  <behavior>
  - V2 missing: a scratch dist with one contract page deleted → exit 1 reporting the missing URL.
  - V2 extra: a scratch dist with one extra HTML page added (leak guard) → exit 1 reporting the extra URL.
  - V3 broken asset: a scratch dist whose landing HTML references a nonexistent hashed asset → exit 1 naming the unresolved reference.
  - V4 font origin: a scratch dist with a font-CDN reference injected into one HTML file → exit 1 naming the file; also fails when no woff2 exists under the hashed assets directory.
  - V5 search: a scratch dist with the pagefind directory removed → exit 1.
  - V9 sitemap: a scratch dist with a sitemap entry pointing at a nonexistent page → exit 1.
  - Clean: the real dist from npm run build → exit 0.
  </behavior>
  <read_first>
  - 01-RESEARCH.md § Pattern 7 (the verified enumeration to transcribe — 32 docs URLs; plus the landing) and § Validation Architecture V2/V3/V4/V5/V9 rows
  - 01-PATTERNS.md § scripts/verify-build.mjs (dimension anchors: equality not superset, href/src resolution against dist, font-origin grep, pagefind presence, sitemap mapping) and § design/url-contract.json (plain-JSON shape following the manifest style)
  - .github/workflows/deploy-pages.yml (insertion point: after the Build site step, before upload-pages-artifact)
  - package.json (script entry to add)
  </read_first>
  <files>design/url-contract.json, scripts/verify-build.mjs, package.json, .github/workflows/deploy-pages.yml</files>
  <action>
  Create design/url-contract.json: plain JSON with an origin field (https://jarvis-intelligence.github.io/jarvis-index), a generated_from note (live sitemap enumeration 2026-08-21 plus artifact audit), and a urls array listing exactly 34 entries: /, /brand-logo.html (live today via the old site/ copy — preserved by 01-02; Phase 2 classification decides its keep/retire and updates this contract in the same commit), and the 32 docs URLs transcribed verbatim from RESEARCH Pattern 7 (docs home, quickstart, guide/install, tools index + 9 tools, cli index + 6 commands, 5 concepts, integrations index + 3, troubleshooting index + 2). Create scripts/verify-build.mjs: zero-dependency ESM accepting an optional first argument for the dist directory (default dist) and optional second for the contract path (default design/url-contract.json) — the scratch-test seam. Dimensions: V2 walk every .html under the dist dir excluding 404.html, convert each file to its canonical URL path (index.html → directory root, otherwise strip .html), and assert SET EQUALITY with the contract urls — report missing and extra separately; V3 parse the built landing and docs home HTML, extract every href/src attribute, resolve internal references (strip the base prefix or resolve relative) to dist files (trying the path, path + .html, path + /index.html) and fail on any unresolvable reference — this is the pre-deploy twin of the 01-01 live bug; V4 fail if any built HTML or CSS file under dist references either Google font CDN hostname, and require at least one woff2 under the hashed assets directory; V5 require the pagefind directory under the docs output containing at least one .js and one .json file; V9 parse every sitemap xml file under dist, strip origin + base from each URL, and require each to resolve to a dist file. Prefix every failure line with its dimension tag (V2..V9) plus the file or URL. Prove fail-first on scratch copies of dist (cp -r dist to a mktemp dir, apply one mutation per V2-missing, V2-extra, V3, V4, V5, V9 case, run the script against it expecting exit 1) before the clean run. Wire package.json script verify, and insert a "Verify build" step into deploy-pages.yml between Build site and upload-pages-artifact running node scripts/verify-build.mjs.
  </action>
  <verify>
    <automated>npm run build && node scripts/verify-build.mjs && node -e "const c=require('./design/url-contract.json');process.exit(c.urls.length===34?0:1)" && BROKEN=$(mktemp -d) && cp -r dist "$BROKEN/dist" && rm "$BROKEN/dist/docs/quickstart.html" && if node scripts/verify-build.mjs "$BROKEN/dist" 2>/tmp/vb.err; then exit 1; fi && grep -q 'V2' /tmp/vb.err && grep -q 'Verify build' .github/workflows/deploy-pages.yml && node -e "const p=require('./package.json');process.exit(p.scripts.verify?0:1)"</automated>
  </verify>
  <acceptance_criteria>
  - Behavior assertion: node scripts/verify-build.mjs exits 0 against the real dist (all five dimensions green).
  - Behavior assertion: the scratch proofs each produced exit 1 with the dimension tag — the V2-missing case is asserted in the automated verify; V2-extra, V3, V4, V5, V9 captured in SUMMARY.
  - Source assertion: design/url-contract.json has 34 urls entries and contains /docs/tools/go-to-definition and /brand-logo.html.
  - Source assertion: scripts/verify-build.mjs imports only node builtins and accepts the dist-path argument.
  - Source assertion: deploy-pages.yml contains a Verify build step after the build step and before upload-pages-artifact; package.json contains script verify.
  - CLI: npm run verify exits 0 end-to-end.
  </acceptance_criteria>
  <done>The URL contract is enforced data, every deploy passes five dist assertions, and the assertions themselves are proven failable.</done>
</task>

<task type="auto">
  <name>Task 3: Redirect mechanism — redirects scaffold in astro.config.mjs + 404 assertion + stub-emission proof</name>
  <reversibility rating="costly">The URL contract's shape (and the extensionless URL form it pins via build.format file) is a published-contract commitment: relaxing it later breaks inbound links, so changes must go through contract edits + redirects, never a silent format flip.</reversibility>
  <read_first>
  - astro.config.mjs (current config — the redirects key is added at top level)
  - 01-RESEARCH.md § Standard Stack Astro redirects row (static output emits meta-refresh stubs — verified in astro/dist/core/routing source) and § Anti-Patterns ("hand-written HTML at artifact root", "Carrying srcExclude thinking")
  - 01-PATTERNS.md § astro.config.mjs translation table (redirects row) and § Don't Hand-Roll (never hand-author stub HTML per URL)
  </read_first>
  <files>astro.config.mjs</files>
  <action>
  Add a redirects entry to astro.config.mjs: an empty map ({} — Phase 1 changes no URLs; entries arrive with Phase 2's DOCS-09 classification) preceded by a comment block stating the contract discipline: adding a page requires a url-contract.json edit in the same commit; retiring a URL requires a redirects entry in the same commit, keyed old-path → new-path; stubs are generated by Astro as refresh HTML and true 301s are impossible on GitHub Pages (documented limitation, honest by design). Prove the mechanism end-to-end on a scratch entry (not committed): temporarily add one entry mapping a fictional old path to /docs/quickstart, build, locate the emitted stub file under dist (observe whether format file emits old-path.html or old-path/index.html and record it), assert the stub contains the refresh directive with the destination and the base prefix resolved, then REMOVE the scratch entry and rebuild clean (the final dist must contain zero extra pages — verify-build V2 equality would catch a stray). Assert Starlight's injected 404 route yields dist/404.html (the lookup-table 404 content itself is Phase 2 scope; Phase 1 proves the route exists and is theme-consistent).
  </action>
  <verify>
    <automated>test -f dist/404.html && node scripts/verify-build.mjs && node -e "const m=require('fs').readFileSync('astro.config.mjs','utf8');process.exit(/redirects/.test(m)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
  - Source assertion: astro.config.mjs contains a redirects key with an empty map and the discipline comment (add-page/retire-URL rules, 301 limitation documented).
  - Behavior assertion (recorded in SUMMARY with the observed file path): the scratch-entry build emitted a stub HTML containing the refresh directive pointing at the destination under the base prefix; the entry was removed and the final verify-build run is green (34-page equality holds — no scratch residue).
  - Behavior assertion: dist/404.html exists after the clean build (Starlight 404 route).
  - CLI: node scripts/verify-build.mjs exits 0.
  </acceptance_criteria>
  <done>The SITE-06 mechanism is decided, demonstrated, documented at the config, and leaves no residue; 404 route proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo manifests → plugin clients | The three manifests + MCP pair are the published install contract for Claude Code / Codex / Cursor |
| CI → deploy | verify-build gates what reaches Pages; checks.yml guards the manifests on PRs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Tampering | plugin manifests (version/config drift) | medium | mitigate | check-manifests.mjs: three assertions, fail-first proven, PR-visible via checks.yml path filters covering manifest-only changes |
| T-01-12 | Tampering | public URL set (silent retire/leak) | medium | mitigate | verify-build V2 set-equality gates every deploy; a retire without redirect fails the build |
| T-01-13 | Elevation of Privilege | checks.yml | low | mitigate | Workflow declares contents: read only — no pages/id-token grants it never needs (CI least-privilege is canon; the concrete grant-minimization is the plan-level mitigation) |
| T-01-14 | Information Disclosure | maintainer docs via dist | medium | mitigate | V2 extra-page assertion is the leak guard: any HTML appearing in dist beyond the contract fails the deploy |
</threat_model>

<verification>
- Quick command (per task commit): npm ci --silent && npm run build && node scripts/verify-build.mjs && node scripts/check-manifests.mjs — the phase's standing quick command now fully assembled (V1-V5, V7, V9 locally).
- Full command (plan close): quick + the scratch-break battery outputs recorded in SUMMARY + preview-server deep-URL probes + dispatched deploy green with the Verify build step.
- Instrument mapping (01-VALIDATION.md): this plan creates the Wave-0 instruments (verify-build.mjs, check-manifests.mjs) and completes V2/V3/V4/V9/V7; tasks 1-05-01's commands are Tasks 1-2 automated verifies.
</verification>

<success_criteria>
- 34-entry URL contract enforced by a failable, CI-gated build assertion; zero extras, zero missing.
- Three manifest checks green, each proven red on a broken copy, visible on manifest-only PRs.
- Redirect mechanism proven (scratch entry) and documented; dist/404.html exists.
</success_criteria>

<output>
Create `.planning/phases/01-site-foundation-identity/01-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces (01-05)

- New files: `scripts/check-manifests.mjs`, `scripts/verify-build.mjs` (zero-dep ESM; both take a path argument for scratch testing), `design/url-contract.json` (origin + generated_from + 34-entry urls array), `.github/workflows/checks.yml` (job: manifest checks; triggers push+pull_request paths plugin/**, .codex-plugin/**, .claude-plugin/**, .cursor-plugin/**, the script, the workflow; permissions contents: read; node 22)
- Modified: `package.json` (scripts check:manifests, verify), `.github/workflows/deploy-pages.yml` (new step Verify build between Build site and upload-pages-artifact), `astro.config.mjs` (redirects: {} scaffold + discipline comment)
- npm deps: none
- Script commands: npm run check:manifests, npm run verify, node scripts/verify-build.mjs [distDir] [contractPath], node scripts/check-manifests.mjs (cwd-seam)
- Workflow jobs: Deploy (added Verify build step), Manifest checks (new workflow)

## Flagged Assumptions (spec-less probe fallback — unclassified, unresolved)

| Requirement | Probe status | Flagged assumption | Resolution path |
|-------------|--------------|--------------------|-----------------|
| SITE-06 | unclassified / unresolved | The 34-entry enumeration derives from the live sitemap (32 docs URLs) plus an artifact audit that caught /brand-logo.html. Any URL discoverable ONLY outside the sitemap and the artifact tree (external inbound links to undocumented paths) remains unenumerated. Phase 2's DOCS-09 per-page classification re-derives the set; Phase 5's VRFY-03 old-URL crawl is the final backstop. | Phase 2 classification + Phase 5 crawl; contract edits ride those commits |
| SITE-07 | unclassified / unresolved | The three checks cover exactly the drift class SITE-07 names (version agreement, MCP-pair identity, parse). Other manifest invariants — the jarvis-mcp>=0.6.0 floor, Cursor name-matching rules — remain convention/documentation-enforced until Phase 4's SKIL-04 makes the release protocol explicit. | Phase 4 release protocol; extend check-manifests.mjs then if wanted |
