---
phase: 01-site-foundation-identity
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - design/tokens.css
  - src/styles/starlight-tokens.css
  - src/styles/landing.css
  - astro.config.mjs
  - src/pages/index.astro
  - docs/brand-spec.md
  - plans/0807-2314-landing-page/plan.md
  - docs/superpowers/specs/2026-08-07-landing-page-design.md
autonomous: true
requirements:
  - SITE-03
  - SITE-04
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "design/tokens.css is the single file defining colors and font stacks (--jv-* custom properties with [data-theme='dark'] overrides) — consumed by BOTH surfaces: Starlight via customCss remap onto --sl-* variables, landing via direct var(--jv-*) references"
    - "The built site makes ZERO third-party font requests: no built HTML or CSS references either Google font CDN hostname, and the Geist, Geist Mono, and Rajdhani woff2 files ship as same-origin hashed assets under dist/_astro/"
    - "Two consecutive clean builds on the same lockfile emit an identical set of woff2 assets under dist/_astro/ — fonts are npm-pinned Fontsource files, not build-time network fetches (deterministic, interrupt-safe: a re-run after interruption reproduces the same asset set)"
    - "The three superseded spec artifacts (docs/brand-spec.md, plans/0807-2314-landing-page/plan.md, docs/superpowers/specs/2026-08-07-landing-page-design.md) carry a superseded-by header pointing at design/tokens.css"
  artifacts:
    - path: "design/tokens.css"
      provides: "the full light+dark --jv-* token set (colors, fonts, radius/shadow) with Fontsource imports and a provenance header marking values provisional"
      contains: "--jv-accent"
    - path: "src/styles/starlight-tokens.css"
      provides: "remap layer translating --jv-* onto Starlight --sl-* variables (never redefines a color)"
      contains: "--sl-color-accent: var(--jv-accent)"
    - path: "src/styles/landing.css"
      provides: "landing layout CSS consuming var(--jv-*) — extracted from the inline style block"
      contains: "var(--jv-"
  key_links:
    - from: "design/tokens.css"
      to: "Starlight docs pages"
      via: "astro.config.mjs customCss array entries '../design/tokens.css' and './src/styles/starlight-tokens.css' (package imports in customCss are schema-supported)"
      pattern: "customCss"
    - from: "@fontsource-variable/* + @fontsource/rajdhani"
      to: "same-origin woff2 under dist/_astro/"
      via: "Fontsource CSS @font-face imports inside tokens.css — Vite bundles the font files as hashed assets"
      pattern: "@fontsource"
    - from: "src/styles/landing.css"
      to: "src/pages/index.astro"
      via: "import in the page frontmatter replacing the deleted inline style block"
      pattern: "import '../styles/landing.css'"
  prohibitions:
    - requirement_id: SITE-03
      category: values
      statement: "MUST NOT define colors in two places — design/tokens.css is the single source of truth; starlight-tokens.css only remaps onto engine variables and landing CSS only references var(--jv-*); any literal color value outside tokens.css is a regression of the requirement's core promise"
    - requirement_id: SITE-03
      category: transparency
      statement: "MUST NOT present the Phase 1 palette as final brand decisions — values are provisional (transplanted from the current live site) and tokens.css must say so in its header; Phase 3 refines the same variables, it does not fork them"
    - requirement_id: SITE-04
      category: privacy
      statement: "MUST NOT let any third-party font request survive — zero means zero, including so-called fallback CDN links; local-first font serving is a stated privacy positioning, not a performance nicety"
    - requirement_id: SITE-04
      category: values
      statement: "MUST NOT silently lose the Rajdhani wordmark font in the migration — it ships via @fontsource/rajdhani, or its conscious removal is recorded as a decision; an accidental gap where the wordmark falls back to Geist is brand erosion nobody asked for"
---

<objective>
Establish the single visual truth: design/tokens.css carrying the jarvis identity (--jv-* custom properties, light + dark) consumed by both surfaces, with Fontsource self-hosted variable fonts replacing the Google Fonts CDN — zero third-party font requests — and the stale opengsd-derived spec artifacts marked superseded (SITE-03 + SITE-04).

Purpose: Both surfaces drawing from one token sheet is what makes Phase 3's landing rebuild and Phase 2's docs styling coherent without duplication; self-hosted fonts close the only third-party request the static site makes.

Output: design/tokens.css + starlight-tokens.css remap + landing.css extraction, customCss wiring, superseded-by headers.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-site-foundation-identity/01-RESEARCH.md
@.planning/phases/01-site-foundation-identity/01-PATTERNS.md

Key anchors:
- 01-RESEARCH.md § Pattern 3 (token sheet → --sl-* remap + customCss wiring, exact example), § Pattern 4 (theme key unification context), § Anti-Patterns ("Keeping any Google Fonts link", "second storage key"), § Pitfall 7 (stale specs)
- 01-PATTERNS.md § design/tokens.css (value source: site/index.html:13-52 exact values + brand-spec tabulation + structural template from docs/.vitepress/theme/style.css), § src/styles/starlight-tokens.css (remap example verbatim), § src/pages/index.astro (transplant map: which head lines are deleted, where inline CSS goes)
- Source: site/index.html is gone after 01-02 — the values live in 01-PATTERNS.md's excerpts and in src/pages/index.astro's carried-over inline blocks; read src/pages/index.astro for the current inline style block
- Font packages are already installed by 01-02's atomic stack commit (exact pins in its Artifacts section)
</context>

<tasks>

<task type="tracer">
  <name>Task 1: tokens.css + Fontsource imports + customCss wiring on both surfaces</name>
  <read_first>
  - src/pages/index.astro (the transplanted landing — its head carries the three CDN font stylesheet links to delete, and its inline :root block holds the live token values)
  - 01-PATTERNS.md § design/tokens.css (exact light values from site/index.html:13-36 and dark overrides :39-52, including chip/radius/frame/shadow vars and the three font stacks) and § docs/.vitepress/theme/style.css structural template (provenance-header shape)
  - 01-RESEARCH.md § Pattern 3 (customCss array + remap example — copy verbatim) and § Standard Stack Fontsource rows
  - astro.config.mjs (current starlight block from 01-02 — customCss is added here)
  </read_first>
  <files>design/tokens.css, src/styles/starlight-tokens.css, astro.config.mjs, src/pages/index.astro</files>
  <action>
  Create design/tokens.css: a provenance header comment (values source: live landing :root + dark block, transplanted 2026-08-21; STATUS: provisional — Phase 3 refines these variables, never forks them; dark selector is [data-theme='dark'] by project convention), then @import statements for the three Fontsource packages (@fontsource-variable/geist, @fontsource-variable/geist-mono, @fontsource/rajdhani — Rajdhani is static weights only, no variable package exists; import the weights the wordmark uses, at minimum 600 and 700), then the full --jv-* set: rename every live landing custom property with the jv- prefix (--bg→--jv-bg, --surface→--jv-surface, --fg→--jv-fg, --muted→--jv-muted, --border→--jv-border, --accent→--jv-accent, --accent-strong→--jv-accent-strong, --accent-soft→--jv-accent-soft, plus the chip/radius/frame/shadow variables from PATTERNS lines 23-33, and --jv-sans / --jv-mono / --jv-logo font stacks with the same family lists). Light values on :root, dark overrides under [data-theme='dark'] — transcribe values verbatim from the PATTERNS excerpts. Create src/styles/starlight-tokens.css per RESEARCH Pattern 3: remap ONLY (never redefine a color) — map at minimum --sl-color-accent, --sl-color-accent-text, --sl-color-bg, --sl-color-bg-inline-code, --sl-font, and --sl-font-mono onto their --jv-* counterparts. Wire astro.config.mjs: add customCss: ['../design/tokens.css', './src/styles/starlight-tokens.css'] to the starlight options. In src/pages/index.astro, DELETE the three CDN font stylesheet link tags from the head (PATTERNS maps them at old site/index.html:9-11) and import the tokens sheet so the landing gets the Fontsource @font-face rules (import '../design/tokens.css' in the page frontmatter). Leave the inline layout CSS and inline :root block in place for now — Task 2 extracts them (the page must not lose its styles mid-plan).
  </action>
  <verify>
    <automated>npm run build && test "$(find dist/_astro -name '*.woff2' | wc -l | tr -d ' ')" -ge 3 && ! grep -q -E 'fonts\.g(oogleapis|static)\.com' dist/index.html && ! grep -q -E 'fonts\.g(oogleapis|static)\.com' dist/docs/index.html && grep -q -- '--jv-accent' dist/_astro/*.css && grep -q -- '--sl-color-accent: var(--jv-accent)' src/styles/starlight-tokens.css</automated>
  </verify>
  <acceptance_criteria>
  - Source assertion: design/tokens.css contains --jv-accent, --jv-accent-strong, --jv-accent-soft, --jv-sans, --jv-mono, --jv-logo and a [data-theme='dark'] override block; its header states the values are provisional.
  - Source assertion: src/styles/starlight-tokens.css maps --sl-color-accent to var(--jv-accent) and contains no literal hex color value (remap only).
  - Source assertion: astro.config.mjs starlight options contain customCss with both file entries.
  - Source assertion: src/pages/index.astro head no longer contains any element whose href or src points at a font CDN host, and imports the tokens sheet.
  - Behavior assertion: dist/_astro contains at least 3 woff2 files (Geist, Geist Mono, Rajdhani) served same-origin.
  - Behavior assertion: grepping the built landing and docs home HTML for either Google font CDN hostname returns nothing (zero third-party font requests — the two surfaces are the assertion scope; a repo-wide dist grep lands with 01-05's verify-build V4).
  - CLI: npm run build exits 0.
  </acceptance_criteria>
  <done>One token sheet feeds both surfaces; fonts are npm-bundled same-origin woff2; no font CDN reference remains in the built landing or docs home.</done>
</task>

<task type="auto">
  <name>Task 2: Landing re-token — extract inline CSS to landing.css consuming var(--jv-*)</name>
  <read_first>
  - src/pages/index.astro (the full inline style block — layout/section CSS plus the :root and [data-theme='dark'] custom-property blocks to remove)
  - 01-PATTERNS.md § src/pages/index.astro transplant map (inline layout CSS :54-566 → src/styles/landing.css) and § Shared Patterns "Token layering" (values → tokens → engine remap; never define colors in two places)
  - 01-RESEARCH.md § Pitfall 6 (identity scope creep — mechanical swap only, no copy/section edits)
  </read_first>
  <files>src/styles/landing.css, src/pages/index.astro</files>
  <!-- planner-region-allow: :root — Task 1 legitimately leaves the inline :root block in src/pages/index.astro at ITS verification time; Task 2 then removes it file-wide by design (sequential tasks, not simultaneous regions) -->
  <action>
  Move the inline layout/section CSS out of src/pages/index.astro into src/styles/landing.css and mechanically rewrite every custom-property reference to its --jv- prefixed counterpart (var(--bg) becomes var(--jv-bg), and so on for every reference — a pure prefix rename, no value edits). DELETE the inline :root and [data-theme='dark'] custom-property blocks from the page — after this task the ONLY definitions of those properties live in design/tokens.css. Import landing.css from the page (replacing the raw inline style block); landing.css itself must not import tokens.css again if the page already imports it — exactly one import chain per page. Do NOT touch the markup: same sections, same ids/classes, same copy, same theme toggle script (re-keying is 01-04's task). If any rule referenced a property with no --jv- counterpart, add the missing token to design/tokens.css with its transplanted value rather than leaving a literal in landing.css.
  </action>
  <verify>
    <automated>npm run build && grep -q 'var(--jv-' src/styles/landing.css && test "$(grep -c ':root' src/pages/index.astro | tr -d ' ')" = "0" && grep -q 'assets/jarvis-mark.svg' dist/index.html && (npm run preview -- --port 4323 & PV=$!; sleep 8; curl -fsS -o /tmp/pv-l.html http://localhost:4323/jarvis-index/; kill $PV) && grep -q 'id="hero"' /tmp/pv-l.html</automated>
  </verify>
  <acceptance_criteria>
  - Source assertion: src/pages/index.astro contains no :root custom-property block and no inline layout CSS; it imports landing.css.
  - Source assertion: src/styles/landing.css references var(--jv-*) tokens and contains no literal color values that duplicate a token (hex colors appear only in design/tokens.css).
  - Behavior assertion: built landing still renders the same structure — the hero section id and the favicon reference are present in dist/index.html.
  - Behavior assertion: build + preview serve the landing 200 with the hero markup intact.
  - CLI: npm run build exits 0.
  </acceptance_criteria>
  <done>Landing is fully token-driven: zero inline style definitions, all values flowing from design/tokens.css.</done>
</task>

<task type="auto">
  <name>Task 3: Superseded-by headers on the three stale spec artifacts</name>
  <read_first>
  - docs/brand-spec.md (lines 10-30: the old token names/values the new sheet supersedes)
  - plans/0807-2314-landing-page/plan.md and docs/superpowers/specs/2026-08-07-landing-page-design.md (their first lines — headers go at top)
  - 01-RESEARCH.md § Pitfall 7 (why: a later phase implementing from a stale spec regresses the tokens)
  </read_first>
  <files>docs/brand-spec.md, plans/0807-2314-landing-page/plan.md, docs/superpowers/specs/2026-08-07-landing-page-design.md</files>
  <action>
  Prepend a short superseded-by notice to each of the three artifacts: a first-line note stating the artifact is HISTORICAL, superseded by design/tokens.css as the identity source of truth as of 2026-08-21, and must not be implemented from. Do not otherwise edit the files (they remain as history). These are maintainer docs outside the content collection — they never publish.
  </action>
  <verify>
    <automated>for f in docs/brand-spec.md plans/0807-2314-landing-page/plan.md docs/superpowers/specs/2026-08-07-landing-page-design.md; do head -5 "$f" | grep -i 'superseded' || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
  - Source assertion: each of the three files contains a superseded notice naming design/tokens.css within its first 5 lines.
  - Behavior assertion: none of the three files appear under dist/ (they are outside src/content/docs/ — structural exclusion holds after this edit).
  </acceptance_criteria>
  <done>No stale spec can be mistaken for the current identity source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → static assets | Every font request the pages make reveals visitor metadata to whoever hosts the font |
| npm packages → build output | Fontsource packages inject @font-face rules and woff2 binaries into the artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Information Disclosure | font loading | medium | mitigate | Zero-CDN assertion (Task 1 acceptance): all fonts same-origin hashed woff2 from npm-pinned Fontsource packages — no third party observes font requests |
| T-01-07 | Tampering | @fontsource packages | low | mitigate | Exact npm pins + lockfile from 01-02's legitimacy-gated install; fonts are static binaries hashed into the artifact |
| T-01-08 | Information Disclosure | maintainer specs | low | mitigate | Superseded headers prevent future phases implementing stale (possibly soon-public-by-accident) token tables; the docs stay structurally unpublished |
</threat_model>

<verification>
- Quick command: npm ci --silent && npm run build plus Task 1's automated grep set (font origin, woff2 presence, token wiring) — V4 sampled at task level.
- Full command: quick + preview curl of / and /docs/ confirming both surfaces reference the hashed tokens CSS.
- Manual note: visual sanity (both surfaces readable, light + dark) is a 1-minute browser check recorded in the SUMMARY; the standing V4 instrument (repo-wide font-origin grep in verify-build.mjs) lands with 01-05.
</verification>

<success_criteria>
- design/tokens.css exists, provisional, dark-capable, and is provably the only color-definition file.
- Both built surfaces consume it; zero third-party font requests; at least 3 same-origin woff2 assets.
- Stale specs marked historical.
</success_criteria>

<output>
Create `.planning/phases/01-site-foundation-identity/01-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces (01-03)

- New files: `design/tokens.css` (--jv-* custom properties: --jv-bg, --jv-surface, --jv-fg, --jv-muted, --jv-border, --jv-accent, --jv-accent-strong, --jv-accent-soft, chip/radius/frame/shadow vars, --jv-sans, --jv-mono, --jv-logo; [data-theme='dark'] overrides; Fontsource @import statements), `src/styles/starlight-tokens.css` (--sl-* remap), `src/styles/landing.css`
- Modified: astro.config.mjs (starlight.customCss array), src/pages/index.astro (CDN font links removed; tokens + landing.css imported; inline style blocks removed)
- npm deps: none new — consumes the three Fontsource packages installed by 01-02 (@fontsource-variable/geist@5.3.0, @fontsource-variable/geist-mono@5.3.0, @fontsource/rajdhani@5.3.0)
- Script commands: none new
- Workflow jobs: none new

## Flagged Assumptions (spec-less probe fallback)

| Requirement | Probe status | Flagged assumption | Resolution path |
|-------------|--------------|--------------------|-----------------|
| SITE-03 | unclassified / unresolved | "New jarvis identity" is interpreted as: Phase 1 ships the CURRENT live values under the --jv-* architecture, explicitly provisional; the actual palette/typography refresh is Phase 3 work against the same variables. If the user intended new values NOW, this plan under-delivers on purpose per ROADMAP phase split. | Confirm at /gsd-verify-work or in Phase 3 planning; tokens.css header records the provisional status |
