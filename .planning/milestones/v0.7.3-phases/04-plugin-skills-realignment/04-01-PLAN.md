---
wave: 1
depends_on: []
files_modified:
  - plugin/skills/jarvis-setup/SKILL.md
  - plugin/skills/jarvis-use/SKILL.md
  - plugin/skills/jarvis-use/references/tool-roster.md
  - .codex-plugin/plugin.json
autonomous: true
requirements:
  - SKIL-01
  - SKIL-02
  - SKIL-03
---

# 04-01: Skill Content Realignment — CLAUDE.md fix, vocabulary, links, tool-roster drift

## Objective

Fix the broken CLAUDE.md reference in jarvis-setup (SKIL-01), adopt the settled positioning vocabulary across all three skills (SKIL-02), retarget the two `blob/main` reading-links in `.codex-plugin/plugin.json` to `v0.7.3` (SKIL-02, link policy), and fix the stale `getIndexStatus` return shape, `typeHierarchy` caveat, and freshness description in `references/tool-roster.md` (SKIL-03). After this plan an agent reading any of the three skills sees correct docs-site links, the new voice, and no dangling CLAUDE.md reference. The `jarvis-issues` skill gets no content changes beyond link policy (zero URLs in it; minimal touch per CONTEXT.md). MCP configs are not touched.

## must_haves

### truths

- Zero matches for `CLAUDE\.md` inside `plugin/skills/` (the broken reference at jarvis-setup:31 is replaced with a docs-site deep link to `/guide/requirements/`)
- `plugin/skills/jarvis-setup/SKILL.md` line 31 (replacement) contains the absolute URL `https://jarvis-intelligence.github.io/jarvis-index/docs/guide/requirements/`
- `plugin/skills/jarvis-setup/SKILL.md` frontmatter `description` uses the phrase "structural code intelligence" (not "code-intelligence MCP server")
- `plugin/skills/jarvis-use/SKILL.md` frontmatter `description` includes "structural" (settled voice per CONTEXT.md)
- Zero matches for `blob/main/` in `plugin/skills/` (reading links use tag URLs or docs-site absolute URLs, never `main`)
- Zero matches for `blob/main/` in `.codex-plugin/plugin.json` (the two reading-links at lines 35-36 retargeted to `v0.7.3`)
- The functional install command `jarvis-index/main/setup.sh` remains at `main` in `jarvis-setup/SKILL.md:22` (exactly one match — the canonical install URL per CONTEXT.md link policy)
- `plugin/skills/jarvis-use/references/tool-roster.md` `getIndexStatus` section contains the field names `last_index_run` and `capabilities`
- `plugin/skills/jarvis-use/references/tool-roster.md` `typeHierarchy` section reflects that fork-fixed scip makes it work, not "Returns an error on real indexes"
- `plugin/skills/jarvis-use/references/tool-roster.md` Freshness section describes flat fields (`stale`, `freshness`, `commit`, `generated_at`, `checked_at`) rather than a nested `freshness` object
- `setup.sh` is never edited (ownership rule)
- `plugin/.mcp.json` and `plugin/mcp.json` are never edited (no MCP config changes in this plan)
- All three manifest `version` fields remain `"0.7.2"` after this plan (version bump is plan 04-02)

## Tasks

<task type="tracer">
  <name>Fix CLAUDE.md reference, align vocabulary/links, fix tool-roster drift</name>
  <files>plugin/skills/jarvis-setup/SKILL.md, plugin/skills/jarvis-use/SKILL.md, plugin/skills/jarvis-use/references/tool-roster.md, .codex-plugin/plugin.json</files>
  <read_first>
    - plugin/skills/jarvis-setup/SKILL.md (full file — target lines 3 (description), 31 (CLAUDE.md ref), 22 (install command to keep at main))
    - plugin/skills/jarvis-use/SKILL.md (full file — target line 3 (description), line 20 (typeHierarchy caveat), line 65 (typeHierarchy gotcha))
    - plugin/skills/jarvis-use/references/tool-roster.md (full file — target getIndexStatus section at line 26-28, typeHierarchy section at line 23-24, Freshness section at lines 42-44)
    - .codex-plugin/plugin.json (lines 35-36 — two `blob/main` reading-links to retarget)
    - .planning/phases/04-plugin-skills-realignment/04-RESEARCH.md (sections 2-5: CLAUDE.md fix, vocabulary drift table, command drift, tool-roster drift table)
    - .planning/phases/04-plugin-skills-realignment/04-CONTEXT.md (link policy, content scope, Claude's Discretion)
    - src/content/docs/docs/tools/get-index-status.md:24-46 (authoritative return shape with last_index_run and capabilities)
    - src/content/docs/docs/tools/go-to-definition.md:23-38 (authoritative freshness flat fields)
    - src/content/docs/docs/tools/type-hierarchy.md (authoritative typeHierarchy behavior post fork-fix)
  </read_first>
  <action>
    1. **Fix CLAUDE.md reference** (SKIL-01, D-01 link policy): In `plugin/skills/jarvis-setup/SKILL.md`, replace the sentence at line 31 (`See CLAUDE.md for the detail.`) with `See [Requirements &amp; Limits](https://jarvis-intelligence.github.io/jarvis-index/docs/guide/requirements/) for the full language-support matrix and caveats.` — the replacement target is the docs-site absolute URL per CONTEXT.md link policy.

    2. **Update jarvis-setup frontmatter description** (SKIL-02, settled voice): In `plugin/skills/jarvis-setup/SKILL.md` line 3, replace `local-first code-intelligence MCP server` with `local-first structural code intelligence` — the phrase "structural code intelligence" is the settled voice per CONTEXT.md decisions and RESEARCH.md vocabulary drift table.

    3. **Update jarvis-use frontmatter description** (SKIL-02, Claude's Discretion): In `plugin/skills/jarvis-use/SKILL.md` line 3, the current description already says "code structure queries". Tighten to include the settled voice: add "structural code intelligence" phrasing. The description must remain a trigger surface — keep the concrete tool names and query types. Do not change the jarvis-issues description (issue-filing scope, no positioning language needed per RESEARCH).

    4. **Retarget `.codex-plugin/plugin.json` reading-links** (SKIL-02, link policy): In `.codex-plugin/plugin.json`, change `blob/main` to `blob/v0.7.3` at both lines 35 and 36. These are reading-links (privacy policy, terms of service) that must resolve at the release tag per CONTEXT.md. Do NOT change any other URLs in this file.

    5. **Fix tool-roster `getIndexStatus` return shape** (SKIL-03, drift fix): In `plugin/skills/jarvis-use/references/tool-roster.md`, update the `getIndexStatus` section (lines 26-28) to add the `last_index_run` and `capabilities` fields to the return shape description. The authoritative shape is at `src/content/docs/docs/tools/get-index-status.md:24-46`. The existing `freshness` and `searchCoverage` fields remain correct — only the two new top-level fields are missing.

    6. **Fix tool-roster `typeHierarchy` caveat** (SKIL-03, drift fix): In `plugin/skills/jarvis-use/references/tool-roster.md` line 24, replace the caveat "Returns an `error` on real indexes — upstream `scip expt-convert` never populates `relationships`. Treat the error as \"unavailable\", not as \"no supertypes\"." with a caveat reflecting that setup.sh now installs a fork-fixed scip that populates relationships, so `typeHierarchy` works on freshly reindexed repos. The authoritative behavior is at `src/content/docs/docs/tools/type-hierarchy.md`.

    7. **Fix tool-roster Freshness section** (SKIL-03, drift fix): In `plugin/skills/jarvis-use/references/tool-roster.md` lines 42-44, update the Freshness field description. The current text says "a `freshness` object" but the actual return shape uses flat top-level fields: `stale` (bool), `freshness` (string: "fresh"/"stale"), `commit` (string), `generated_at` (ISO timestamp), `checked_at` (ISO timestamp). The authoritative shape is at `src/content/docs/docs/tools/go-to-definition.md:23-38`. Describe flat fields, not a nested object.
  </action>
  <verify>
    <automated>cd /Users/ddphuong/Projects/jarvis-ai/jarvis-index &amp;&amp; grep -rn 'CLAUDE\.md' plugin/skills/ ; grep -rn 'blob/main/' plugin/skills/ ; grep -rn 'blob/main/' .codex-plugin/plugin.json ; grep -c 'structural code intelligence' plugin/skills/jarvis-setup/SKILL.md ; grep -c 'guide/requirements/' plugin/skills/jarvis-setup/SKILL.md ; grep -c 'last_index_run' plugin/skills/jarvis-use/references/tool-roster.md ; grep -c 'capabilities' plugin/skills/jarvis-use/references/tool-roster.md ; grep -c 'jarvis-index/main/setup.sh' plugin/skills/</automated>
  </verify>
  <done>
    - `grep -rn 'CLAUDE\.md' plugin/skills/` returns zero output (no dangling CLAUDE.md references)
    - `grep -rn 'blob/main/' plugin/skills/` returns zero output (no reading-links at main)
    - `grep -rn 'blob/main/' .codex-plugin/plugin.json` returns zero output (both retargeted to v0.7.3)
    - `grep -c 'structural code intelligence' plugin/skills/jarvis-setup/SKILL.md` returns at least 1 (frontmatter description updated)
    - `grep -c 'guide/requirements/' plugin/skills/jarvis-setup/SKILL.md` returns 1 (docs-site deep link replaces CLAUDE.md ref)
    - `grep -c 'jarvis-index/main/setup.sh' plugin/skills/` returns exactly 1 (functional install command stays at main)
    - `grep -c 'last_index_run' plugin/skills/jarvis-use/references/tool-roster.md` returns at least 1 (stale field added)
    - `grep -c 'capabilities' plugin/skills/jarvis-use/references/tool-roster.md` returns at least 1 (stale field added)
    - `grep -c 'typeHierarchy.*error' plugin/skills/jarvis-use/references/tool-roster.md` returns 0 (old caveat removed)
    - The Freshness section in tool-roster.md describes flat fields, not a nested object
    - `setup.sh` is byte-unchanged (ownership rule)
    - `plugin/.mcp.json` and `plugin/mcp.json` are byte-unchanged (no MCP config changes)
    - All three manifest versions remain `"0.7.2"`
  </done>
  <reversibility rating="reversible">All changes are prose/URL edits in Markdown and JSON — fully revertible via git.</reversibility>
</task>

## Artifacts This Phase Produces

- `plugin/skills/jarvis-setup/SKILL.md` — CLAUDE.md reference replaced with docs-site link, description uses "structural code intelligence"
- `plugin/skills/jarvis-use/SKILL.md` — description tightened with settled voice
- `plugin/skills/jarvis-use/references/tool-roster.md` — `getIndexStatus` return shape updated (last_index_run, capabilities), `typeHierarchy` caveat corrected, Freshness section describes flat fields
- `.codex-plugin/plugin.json` — two `blob/main` reading-links retargeted to `blob/v0.7.3`

## Verification

```xml
<verify>
  <automated>cd /Users/ddphuong/Projects/jarvis-ai/jarvis-index &amp;&amp; grep -rn 'CLAUDE\.md' plugin/skills/ ; grep -rn 'blob/main/' plugin/skills/ ; grep -rn 'blob/main/' .codex-plugin/plugin.json ; grep -c 'structural code intelligence' plugin/skills/jarvis-setup/SKILL.md ; grep -c 'guide/requirements/' plugin/skills/jarvis-setup/SKILL.md ; grep -c 'last_index_run' plugin/skills/jarvis-use/references/tool-roster.md ; grep -c 'capabilities' plugin/skills/jarvis-use/references/tool-roster.md ; grep -c 'jarvis-index/main/setup.sh' plugin/skills/</automated>
</verify>
```

Structural assertions (grep gates):
- Zero `CLAUDE\.md` in `plugin/skills/` (broken reference removed)
- Zero `blob/main/` in `plugin/skills/` (reading-links at tags)
- Zero `blob/main/` in `.codex-plugin/plugin.json` (retargeted to v0.7.3)
- At least 1 `structural code intelligence` in `jarvis-setup/SKILL.md` (description updated)
- Exactly 1 `guide/requirements/` in `jarvis-setup/SKILL.md` (docs-site deep link)
- Exactly 1 `jarvis-index/main/setup.sh` in `plugin/skills/` (functional install preserved at main)
- At least 1 `last_index_run` in `tool-roster.md` (stale field fixed)
- At least 1 `capabilities` in `tool-roster.md` (stale field fixed)

## Source Coverage Audit

| Source | Item | Covered By | Status |
|--------|------|------------|--------|
| GOAL | CLAUDE.md reference removed | 04-01 T1 | covered |
| GOAL | Docs links resolve at tags | 04-01 T1 | covered |
| GOAL | Commands/examples synced with docs | 04-01 T1 (tool-roster) | covered |
| GOAL | New positioning vocabulary | 04-01 T1 | covered |
| REQ SKIL-01 | Broken CLAUDE.md reference removed | 04-01 T1 (action 1) | covered |
| REQ SKIL-02 | Skills realigned to new positioning + docs URLs at tags | 04-01 T1 (actions 2-4) | covered |
| REQ SKIL-03 | Commands/examples synced with rebuilt docs | 04-01 T1 (actions 5-7) | covered |
| RESEARCH §2 | CLAUDE.md reference fix | 04-01 T1 (action 1) | covered |
| RESEARCH §3 | Vocabulary drift (8 items) | 04-01 T1 (actions 2-3) | covered |
| RESEARCH §4 | Command/example drift | 04-01 T1 (actions 5-7) | covered |
| RESEARCH §5 | tool-roster drift (3 items) | 04-01 T1 (actions 5-7) | covered |
| CONTEXT link policy | Tag URLs for reading, main for install | 04-01 T1 (actions 1, 4) | covered |
| CONTEXT content scope | jarvis-issues minimal touch | 04-01 T1 (not touched) | covered |
| CONTEXT settled voice | "structural code intelligence" | 04-01 T1 (actions 2-3) | covered |

## Threat Model

<threat_model>
**ASVS Level 1, blocking threshold: high**

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Stale link in plugin | Low | All reading-links point at tag v0.7.3 (verified by grep gate). The functional install command stays at main (correct — it is the canonical install URL). |
| Incorrect tool shape in roster | Medium | Changes validated against Phase-2 docs pages (authoritative source). check-manifests.mjs in CI catches JSON breakage. |
| setup.sh edit | N/A | Ownership rule enforced — setup.sh is never a target file in this plan. |
</threat_model>

## Estimate

`estimate` tokens=8000 raw_tokens=8000 confidence=no-calibration-data (Phase 4 is small: 4-file prose/URL edit + 1 drift fix; avg prior phase 12 min/plan for similar static-content tasks)
